/* GENERATED by scripts/gen-worker-topics.mjs. Do not edit.
   Run: npm run gen:topics  (also runs as part of the worker deploy) */

export const TOPICS: Record<string, string> = {"request-lifecycle":"The life of a request\n\nTyping a URL sets off DNS resolution, a TCP handshake, a TLS handshake, and then the HTTP request itself.\n\nEach of those costs at least one round trip, TLS 1.3 needs one, TLS 1.2 needs two, and round trips are what you actually pay for. A user 200ms away pays that 200ms every single time.\n\nFour round trips before one byte of content is 800ms of blank screen. Bandwidth does not help here, because the pipe is not full, it is idle and waiting. That is why latency, not bandwidth, decides perceived speed on any connection fast enough to matter.\n\nName the steps and most performance work becomes obvious. Remove a round trip, or move the server closer.\n\nInterviewers open with this because it reveals whether you think in terms of round trips or in terms of vague slowness. The answer that names DNS, TCP, TLS and HTTP separately is the one that can then reason about a CDN.","tcp-vs-udp":"TCP and UDP\n\nTCP guarantees delivery and order: lost packets are retransmitted, and the receiver waits. UDP does neither. It sends and forgets.\n\nThat guarantee is not free. One lost packet stalls everything queued behind it, which is exactly right for a file and useless for a live call, where the retransmitted frame is worthless by the time it lands. Video calls, games and DNS run on UDP and handle loss themselves; nearly everything else takes the guarantee and is glad of it.\n\nChoosing UDP is choosing to write your own reliability for the parts that need it. Worth it when stale data is useless; a mistake when you end up reimplementing TCP badly.","http-versions":"HTTP/1.1, HTTP/2 and HTTP/3\n\nHTTP/1.1 pipelining is in the spec but effectively unused, so in practice a connection carries one request at a time. Browsers open six per host and developers bundle files to work around it.\n\nHTTP/2 multiplexes many streams over one connection, which removes the need for most bundling tricks. It still runs on TCP, so one lost packet blocks every stream sharing that connection.\n\nHTTP/3 moves to QUIC over UDP, where each stream is independent. Loss affects one stream instead of all of them, which matters most on mobile networks.\n\nSprite sheets, domain sharding and aggressive bundling were workarounds for an HTTP/1.1 limit. Carrying them into an HTTP/2 or HTTP/3 world adds complexity for no gain, and can hurt caching.","dns":"DNS and why it hurts you\n\nDNS turns a name into an address, and the answer is cached at every layer between you and the client for as long as the TTL you set. Low TTL, fast changes and more lookups; high TTL, the reverse.\n\nThe catch is that your TTL is a request, not an instruction. Some resolvers hold answers well past it, so a migration planned around 60 seconds can still be sending traffic to the old address hours later. This is why cutovers change what the address points at, rather than changing the address.\n\nLowering TTL days ahead of a migration, then failing over at the load balancer instead of in DNS, is the difference between a clean cutover and a long tail of traffic hitting a decommissioned box.","cdn":"CDNs and edge caching\n\nA CDN puts copies of your content in data centres near your users.\n\nThe round trips that dominate page load then take tens of milliseconds instead of hundreds. Nothing about your origin got faster. The distance got shorter.\n\nStatic assets are the easy part. The interesting work is caching HTML and API responses at the edge, which forces you to be deliberate about cache keys and invalidation, which are the two things that go wrong.\n\nModern edge platforms also run code at those locations, so personalisation and auth checks can happen near the user instead of at origin.\n\nA CDN is usually the highest-leverage performance change available, because it attacks latency instead of throughput. Adding servers makes a busy system faster; moving content closer makes a distant system faster.","big-o":"Big-O without the ritual\n\nBig-O describes how cost grows as input grows. It deliberately ignores constants, because for large inputs the shape of the curve dominates everything else.\n\nThat also makes it misleading for small inputs. An O(n log n) algorithm with a heavy constant loses to an O(n squared) one on a list of twenty items, which is why real sort implementations switch to insertion sort for small slices.\n\nSay which n you mean. 'O(n) in the number of users' and 'O(n) in the number of requests' are very different claims.\n\nQuoting complexity without naming n is the most common way to sound rigorous and say nothing. Naming it is what turns the answer into engineering.","hash-maps":"Hash maps and their worst case\n\nA hash map gives average constant-time lookup by turning a key into a bucket index, handling collisions by chaining or by open addressing.\n\nThe average case assumes keys spread evenly. Let an attacker choose keys that all hash to one bucket and every lookup degrades to scanning a list, which turns your hash map into a denial-of-service vector. Languages now randomise the hash seed per process for exactly this reason.\n\nReaching for a hash map is right almost always. Knowing the degenerate case is what separates using one from being able to defend the choice.","trees-indexes":"B-trees and why databases use them\n\nA balanced binary tree is fine in memory. On disk it is poor, because each level is a separate read and disk reads are expensive.\n\nA B-tree stores many keys per node, matched to the size of a disk page. Fanout is high, depth is low, and a lookup in a large table takes a handful of reads instead of dozens.\n\nThis is why almost every relational index is a B-tree, and why index depth barely grows as tables get large.\n\nThe structure is chosen to match the storage medium, not for elegance. That is the general lesson: data structure choice follows the cost model of where the data lives.","bloom-filters":"Bloom filters\n\nA Bloom filter answers one question cheaply: is this item definitely absent, or possibly present. False positives happen. False negatives cannot.\n\nThat asymmetry is the whole point. Put one in front of an expensive lookup and everything definitely not there skips the lookup entirely, for a few bits per item, far less than storing the keys would cost.\n\nUseful precisely where a definite no is valuable and a maybe is cheap to verify. Storage engines use them to avoid reading files that cannot contain a key.","acid":"ACID, stated plainly\n\nAtomicity: all of a transaction happens, or none of it. Consistency: it moves the database between valid states. Isolation: concurrent transactions do not see each other's partial work. Durability: once committed, it survives a crash.\n\nThree of those four are largely settled. Isolation is where the detail lives, and where the surprises are.\n\nMost databases default to read committed, not full serialisability, because full isolation is expensive: the database has to behave as though transactions ran one after another, and enforcing that costs you either locks or aborts.\n\nThat default permits anomalies most developers never think about. Two transactions read the same row, each computes from what it read, and one result quietly overwrites the other.\n\n'We use a relational database so we get ACID' is only true at the isolation level you actually configured. Knowing your default is the difference between a guarantee and an assumption.","indexes":"Indexes and their cost\n\nAn index is a sorted structure that turns a full table scan into a targeted lookup. Reads get faster, sometimes by orders of magnitude.\n\nEvery index must be updated on write, so each one makes inserts and updates slower and takes disk space. An unused index is pure cost.\n\nComposite indexes only help when the query uses a leading prefix of the columns, in order. An index on (a, b) helps a query on a, and on a and b, but not one on b alone.\n\nThe instinct to add an index per slow query produces tables with fifteen indexes and slow writes. The question is always which queries matter, not which are slow.","normalisation":"Normalisation and when to break it\n\nNormalising stores each fact once, so an update touches one row and the data cannot contradict itself. It costs joins on read.\n\nDenormalising duplicates data to avoid those joins. Reads get faster and simpler, and every copy becomes something that can drift. The usual shape is a normalised source of truth plus deliberately denormalised read models, kept up to date asynchronously.\n\nDenormalisation is not a shortcut, it is a trade of write complexity for read speed. Make it when reads dominate and you have a reliable way to keep copies current.","sql-vs-nosql":"Relational or document\n\nRelational databases are the default for a reason: flexible querying, real constraints, and transactions across rows. Choose otherwise only with a reason you can state.\n\nDocument stores suit data read as a whole unit, with a shape that varies per record. Key-value stores suit lookups by a single known key at very high volume.\n\nThe honest version is that most applications fit a relational model, and Postgres handles JSON well enough to cover the semi-structured parts.\n\n'It scales better' is not a reason on its own, a managed Postgres handles more load than most products ever see. The reasons that hold up are access pattern and data shape.","connection-pooling":"Connection pooling\n\nEach database connection costs memory and, in Postgres, an entire backend process. It struggles well before most people expect, often in the low hundreds.\n\nServerless makes this worse. Every instance opens its own connections, and instances scale with traffic, so connections multiply exactly when load is highest.\n\nA pooler sits in front and multiplexes many client connections onto a small number of real ones.\n\nWhich pooling mode you pick decides how much that buys you. Session pooling holds a real connection for the client's whole session and helps very little. Transaction pooling hands it back at every commit and helps enormously, at the price of losing anything that outlives a transaction: prepared statements, session variables, advisory locks.\n\nThis is the failure that looks like a database problem and is not. The database is fine; you have exhausted its connection slots. Adding read replicas does not help, and a pooler does.","process-thread-async":"Processes, threads and async\n\nProcesses have separate memory and are isolated. Threads share memory within a process, which makes communication cheap and data races possible. Async runs many tasks on one thread, switching whenever a task waits.\n\nAsync suits IO-bound work, where tasks spend most of their time waiting on network or disk. It does nothing for CPU-bound work, because there is no waiting to exploit, which is why a CPU-heavy function in Node.js or Python asyncio blocks everything else until it finishes.\n\nChoosing async for CPU-bound work is a common and expensive mistake. The question is what the work waits on, not which model is modern.","race-conditions":"Race conditions and locks\n\nA race condition is a correctness bug that depends on timing. Read-modify-write is the classic shape: two requests read the same value, both compute from it, and one result is lost.\n\nLocks prevent it by serialising access, at the cost of contention and the risk of deadlock when locks are taken in different orders.\n\nOften the better fix is to avoid the read entirely: an atomic increment, or a conditional update that fails if the row changed.\n\nReaching for a lock is the first instinct and rarely the best one. Making the operation atomic removes the race instead of guarding it, with no contention and no deadlock.","idempotency":"Idempotency\n\nAn idempotent operation can run repeatedly with the same result as running once. This matters because networks make retries unavoidable, and a timeout tells you nothing at all about whether the work happened.\n\nThe standard approach is an idempotency key supplied by the client. The server records the key alongside its result, and returns the stored result if the key comes back. Without it, a retried payment is simply a second payment.\n\nAny endpoint that changes state and can be retried needs this. It is the difference between at-least-once delivery being safe and being a liability.","backpressure":"Backpressure\n\nWhen a system accepts work faster than it can finish it, the backlog grows. Queues fill, memory climbs, latency climbs, and it fails at the worst possible moment.\n\nBackpressure means refusing or slowing intake when downstream cannot keep up, bounded queues, load shedding, rejecting early. Failing fast under overload is better behaviour than accepting everything and then collapsing.\n\nUnbounded queues look like resilience and are the opposite: they convert a visible, recoverable rejection into a hidden backlog that fails later and harder.","cache-strategies":"Cache-aside, write-through, write-behind\n\nCache-aside: the application checks the cache, and on a miss reads the database and populates the cache. Simple, and the default nearly everywhere.\n\nWrite-through: writes go to cache and database together. The cache is never stale, but every write pays both costs.\n\nWrite-behind: writes go to cache and are flushed to the database later. Fastest writes, and a crash between the two loses data.\n\nCache-aside wins by default because the failure mode is mild, a miss costs a database read. Write-behind trades durability for speed, which is only acceptable when the data is genuinely disposable.","invalidation":"Invalidation: TTL, explicit, and versioned keys\n\nTTL expiry is the simplest: entries die after a set time. You accept staleness up to the TTL in exchange for needing no invalidation logic at all.\n\nExplicit invalidation deletes the key when the underlying data changes. Fresher, but every write path must know every key it affects, and one missed path means permanently stale data.\n\nVersioned keys sidestep deletion entirely. Include a version or timestamp in the key, and bump it on write. Old entries are never read again and expire on their own.\n\nVersioned keys are usually the best of the three because they make invalidation a write to one value instead of a fan-out of deletes. Deleting keys correctly requires knowing every key derived from a piece of data, and that knowledge rots.","redis-structures":"Redis beyond get and set\n\nRedis stores structures, not just strings. Sorted sets give you leaderboards and time-ordered feeds with range queries. Hashes let you update one field of an object without rewriting the whole thing.\n\nSets handle membership and deduplication, lists work as simple queues, and streams add consumer groups and acknowledgements for real message processing. Picking the right one is often the difference between a single operation and a read-modify-write round trip.\n\nTreating Redis as a string cache is the most common way to leave performance on the table. A sorted set does ranking server-side; strings force you to fetch, sort in the application, and write back.","cache-failures":"Stampedes, avalanches and hot keys\n\nA stampede happens when a popular key expires and every concurrent request misses at once, all hitting the database together. The fix is a short lock so one request recomputes while others wait or serve stale.\n\nAn avalanche is the same thing at scale: many keys given identical TTLs expire simultaneously. Jitter the TTLs so expiry spreads out.\n\nA hot key is one entry so popular that a single Redis node becomes the bottleneck. Replicate it across nodes or add a small local in-process cache in front.\n\nThese are the failures that only appear under real traffic, which is why they are asked about. All three are caused by synchronised behaviour, and all three are fixed by deliberately desynchronising it.","what-not-to-cache":"What not to cache\n\nCaching adds a second source of truth and a new class of bug. It earns that when reads dominate, the data tolerates staleness, and recomputation is genuinely expensive.\n\nData that changes on nearly every read gains nothing, you pay the write cost and still miss, and per-user data with no reuse usually lands there too. Anything where stale means wrong, such as permissions or balances, should be read from the source, or cached with very tight bounds and explicit invalidation.\n\nThe honest question is not what to cache but what staleness is acceptable for. If the answer is none, caching is the wrong tool and the fix is a faster query or a better index.","lb-algorithms":"Round robin, least connections, hashing\n\nRound robin sends each request to the next server. Fine when requests cost roughly the same, poor when they do not.\n\nLeast connections routes to whichever server is handling fewest requests, which copes far better with uneven work.\n\nConsistent hashing sends the same key to the same server, which matters when servers hold local state or a cache.\n\nThe choice follows from what your requests look like. Uniform and stateless, and round robin is enough. Wildly variable durations, and it is least connections. Anything cached or held per server, and it is hashing, accepting that you have just made your traffic distribution depend on your key distribution.\n\nRound robin is the default and is wrong whenever request cost varies wildly, one slow endpoint drags a server down while the balancer keeps feeding it work.","consistent-hashing":"Consistent hashing\n\nHashing a key modulo the number of servers works until that number changes. Add one server and almost every key maps somewhere new, which invalidates every cache at once.\n\nConsistent hashing places servers and keys on a ring, so adding or removing a node only moves the keys between it and its neighbour, roughly one over n of the total. Virtual nodes spread each physical server across many ring positions, which evens out a distribution that would otherwise be lumpy.\n\nThis is the technique that makes distributed caches and sharded stores survivable. Without it, scaling the cluster is an outage.","health-checks":"Health checks and draining\n\nA shallow health check confirms the process is alive. A deep one confirms it can reach its dependencies.\n\nDeep checks catch more, and can take an entire fleet out when a shared dependency wobbles. The failure is correlated by construction: every node checks the same database, so every node fails the check in the same second.\n\nThe usual compromise is a deep check that degrades instead of failing. Report unhealthy only after several consecutive failures, and never let a dependency the request path does not need mark you down.\n\nConnection draining then lets a server finish its in-flight requests before it leaves the pool, so a deploy does not drop live traffic. Keep readiness and liveness separate while you are there, not ready yet and needs restarting call for very different responses.\n\nMaking the health check depend on the database means a brief database blip marks every server unhealthy simultaneously, turning a degraded system into a total outage.","why-queues":"What a queue actually buys you\n\nA queue lets a request return before the work is done. The user gets a fast response, and the work happens behind them. It also absorbs spikes: a burst that would overwhelm a synchronous system becomes a backlog that drains at whatever rate the workers manage.\n\nThe cost is that the system is now eventually consistent, and you owe the user a way to see the outcome.\n\nAdding a queue converts a latency problem into a state problem. That is usually a good trade for email, image processing or reports, and a bad one for anything the user is waiting to see.","delivery-guarantees":"At-most-once, at-least-once, exactly-once\n\nAt-most-once may drop messages. At-least-once may deliver twice. Exactly-once is what everyone wants, and end to end in a distributed system it is not achievable.\n\nWhat gets sold as exactly-once is at-least-once delivery plus idempotent processing, so a duplicate has no additional effect. That is a real and reachable goal, and it puts the responsibility in the consumer, not the broker, which is why buying a broker does not buy it for you.\n\nSelecting a broker for its exactly-once badge and skipping idempotency is the classic mistake. The guarantee applies within the broker, not across your side effects, a duplicate email has already been sent.","ordering":"Ordering and partitions\n\nOrdering guarantees are usually per partition, not global. Kafka orders within a partition; across partitions there is no order at all.\n\nKeying by entity, user id, order id, puts all of that entity's events in one partition and preserves their order relative to each other, which is almost always the ordering you actually needed. Global ordering means one partition, which means one consumer, which means no parallelism. That is the trade.\n\nWanting strict global ordering usually means the design is wrong. Per-entity ordering is almost always what is actually needed, and it parallelises.","dead-letter":"Retries and dead letter queues\n\nA message that fails is usually retried. Without a limit, a permanently broken message is retried forever, consuming capacity and burying real work.\n\nA dead letter queue holds messages that failed repeatedly, so the main queue keeps flowing and failures can be inspected.\n\nRetries should back off exponentially with jitter, or every consumer retries in lockstep and hammers the failing dependency together.\n\nAn unmonitored dead letter queue is the same as dropping messages, just slower. The queue is only useful if someone is alerted when it fills.","read-replicas":"Read replicas and replication lag\n\nA read replica copies the primary and serves reads, which scales read capacity. Writes still go to one place.\n\nReplication is asynchronous by default, so a replica is always slightly behind, usually milliseconds, occasionally a great deal more under load.\n\nThat gap causes read-your-own-writes bugs. A user saves something, the next read lands on a replica, and they see the old value and conclude it did not save.\n\nThe fix is not stronger consistency but a narrower rule: after a user writes, pin that user's reads to the primary for a few seconds. It costs almost nothing and it removes the only staleness anybody actually notices.\n\nThe standard fix is to route a user's reads to the primary briefly after they write. Making all reads go to the primary defeats the point of having replicas at all.","sharding":"Sharding and choosing a key\n\nSharding splits data across databases so writes scale. The shard key decides which shard holds a row, and it is the hardest decision to reverse.\n\nA poor key creates hotspots. Sharding by country puts most traffic on one shard; sharding by timestamp puts every current write on the newest one.\n\nQueries that span shards get slow and complicated, so the key has to match how the data is read, not how it is naturally grouped.\n\nResharding later means moving live data while still serving traffic from it. That is why the honest order is replicas, caching, better indexes and a bigger machine first, all of which are reversible, and none of which this is.\n\nShard last. Read replicas, caching, better indexes and a bigger instance all come first, because they are reversible. Sharding changes your data model permanently.","cap":"CAP, stated usefully\n\nWhen a network partition splits your system, you either refuse requests to stay consistent, or answer them and risk divergence. That is the whole choice.\n\nPartitions are not optional, so the real question is what to do during one, not whether to sacrifice partition tolerance.\n\nIt is rarely uniform within one company. A core ledger chooses consistency and refuses; the ATM in the lobby chooses availability, dispenses anyway, and reconciles later with an overdraft fee, which is Brewer's own illustration of the trade.\n\n'We chose AP' is meaningless without saying what happens to conflicting writes afterwards. The interesting engineering is the reconciliation, not the letter.","eventual-consistency":"Eventual consistency in the interface\n\nEventual consistency means replicas converge given no new writes. It says nothing about how long that takes, and users notice the gap.\n\nThe engineering work is mostly in the interface: show the pending state, use optimistic updates, and do not pretend an action is complete when it is merely queued. Read-your-own-writes is the guarantee users actually care about, and it is far cheaper than full consistency.\n\nMost consistency complaints are interface problems, not database problems. Showing 'processing' honestly costs nothing and removes the perception of a bug.","rate-limiting":"Token bucket and sliding window\n\nA fixed window counter is simple, and it allows double the limit across a boundary: a full quota at the end of one window, another full quota at the start of the next.\n\nA sliding window smooths that by weighting the previous window; a token bucket refills at a steady rate and permits bursts up to the bucket size. Token bucket usually fits an API best, because real traffic is bursty and a strictly even rate feels broken to whoever is using it.\n\nChoose based on whether bursts are acceptable. Token bucket permits them deliberately; sliding window suppresses them. Fixed window is simplest and has a known flaw at the edges.","circuit-breakers":"Circuit breakers and timeouts\n\nA slow dependency is worse than a dead one: callers pile up waiting, threads and connections are consumed, and the failure spreads upstream.\n\nA circuit breaker trips after repeated failures and fails fast for a while, then lets a trial request through to test recovery.\n\nEvery network call needs a timeout. A missing timeout means waiting indefinitely, which is how one slow service takes down everything that calls it.\n\nRetries without a breaker amplify an outage, a struggling service gets more traffic exactly when it is least able to serve it. The breaker is what stops retries becoming an attack on your own system.","graceful-degradation":"Graceful degradation\n\nNot every dependency is essential. If recommendations are down, the product page should still render without them.\n\nThat requires deciding in advance which features are optional and what each fallback is, cached data, a default, or simply hiding the section. The alternative is that any dependency failure becomes a total failure, which is a design decision made by omission.\n\nThis is the difference between an outage and a degraded experience most users never notice. It costs almost nothing at design time and is expensive to retrofit.","logs-metrics-traces":"Logs, metrics and traces\n\nLogs record discrete events with detail. Metrics are aggregated numbers over time, cheap to store and query. Traces follow one request across services.\n\nMetrics tell you something is wrong. Traces tell you where. Logs tell you why.\n\nUsing only logs means expensive storage and slow answers to simple questions; using only metrics means knowing something broke without knowing what.\n\nThe three are complementary and priced very differently. Logging everything at high volume is a large bill for questions a metric answers instantly.","percentiles":"Percentiles, not averages\n\nAn average hides the tail. A system averaging 100ms can still be taking five seconds on one request in a hundred, and p50 describes the typical experience while p95 and p99 describe the worst of it.\n\nThe tail is where users churn, and it is invisible in the mean. One caution while you are moving to percentiles: averaging them across servers is meaningless. A percentile has to be computed over the whole population, not averaged out of per-host summaries.\n\nReporting p99 instead of the mean is a small change that surfaces problems users complain about but dashboards do not show. It also makes capacity conversations honest.","slo":"SLIs, SLOs and error budgets\n\nAn SLI is a measurement, such as the fraction of requests served under 300ms. An SLO is the target for it. The gap between that target and 100 percent is the error budget.\n\nThe budget turns reliability into a resource. Spend it shipping quickly; when it runs out, stop shipping and fix stability. That replaces the argument about whether features or reliability come first with a number both sides already agreed to.\n\nChasing 100 percent is the wrong target: each extra nine costs disproportionately more, and perfect reliability means you shipped too slowly. The budget makes the trade explicit instead of political.","rest":"REST and resource modelling\n\nREST models the system as resources addressed by URL, with HTTP verbs describing the action and status codes describing the outcome.\n\nThe value is that everything already understands it, caches, proxies, browsers and load balancers all act correctly on a GET without being told anything about your application. Most APIs called RESTful are really HTTP-with-JSON, which is fine; the parts worth keeping are correct verbs, correct status codes, and cacheable GETs.\n\nSticking to standard verbs and codes is not pedantry: it is what lets a CDN cache your GET, a proxy retry safely, and a client library behave sensibly without custom logic.","graphql":"GraphQL and its costs\n\nGraphQL lets the client ask for exactly the fields it needs in one request, which solves over-fetching and the round trips of a chatty REST API.\n\nIt moves the complexity to the server. Arbitrary client queries can be arbitrarily expensive, so you need depth limits, cost analysis and dataloader batching to avoid N+1 database calls, and HTTP caching largely stops working, because every query is a POST to a single endpoint.\n\nGraphQL earns its keep with many clients that need different shapes of the same data, a mobile app and a web app, say. For a single first-party client it is usually complexity without payoff.","grpc":"gRPC and binary protocols\n\ngRPC uses Protocol Buffers over HTTP/2: a compact binary encoding with a schema, generated clients, and streaming in both directions.\n\nIt is markedly faster and smaller on the wire than JSON, and the schema makes breaking changes visible at build time. It is also awkward from a browser without a proxy, and much harder to debug by hand, because you cannot read it.\n\nThe usual split is gRPC between your own services, where performance and schemas matter, and REST or GraphQL at the public edge, where reach and debuggability matter more.","webhooks":"Webhooks and delivery\n\nA webhook inverts the direction. Instead of clients polling you, you call them when something happens, which removes the polling load and cuts the latency.\n\nYou now own a delivery problem. Receivers go down, respond slowly, or fail intermittently, so you need retries with backoff, a dead letter path, and visibility into what is failing.\n\nReceivers need to verify the payload came from you: an HMAC signature over the body with a shared secret, plus a timestamp so an old delivery cannot be replayed.\n\nOrder is the part people forget. Retries mean a later event can arrive before an earlier one, so the payload should carry a sequence number or a timestamp, and the receiver should be willing to ignore anything older than what it already holds.\n\nSending a webhook is easy; delivering reliably is the actual product. Without signing, your webhook endpoint is an unauthenticated write API for anyone who learns the URL.","versioning":"Versioning and breaking changes\n\nAdditive changes are safe: new optional fields, new endpoints. Removing a field, renaming one, or tightening validation breaks existing clients.\n\nURL versioning is explicit and easy to route. Header versioning keeps URLs stable. Either works, and consistency matters more than which you pick.\n\nThe hard part is retirement. Old versions live exactly as long as clients use them, so you need usage telemetry per version and a deprecation process with real deadlines.\n\nWithout that telemetry every version is maintained forever, because nobody can prove it is safe to remove. Measuring per-version usage is what turns deprecation into a decision rather than a hope.\n\nMost teams version and then never remove anything, so every version is maintained forever. Measuring usage per version is what makes deprecation possible at all.","push-options":"Polling, long polling, SSE, WebSockets\n\nPolling asks repeatedly on a timer. Simple, wasteful, and latency is bounded by whatever interval you chose.\n\nLong polling holds the request open until there is news, which cuts the waste but ties up a connection for the duration.\n\nServer-sent events are a one-way stream from server to client over plain HTTP, with automatic reconnection built into the browser.\n\nWebSockets give a persistent two-way channel, which is what you need when the client also sends frequently. Read the four as a cost ladder: each step buys lower latency and charges you in held connections and server-side state.\n\nSSE is underrated: if data only flows server to client, notifications, live prices, progress, it is far simpler than WebSockets and works through ordinary HTTP infrastructure.","scaling-connections":"Scaling persistent connections\n\nPersistent connections are stateful, which breaks the usual assumption that any server can handle any request. A message for a user must reach the exact server holding that user's socket.\n\nThe standard answer is a pub/sub layer: servers subscribe to channels, and a publish fans out to whichever server holds the connection.\n\nDeploys become disruptive, because restarting a server drops every connection it holds. Clients need reconnection with backoff, and ideally resume from a last-seen message id.\n\nThis is why chat and presence systems are hard. Statelessness is what makes normal web services easy to scale, and holding a socket throws it away.","presence":"Presence and typing indicators\n\nPresence looks trivial and is one of the most expensive features in a chat product. Every state change potentially notifies everyone who can see that user, which is quadratic in the worst case.\n\nThe data is also worthless within seconds, so it lives in memory behind short TTLs rather than being persisted. Typing indicators get throttled hard and dropped under load, because they are the first thing worth sacrificing.\n\nPresence is the standard example of a feature whose cost is invisible in the spec. Recognising it as a fan-out problem rather than a storage problem is the insight being tested.","inverted-index":"The inverted index\n\nA normal index maps a row to its values. An inverted index maps each term to the list of documents containing it, which is what makes full-text search fast.\n\nText is normalised first: lowercased, split into tokens, stemmed so 'running' matches 'run', and stripped of stop words.\n\nA LIKE '%term%' query cannot use a B-tree index at all, so it scans every row. That is fine at ten thousand rows and hopeless at ten million.\n\nPostgres will do this for you with a GIN index over a tsvector, which is usually the right first move. A separate search cluster is a second datastore to keep in sync, and that cost only starts paying once you need ranking, faceting, or a scale the database cannot reach.\n\nThis is why search moves to a dedicated engine. It is not that the database is slow; it is that the data structure required for text search is a different one.","relevance":"Relevance and ranking\n\nMatching is the easy half. Ranking decides which of ten thousand matches appear first, and that is what users judge.\n\nTF-IDF and BM25 weight terms by how often they appear in a document against how common they are overall, so rare terms count for more.\n\nReal systems blend text relevance with business signals: recency, popularity, stock, personalisation. Tuning that blend is ongoing work, not a one-off.\n\nTreating search as a matching problem produces technically correct results that feel broken. The measurable target is click-through and successful sessions, not recall.","search-sync":"Keeping the index in sync\n\nThe search index is a second copy of the data, so it can drift.\n\nDual writes, writing to the database and the index in the same request, fail the moment one succeeds and the other does not, and nothing about that failure is visible until somebody searches for the missing thing.\n\nThe reliable pattern is to write to the database and derive the index from its change log, so the database stays the single source of truth and the index is always a function of it.\n\nYou also need a full reindex path. Mappings change, drift accumulates, and eventually the only honest fix is to rebuild into a fresh index and swap the alias over.\n\nChange data capture beats dual writes because it has one commit point. With dual writes there is always a window where a crash leaves the two permanently inconsistent.","object-storage":"Object storage versus databases and disks\n\nObject storage such as S3 holds immutable blobs addressed by key, with effectively unlimited capacity and a very low cost per gigabyte.\n\nPutting files in a database instead inflates backups, slows replication, and spends expensive storage on data nothing will ever query. The standard shape is the file in object storage and its metadata, key, size, owner, content type, in the database.\n\nThe rule is to store what you query in the database and what you serve in object storage. Blobs in Postgres make every operational task on that database slower forever.","presigned-uploads":"Direct uploads with pre-signed URLs\n\nRouting uploads through your API means large files consume your bandwidth, your memory and your request timeouts, to nobody's benefit.\n\nA pre-signed URL lets your server authorise the upload and hand the client a time-limited URL to send the bytes straight to object storage. The server hears about completion from an event, and validates type and size before treating the object as real.\n\nThis removes an entire scaling problem rather than solving it. Your API stays small and fast because the large payloads never touch it.","video-delivery":"Video: transcoding and adaptive bitrate\n\nAn uploaded video is transcoded into several resolutions and bitrates, then split into short segments with a manifest listing what is available.\n\nThe player measures throughput and switches renditions per segment, so a failing connection degrades quality instead of stalling. This is HLS or DASH, and it is why streaming survives a train tunnel when a single MP4 does not.\n\nAdaptive bitrate exists because bandwidth is variable and unpredictable. Serving one file forces a choice between buffering for slow connections and wasting quality on fast ones.","authn-authz":"Authentication and authorisation\n\nAuthentication establishes who you are. Authorisation decides what you may do. They fail differently, and they are constantly confused in code.\n\nMost serious access-control bugs are authorisation bugs. The user is correctly identified, and the system simply never checks whether this user may touch this particular record.\n\nThe reason is structural. Authentication happens once, in one place, so it is hard to forget. Authorisation happens on every request against every resource, so it is easy to miss exactly one.\n\nWhich is why the durable fix is to check ownership where the data is fetched rather than in each handler. A query that cannot return another tenant's row is safer than a hundred handlers that each have to remember to ask.\n\nInsecure direct object reference, changing an id in a URL and seeing someone else's data, is consistently among the most common real vulnerabilities, and it is purely a missing authorisation check.","sessions-vs-jwt":"Sessions or JWTs\n\nA session id is a reference. The server holds the state, can revoke it instantly, and pays a lookup on every request.\n\nA JWT carries its claims and a signature, so it validates without a lookup, and that is exactly why it cannot be revoked before it expires.\n\nThe usual compromise is short-lived access tokens alongside longer-lived refresh tokens that can be revoked.\n\nWorth saying plainly: for one application talking to a database it already has open, a session is simpler and better. JWTs earn their keep when the validating service cannot reach your session store, across services, across companies, or at an edge with no database at all.\n\nJWTs are frequently chosen for statelessness and then paired with a revocation list, which reintroduces the lookup and leaves you with the drawbacks of both.","oauth":"OAuth and OpenID Connect\n\nOAuth is delegated authorisation: it lets an application act on a user's behalf without holding their password. It is not a login protocol.\n\nOpenID Connect is the layer on top that adds identity, returning an ID token describing who the user is.\n\n'Sign in with Google' is OpenID Connect. Using raw OAuth for login means inferring identity from an access token, which is the source of several classic vulnerabilities.\n\nConflating the two is the common error. If you need to know who the user is, you want OIDC's ID token, not an access token that merely proves you may call an API.","secrets":"Secrets and encryption\n\nSecrets belong in a manager with rotation and an audit trail, not in environment variables committed to a repository or baked into an image.\n\nPasswords are a separate problem. They are hashed with a slow algorithm built for the job, bcrypt, scrypt or Argon2, never a fast general-purpose hash like SHA-256, which is brute-forced trivially.\n\nEncryption in transit is table stakes, and essentially free.\n\nEncryption at rest is worth less than people assume. It defends against a stolen disk and it satisfies an auditor. It does nothing against an attacker holding application credentials, which is how the data usually leaves.\n\nUsing SHA-256 for passwords is fast, which is precisely the flaw: an attacker with the hashes can try billions per second. The slowness of bcrypt is the feature.","leader-election":"Leader election\n\nMany systems need exactly one node doing something, running a scheduled job, accepting writes, coordinating a cluster. Leader election picks that node and replaces it when it dies.\n\nDoing it correctly is subtle, and two nodes both believing they lead is a split brain that corrupts data quietly. Almost nobody should implement this themselves: use etcd, ZooKeeper, or a database lease with fencing tokens.\n\nThe naive version, a lock row with a timeout, fails when the leader pauses for garbage collection, wakes up believing it still holds the lock, and writes over the new leader. Fencing tokens exist to reject those late writes.","consensus":"Consensus: Raft and Paxos\n\nConsensus is getting a group of nodes to agree on a value despite failures. Raft and Paxos are the standard algorithms; Raft is deliberately easier to understand.\n\nThey work by majority quorum, which is why clusters are sized 3 or 5. A majority must agree, so the cluster survives losing fewer than half its members.\n\nEvery write costs a round trip to a quorum, so consensus is correctness bought with latency.\n\nThis is why you size clusters odd and why cross-region consensus is painful: a quorum spanning continents pays the worst inter-region latency on every write.","saga":"Distributed transactions and sagas\n\nA transaction across several services cannot use a database transaction. Two-phase commit exists but blocks when the coordinator fails, so it is rare in practice.\n\nA saga breaks the work into local transactions, each with a compensating action that undoes it. Book the flight, book the hotel, and if the hotel fails, cancel the flight.\n\nCompensation is not rollback: the intermediate state was visible, and undoing may be impossible for actions like sending an email.\n\nThe design work is deciding what compensation means for each step. Some actions genuinely cannot be undone, which forces you to order the saga so the irreversible steps come last.","unique-ids":"Generating unique ids at scale\n\nAuto-increment ids need a single coordinator, which becomes a bottleneck and does not survive sharding.\n\nRandom UUIDs need no coordination but are large and, being random, scatter B-tree inserts across the index, which hurts write performance badly.\n\nSnowflake-style ids pack a timestamp, a machine id and a counter into 64 bits: unique without coordination, and roughly time-ordered so inserts stay sequential.\n\nTime ordering is the underrated property. Random UUIDs as a primary key cause page splits across the whole index; UUIDv7 and Snowflake ids keep inserts near the end where they belong.","oltp-olap":"OLTP and OLAP\n\nTransactional databases are tuned for many small reads and writes of individual rows, and store data row by row. Analytical stores are tuned for scanning a few columns across billions of rows, and store it column by column, so the columns you did not ask for cost nothing.\n\nRunning heavy analytics on your production database puts them in competition for the same resources that are serving users. That is how a dashboard causes an outage.\n\nColumnar storage is the reason a warehouse scans a billion rows in seconds. It is a different physical layout, not just a bigger machine.","batch-vs-stream":"Batch and streaming\n\nBatch processes bounded chunks on a schedule. It is simpler, easy to reason about, cheap to re-run, and results are as old as the last run.\n\nStreaming processes events as they arrive, giving low latency at the cost of handling late and out-of-order data, and windowing.\n\nMost organisations need batch and think they need streaming. The question is whether a decision is actually made on fresher data.\n\nStreaming is meaningfully harder to operate and debug. It is worth it when freshness changes an outcome, fraud, pricing, alerting, and rarely worth it for dashboards read each morning.","cdc":"Change data capture\n\nCDC reads the database's own replication log and turns committed changes into a stream of events. It captures every change, in commit order, without the application doing anything at all.\n\nCompared with polling for updated rows it misses nothing, it catches deletes, and it adds no query load. That is what makes it the standard way to feed search indexes, caches, warehouses and downstream services from one source of truth.\n\nCDC solves the dual-write problem structurally: there is one commit, and everything downstream derives from it. That is why it beats having the application publish events alongside its writes.","what-docker-solves":"What Docker actually solves\n\nA container packages your application with its dependencies and a filesystem, sharing the host kernel. It starts in milliseconds because there is no operating system to boot.\n\nThe problem it solves is environment drift: the same image runs identically on a laptop, in CI and in production, because the image is the environment. A virtual machine gives stronger isolation by running its own kernel, at the cost of size and of startup time.\n\nContainers are about reproducibility first and density second. If your deployment already produces an identical artefact, a single Go binary, a Lambda zip, the reproducibility argument is weaker and containers buy you less.","docker-vs-k8s":"Docker or Kubernetes, and why\n\nDocker builds and runs containers. Kubernetes schedules them across many machines, restarting failures, rolling out new versions, scaling on demand, routing traffic to healthy instances.\n\nThey are not alternatives. The real question is whether you need an orchestrator at all, and for most products the honest answer is no.\n\nKubernetes brings genuine operational weight: networking, ingress, RBAC, upgrades, and a large surface to secure and to debug.\n\nThe middle option is the one people skip. A managed container platform, Cloud Run, ECS Fargate, App Runner, takes your image and runs it with autoscaling and rollouts while handing you almost none of the cluster. That covers a great many systems that reached for Kubernetes instead.\n\nReach for Kubernetes when you have many services with different scaling profiles, need bin-packing across a fleet, or are standardising across teams. For a handful of services, a managed container platform, ECS Fargate, Cloud Run, App Runner, gives most of the benefit with a fraction of the operational cost. Choosing Kubernetes for one service is buying a scheduling problem you did not have.","image-hygiene":"Image size, layers and security\n\nImages are layered and cached. Ordering a Dockerfile so dependencies install before source is copied means a code change rebuilds one layer instead of everything.\n\nMulti-stage builds compile in a full toolchain image and copy only the artefact into a minimal runtime, cutting hundreds of megabytes.\n\nSmaller images are also a smaller attack surface. A distroless or Alpine base contains far fewer packages that can carry a CVE.\n\nLayer ordering is the highest-leverage build optimisation available and costs nothing. Copying source before installing dependencies invalidates the dependency cache on every commit.","pipeline":"What a pipeline should do\n\nContinuous integration runs the checks, build, tests, lint, type check, on every change, so breakage surfaces in minutes, not at release. Continuous delivery keeps every passing commit deployable; continuous deployment goes further and ships it automatically.\n\nThe value is proportional to speed. A pipeline taking forty minutes has stopped being feedback and become something people work around.\n\nPipeline duration is a product decision, not an infrastructure detail. Past roughly ten minutes people stop waiting, start batching changes, and the benefit of small deploys disappears.","deploy-strategies":"Blue-green, canary and rolling\n\nRolling replaces instances gradually. Simple and cheap, but both versions run at once, so the change must be backwards compatible.\n\nBlue-green runs a complete second environment and switches traffic in one step, which makes rollback instant at the cost of double the infrastructure.\n\nCanary sends a small percentage to the new version and watches error rates before proceeding, which limits blast radius to the sampled traffic.\n\nCanary is the strongest default because it fails small: problems surface on one percent of traffic, not on all of it. It requires metrics good enough to make the go or no-go call automatically.","migrations":"Database migrations without downtime\n\nSchema changes and code deploys are not atomic, so during any deploy the old code may run against the new schema, or the reverse.\n\nThe expand-and-contract pattern handles this: add the new column, write to both, backfill, switch reads, and drop the old column in a later release.\n\nRenaming or dropping a column in the same release as the code change guarantees a window where one of the two is broken.\n\nWatch the lock as well. Adding a nullable column is instant on a modern Postgres; changing a type, or building an index the ordinary way, takes a lock that queues every other query behind it. CREATE INDEX CONCURRENTLY exists precisely because the obvious version takes the table down.\n\nThis is the most common cause of self-inflicted deploy outages. Splitting a rename into three deploys feels slow and is the only version that stays up.","feature-flags":"Feature flags\n\nA flag separates deploying code from releasing behaviour. Code ships dark, is enabled for a cohort, then for everyone, which turns a rollback into a configuration change, not a redeploy, and that is a great deal faster during an incident.\n\nFlags accumulate. Every one is a branch in the code, and stale flags become permanent complexity, so removing them has to be part of the process, not something everyone means to get to.\n\nFlags are how you deploy on Friday safely. The discipline that makes them work is deleting them: a codebase with two hundred live flags has an untestable number of behaviour combinations.","core-services":"The core services, by concept\n\nThe three major clouds offer the same primitives under different names: compute, object storage, managed relational databases, queues, a CDN. AWS has the broadest catalogue and the most third-party support; GCP is strong on data and Kubernetes; Azure wins where an organisation already runs Microsoft identity and licensing.\n\nLearn the concepts, not the product names. An interviewer asking about object storage does not care whether you say S3, GCS or Blob Storage.\n\nChoosing a cloud is usually decided by existing commitments, team familiarity and pricing agreements, and seldom on technical superiority. The technical differences matter far less than the migration cost of being wrong.","right-sizing":"Right-sizing: build for the traffic you have\n\nTen thousand visitors a month is roughly four requests a minute at peak. A single small server, or a static site on a CDN, handles that without noticing. Multi-region clusters, autoscaling groups and service meshes answer problems that begin several orders of magnitude higher.\n\nOver-engineering costs money, and the larger cost is complexity, more moving parts to operate, debug and secure, while the product is still unproven.\n\nThe right architecture for an unvalidated product is the one you can change quickly. Premature scale locks in decisions before you know the access patterns, and those are the expensive ones to reverse.","cost-drivers":"Where cloud bills actually come from\n\nEgress bandwidth is the most commonly underestimated line. Data leaving the cloud is charged, and serving media directly from object storage is expensive compared with a CDN in front.\n\nIdle provisioned capacity is the next: instances and databases sized for a peak that happens twice a year, running at that size continuously.\n\nManaged services trade money for operational time. That is usually a good trade for a small team and a bad one at very large scale, which is why big companies eventually build their own.\n\nNetflix building Open Connect and Dropbox moving off S3 are both the same calculation: past a certain volume, the margin a provider charges exceeds the cost of doing it yourself. Below that volume, managed wins easily.","serverless":"Serverless, containers or VMs\n\nServerless functions scale to zero and cost nothing when idle, which suits spiky or low-volume workloads. Cold starts add latency, and long-running work does not fit.\n\nContainers on a managed platform give steady performance and no cold starts, at the cost of paying for idle capacity.\n\nVMs give the most control and the most operational responsibility, and remain the right answer for anything with unusual system requirements.\n\nThe deciding factor is usually traffic shape, not preference. Spiky and low-volume favours serverless; steady and predictable favours containers, where always-on capacity is cheaper than per-invocation pricing.","test-pyramid":"The testing pyramid\n\nUnit tests are fast, numerous, and isolate one piece of logic. Integration tests check that components work together. End-to-end tests drive the real system through its interface.\n\nThe pyramid says have many of the first and few of the last, because cost and flakiness both rise with scope. An inverted pyramid, mostly end-to-end, produces a suite that is slow, flaky, and eventually ignored by everyone.\n\nThe shape follows from feedback speed. A failing unit test names the broken function; a failing end-to-end test tells you something in the system is wrong, and you still have to find it.","coverage":"Coverage and what it does not tell you\n\nCoverage measures which lines executed during tests. It says nothing about whether anything was asserted, or whether the assertions mean anything.\n\nA test that calls a function and checks nothing produces full coverage of that function and catches no regression whatever. Which is why mandating a coverage percentage reliably produces tests written to satisfy the number rather than to catch failures.\n\nCoverage is useful as a discovery tool, finding untested areas, and harmful as a target, because it is trivially gameable and becomes the goal instead of correctness.","flaky-tests":"Flaky tests\n\nA flaky test passes and fails without the code changing, usually from timing, shared state, ordering assumptions or real network calls.\n\nThe damage is cultural rather than technical: once a suite is known to fail randomly, real failures get re-run instead of investigated.\n\nQuarantine flaky tests immediately so the signal stays trustworthy, then fix or delete them. A test nobody believes is worse than no test.\n\nTolerating flakiness destroys the value of the entire suite, not just the flaky test. The correct response is urgent, because trust does not degrade gracefully.","waterfall":"Waterfall\n\nWaterfall runs in sequence: gather requirements, design, build, test, release. Each stage completes before the next begins, and the client sees the result at the end.\n\nIt works when requirements genuinely cannot change, regulated work, physical manufacturing, fixed-scope contracts, and where the cost of change late is enormous.\n\nIt fails for software because requirements are discovered by using the thing, and waterfall defers that discovery to the point where change is most expensive.\n\nThe cost-of-change curve is the whole argument. If change is cheap and information arrives during building, front-loading every decision is the wrong bet.","agile":"Agile, scrum and kanban\n\nAgile is a set of preferences: working software over documentation, responding to change over following a plan, short cycles with real feedback.\n\nScrum implements that with fixed sprints, defined roles and ceremonies. Kanban drops sprints and limits work in progress instead, pulling the next item as capacity frees. Kanban suits interrupt-driven work such as support; scrum suits planned feature work with a stable team.\n\nBoth fail the same way: adopting the ceremonies without the feedback. Standups and sprints with no working software to show and no willingness to change the plan is waterfall with extra meetings.","code-review":"Code review\n\nReview catches defects, but its larger effects are spreading context across the team and keeping the codebase coherent.\n\nSmall pull requests get real review. Large ones get approved, because nobody can hold two thousand lines in their head.\n\nSeparating blocking concerns from suggestions makes review faster and less adversarial. Say which comments must be addressed and which are taste.\n\nThe most useful comment names the case that breaks rather than the preference that was violated. 'This is null when the user has never logged in' can be acted on. 'I would extract this' starts an argument about style with no way to settle it.\n\nPR size is the single biggest lever on review quality. Splitting work into reviewable pieces is a design skill, and the reason large PRs get rubber-stamped is capacity, not laziness.","incidents":"Incidents and blameless postmortems\n\nDuring an incident, restore service first and investigate afterwards. Mitigation and diagnosis compete, and users care about the first.\n\nA postmortem asks what made the failure possible, not who typed the command. Systems that fail when one person errs are the finding.\n\nBlameless does not mean consequence-free. It means the output is a change to the system rather than a name.\n\nBlame produces hidden incidents. If reporting a mistake is punished, people stop reporting, and you lose the information that prevents recurrence.","coupling":"Coupling and cohesion\n\nCoupling is how much modules depend on each other. Cohesion is how related the contents of one module are. The target is low coupling and high cohesion.\n\nTight coupling means a change in one place forces a change in another, and it is why some codebases resist every modification. Splitting by layer, all controllers together, all models together, usually produces low cohesion, because a single feature ends up spread across every folder you have.\n\nOrganising by feature rather than by technical layer usually raises cohesion: everything that changes together lives together, so a change touches one directory.","premature-abstraction":"Premature abstraction\n\nAbstracting after one use is a guess at what varies. The guess is usually wrong, and the abstraction then obstructs the very change it was built to accommodate.\n\nDuplication is cheaper to fix than a wrong abstraction: you can see all the copies, whereas an abstraction hides the differences behind parameters. Wait for the third occurrence, when the actual axis of variation is finally visible.\n\nThe cost asymmetry is the point. Removing duplication later is mechanical; unwinding a wrong abstraction means untangling every caller that adapted to it.","tech-debt":"Technical debt as a decision\n\nDeliberate debt is a shortcut taken knowingly to hit a date, recorded with a plan to repay. That is a legitimate engineering trade.\n\nAccidental debt is what accumulates from not knowing better, and it is not really debt so much as damage.\n\nThe interest is real: every future change in that area costs more, and the rate compounds as more code depends on the shortcut.\n\nThe distinction matters because only deliberate debt can be argued for. 'We chose this and here is the repayment plan' is a decision; 'the code is messy' is a complaint.","netflix-cdn":"Open Connect: their own CDN\n\nNetflix is a large share of internet traffic at peak. Buying that from a commercial CDN would be enormously expensive and still not fast enough.\n\nSo they build Open Connect appliances and give them to ISPs free, installed inside the ISP's own network, the video is already past the congested peering links before your request happens. Popular titles are pushed to those boxes overnight, during off-peak hours, predicted per region.\n\nThe insight is that streaming is a predictable, cacheable workload. You know tomorrow's popular titles, so you can pre-position the bytes and turn a bandwidth problem into a storage problem, storage being far cheaper.","netflix-microservices":"Microservices and the failure budget\n\nThe Netflix backend is hundreds of services. A single home page assembles rows from many of them, so the probability that all are healthy at once is low.\n\nThe system is therefore designed to render with whatever is available: if personalisation fails, show a sensible default row instead of an error.\n\nChaos engineering came from here. Terminating instances deliberately in production is how you find out whether the fallbacks actually work.\n\nAt that number of dependencies, treating any failure as fatal guarantees constant outages. Fallbacks are not a nicety, they are what makes the architecture viable at all.","netflix-caching":"EVCache and cache invalidation across regions\n\nEVCache is Netflix's distributed cache layer, built on memcached, replicated within a region and across availability zones.\n\nRather than coordinating precise global invalidation, they lean on short TTLs and versioned keys: a write bumps a version, and old entries simply become unreachable and expire. Cross-region coordinated deletes would be slower, more fragile, and would fail exactly when the network was already degraded.\n\nThis is the practical answer to cache invalidation at global scale: make staleness bounded and self-healing instead of trying to make it zero. A delete that must succeed everywhere is a distributed transaction in disguise.","uber-geo":"Geospatial indexing with H3\n\nFinding nearby drivers by comparing latitude and longitude means scanning everything, because neither coordinate on its own narrows the search usefully.\n\nUber divides the world into hexagonal cells, their H3 library, so a location becomes a cell id, and finding nearby drivers becomes looking up a handful of ids. Hexagons rather than squares because every neighbour is equidistant, which makes expanding the search ring uniform.\n\nThe move is turning a continuous two-dimensional problem into a discrete key lookup. Once location is a key, ordinary tools, hash maps, caches, shards, all work again.","uber-surge":"Surge pricing as a control loop\n\nSurge is not primarily a revenue mechanism, it is a feedback loop balancing supply and demand within a geographic cell.\n\nWhen requests outnumber available drivers in a cell, the multiplier rises. That suppresses some demand and attracts drivers from neighbouring cells, and the imbalance closes.\n\nIt must be computed per small area and updated continuously, because conditions differ street by street and change within minutes.\n\nFraming it as a control system, not a pricing lever, explains the design: it needs fast feedback, small granularity, and damping so it does not oscillate.","uber-sharding":"Sharding by city\n\nTrips are overwhelmingly local. A rider and a driver are in the same city, and almost no query needs to join across cities.\n\nThat makes city, or region, an excellent shard key: traffic distributes naturally and cross-shard queries are rare. It also isolates failure and allows per-city configuration, which matters because regulation and pricing differ by market.\n\nA good shard key follows a natural boundary in the domain. City works because it matches how the data is actually queried; user id would scatter the two halves of every trip.","fanout":"Fan-out on write or on read\n\nFan-out on write pushes each post into every follower's precomputed timeline. Reads become a single fast lookup, and writes get expensive.\n\nFan-out on read builds the timeline when it is requested, by querying everyone you follow. Writes are cheap and reads are expensive.\n\nNeither survives the extremes. A million followers means a million inserts per post; following thousands of accounts means an enormous query on every refresh.\n\nWhich is why real systems run both. Fan out on write for ordinary accounts, leave the handful with millions of followers out of it entirely, and merge their posts in at read time. The cost then tracks the median user, not the most extreme one in the system.\n\nNeither works alone, which is the actual answer: fan out on write for ordinary accounts, and merge in celebrity posts at read time. The hybrid exists because the follower distribution is extremely skewed.","timeline-ranking":"Ranking a timeline\n\nReverse chronological is simple and predictable. Ranked feeds score each candidate on predicted engagement, recency, affinity and content type.\n\nRanking needs candidates first: retrieve a few hundred plausible posts cheaply, then score those expensively, because scoring everything is not affordable. That two-stage shape, cheap retrieval, expensive ranking, is how very nearly every recommendation system is built.\n\nCandidate generation then ranking is the general pattern worth carrying into any recommendation question. It bounds the expensive step regardless of corpus size.","chat-delivery":"Delivery, receipts and offline users\n\nA message is stored server-side until delivered, then usually deleted. The server is a relay with a queue attached, not an archive.\n\nThe three ticks, sent, delivered, read, are acknowledgements flowing back at each stage, each one a separate event. Offline users make this a queue-per-recipient problem, drained when the device finally reconnects.\n\nTreating chat as a per-recipient queue instead of a shared log is what makes offline delivery and multi-device sync tractable.","e2e":"End-to-end encryption and its consequences\n\nWith end-to-end encryption the server relays ciphertext it cannot read. Keys live on devices, and each conversation has its own session.\n\nThat removes entire categories of server-side feature: search across history, server-side spam classification on content, and web access without a linked device.\n\nMulti-device support becomes hard, because each device needs its own keys and its own copy of the session state.\n\nThis is the clearest example of a security decision constraining the product. Choosing E2E means accepting that the server cannot help with anything requiring message content.","url-shortener":"URL shortener\n\nThe core is a mapping from short key to long URL, read far more often than it is written, which makes it a caching problem more than a storage one.\n\nKeys can be generated by base62-encoding a counter, or by hashing and handling collisions. A counter gives short sequential keys and leaks your volume to anyone who looks; hashing does not.\n\nRedirects should be 301 or 302 deliberately. A 301 is cached by the browser, which is fast and makes click analytics impossible.\n\nThe scale is smaller than it looks. Seven base62 characters is about 3.5 trillion keys, and the hot set fits in memory on one machine. This is a question about which tradeoffs you notice, not about capacity.\n\nThe 301-versus-302 choice is the interesting decision. If you need per-click analytics you must use 302 and accept the traffic, because a cached 301 never reaches your server again.","ticket-booking":"Ticket booking and seat reservation\n\nThe defining constraint is that a seat must not be sold twice, under a load spike concentrated on a few popular events.\n\nSeats are held with a short-lived reservation, typically a few minutes, created atomically, so a user has exclusive claim while paying.\n\nExpired holds must be released reliably, which means a background reaper or a TTL, not just an application timer.\n\nThis is where optimistic concurrency stops working. Under contention for the same rows, optimistic retries mostly fail, so an explicit hold with a TTL is the correct model.","file-sync":"File sync, Dropbox style\n\nFiles are split into chunks, each hashed. Only chunks whose hash changed are uploaded, so editing one page of a large document transfers very little.\n\nIdentical chunks across users are stored once, which is deduplication and a large storage saving.\n\nConflicts happen when two devices edit while offline. The usual resolution is to keep both as a conflicted copy instead of silently picking a winner.\n\nContent-addressed chunking gives deduplication, delta sync and integrity checking from one idea. Preferring a conflicted copy over automatic merge is a deliberate choice: silent data loss is worse than a confusing filename.","framework":"A structure for the 45 minutes\n\nSpend the first five minutes on requirements. What must it do, what scale, what matters most, latency, consistency, cost. Write the numbers down.\n\nThen a rough estimate, a high-level design of five or six boxes, and depth on the two or three parts that are genuinely hard. Leave five minutes for bottlenecks and what you would change at ten times the load.\n\nCandidates who start drawing boxes immediately fail on scope, not knowledge. Establishing constraints first is what makes every later decision defensible instead of arbitrary.","estimation":"Back-of-envelope estimation\n\nA day is 86,400 seconds, near enough 100,000. A million daily events is roughly 12 per second; a billion is roughly 12,000 per second.\n\nPeak is commonly two to three times average, and traffic is rarely spread evenly through the day.\n\nStorage: a thousand bytes per record and a million records a day is a gigabyte a day, about a third of a terabyte a year.\n\nThe point is not precision, it is knowing whether you are designing for 10 requests per second or 100,000, because those are entirely different systems. Being off by 20 percent is fine; being off by a factor of a thousand is the failure.","tradeoffs":"Talking about tradeoffs\n\nEvery design decision costs something, and naming the cost is the strongest signal available, because it shows you have run a system, not read about one.\n\n'I would cache this' is weak. 'I would cache this with a 60-second TTL, accepting up to a minute of staleness because prices update hourly' is a decision. Volunteering the failure mode is better still: say what breaks under partition, under a hot key, or when the cache is cold.\n\nInterviewers are testing judgement, not recall. Anyone can name Redis; the differentiator is knowing when it is wrong and saying so unprompted.","senior-vs-staff":"Senior versus staff\n\nA senior engineer owns delivery of a complex system end to end: designs it, builds it, handles the failure modes, and is trusted without supervision.\n\nA staff engineer works across teams, on problems that are ambiguous before they are hard. Much of the work is deciding what should be built, and getting agreement.\n\nThe visible difference in interviews is scope: seniors discuss the system, staff discuss the system in the context of the organisation running it.\n\nPromotion to staff is rarely earned by writing more code. It comes from multiplying other people's output and choosing the right problem, which is why the interview probes influence and judgement more than depth alone.","behavioural":"Behavioural answers with substance\n\nStructure the answer: the situation, what you specifically did, and what happened. Vague collective 'we' answers are the most common weakness.\n\nInclude the trade you made and what you would do differently, certainty about a past decision reads as not having examined it. Have two or three real stories with numbers, including one where the outcome was poor and you took something concrete from it.\n\nThe failure story is the one that separates candidates. Everyone has one; the ones who can describe it precisely, without deflecting, are demonstrating exactly the self-assessment senior work requires.","coding-round":"The coding round\n\nRestate the problem and confirm the examples before writing anything. Clarifying an ambiguous constraint early is part of what is scored.\n\nSay the brute force, state its complexity, then improve. A working slow solution beats an unfinished clever one.\n\nTest with an edge case unprompted: empty input, one element, duplicates. Doing that without being asked is a strong signal.\n\nThe interview is a proxy for working with you. Silent brilliance scores worse than narrated competence, because collaboration is the thing actually being sampled.","the-common-pool":"The same thirty questions\n\nPublicly shared interview reports from Google, Meta, Amazon, Microsoft, Netflix, Uber, Stripe and Airbnb describe largely the same pool of twenty-five to thirty problems. Preparing for one company prepares you for most of them.\n\nThe recurring set: a news feed, a chat system, a video streaming service, a URL shortener, a rate limiter, a ride-hailing match, a payment flow, a notification system, a search or autocomplete, a file sync service, a ticket booking system, and a web crawler.\n\nThe variation between companies is less in the question than in what they press on afterwards.\n\nThis is why breadth beats memorising one company's list. Each question exercises two or three underlying decisions, fan-out, geospatial indexing, consistency under contention, and those decisions repeat across all of them.","by-company":"What each company tends to press on\n\nGoogle leans toward scale and data-intensive problems: crawlers, indexing, autocomplete, distributed storage. Expect questions about what happens at a billion of something.\n\nMeta favours social graph problems, news feed, friend recommendation, live comments, where fan-out and the celebrity case are the crux.\n\nAmazon presses on operational ownership and cost, in keeping with its leadership principles. Uber and Lyft go to geospatial matching and surge. Stripe and payment companies go to consistency, idempotency and exactly-once semantics under retries.\n\nKnowing the emphasis lets you steer the depth phase toward what that interviewer values. At a payments company, volunteering idempotency early is worth more than describing a CDN.","cost-and-ops":"Cost and operations are part of the answer\n\nThe questions have barely changed in years. What separates candidates has shifted toward the dimensions that used to be treated as extra credit.\n\nExpect to be asked what it costs to run, what happens at 3am when it breaks, who is paged, and how you roll it back. A design with no answer to those reads as unfinished.\n\nSome loops run without a whiteboard at all, asking you to design aloud and then revise the requirements on the spot.\n\nA technically correct design with no cost estimate and no failure story now reads as incomplete. Saying 'this runs about two hundred a month, and if the cache tier fails we serve stale for up to five minutes rather than going down' is the differentiator.","practice-plan":"A practice plan that works\n\nPick six problems spanning the underlying decisions rather than twenty that repeat the same one: a feed, a chat system, a ride match, a rate limiter, a video service, and a payment flow.\n\nDo each one out loud against a timer, because the constraint in the real thing is the clock and the talking, not the knowledge.\n\nThen redo one of them with a changed constraint, ten times the traffic, or a hard consistency requirement, and see whether your design bends or breaks.\n\nRedoing a problem under a changed constraint is the highest-value drill available. It rehearses the thing interviewers actually do, which is push on your design until something gives.","sql-injection":"Injection and parameterised queries\n\nInjection happens when input is concatenated into a command, SQL, a shell line, an LDAP filter, so the input can end the data and start being instruction.\n\nEscaping is the wrong fix, because it requires getting every context right forever. Parameterised queries send the statement and the values on separate channels, so the value can never be parsed as syntax.\n\nAn ORM is not automatic protection. Most expose a raw query escape hatch, and that is where injection reappears.\n\nParameterisation removes the class of bug rather than the instance. Escaping means every new call site is another chance to get it wrong, and one miss is enough.","xss":"Cross-site scripting\n\nXSS is injection into a page rather than a query: attacker-controlled text is rendered as markup, so their script runs with your origin's privileges, including the user's session.\n\nReact escapes interpolated values by default, which removes most of it. The holes are dangerouslySetInnerHTML, injecting into a href or src, and anything written into a script or style context.\n\nA Content Security Policy limits the damage when something does slip through, by refusing to execute inline or third-party script.\n\nFramework escaping handles the common path, so the remaining risk concentrates in the few places you deliberately bypass it. Those are worth auditing by name rather than trusting the framework globally.","csrf":"CSRF and SameSite\n\nCSRF abuses the fact that browsers attach cookies automatically. Another site submits a form to yours, the cookie rides along, and the request is authenticated even though the user never intended it.\n\nSameSite=Lax on session cookies stops the cross-site case for form posts, and is the default in current browsers. A synchroniser token is the belt-and-braces version.\n\nAuthorization headers are not attached automatically, so token-in-header APIs are not exposed to classic CSRF the way cookie sessions are.\n\nWhether you need CSRF protection follows directly from how you carry the session. Cookies are ambient authority and need it; an explicit Authorization header is not sent by a third-party page and does not.","ssrf":"SSRF and the metadata endpoint\n\nSSRF is making your server fetch a URL an attacker chose. Any feature that takes a URL, webhook registration, image import, link preview, is a candidate.\n\nThe damage is that your server sits inside the network. It can reach internal services, admin panels and, on a cloud instance, the metadata endpoint that hands out credentials.\n\nBlocklisting hostnames fails. DNS can resolve to an internal address, and a redirect moves the target after you have validated it.\n\nWhat works is checking the resolved address rather than the string, re-checking it on every redirect hop, or egressing through a proxy that only permits known hosts. The proxy is the only one of the three that stays correct when somebody adds a new URL-fetching feature and never hears about the rule.\n\nValidating the URL string is the intuitive fix and the one that does not hold, because the string is not what gets connected to. The check has to happen on the resolved address, at connect time, on every hop.","supply-chain":"Dependencies and supply chain\n\nMost of what ships is code you did not write. A postinstall script in any transitive dependency runs with your build's privileges, which is a direct path to your CI secrets.\n\nLockfiles pin versions, which is necessary and not sufficient, a compromised version can be published under a number you have already pinned to, and typosquatted names sit one keystroke from real ones.\n\nThe cheap wins are a lockfile, automated updates so you are never far behind, minimal CI token scope, and treating the dependency count itself as a cost.\n\nThe threat model is that adding a dependency grants its author execution on your build machine. That reframes 'is this library any good' into 'do I trust this author with my deploy credentials', which is the question that actually matters.","isolation-levels":"The four levels and what each permits\n\nRead uncommitted allows dirty reads. Read committed prevents those but allows non-repeatable reads: the same query twice in one transaction can return different rows.\n\nRepeatable read fixes that for rows you have read. Serializable is the only level that guarantees the result matches some serial order of the transactions.\n\nPostgres defaults to read committed; MySQL InnoDB defaults to repeatable read. Knowing which you are on is the difference between a guarantee and an assumption.\n\nEvery level above read committed costs concurrency, which is why nobody defaults to serializable. The engineering question is which anomalies your specific workload can tolerate, not which level sounds safest.","mvcc":"MVCC and snapshot isolation\n\nMulti-version concurrency control keeps several versions of each row, so a reader sees a consistent snapshot from when its transaction began while writers carry on. Readers do not block writers and writers do not block readers.\n\nThe cost is that old versions accumulate and must be cleaned up, Postgres calls this vacuum, and a long-running transaction holds back the cleanup for everyone.\n\nSnapshot isolation is what this buys, and it is not serializable: it permits write skew.\n\nThe failure this creates in production is rarely a correctness bug, it is an idle transaction left open by a connection pool, blocking vacuum until the table bloats and queries slow down.","write-skew":"Write skew\n\nTwo transactions read an overlapping set, each check a constraint that still holds, and each write a different row. Neither conflicts directly, both commit, and the constraint is now violated.\n\nThe textbook case is on-call: two doctors each check that someone else is on duty and each take themselves off. Both checks passed against the pre-write state.\n\nSnapshot isolation permits it. Fixes are serializable isolation, materialising the conflict onto a single row both must lock, or an explicit predicate lock.\n\nThis is the anomaly people assume their database prevents. It is invisible in testing because it needs concurrency and a constraint spanning rows, which is exactly the shape of most real business rules.","wal-outbox":"The write-ahead log and the outbox\n\nDatabases write changes to a log before applying them, which is what makes durability and crash recovery possible, and what replication and change data capture read from.\n\nThe outbox pattern uses this. Instead of writing to the database and publishing an event separately, you insert the event into an outbox table inside the same transaction, and a relay reads that table and publishes.\n\nOne commit, so the event and the state change cannot disagree. The relay may publish twice, which is why consumers still need idempotency.\n\nThis is the concrete answer to the dual-write problem. Publishing after commit means a crash in between loses the event; publishing before means it may describe a state that rolled back. The outbox makes both impossible with the transaction you already have.","quorums":"Quorum reads and writes\n\nIn a leaderless system every replica accepts writes. With N replicas, a write waits for W acknowledgements and a read collects R responses.\n\nWhen R plus W exceeds N, the read and write sets must overlap, so a read is guaranteed to see at least one copy of the latest write. Versioning decides which of the returned values wins.\n\nTuning the numbers tunes the tradeoff: W equal to N gives durable writes and no write availability if any node is down; W of one is the reverse.\n\nR plus W greater than N is where consistency becomes a dial rather than a mode. It is also the answer to why Dynamo-style stores can offer both behaviours from one design.","pacelc":"PACELC: the half of CAP nobody quotes\n\nCAP describes behaviour during a partition. PACELC adds the case that actually dominates: else, when the network is fine, you still trade latency against consistency.\n\nReading from the nearest replica is fast and possibly stale. Reading through a quorum is consistent and pays the round trips. No partition is required for that choice to exist.\n\nSo a system is described as PA/EL or PC/EC, what it does when partitioned, and what it does the rest of the time.\n\nPartitions are rare and the else branch is every single request. Discussing only CAP means discussing the exceptional case and ignoring the one that determines how the system feels in normal operation.","cells":"Cells, bulkheads and blast radius\n\nA cell is a complete, independent copy of the stack serving a subset of users. Nothing is shared between cells, so a failure inside one cannot reach the others.\n\nThis bounds the blast radius by construction: a bad deploy or a poison request takes out one cell's users rather than everyone. Deploys go cell by cell for the same reason.\n\nThe cost is real, more infrastructure, and any operation genuinely spanning all users becomes awkward.\n\nIt converts availability from a probability into an arithmetic fact. Rather than arguing about how likely total failure is, you decide in advance that the worst single failure affects one over n of your users.","tail-at-scale":"Tail latency: hedged requests and Little's Law\n\nIn a request that fans out to many services, the slowest response decides the total. Fan out to a hundred and your p99 per service becomes roughly your median overall.\n\nA hedged request sends a duplicate to another replica once the first exceeds some threshold, and takes whichever answers. A small percentage of extra load buys a large cut in the tail.\n\nLittle's Law connects the three numbers you actually control: concurrency equals arrival rate multiplied by latency. If latency doubles under load, in-flight work doubles with it, which is how queues run away.\n\nTail latency is a structural property of fan-out, not a slow service you can find and fix. Hedging attacks the distribution directly, which is why it works when tuning individual services has stopped helping."};
