/* GENERATED by scripts/gen-worker-topics.mjs. Do not edit.
   Run: npm run gen:topics  (also runs as part of the worker deploy) */

export const TOPICS: Record<string, string> = {"request-lifecycle":"The life of a request\n\nTyping a URL sets off DNS resolution, a TCP handshake, a TLS handshake, and then the HTTP request itself. Naming those four steps separately is most of the skill, because each one costs a round trip and round trips are what you actually pay for.\n\nThe prices are known. DNS is one round trip if nothing is cached, and often several as the resolver walks the chain. TCP's three-way handshake is one. TLS 1.3 is one, TLS 1.2 is two, and TLS 1.3 with session resumption can be zero for a returning client. Then the request itself. A user 200ms away pays that 200ms on each of them: four round trips is 800ms of blank screen before a single byte of content moves.\n\nBandwidth does not help with any of this. The pipe is not full during a handshake, it is idle and waiting, which is why upgrading a connection from 50 to 500 megabits changes page load far less than people expect and why moving the server closer changes it far more. Above a few megabits, latency decides perceived speed and bandwidth decides how long large files take.\n\nIt gets worse in sequence. Anything that has to happen in order pays the price again: a redirect from the apex to www is one extra round trip plus, if it crosses to a different host, another DNS lookup and another handshake. A page that fetches a script that then fetches configuration that then fetches data is three sequential trips before anything renders, and the browser cannot start any of them early because it does not know they exist.\n\nThen the connection is reused, which is the part that makes the arithmetic bearable. Keep-alive means subsequent requests on the same connection skip DNS, TCP and TLS entirely, so the first request is expensive and the tenth is nearly free. That is why third-party scripts on other hosts are disproportionately costly, and why domain sharding, once a performance technique, is now the opposite.\n\nThe practical conclusion is short. Remove a round trip, move the server closer, or make the trips happen in parallel rather than in sequence. Everything else in web performance is a variation on one of those three.\n\nInterviewers open with this because it reveals whether you think in terms of round trips or in terms of vague slowness. The answer that names DNS, TCP, TLS and HTTP separately is the one that can then reason about a CDN.","tcp-vs-udp":"TCP and UDP\n\nTCP guarantees delivery and order: lost packets are retransmitted, and the receiver holds everything after the gap until it is filled. UDP does neither. It sends a datagram and forgets it, and anything that arrives, arrives. That guarantee is not free, and the price is head-of-line blocking. One lost packet stalls everything queued behind it, which is exactly right for a file, where byte 400 is meaningless without byte 399, and useless for a live call, where a frame retransmitted 400ms later is worthless by the time it lands. Video calls, games and DNS run on UDP and handle loss themselves; nearly everything else takes the guarantee and is glad of it.\n\nTCP also does congestion control, which is the part people forget when they reach for UDP. It probes for available capacity, backs off when it sees loss, and shares the path with other flows in a roughly fair way. Choosing UDP means either implementing that yourself or being the traffic that ruins the network for everyone else, and the internet works partly because most flows are well behaved.\n\nThere is a third cost worth knowing: connection setup. TCP's three-way handshake is a full round trip before any data, and TLS adds one or two more on top. UDP has no handshake at all, which is why DNS uses it for a query and a reply that would otherwise cost three exchanges to set up a connection carrying one small message.\n\nQUIC is the interesting middle. It runs on UDP and rebuilds reliability, ordering and congestion control in user space, per stream rather than per connection, so a lost packet stalls only the stream it belonged to. It also folds the transport and TLS handshakes together, so a connection is established in one round trip and a resumed one in zero. That is TCP's guarantees without TCP's blocking, at the cost of being much newer and living in user space where it can be updated.\n\nThe rule of thumb is unchanged: take the guarantee unless stale data is worthless. If you find yourself adding sequence numbers, acknowledgements and retransmission on top of UDP, you are reimplementing TCP, and the version you write will be worse than the one in the kernel.\n\nChoosing UDP is choosing to write your own reliability for the parts that need it. Worth it when stale data is useless; a mistake when you end up reimplementing TCP badly.","http-versions":"HTTP/1.1, HTTP/2 and HTTP/3\n\nHTTP/1.1 carries one request at a time per connection. Pipelining is in the specification and is effectively unused, because a slow response blocks the ones behind it and proxies handled it badly, so browsers instead open around six connections per host and developers bundled files to fit more work through them.\n\nThat constraint produced an entire generation of workarounds: sprite sheets combining twenty icons into one image, inlined CSS, concatenated JavaScript, and domain sharding, which spread assets across asset1, asset2 and asset3 to get eighteen connections instead of six. Every one of them was a workaround for a protocol limit, and every one of them costs something: a sprite sheet invalidates entirely when one icon changes, and sharding multiplies DNS lookups and handshakes.\n\nHTTP/2 multiplexes many streams over one connection, which removes the reason for all of it. It also compresses headers with HPACK, which matters more than it sounds when a request carries a kilobyte of cookies and the body is a hundred bytes, and it supports server push, which was so hard to use correctly that Chrome removed support for it.\n\nWhat HTTP/2 does not fix is that it still runs on TCP. TCP delivers one ordered byte stream, so a single lost packet stalls every stream sharing that connection, which is worse than HTTP/1.1's six independent connections on a lossy network. On a good connection HTTP/2 wins comfortably; on a bad mobile link the comparison is closer than the version numbers suggest.\n\nHTTP/3 moves to QUIC over UDP, where each stream is ordered independently, so loss affects one stream instead of all of them. It also merges the transport and TLS handshakes into one round trip, and connections are identified by a connection id rather than by the four-tuple of addresses and ports, so a phone moving from wifi to mobile data keeps its connection instead of starting again.\n\nThe practical advice is to stop carrying the workarounds forward. Bundling still helps a little, because compression works better across concatenated files and there is per-request overhead even when multiplexed, but the aggressive version hurts caching: one changed line invalidates a megabyte. Domain sharding is now actively harmful, since it defeats connection reuse and adds handshakes that HTTP/2 was designed to avoid.\n\nSprite sheets, domain sharding and aggressive bundling were workarounds for an HTTP/1.1 limit. Carrying them into an HTTP/2 or HTTP/3 world adds complexity for no gain, and can hurt caching.","dns":"DNS and why it hurts you\n\nDNS turns a name into an address, and the answer is cached at every layer between you and the client: the browser, the operating system, the corporate resolver, the ISP's resolver. Each holds it for as long as the TTL you published, which makes TTL the single most consequential number in a migration plan. Low TTL means fast changes and more lookups; high TTL means the reverse.\n\nThe catch is that your TTL is a request, not an instruction. Some resolvers clamp it to a minimum of their own, some ignore it, and some clients cache for the life of the process regardless. A migration planned around 60 seconds can still be sending traffic to the old address hours later, from a client nobody can identify, which is why the old address has to keep working rather than being decommissioned on schedule.\n\nThe folk explanation, that changes take hours to propagate down from the root servers, is wrong and worth unlearning. Nothing propagates: authoritative servers are updated immediately, and the delay is entirely caches holding the previous answer until it expires. That distinction matters because it tells you what to do, which is to lower the TTL well in advance rather than to wait and hope.\n\nThe practical technique follows. Days before a cutover, drop the TTL to 60 seconds and let the old, long TTL expire everywhere. Make the change. Keep the old address serving, ideally proxying to the new one, until traffic to it has stopped for longer than any plausible cache. Then raise the TTL again, because a permanently low TTL costs a lookup on every cold client and makes your resolver a hard dependency of every page load.\n\nBetter still, do not fail over in DNS at all. Point the name at something stable, a load balancer or an anycast address, and move traffic behind it. Then a failover takes effect in seconds for everyone, rather than in an interval decided by other people's caches. This is why every large system's public name resolves to an address that has not changed in years. One more property worth knowing: negative answers are cached too. If a name does not exist when someone looks it up, that absence is cached according to the SOA record's minimum, so creating a record after a client has already asked for it can leave that client failing for the duration. Create the record before you tell anyone to try it.\n\nLowering TTL days ahead of a migration, then failing over at the load balancer instead of in DNS, is the difference between a clean cutover and a long tail of traffic hitting a decommissioned box.","cdn":"CDNs and edge caching\n\nA CDN puts copies of your content in data centres near your users, so the round trips that dominate page load take tens of milliseconds instead of hundreds. Nothing about your origin got faster. The distance got shorter, and distance was the bill.\n\nIt helps even when nothing is cached, which surprises people. The connection is established with a nearby edge node, so the expensive handshakes happen over a short path, and the edge holds a warm, tuned connection back to your origin. A cache miss served through an edge is often meaningfully faster than the same miss served directly, purely from terminating TLS close to the user.\n\nStatic assets are the easy part, and the reason they are easy is content addressing: a hashed filename can be cached forever because a new build is a new URL. The interesting work is caching HTML and API responses, where you have to be deliberate about the cache key and about invalidation, which are the two things that go wrong and the two things nobody owns.\n\nCache keys are where hit ratios are won and lost. The key is the URL plus whatever Vary names, so an analytics parameter appended to a shared link produces a distinct entry per recipient, and Vary on a header with high cardinality shatters one entry into thousands. Normalising the key, stripping parameters that do not change the response, is routinely the largest single improvement available and costs nothing to run.\n\nModern edge platforms also run code at those locations, which changes what can be moved. Authentication checks, personalisation, redirects, A/B assignment and geographic routing can happen near the user rather than at origin, so a personalised page can still be served from a cached shell with the personal part filled in at the edge. That is the difference between a CDN as a file server and a CDN as part of the application.\n\nThe failure mode to respect is that an edge cache is very good at holding whatever you told it to hold. A wrong header applied by path rather than by response can pin the wrong content under the right URL for as long as you specified, and no purge reaches copies already held in browsers. Immutable caching is a promise about the URL, and it should only be made where the URL identifies the bytes.\n\nA CDN is usually the highest-leverage performance change available, because it attacks latency instead of throughput. Adding servers makes a busy system faster; moving content closer makes a distant system faster.","big-o":"Big-O without the ritual\n\nBig-O describes how cost grows as input grows, and it deliberately ignores constants because for large inputs the shape of the curve dominates everything else. That is its strength and the source of most of its misuse: it answers which algorithm wins as n grows and says nothing about which is faster at the n you have. It is actively misleading for small inputs. An n log n algorithm with a heavy constant loses to a quadratic one on a list of twenty items, which is why real sort implementations switch to insertion sort below a threshold of a few dozen elements. The asymptotic class is a statement about the limit, and production code spends most of its life nowhere near it.\n\nSay which n you mean, because it is where most vague answers hide. Linear in the number of users, linear in the number of requests, and linear in the number of rows returned are three different claims with three different consequences, and an algorithm that is constant per request and linear per user is a completely different system from one that is the reverse.\n\nThe numbers worth carrying are these. At a million items, log n is about twenty operations, n log n is about twenty million, and n squared is a trillion. That is the difference between instant, a second, and a fortnight, and it is why a nested loop over two large collections is the first thing to look for when something is unexpectedly slow.\n\nAverages, worst cases and amortised costs are separate claims and are worth distinguishing. A hash map is constant on average and linear in its degenerate case. A dynamic array append is amortised constant and occasionally linear when it grows. Quicksort is n log n typically and quadratic on an unlucky pivot. Whether the worst case matters depends on whether an attacker or an unlucky input can reach it.\n\nFinally, complexity is about operations rather than about time, and the two diverge when the operations differ in price. Twenty sequential disk reads beat a thousand in-memory comparisons, and one network call beats both by a mile in the other direction. Cache locality is why a linear scan of an array frequently outperforms a linked list traversal with the same asymptotic class, and why the array is the better default.\n\nQuoting complexity without naming n is the most common way to sound rigorous and say nothing. Naming it is what turns the answer into engineering.","hash-maps":"Hash maps and their worst case\n\nA hash map turns a key into a bucket index and looks there, which is why it is constant time on average regardless of size. Collisions are handled either by chaining, where a bucket holds a small list, or by open addressing, where a colliding entry moves to the next free slot. Chaining is simpler and tolerates a fuller table; open addressing is faster in practice because it stays within a cache line more often.\n\nThe average case assumes keys spread evenly, and that assumption is an attack surface. Choose keys that all hash to one bucket and every lookup becomes a scan of a list, turning constant time into linear time on every operation. That was a real, widely exploited denial-of-service class, and the fix now standard in every major language is a hash seed randomised per process, so an attacker cannot compute the collisions in advance.\n\nLoad factor is the parameter that decides performance, and it is the ratio of entries to buckets. Past roughly 70 to 75 per cent, collisions rise sharply, so the map grows by allocating a larger table and rehashing every entry into it. That resize is a linear pause, which is fine for throughput and shows up in tail latency, and it is why pre-sizing a map you know the size of is a genuine optimisation.\n\nOrdering is the property people accidentally depend on. A hash map has none by definition, and any apparent order is an implementation detail that will change. Python's dictionaries preserve insertion order as a guarantee since 3.7; most other languages do not, and code that iterates a map and expects stability across runs or versions is code waiting to break.\n\nKeys must be hashable and stable, which in practice means immutable. Mutating an object after using it as a key changes its hash and strands the entry: it is still in the table, in the bucket it originally hashed to, and no lookup will ever find it again. Languages that allow mutable keys make this easy to do by accident, and the resulting bug looks like data loss.\n\nReaching for a hash map is right almost always. Knowing the degenerate case is what separates using one from being able to defend the choice.","trees-indexes":"B-trees and why databases use them\n\nA balanced binary tree is a fine in-memory structure and a poor on-disk one, because every level is a separate read and a read from storage costs orders of magnitude more than a memory access. Be honest about which storage: a seek on a spinning disk is around ten milliseconds against roughly a hundred nanoseconds for main memory, a ratio near a hundred thousand, while an NVMe read is tens of microseconds and the ratio is closer to a thousand. Either way a tree of depth thirty is thirty of them, and the shape of the structure, not the algorithm, is what makes it slow. The B-tree's argument survives the faster medium because a thousandfold is still a thousandfold.\n\nA B-tree fixes it by storing many keys per node, sized to match a disk page, usually a few kilobytes. Fanout of several hundred means depth stays tiny: a table of a hundred million rows is typically three or four levels deep, so a lookup is three or four reads, and the top levels are cached in memory anyway. That is why index depth barely grows as tables get large, which is the property that makes relational databases scale gracefully on reads.\n\nAlmost every relational index is a B-tree, and the variant in use is nearly always a B+ tree, where only the leaves hold values and the leaves are linked to each other. That linking is what makes range scans cheap: find the start, then walk sideways, without returning to the root for each subsequent key. Ordered results and BETWEEN queries both come from that one detail.\n\nLSM trees are the other family and they make the opposite trade. Writes go to an in-memory table and are flushed as sorted files, so writes are sequential and fast, and reads may have to consult several files, which is why they lean on Bloom filters to skip the ones that cannot contain a key. Compaction merges those files in the background, spending disk bandwidth later to keep reads from degrading. Cassandra, RocksDB and most modern key-value stores are built this way.\n\nThe choice between them is a workload question rather than a fashion. B-trees favour reads and in-place updates; LSM trees favour heavy sustained writes and pay for it in read amplification and background compaction that has to be operated. Knowing which one is underneath your database explains a surprising number of its performance characteristics.\n\nThe general lesson is the one worth keeping: the structure is chosen to match the cost model of the medium it lives on. Memory rewards pointer-light layouts and cache locality, disk rewards few large sequential reads, and the network rewards fewer round trips. The same data deserves a different structure in each.\n\nThe structure is chosen to match the storage medium, not for elegance. That is the general lesson: data structure choice follows the cost model of where the data lives.","bloom-filters":"Bloom filters\n\nA Bloom filter answers exactly one question cheaply: is this item definitely absent, or possibly present. It is a bit array plus a handful of hash functions. Adding an item sets the bits those hashes point at; checking one reads them. If any bit is zero the item was never added, which is certain. If all are set, it probably was, and it might be a coincidence of other items' bits.\n\nThat asymmetry is the whole point. Put one in front of an expensive lookup and everything definitely absent skips the lookup entirely, for a few bits per item rather than the cost of storing the keys. Around ten bits per item gives roughly a one per cent false positive rate, which means a filter for a hundred million keys fits in about 120 megabytes while the keys themselves would be many gigabytes.\n\nThe parameters trade against each other in a known way: more bits per item lowers the false positive rate, and the number of hash functions has an optimum for a given ratio. What you cannot do is delete. Clearing bits would break other items that share them, which is why a standard Bloom filter is add-only and why counting variants exist for the cases that need removal.\n\nIt appears constantly once you recognise it. Storage engines keep one per data file so a read can skip files that cannot contain the key. Caches use one to answer the penetration problem, where requests for keys that exist nowhere would otherwise reach the database every time. Browsers and mail systems have used them for candidate checks against large blocklists.\n\nThe relatives are worth knowing by name. A cuckoo filter supports deletion and is often smaller at low error rates. HyperLogLog answers a different question, how many distinct items are there, in kilobytes rather than gigabytes with a couple of per cent error, which is why every analytics product uses it to count unique visitors. Both belong to the same family: give up exactness, buy an enormous reduction in space.\n\nUseful precisely where a definite no is valuable and a maybe is cheap to verify. Storage engines use them to avoid reading files that cannot contain a key.","acid":"ACID, stated plainly\n\nAtomicity: all of a transaction happens, or none of it. Consistency: it moves the database between valid states, meaning the constraints you declared still hold afterwards. Isolation: concurrent transactions do not see each other's partial work. Durability: once committed, it survives a crash.\n\nThree of those four are largely settled and you can rely on them without thinking. Isolation is where the detail lives, where the defaults differ between databases, and where the surprises are.\n\nMost databases default to read committed rather than serialisable, because full isolation is expensive: the database must behave as though transactions ran one after another, and enforcing that costs either locks that others wait on or aborts that the application must retry. The default is a performance decision made on your behalf, and it is usually right and occasionally the cause of a bug nobody can reproduce.\n\nWhat read committed permits is worth stating concretely. Two transactions read a balance of 100 into application memory, each subtracts 60, each writes 40, and the second write silently overwrites the first. Nothing errors. The same operation expressed as a single update statement is safe, because the row lock serialises it, so the bug lives entirely in the gap between reading and writing rather than in the database.\n\nDurability also has a dial that people rarely look at. A commit is durable once the write-ahead log is flushed to disk, and most databases let you relax that: Postgres has synchronous_commit, MySQL has its flush setting, and turning either down makes writes markedly faster while putting the last fraction of a second at risk in a hard crash. That is a legitimate choice for analytics ingestion and a poor one for orders. The useful habit is to state the level you are running at and the anomalies it permits, rather than saying you use a relational database and therefore have ACID. The letters are a promise about a configuration, and the configuration has a default that nobody chose.\n\n'We use a relational database so we get ACID' is only true at the isolation level you actually configured. Knowing your default is the difference between a guarantee and an assumption.","indexes":"Indexes and their cost\n\nAn index is a sorted structure, in nearly every case a B-tree, that turns a full table scan into a targeted lookup. On a large table that is the difference between reading a million pages and reading four, which is why the first index on a hot query is often a thousandfold improvement and the fifteenth is not.\n\nEvery index must be maintained on write, so each one slows inserts, updates and deletes and occupies disk and memory. An unused index is pure cost with no benefit, and most mature databases can tell you which of theirs have never been scanned. Removing those is the rare optimisation that improves writes without risking reads.\n\nComposite indexes follow the prefix rule, which is the single most useful piece of indexing knowledge: an index on (a, b) serves a query on a, and on a and b together, and cannot seek on b alone. It is a phone book ordered by surname then first name. Choosing the column order is choosing which queries the index can answer, and the usual guidance is equality columns first, then the range or ordering column.\n\nA covering index is the next step: if the index contains every column the query needs, the database answers from the index without touching the table at all. On a read-heavy endpoint that removes half the work, and it is why adding one included column sometimes beats adding a cache.\n\nThere are ways to disable an index by accident, and they are worth memorising because each looks harmless in review. Wrapping the column in a function, comparing it against a different type so the database has to cast, and starting a LIKE pattern with a wildcard all mean the index's ordering no longer matches the question being asked. Expression indexes exist for the first case, when the function is genuinely needed.\n\nFinally, indexes are not only B-trees. Hash indexes serve equality alone, GIN and inverted indexes serve containment and full text, and BRIN indexes are tiny and effective for columns that correlate with physical order, such as an append-only timestamp. Recognising when the default is the wrong shape is worth more than tuning the default.\n\nThe instinct to add an index per slow query produces tables with fifteen indexes and slow writes. The question is always which queries matter, not which are slow.","normalisation":"Normalisation and when to break it\n\nNormalising means storing each fact exactly once, so an update touches one row and the data cannot contradict itself. The customer's address lives in the customer table and every order refers to it. That is the default, it is right most of the time, and it costs joins on read. Denormalising duplicates data to avoid those joins. Reads get faster and simpler, and every copy becomes a thing that can drift from the original. The trade is write complexity for read speed, and it is worth making when reads dominate and there is a reliable mechanism for keeping the copies current: a trigger, an outbox, a stream consumer, a scheduled rebuild.\n\nThere is a category of duplication that is not denormalisation at all, and confusing the two causes real bugs. The price on an order line is not a cached copy of the product price; it is the price at the time of sale, and it must not change when the product is repriced. Any value that is part of a historical record belongs on that record permanently. Copying it is correct modelling rather than an optimisation.\n\nThe usual mature shape is a normalised source of truth plus deliberately denormalised read models, updated asynchronously. That is the same idea as a materialised view, and where the database offers one, using it is preferable to hand-rolled duplication, because the refresh path is somebody else's tested code rather than yours.\n\nThe classical normal forms are worth knowing at a level of one sentence each. First: no repeating groups, one value per column. Second: every non-key column depends on the whole key. Third: and on nothing but the key. Almost every practical schema stops there, and the higher forms exist mostly in exam papers.\n\nThe one thing to avoid is denormalising early because joins feel expensive. A join on an indexed foreign key is one of the cheapest operations a relational database performs, and the intuition that it is slow usually comes from a missing index rather than from the join itself.\n\nDenormalisation is not a shortcut, it is a trade of write complexity for read speed. Make it when reads dominate and you have a reliable way to keep copies current.","sql-vs-nosql":"Relational or document\n\nRelational databases are the default for reasons that are easy to undervalue until they are gone: you can query the data in ways nobody anticipated, the database enforces constraints rather than trusting every writer, and a transaction can span several rows in several tables. Choose otherwise with a reason you can state in a sentence.\n\nDocument stores suit data that is read as a whole unit and whose shape varies per record: a product with wildly different attributes per category, an event payload, a document that genuinely is a document. The gain is that the record matches the object, and the loss is that a question the schema did not anticipate becomes a scan or a second copy of the data.\n\nKey-value stores serve lookups by one known key at very high volume, and nothing else. Wide-column stores such as Cassandra sit near them and are designed around the query: you model the tables from the access patterns, accept duplication as normal, and get linear write scaling and no ad hoc querying at all.\n\nThe honest summary is that most applications fit a relational model, and a managed Postgres will carry far more load than most products ever see. It also handles JSON well, with indexing on document fields, which covers the semi-structured part of a schema without a second database and a second operational burden.\n\nSchemaless is the claim that deserves the most scepticism, because there is always a schema: it either lives in the database, where it is enforced once, or in every piece of code that reads the data, where each has its own slightly different idea of what a record looks like. The second option is not the absence of a schema, it is a schema nobody can query and nobody maintains.\n\nThe strongest argument for a second store is a genuinely different access pattern rather than a performance claim: full text search, time series at high ingest rates, a graph traversal that would be a self-join twelve levels deep. Those are real, and they are much rarer than the number of polyglot architectures in the wild suggests.\n\n'It scales better' is not a reason on its own, a managed Postgres handles more load than most products ever see. The reasons that hold up are access pattern and data shape.","connection-pooling":"Connection pooling\n\nA database connection is not a cheap handle. In Postgres each one is an entire operating system process with its own memory, so the cost is measured in megabytes rather than kilobytes, and the practical ceiling arrives well before most people expect, often in the low hundreds. Past it the failure is not gradual: the database refuses new connections outright while the ones it holds are still being served, so the symptom is a wall rather than a slope.\n\nThe instinct is to raise the limit, and it buys very little. Each additional slot is another process, so the ceiling moves a little and then the machine runs out of memory instead, and along the way every connection makes the shared work more expensive, because the internal bookkeeping the database does across its backends grows with their number. There is a real effect where throughput peaks at a concurrency close to the number of cores and then declines as more connections compete, which means a database with four hundred connections can complete less work per second than the same database with fifty.\n\nA pooler sits in front and multiplexes many client connections onto a small number of real ones. Clients connect to it, it holds a modest set of server connections, and it hands one to whichever client currently has work. The application sees the connection count it wants and the database sees the one it can serve, which is the whole trick and the reason this is a configuration change rather than an architectural one.\n\nWhich pooling mode you choose decides how much that buys. Session pooling holds a real connection for the duration of a client's session and therefore helps almost not at all, since the ratio is unchanged for any client that stays connected. Transaction pooling returns the connection at every commit, so a hundred idle clients occupy no server connections at all, and that is where the large multiple comes from. Statement pooling goes further still and is rarely usable.\n\nTransaction pooling has a price, and it is specific: anything whose lifetime exceeds a transaction stops working, because the next statement may arrive on a different server connection. Session variables, temporary tables, advisory locks, LISTEN and NOTIFY, and prepared statements are the usual casualties, and prepared statements are the one that bites hardest because ORMs and drivers often use them silently. Modern poolers can track and replay prepared statements per connection, which removes most of that, and it is worth confirming rather than assuming.\n\nServerless makes the whole problem sharper rather than different. Instance count follows traffic and each instance opens its own connections, so demand for slots peaks exactly when the database is busiest, and a function that scales to a thousand concurrent invocations will ask for a thousand connections from something that can serve two hundred. That is why every serverless platform either ships a pooler or expects you to put one in front, and why a data API over HTTP is the other common answer: it removes the connection from the equation entirely.\n\nThis is the failure that looks like a database problem and is not. The database is fine; you have exhausted its connection slots. Adding read replicas does not help, and a pooler does.","process-thread-async":"Processes, threads and async\n\nProcesses have separate memory and are isolated: one crashing does not take the others with it, and communication between them costs a copy through the kernel. Threads share memory within a process, which makes communication free and data races possible in the same breath. Async runs many tasks on one thread, switching whenever a task waits, which makes concurrency cheap and parallelism impossible.\n\nThe distinction that matters is concurrency versus parallelism. Concurrency is dealing with many things at once, which is a structure. Parallelism is doing many things at once, which needs cores. Async gives you the first and none of the second, and most of the confusion in this area comes from a system that needed the second being given the first.\n\nAsync suits IO-bound work, where tasks spend most of their time waiting on a network or a disk. A web handler that spends 95% of its time waiting on a database can serve hundreds of concurrent requests from one thread, because at any moment nearly all of them are parked. That is not a trick, it is the correct shape for work that is mostly waiting.\n\nIt does nothing for CPU-bound work, because there is no waiting to exploit. A CPU-heavy function in Node or in Python's asyncio blocks the event loop until it returns, and every other request on that loop waits, including the health check. This is why an image resize or a large JSON parse dropped into an async handler produces a service that is fast until someone uploads a large photograph.\n\nThe costs of threads are worth naming, because the usual advice to just use threads has a bill. Each one carries a stack, typically measured in megabytes of reserved address space, so tens of thousands of threads is not a plan. Every context switch costs the kernel a few microseconds and evicts cache lines that were warm. And shared memory means locks, which means contention and the possibility of deadlock. Async avoids all three by making the switching explicit and cooperative, at the cost of one badly behaved task ruining everything. In practice the answer is usually all three in layers: processes for isolation and for using every core, a small pool of threads inside each for CPU-bound work, and async for the IO-bound majority. The question is never which model is best, it is what this particular work waits on, and there is usually more than one answer in one service.\n\nChoosing async for CPU-bound work is a common and expensive mistake. The question is what the work waits on, not which model is modern.","race-conditions":"Race conditions and locks\n\nA race condition is a correctness bug that depends on timing, which is why it passes every test on a laptop and appears in production at a rate proportional to traffic. Read-modify-write is the classic shape: two requests read the same value, both compute from it, and one result is silently lost. Nothing errors, nothing logs, and the total is simply wrong.\n\nLocks prevent it by serialising access, and they are the first instinct because they are the most obvious. The costs are contention, since everyone waits their turn on the hot row, and deadlock, when two paths take the same two locks in opposite orders and each waits for the other forever. Deadlock is preventable by always acquiring locks in a fixed global order, which is a rule that has to be written down somewhere because it cannot be enforced by the type system.\n\nThe better fix is usually to remove the gap rather than guard it. An atomic increment does the read and the write as one operation the database cannot interleave. A conditional update, set stock to 0 where stock is currently 1, fails cleanly when someone else got there first, and the caller retries or reports a conflict. Both turn a timing bug into a visible outcome.\n\nThat conditional update is optimistic concurrency control, and the general form is a version column: read the row and its version, write with a condition that the version is unchanged, and bump it. Zero locks are held between the read and the write, which is the point, because the user's thinking time is not something to hold a lock across. It is the right default when conflicts are rare, and the wrong one when they are common, because every conflict costs a wasted round trip.\n\nPessimistic locking is the other half of that pair: take the lock first, hold it for the duration, and make everyone else wait. SELECT FOR UPDATE is the usual form. It is right when conflicts are frequent enough that retrying is more expensive than queueing, and it needs a timeout, because a lock held by a process that died is a lock held forever unless something reclaims it.\n\nDistributed locks deserve suspicion. Once the lock lives in another system, a process can be paused by garbage collection, lose its lease, and resume believing it still holds it, while another process holds it for real. Fencing tokens are the standard mitigation: the lock hands out an increasing number, the resource rejects any write carrying a number lower than the highest it has seen, and the zombie's late write is refused. If you can restructure the problem to avoid needing the lock, that is almost always cheaper than getting this right.\n\nReaching for a lock is the first instinct and rarely the best one. Making the operation atomic removes the race instead of guarding it, with no contention and no deadlock.","idempotency":"Idempotency\n\nAn idempotent operation can run repeatedly with the same result as running once. This matters because networks make retries unavoidable, and a timeout tells you nothing at all about whether the work happened: the request may never have arrived, or it may have completed and the response been lost on the way back.\n\nThe standard approach is an idempotency key supplied by the client, unique per logical operation rather than per attempt. The server records the key alongside its result, and returns the stored result if the key comes back. Without it, a retried payment is simply a second payment.\n\nThe subtlety is what you store and when. Recording the key before doing the work means a crash mid-operation leaves a key with no result and the retry is refused forever. Recording it after means two concurrent retries both pass the check. The usual answer is to insert the key immediately with a pending state, under a unique constraint so the second attempt collides, then update it with the result. A pending key older than a few minutes is stale and can be reclaimed.\n\nNot everything needs a key. GET, PUT and DELETE are idempotent by their own semantics, and an update that sets an absolute value is naturally safe to repeat where one that increments is not. The key is for the operations that are neither, which in practice means anything that creates something or moves money.\n\nThe interesting half is the concurrency, not the concept. Storing a key is easy; deciding what happens when two retries arrive at once, or when the first attempt died between charging the card and writing the result, is where the design actually lives.","backpressure":"Backpressure\n\nWhen a system accepts work faster than it can finish it, the backlog grows, and there is no version of that story with a happy ending. Queues fill, memory climbs, latency climbs, and the failure arrives at the worst possible moment, which is later and larger than the failure you would have had if you had simply said no at the start. Backpressure is the mechanism for saying no: refusing or slowing intake when what is downstream cannot keep up. Bounded queues, load shedding, rejecting early with a 503 and a Retry-After. It feels like worse behaviour and it is better behaviour, because a rejection is a signal the caller can act on while a growing backlog is information nobody has.\n\nTCP is the reference implementation and it is worth studying because everyone already depends on it. The receiver advertises a window, the amount it is prepared to accept; when the application stops reading, the window shrinks to zero and the sender stops sending. No messages are lost, no buffer grows without bound, and the pressure propagates back to the source through nothing more than an advertised number. Every good backpressure design is a version of that.\n\nIn an application it usually means bounding the things that are unbounded by default. A queue with a maximum depth that rejects rather than growing. A connection pool with a fixed size and a short wait, so a slow database produces immediate errors rather than a thousand threads waiting on a pool. A concurrency limit per dependency. Each of these converts an invisible accumulation into an error with a name.\n\nThe subtle failure is backpressure that stops at a boundary. A service that rejects work correctly, in front of a queue that accepts everything, has moved the unbounded buffer one step upstream and made it someone else's surprise. The property you want holds end to end: if the slowest component in a chain can only manage 100 a second, the intake at the front should be admitting roughly 100 a second, not 10,000 into a buffer.\n\nWhere it cannot propagate, drop deliberately and say so. For a live telemetry stream or a metrics pipeline, the right answer under overload is to shed the excess and record how much was shed, because stale telemetry has no value and a growing buffer of it has negative value. That decision belongs in the design with a number attached, rather than emerging at 3am as an out-of-memory kill.\n\nUnbounded queues look like resilience and are the opposite: they convert a visible, recoverable rejection into a hidden backlog that fails later and harder.","values-and-types":"Values, types and memory\n\nA variable is a name bound to a value, and the single most useful thing to understand early is what the name actually holds. For a small value such as a number, it usually holds the value itself. For anything larger, an object, a list, a string in most languages, it holds a reference to something stored elsewhere, and copying the variable copies the reference rather than the thing.\n\nThat distinction explains an entire category of confusing bugs. Passing a list to a function and finding it changed afterwards is not the function misbehaving, it is both names pointing at one list. Languages that avoid this do so by making values immutable, so there is nothing to change, which is why immutability keeps reappearing as a recommendation from people who have debugged the alternative.\n\nTypes are the other half. A static type system checks at compile time that the operations you wrote make sense for the values you have; a dynamic one checks at run time, when the operation happens. Neither is universally better and the trade is well understood: static typing catches a class of mistakes before anything runs and costs you ceremony, dynamic typing gets out of the way and moves those failures into production.\n\nWorth knowing regardless of language: integers have limits and floating point numbers are approximations. 0.1 plus 0.2 does not equal 0.3 in any language using IEEE 754 doubles, because neither value is exactly representable in binary. Money belongs in integers of the smallest unit, or in a decimal type, and never in a float, which is a rule with a long trail of financial incidents behind it.\n\nMost early confusion in any language comes from not knowing whether a name holds a value or a reference to one, and most numeric bugs come from treating floating point as though it were mathematics. Both are cheap to learn and expensive to discover.","control-and-functions":"Control flow, functions and scope\n\nControl flow is the small vocabulary every language shares: do this if that, do this repeatedly, stop early, call something else. What differs is the cost of each. An early return is usually clearer than a nested condition, and a loop that mutates a shared accumulator is usually clearer as a map or a fold, but the version your team reads fastest wins over the version a style guide prefers.\n\nA function is the unit of naming, and naming is most of what makes code readable. The useful test is whether the name says what it does without saying how: send_welcome_email is a name, do_email_stuff_v2 is an apology. A function that needs the word and in its name is usually two functions.\n\nScope decides which names are visible where, and closures are the part that surprises people. A closure is a function that carries the variables it referenced when it was created, which is what makes callbacks and decorators work, and which is also why a loop that creates functions capturing the loop variable can produce a set of functions that all see the same final value.\n\nArguments are passed either by value or by reference depending on the language and the type, and the practical consequence is the same as with variables: a function given a mutable structure can change what the caller holds. Making that explicit, by returning a new value instead of modifying the argument, removes a category of bug that is very hard to find by reading.\n\nFunctions are how a program is made comprehensible: each one is a promise that the reader does not need to look inside. A function that changes something the caller could not predict has broken that promise, which is why side effects deserve to be named in the signature or avoided.","data-structures-choice":"Choosing a data structure\n\nNearly all everyday code is served by four structures, and knowing their costs by heart removes most performance questions before they arise. An array or list gives you position: constant-time access by index, linear-time search. A hash map gives you lookup by key in roughly constant time, at the cost of memory and no ordering. A set is a hash map without values, for membership and deduplication. A queue or stack gives you an order of processing.\n\nThe most common avoidable mistake in real code is searching a list inside a loop. Checking whether each of ten thousand items appears in a list of ten thousand is a hundred million comparisons; the same check against a set is ten thousand lookups. It is the same program with one line changed, and it is the difference between two minutes and a few milliseconds.\n\nOrdering is the next question. A sorted structure, a balanced tree or a sorted list with binary search, gives you range queries and nearest-neighbour lookups that a hash map cannot answer at all. If your access pattern includes everything between these two values, that is the tell.\n\nThen there are the specialised few worth recognising when you meet them: a heap for repeatedly taking the smallest item, a trie for prefix search, a bloom filter for a cheap definitely-not-present answer, a ring buffer for a fixed-size stream of recent items. You will rarely implement one; recognising which problem each solves is what stops you writing a slow version by hand.\n\nChoosing the structure is choosing the complexity, and it is a decision made once at the start rather than tuned later. Most code that needs optimising does not need a faster language, it needs a hash lookup where it currently has a scan.","recursion-and-iteration":"Recursion, iteration and state\n\nRecursion expresses a problem in terms of a smaller version of itself, and it is the natural shape for anything tree-like: directory trees, nested JSON, parsers, divide and conquer sorts. Iteration expresses the same thing as a loop with explicit state. Every recursion can be rewritten as iteration with a stack, and the choice is about which one reads more clearly for the shape of the data.\n\nThe practical constraint is the call stack. Each recursive call consumes a frame, and stacks are finite: CPython's default recursion limit is 1,000, and while most compiled languages allow far more, none allow unlimited. Recursing over a list of a million elements will exhaust it. Some languages optimise tail calls, where the recursive call is the last thing the function does, into a loop; many, including Python and most JavaScript engines in practice, do not.\n\nMemoisation is where recursion becomes practical for overlapping subproblems. The naive recursive Fibonacci recomputes the same values exponentially often: the call count is exactly two times the next Fibonacci number minus one, so it grows as roughly 1.6 to the power of n rather than the 2 to the n that gets quoted. Caching each result makes it linear. That single change is also the entire idea behind dynamic programming, which sounds like a separate topic and is mostly this observation applied deliberately.\n\nThe state question sits underneath both. A loop that mutates variables is easy to write and easy to get subtly wrong when it grows; a recursive or functional version passes state explicitly, which is more verbose and harder to break. Neither is a rule, but when a loop body reaches thirty lines and four mutable variables, the bug you cannot find is usually one of them being updated in the wrong order.\n\nRecursion is the right tool for recursive data and the wrong tool for long flat sequences, and the stack is what decides which is which. Memoisation turns the classic exponential recursion into a linear one, which is the whole trick behind most dynamic programming problems.","errors-and-failure":"Errors, exceptions and failure\n\nThere are two families of error handling and it is worth knowing which one you are in. Exceptions are out-of-band: a failure unwinds the stack until something catches it, so the happy path stays uncluttered and the failure path is invisible in the signature. Returned errors are in-band: a function returns either a value or an error and the caller must deal with both, which makes failure visible and the code longer.\n\nBoth fail in the same way, which is silently swallowing the problem. An empty catch block, or an error return assigned and ignored, converts a loud failure into a wrong answer produced quietly. If there is genuinely nothing to do about an error, the minimum is to record it with enough context to identify it later.\n\nThe distinction that matters more than the syntax is between expected and unexpected failures. A file not existing, a validation rule failing, a payment being declined: these are outcomes, and modelling them as return values rather than exceptions usually produces clearer code. A null dereference or an out-of-range index is a bug, and it should be loud, uncaught and fixed rather than handled.\n\nFinally, errors should carry context that is useful to whoever reads the log at three in the morning. Failed to save is not an error message; failed to save order 12345 for customer 678, database timeout after 5s is. Wrapping an error as it moves up the stack, adding what each layer knows, is the cheapest debugging investment available.\n\nThe failure path is code, and it is the code least likely to be tested and most likely to run during an incident. Deciding deliberately which failures are outcomes and which are bugs is what keeps the first kind handled and the second kind visible.","complexity-intuition":"Complexity without hand-waving\n\nBig O describes how the cost of an operation grows with the size of the input, and it deliberately ignores constants. That is its strength and the source of most of its misuse: it tells you which algorithm wins as n grows, and it says nothing about which is faster for the n you actually have. A linear scan of 50 items beats a hash lookup with an expensive hash function, and both are irrelevant next to one network call.\n\nThe growth rates worth recognising on sight are constant, logarithmic, linear, linearithmic, quadratic and exponential. The useful boundary is between quadratic and everything below it: at a million items, an n log n algorithm does roughly twenty million operations and a quadratic one does a trillion. That is the difference between a second and a fortnight, and it is why nested loops over large collections are the first thing to look for.\n\nSpace complexity gets less attention and causes at least as many incidents. Loading a whole file into memory works until the file is larger than the container's limit, and the failure is an abrupt kill rather than a slow response. Streaming, processing in chunks, and paginating are the answers, and they are much easier to build in from the start than to retrofit.\n\nThen there is amortised cost, which is where dynamic arrays and hash maps live. Appending to a dynamic array is usually constant time and occasionally linear when it grows and copies, and the average across many appends stays constant. That is fine for throughput and not fine for latency: the request that triggers the resize pays for all the ones that did not, which is the same shape as a garbage collection pause and matters for the same reason.\n\nComplexity analysis is for choosing between algorithms, not for predicting runtime. The two facts that repay learning are that quadratic algorithms fall off a cliff at scale, and that amortised constant time means one unlucky call pays for everyone else, which shows up in the tail rather than in the average.","python-data-model":"Everything is an object\n\nPython has one uniform rule underneath its syntax: every value is an object with a type, and every operator is a method call. Adding two numbers calls __add__, indexing calls __getitem__, len calls __len__, and a for loop calls __iter__ and then __next__ until it is told to stop. Once you know that, the language stops having special cases and starts having a small set of protocols.\n\nThis is what makes user-defined types feel native. Implement __len__ and __getitem__ and your class works with len, indexing, slicing and iteration. Implement __enter__ and __exit__ and it works with with. There is no interface to declare and nothing to inherit from; the method being present is the entire contract, which is duck typing made explicit.\n\nNames are bindings rather than boxes. Assignment binds a name to an object; it never copies. Two names can refer to the same list, and mutating through one is visible through the other. The identity operator is checks whether two names refer to the same object, while == asks the objects whether they are equal, and confusing the two produces bugs that appear to depend on the value being tested.\n\nMemory is managed by reference counting plus a cycle collector. An object is freed the moment its last reference goes away, which is why files closed by scope exit usually work in CPython and are not guaranteed by the language. Reference cycles need the collector, which runs periodically, so a class with __del__ and a cycle can keep memory alive far longer than the code suggests.\n\nThe protocols are the language. Learning the dunder methods turns Python from a collection of conveniences into a system where your own types behave exactly like the built-in ones, which is the difference between writing Python and writing another language in Python syntax.","python-mutable-defaults":"Mutability and the default argument trap\n\nA default argument is evaluated once, when the function is defined, not each time it is called. A function declared with an empty list as a default therefore shares one list across every call that omits the argument, so items appended in one call are visible in the next. It looks like the function is remembering things, and in a sense it is.\n\nThe fix is the idiom you see everywhere: default to None and create the real value inside the body. It looks like ceremony until you know why it exists, at which point it stops looking like a style choice.\n\nThe same underlying fact, that names bind to objects rather than copy them, explains most other surprises. Slicing a list gives you a shallow copy, so the outer list is new and the inner objects are shared; copy.deepcopy exists for when that matters. A tuple is immutable in that its bindings cannot be changed, and a tuple containing a list still lets you mutate that list, which is why a tuple of mutable objects is not hashable in the way people expect.\n\nThis is also where class attributes catch people. An attribute assigned in the class body belongs to the class, so every instance shares it, and mutating it through one instance changes it for all of them. Assigning to it through an instance creates a new instance attribute that shadows the class one, so the two look identical in code and behave differently, which is a genuinely difficult bug to see by reading.\n\nEvery one of these is the same fact wearing a different hat: Python binds names to objects and does not copy. Learning the rule once means recognising the pattern in the default argument, the shallow copy, the shared class attribute and the tuple that is not as immutable as it looks.","python-comprehensions":"Comprehensions, generators and laziness\n\nA comprehension builds a collection in one expression, and its value is not brevity but that it says what is being built rather than how. A list comprehension produces a list immediately; a generator expression, with parentheses instead of brackets, produces an iterator that computes each item when asked, which is the difference between holding a million rows in memory and holding one.\n\nGenerators are the cheapest performance tool in the language for anything sequential. A function with yield in it returns a generator: it runs until the first yield, hands back a value, and resumes where it left off when the next value is requested. That turns a pipeline of transformations into something that streams, so a five gigabyte file can be processed in constant memory by a function that reads like it processes a list.\n\nThe cost is that a generator can only be consumed once, and its laziness moves work later, so an exception can surface in the loop that consumes it rather than in the line that appeared to create it. It also means the timing of side effects is not where it looks, which is why a generator that performs writes is usually a mistake.\n\nThe related tools are worth knowing as a set: enumerate when you need the index, zip to walk two sequences together, itertools for the standard lazy patterns such as chain, islice and groupby, and any and all for short-circuiting checks. Most loops that build a list, filter it and then reduce it are one line of these, and the one line is both faster and easier to check.\n\nLaziness is how Python handles data larger than memory without changing how the code reads. The trade is that work happens where it is consumed rather than where it is written, so errors and side effects appear somewhere other than the line that seems to cause them.","python-gil":"The GIL and how to work with it\n\nCPython has a global interpreter lock: one thread executes Python bytecode at a time, per interpreter. Threads are therefore real operating system threads that take turns, which means threading gives you concurrency and not parallelism for anything that computes. Four threads doing arithmetic on four cores run at roughly the speed of one.\n\nThe lock is released around blocking operations, which is why threading is still the right tool for IO. A thread waiting on a socket, a file or a database has released the lock, so other threads run. For a program that spends its time waiting, threads work exactly as you would hope, and asyncio does the same job with less memory per task and explicit switch points.\n\nFor CPU-bound work the answer is more than one interpreter: multiprocessing, or a process pool, so each process has its own lock and its own memory. The cost is that arguments and results are pickled and copied between processes, so the work per task has to be large enough to be worth the transfer. The other answer is to leave Python for the hot loop, which is what NumPy, Polars and every serious numeric library already do by releasing the lock and running C.\n\nThis is changing, and it is worth being precise about how far. PEP 703 added a free-threaded build in Python 3.13, where the interpreter lock is genuinely absent and threads run on separate cores. It shipped as experimental; PEP 779 moved it to officially supported in Python 3.14. It is still not the default build, single-threaded code pays a small penalty for it, and a long tail of C extensions has to be made thread-safe before it is boring. So the practical advice stands for now, threads for waiting and processes for computing, while the reason behind it is being removed underneath.\n\nThe GIL is the reason a Python program that looks parallel is not, and the reason the standard advice is threads for waiting and processes for computing. Knowing which of the two your workload is decides the concurrency model before any code is written.","python-typing":"Type hints, and what they do not do\n\nPython's type hints are annotations that the interpreter records and does not enforce. Passing a string where an int is annotated runs perfectly happily. The value comes entirely from tools: a checker such as mypy or pyright reads them and tells you before you run anything, and an editor uses them for completion and navigation.\n\nThat makes them a documentation and tooling feature with teeth, and the returns are highest exactly where dynamic typing hurts most: function boundaries in a large codebase, data passed between modules, and anything another team will call. Annotating internal one-line helpers has a much worse ratio.\n\nThe pieces worth learning are Optional for the value that might be None, which is the most common runtime error in any language that has null, union types for genuine alternatives, Protocol for structural typing so a parameter can require behaviour rather than inheritance, and generics for containers whose contents matter. TypedDict is the pragmatic answer for the dictionary shapes that real programs pass around.\n\nThe trap is believing the annotations at run time. Data arriving from a network, a queue or a file is whatever it is, and an annotation saying it is a User does not make it one. Validation at the boundary, with a library such as Pydantic or a hand-written check, is what turns a hint into a guarantee, and the combination of the two is what makes a typed Python codebase actually safer rather than merely better documented.\n\nHints move a class of errors from run time to check time, but only for code that a checker sees. At the edges, where data arrives from outside the program, they describe an intention rather than a fact, and the difference has to be closed by validation.","js-event-loop":"The event loop\n\nJavaScript runs your code on one thread. Everything else, timers, network responses, file reads, user events, happens elsewhere and is handed back as a task to run when the thread is free. The event loop is the mechanism: run the current task to completion, then take the next one.\n\nThe rule that follows is that nothing else happens while your code runs. A loop that takes 300 milliseconds blocks rendering, input handling and every pending callback for 300 milliseconds. On a page, that is a frozen interface; in Node, that is every concurrent request waiting. This is the single most important operational fact about the language.\n\nNot all queued work is equal. Promises resolve on the microtask queue, which is drained completely after the current task and before the next one, while setTimeout schedules a macrotask that waits its turn. So a promise chain that never awaits anything real can starve the timer queue, and a setTimeout with zero delay still runs after every pending promise callback.\n\nAsync and await do not add threads. They mark the points where a function may pause and let the loop run something else, then resume. That is why an await inside a loop serialises the whole loop, and why Promise.all is the difference between ten sequential requests and ten concurrent ones. The work is still on one thread; only the waiting overlaps.\n\nOne thread means the cost of any slow synchronous operation is paid by everything else in the program. Knowing what yields and what does not is the difference between a page that stays responsive and one that freezes for reasons nobody can see in the code.","js-closures-this":"Closures, prototypes and this\n\nA closure is a function plus the variables it captured where it was defined, and it is the mechanism behind most JavaScript patterns: callbacks that remember context, module privacy, function factories, hooks. Understanding that the function holds the variable rather than its value at the time explains both the power and the classic loop bug that let was introduced to fix.\n\nObjects inherit through a prototype chain rather than through classes. A property lookup walks from the object to its prototype and onwards until it finds the name or runs out, and class syntax is a more familiar spelling of exactly that. Knowing the chain exists explains why adding a method to a prototype affects every existing instance, and why a property that shadows one further up hides rather than replaces it.\n\nThen there is this, which is bound by how a function is called rather than where it is defined. Extract a method from an object and call it on its own and this is no longer that object, which is the source of the callback that mysteriously stops working. Arrow functions do not bind their own this, taking it from the enclosing scope, which is why they are the default inside callbacks and the wrong choice for an object method that needs the receiver.\n\nThe practical rules: use arrow functions for callbacks, use regular functions or classes for methods that need this, and prefer passing values explicitly over relying on binding. Most this-related bugs disappear when the code stops depending on how a function will be called later.\n\nClosures and prototypes are the two mechanisms the whole language is built from, and this is the one piece of it decided at the call site rather than at the definition. Every framework pattern that looks like magic is one of the three.","js-coercion":"Equality, coercion and the sharp edges\n\nJavaScript will convert types to make a comparison work, and the rules are more elaborate than anyone can hold in their head. The practical answer is to use strict equality, which compares without converting, and to convert deliberately when you mean to. Almost every surprising comparison in the language comes from the loose operator being allowed to guess.\n\nThe specific facts worth memorising are short. NaN is not equal to itself, which is why isNaN and Number.isNaN exist. typeof null returns object, a bug preserved since 1995 for compatibility. An empty array is truthy on its own and still equal to false and to zero under loose comparison, because the loose operator converts it to an empty string and then to a number, so the same value answers yes and no depending on how it is asked. Adding a number to a string concatenates, and subtracting converts, so the same two values produce a string with one operator and a number with another.\n\nFalsy values are a fixed set worth knowing exactly: false, 0, minus 0, 0n, the empty string, null, undefined and NaN. Everything else is truthy, including empty arrays and empty objects, which is why checking a response by truthiness rather than by a property is a reliable way to accept something empty as success. That is also why the previous paragraph is not a contradiction: an empty array is truthy under if, and equal to false under the loose operator, because those are two different questions with two different conversion rules.\n\nModern syntax removes most of the remaining traps. Optional chaining reads a nested property without throwing when something in the middle is missing, and nullish coalescing supplies a default only for null and undefined rather than for every falsy value, so a legitimate zero or empty string is no longer replaced by a fallback. That last distinction fixes a whole category of quiet bugs in configuration handling.\n\nThe language guesses when you let it, and the guesses are consistent rather than sensible. Strict equality, explicit conversion and nullish coalescing remove the guessing, which is why every serious style guide requires them.","ts-structural":"TypeScript is structural, and it disappears\n\nTypeScript checks shapes rather than names. If an object has the properties a type requires, it satisfies that type, whether or not anyone declared a relationship. That is structural typing, and it is why you can pass an object literal to a function expecting an interface without implementing anything, and why two identically shaped types from different libraries are interchangeable.\n\nThe second fact is that all of it is erased. Types exist during compilation and produce no runtime code at all, so nothing checks a value at the boundary of the program unless you write that check. An API response cast as a User is a User as far as the compiler is concerned and whatever the server actually sent as far as the program is concerned, which is where a large share of production TypeScript errors come from.\n\nThe tools for closing that gap are narrowing and validation. Type guards, discriminated unions and the unknown type let you start from I do not know what this is and prove what it is with code the compiler follows. Using unknown rather than any for external data is the single highest-value habit: any switches the checker off silently, unknown forces you to establish what you have before using it.\n\nBeyond that, the type system is expressive enough to encode real rules: unions to make invalid states unrepresentable, generics to keep containers honest, mapped and conditional types to derive one shape from another so they cannot drift apart. It is also expressive enough to write types nobody can read, and the point at which a type needs a comment to explain it is usually the point to simplify it.\n\nStructural typing makes TypeScript pleasant to adopt gradually, and erasure means it protects the inside of the program and nothing at its edges. Validating external data and preferring unknown over any is what turns compile-time confidence into runtime safety.","js-modules":"Modules, bundling and what you ship\n\nJavaScript spent a decade with no module system, then acquired two. CommonJS, with require and module.exports, resolves at run time and returns whatever the module currently exports. ES modules, with import and export, are static: the imports are known before any code runs, which is what allows a bundler to see the whole graph and remove what nothing uses.\n\nThat difference is the reason tree shaking works with one and not the other. A bundler can prove that an exported function is never imported and drop it, but only when the imports cannot change at run time. Importing a whole library for one function still costs the whole library if that library ships CommonJS, or if it has side effects at module scope that the bundler dare not remove.\n\nWhat actually reaches a browser is a bundle, and its size is the number that matters. Compressed transfer size is the honest measure rather than the raw file size, and the thing to watch is not the total but what a first visit needs before the page is usable. Code splitting by route, and lazy loading anything below the first screen, is how a large application stays fast to start.\n\nThe dependency tree is where the weight hides. A single small utility can pull in a date library, a polyfill set and an internationalisation table, and none of it is visible in the import statement that caused it. Reading the bundle analysis once a quarter is a fifteen minute exercise that routinely finds a megabyte of something nobody meant to include.\n\nTwo smaller decisions carry more weight than they appear to. A dependency added for one helper function is a maintenance and supply chain cost that never goes away; writing the twelve lines yourself is often correct. And a polyfill for a feature every target browser has shipped for years is pure weight, which is what a browserslist configuration is for: it tells the toolchain what you actually support so it can stop compensating for browsers nobody runs.\n\nThe module format decides whether a bundler can remove unused code, and the dependency tree decides how much there is to remove. Both are invisible in the source and obvious in the bundle, which is why the analysis is worth looking at rather than reasoning about.","sql-joins":"Joins and the relational model\n\nSQL is declarative: you describe the result you want and the database decides how to produce it. That is why two queries returning the same rows can differ in cost by orders of magnitude, and why reading the plan matters more than tuning the text.\n\nA join combines rows from two tables on a condition. An inner join keeps only matching pairs; a left join keeps every row from the left side, filling in nulls where there is no match. The most common bug in this area is a left join with a condition on the right table in the WHERE clause, which discards the null rows and silently turns it back into an inner join. Conditions on the outer side belong in the ON clause.\n\nDuplicates are the other classic surprise. Joining to a table with several matching rows multiplies the left rows, so a sum over that result is inflated and looks plausible. When a query starts by adding DISTINCT to fix a total, the real fix is almost always to aggregate the right-hand table first and join to that.\n\nThe mental model that keeps this straight is set-based rather than procedural. A query is not a loop over rows; it is a description of a set. Once that clicks, GROUP BY becomes partitioning a set rather than accumulating a variable, and HAVING becomes filtering the groups after aggregation rather than the rows before it, which is the distinction that trips people in interviews.\n\nThinking in sets rather than in loops is what separates SQL that works from SQL that works on the test data. The two failure modes to recognise are a left join demoted to an inner join by a WHERE clause, and totals inflated by a one-to-many join.","sql-indexes":"Indexes and query plans\n\nAn index is a sorted structure, usually a B-tree, that lets the database find rows without reading the whole table. It costs storage and slows writes, because every insert and update maintains it, which is why indexing every column is not a strategy.\n\nComposite indexes have an ordering rule that decides half of real query performance: an index on (customer_id, created_at) can serve a lookup by customer, and by customer and date together, and cannot serve a lookup by date alone. It is a phone book sorted by surname then first name. Choosing the column order is choosing which queries the index can answer.\n\nIndexes are also easy to disable by accident. Wrapping a column in a function, comparing it to a different type, or starting a LIKE pattern with a wildcard all force a scan, because the index is sorted by the raw value and the query is asking about something else. Most sudden slowdowns after an innocent change are one of those three.\n\nThe way to know rather than guess is EXPLAIN, ideally with ANALYZE so the numbers are measured rather than estimated. What to look for is short: a sequential scan on a large table, an estimated row count far from the actual one, which means the statistics are stale, and a nested loop over many rows where a hash join would be cheaper. A covering index, one that contains every column the query needs, lets the database answer without touching the table at all, which is the largest single win available on a hot read path.\n\nIndexes are the difference between a query that scales and one that works until the table grows. The ordering rule for composite indexes and the three ways to accidentally disable one cover most of what goes wrong in practice.","sql-aggregation":"Aggregation and window functions\n\nGROUP BY collapses rows into one row per group, and every selected column must either be in the group or inside an aggregate, because there is no sensible answer otherwise. WHERE filters rows before grouping and HAVING filters groups after it, which is the distinction that decides whether a query is filtering the input or the result.\n\nWindow functions do the other thing people want, which is to compute across related rows while keeping every row. A running total, a rank within each customer, the difference from the previous row, the average over a trailing seven days: each is one OVER clause. Before window functions the same results needed a self-join or application code, and both were slower and harder to read.\n\nThe three parts of a window are worth learning as a unit: PARTITION BY chooses the group, ORDER BY chooses the order within it, and the frame chooses how many rows around the current one to include. ROW_NUMBER, RANK and DENSE_RANK differ only in how they treat ties, and choosing wrongly is a quiet way to lose or duplicate rows in a top-N-per-group query.\n\nThat top-N-per-group pattern is the one to remember, because it appears constantly: number the rows within each partition by the ordering you care about, then keep those numbered at or below N. It replaces a correlated subquery that gets slower with every group, and it reads as what it does.\n\nWindow functions cover the space between one row per row and one row per group, which is where most reporting questions live. Knowing them turns queries that would need application code into a single statement the database can plan.","sql-transactions":"Transactions in practice\n\nA transaction groups statements so they succeed or fail together. That much is familiar; what matters in practice is what other transactions can see while yours is running, which is the isolation level, and it is usually left at whatever the database defaults to without anyone choosing it.\n\nRead committed, the common default, means you never see uncommitted data and can see different results if you run the same query twice, because other transactions commit in between. Repeatable read fixes the second query to the same snapshot. Serializable behaves as though transactions ran one after another, and pays for it in aborts under contention. The anomalies these prevent, dirty reads, non-repeatable reads, phantoms and write skew, are worth being able to name because each maps to a real bug.\n\nThe practical rule is to keep transactions short and to keep anything slow outside them. A transaction that holds a row lock while calling an external API holds it for the length of that call, and a queue of requests forms behind it. The same applies to user interaction: never hold a transaction open across a form being filled in, which is exactly what optimistic concurrency and a version column exist to replace.\n\nTwo more things that catch people. Deadlocks are normal under concurrency and are resolved by the database aborting one transaction, so application code has to be prepared to retry, which means the work must be safe to repeat. And a transaction rolling back does not undo side effects outside the database: emails sent, files written and messages published are gone regardless, which is why those belong after the commit or behind an outbox.\n\nThe default isolation level is a decision made for you, and it is right often enough that nobody notices until a report double-counts. Short transactions, an explicit choice of level for the paths that need it, and a retry for deadlocks cover almost every case.","sql-schema":"Schema design and constraints\n\nA schema is the one place a rule can be enforced for every writer, including the script somebody runs by hand at midnight and the service written next year by someone who never read your validation code. That is the argument for putting constraints in the database rather than only in the application: application checks hold for the paths that remember them, and constraints hold for all of them.\n\nThe ones that earn their keep are unremarkable. NOT NULL, so a column that must have a value cannot quietly hold nothing. Foreign keys, so a row cannot reference something that does not exist. Unique constraints, which are the honest way to express at most one of these and are also how you make an upsert safe under concurrency. CHECK constraints for the small invariants, such as a quantity that cannot be negative.\n\nNullable columns deserve more suspicion than they get, because NULL is not a value but the absence of one, and it propagates: any comparison with it is unknown rather than false, so a filter excluding one value also excludes the nulls unless you say otherwise. Every nullable column is a branch in every query that touches it, so the question is always whether absence is genuinely a state this thing can be in.\n\nChoose types deliberately rather than by habit. A timestamp with a time zone rather than without, because the version without one stores an instant with no way of knowing what it meant. Text rather than a guessed length limit, since the constraint belongs where it can be validated with a reason. A decimal type or integer minor units for money. An enumerated type or a lookup table for a fixed set, so a typo is rejected rather than persisted.\n\nMigrations are part of the design, not an afterthought. Every change should be reversible, applied in a version-controlled sequence rather than by hand on a console, and safe with both the old and the new application running, which is what expand and contract exists for. A schema that can only be changed during downtime is a schema that stops changing.\n\nConstraints are the only validation that holds for every writer, and they are checked by the database rather than by the code path that happened to be used. Everything else in schema design follows from deciding what must always be true and then saying so where it cannot be bypassed.","jvm-basics":"The JVM: bytecode, JIT and warm-up\n\nJava compiles to bytecode rather than to machine code, and the JVM executes that bytecode on whatever hardware it finds. That indirection is the whole design: one artefact runs anywhere there is a JVM, and the machine code is produced at run time by a compiler that can see what the program is actually doing.\n\nIt starts by interpreting, then compiles the parts that run often. Methods and loops that pass a threshold are handed to the just-in-time compiler, first to a fast compiler that produces adequate code quickly, then to an optimising one for the small number of methods that dominate the profile. That is why a Java service is slow for its first few thousand requests and fast afterwards, and why a benchmark that does not warm up measures the interpreter.\n\nBecause the optimiser sees the running program, it can do things an ahead-of-time compiler cannot: inline a virtual call because only one implementation has ever been loaded, unroll a loop whose bounds are known in practice, remove a lock the escape analysis proves is only ever held by one thread. It also has to undo those decisions when a new class arrives and invalidates them, which is called deoptimisation and is why performance can change hours into a run.\n\nMemory splits into the heap, shared and garbage collected, and per-thread stacks holding frames and local variables. An object lives on the heap and a reference to it lives on a stack, which is the source of the usual confusion about whether Java is pass-by-value. It is: the value being passed is the reference, so a method can mutate the object and cannot repoint the caller's variable.\n\nThe practical consequences are short. Measure after warm-up or do not measure. Expect startup to cost, which is why serverless Java leans on class data sharing and on ahead-of-time compilation with GraalVM. And treat the JVM's flags as a real interface: heap sizing and collector choice are the two settings that change behaviour most, and both have sensible defaults that are worth understanding before overriding.\n\nAlmost everything surprising about Java performance comes from the code being compiled while it runs. It explains the warm-up, the benchmarks that lie, the optimisations no static compiler could make, and the moment three hours in when the profile shifts because something deoptimised.","java-collections":"Collections, equals and hashCode\n\nThe collections framework is small enough to hold in your head and the choices matter more than the syntax. ArrayList is a growable array: constant-time access by index, linear removal from the middle. LinkedList is almost never the right answer despite what its name suggests, because pointer chasing loses to cache locality on modern hardware. HashMap is the workhorse. TreeMap keeps keys sorted, which is what you want when the question includes everything between two values.\n\nThe contract that catches people is between equals and hashCode. Two objects that are equal must return the same hash code, or a HashMap will look in the wrong bucket and fail to find something it is holding. Override one and you must override the other, which is why records exist: a record generates both from its components and removes the whole category.\n\nMutable keys break the same machinery from the other direction. Put an object in a HashSet, change a field that participates in its hash, and the object is now in the wrong bucket: still present, unreachable by lookup, and it will not be found by contains even though iteration will show it. Keys should be immutable, which in practice means records or classes with final fields.\n\nHashMap's implementation is worth one sentence of knowledge: buckets hold a linked list and convert to a balanced tree once enough entries collide, which turns a degenerate O(n) bucket into O(log n) and defuses the collision attack that this change was introduced to address.\n\nTwo habits save real time. Prefer the interface as the declared type, so List rather than ArrayList, because it keeps the implementation a choice rather than a commitment. And be deliberate about mutability: List.of returns an immutable list, Collections.unmodifiableList wraps a mutable one in a view that still changes underneath you, and confusing the two produces a defensive copy that defends nothing.\n\nThe equals and hashCode contract is the one piece of Java that will silently lose data in a collection rather than throwing, and mutable keys are the version of it that survives code review. Both are removed by making key types immutable, which is what records were added for.","java-gc":"Garbage collection and pauses\n\nGarbage collection removes a category of bug rather than a category of work. Nothing is freed by hand, so use-after-free and double-free do not exist, and in exchange the runtime decides when to reclaim memory, which means it sometimes decides during a request you cared about.\n\nThe observation the collectors are built on is that most objects die young. Allocation happens in a small young region, collection there is cheap because it copies the few survivors rather than visiting the many dead, and anything that survives repeatedly is promoted to an older region collected far less often. That generational split is why allocating short-lived objects in Java is much cheaper than intuition suggests.\n\nThe collector you get by default is G1, which divides the heap into regions and collects those with the most garbage first, aiming at a pause time goal you can set rather than a fixed schedule. For workloads that cannot tolerate pauses at all, ZGC and Shenandoah do most of their work concurrently with the application and hold pauses to a small number of milliseconds regardless of heap size, at the cost of some throughput.\n\nWhat actually causes trouble is rarely the collector's algorithm. It is a memory leak wearing a different name: a cache with no eviction, a listener never unregistered, a thread-local on a pooled thread, a static map that only grows. The heap fills, the collector runs more and more often to reclaim less and less, and the service spends its time collecting rather than working before it finally fails.\n\nSo the useful skill is reading the evidence rather than tuning flags. Allocation rate, pause time distribution, and the size of the old generation after each full collection: if that last number is rising over hours, you have a leak and no collector setting will help. A heap dump names the objects and what is holding them, which is the answer rather than the symptom.\n\nTuning collector flags is where people start and it is almost never the fix. The two things worth knowing are that short-lived allocation is cheap by design, and that a rising post-collection heap size is a leak, which is a code problem that no amount of configuration will collect around.","java-concurrency":"Threads, executors and virtual threads\n\nJava threads are operating system threads, so each carries a stack measured in hundreds of kilobytes or more and a context switch costs the kernel real time. That is why a thread per request stopped scaling, why thread pools exist, and why a generation of frameworks went asynchronous and reactive to avoid blocking a thread on IO.\n\nAn executor separates the work from the thread running it, which is the right abstraction and comes with a decision people leave at its default: the queue. An unbounded queue turns overload into an out-of-memory failure rather than a rejection, which is the backpressure argument in its most concrete form. A bounded queue with a sensible rejection policy converts the same overload into an error the caller can act on.\n\nVirtual threads, standard since Java 21, change the arithmetic underneath all of this. They are scheduled by the JVM onto a small pool of platform threads and unmount when they block, so a blocking call no longer occupies an operating system thread and a million concurrent tasks becomes reasonable. The point is that ordinary blocking code becomes the scalable style again, which removes most of the reason to write reactive pipelines that were hard to read and harder to debug.\n\nThere is a caveat worth carrying. Before Java 24 a virtual thread blocking inside a synchronized block pinned its carrier, so a small number of pinned threads could stall everything; JEP 491 removed that specific pin. Native calls and a few other cases can still pin, and the JVM emits an event when it happens, so the modern advice is to measure rather than to avoid synchronized on principle.\n\nThe memory model is the part no thread abstraction removes. Without synchronisation there is no guarantee that one thread sees another's write, at all, ever: the compiler and the processor may reorder, and a field read in a loop may be hoisted out of it. volatile, synchronized and the concurrent utilities all establish the happens-before relationships that make visibility defined, and none of them is optional because a test happened to pass.\n\nVirtual threads make the simple style the fast style again, which is a rare direction of travel. What they do not change is the memory model: shared mutable state still needs synchronisation for the change to be visible at all, and that is a correctness question rather than a performance one.","java-generics":"Generics, erasure and what survives to run time\n\nJava generics are checked at compile time and erased afterwards. A List of String and a List of Integer are the same class at run time, and the compiler inserts the casts that make it safe. That decision was made for compatibility, so that generic code could interoperate with the collections written before generics existed, and everything odd about them follows from it.\n\nThe visible consequences are a short list worth memorising. You cannot ask whether something is a List of String at run time, because the answer no longer exists. You cannot create an array of a generic type. You cannot overload two methods that differ only in their type parameter, because after erasure they have the same signature. And a cast to a generic type is unchecked, which is the compiler telling you it has stopped being able to help.\n\nWildcards are where the syntax earns its reputation and the rule behind them is simple: a producer you read from is declared with extends, a consumer you write to with super. A List of some subtype of Number can be read as Number and cannot be written to, because the compiler does not know which subtype it holds. Once that clicks, the signatures in the standard library stop looking arbitrary.\n\nThe pattern for recovering the erased type is to pass it explicitly, as a Class object or through the trick of subclassing a generic type so the parameter is recorded in the class file. Serialisation libraries do this constantly, which is why deserialising into a generic collection needs a type token rather than a class literal.\n\nIt is worth contrasting this with the alternative, because it explains a real performance gap. C# reifies generics, so a list of integers holds integers rather than boxed objects, while Java boxes every primitive stored in a collection: a list of a million ints is a million objects with headers, pointers and cache misses. Project Valhalla is the long-running effort to close that gap, and until it lands, primitive-heavy code uses arrays or a specialised library rather than the collections framework.\n\nErasure buys compatibility with pre-generic code and charges for it at run time, in reflection that cannot see the type and in boxing that costs memory and locality. Knowing that the type is gone explains every awkward generic signature and every library that asks you to pass a type token.","go-goroutines":"Goroutines and the scheduler\n\nA goroutine is a function running concurrently, started by putting go in front of a call. It begins with a stack of about two kilobytes that grows and shrinks as needed, which is why a program can hold hundreds of thousands of them where the same number of operating system threads would exhaust the machine.\n\nThe runtime multiplexes them onto a small number of operating system threads, one per available core by default. When a goroutine blocks on a channel or on IO the scheduler runs something else on that thread, and when it blocks in a system call the runtime can hand the thread's queue of work to another thread entirely. All of it is invisible from the code, which is the point: you write blocking code and get non-blocking behaviour.\n\nWhat is not invisible is that goroutines are not free of responsibility. Starting one without knowing how it ends is the standard Go bug: a goroutine blocked forever on a channel nobody will send to is a leak, holding its stack and whatever it captured, and nothing will report it. Every goroutine wants an answer to how it stops, usually a context or a closed channel.\n\nThere is also no supervision. An unrecovered panic in any goroutine takes the whole process down, not just that goroutine, so a background worker doing risky work needs its own recover at its top level. And a goroutine's result has to be delivered deliberately, through a channel or a WaitGroup, because there is nothing to return to.\n\nThe mental model that keeps this straight is that go is cheap and coordination is not. Starting work concurrently costs almost nothing; knowing when it finished, what it produced and how it stops is the actual design, and it is the part the language deliberately leaves to you.\n\nCheap concurrency changes what is worth doing concurrently, which is the language's central bet. The cost is that lifecycle is now your problem in every case: a goroutine with no defined ending is a leak that nothing detects and nothing reports.","go-channels":"Channels, select and the patterns that work\n\nA channel is a typed conduit with a rule attached: send and receive block until the other side is ready. An unbuffered channel is a rendezvous, so the send completes at the moment a receive happens, which makes it a synchronisation primitive as much as a data one. A buffered channel decouples them up to its capacity and then behaves like the unbuffered one, which is bounded backpressure by construction.\n\nselect waits on several channel operations and proceeds with whichever is ready, choosing at random between several that are. Combined with a case on a context's done channel, it is how nearly every cancellable Go loop is written, and combined with a default case it is how a non-blocking attempt is expressed.\n\nThe conventions around closing are worth learning exactly, because breaking them panics. The sender closes, never the receiver, since only the sender knows there is nothing more coming. Closing twice panics. Sending on a closed channel panics. Receiving from a closed channel returns the zero value immediately and forever, which is why the two-value receive exists to distinguish a real value from a closed channel.\n\nThree patterns cover most real use. A worker pool: several goroutines receiving from one channel of jobs and sending to one of results. Fan-in: several producers writing to a channel a single consumer drains. And done-channel cancellation: a channel closed to broadcast to every listener at once, which works because a closed channel is permanently readable and is the reason cancellation is a close rather than a send.\n\nThe advice the community repeats, share memory by communicating, is not a prohibition on mutexes. A mutex around a struct is the right answer for protecting shared state, and a channel is the right answer for handing ownership of a value from one goroutine to another. Reaching for a channel where a mutex would do produces the most convoluted Go code there is.\n\nChannels are a synchronisation mechanism that happens to carry data, which is why an unbuffered one is a rendezvous rather than a queue. Buffered channels are the language's bounded queue, and that bound is what turns overload into blocking rather than into unbounded memory growth.","go-interfaces":"Interfaces, satisfied implicitly\n\nA Go type satisfies an interface by having the methods, with no declaration that it intends to. That means an interface can be defined after the types that satisfy it, and by the package that consumes them rather than the one that provides them, which inverts the usual dependency direction and is the single most distinctive thing about designing in Go.\n\nThe convention that follows is to keep interfaces small and to define them where they are used. A consumer that needs one method declares a one-method interface and accepts anything with it, so the provider package does not have to know the consumer exists. The standard library sets the tone: io.Reader and io.Writer are one method each and compose into most of the ecosystem.\n\nThe trap is the typed nil. An interface value holds a type and a value, so an interface holding a nil pointer of a concrete type is itself not nil, and the classic version is a function returning a concrete error type as an error interface: the pointer is nil, the interface is not, and the caller's check for nil is false. Return the interface type, and return a literal nil for the no-error case.\n\nType assertions and type switches recover the concrete type when it is genuinely needed, and the two-value form is the safe one, because the single-value assertion panics on a mismatch. Reaching for them constantly is usually a sign that the interface is the wrong shape rather than that the language is in the way.\n\nGenerics, added in Go 1.18, cover the cases interfaces never could: a function that works over any ordered type without boxing, a container parameterised by element type. They did not replace interfaces, which remain the mechanism for behaviour, and the useful split is that generics abstract over types while interfaces abstract over behaviour.\n\nImplicit satisfaction lets the consumer define the contract, which is a genuinely different way to arrange a dependency: the package that needs the behaviour owns the interface, and the package that provides it never imports the one that uses it.","go-errors":"Errors as values\n\nGo has no exceptions for ordinary failure. A function that can fail returns an error alongside its result, and the caller deals with both. The consequence is visible immediately in the code: the failure path is written out rather than implied, which is verbose and is the point, because the alternative is a failure path nobody looked at.\n\nErrors are values satisfying a one-method interface, so they can be compared, wrapped, inspected and defined by any package. Wrapping with the percent-w verb records the cause, and errors.Is walks that chain to answer whether a particular sentinel is anywhere inside it, while errors.As finds a specific error type and gives you access to its fields. Those three together replaced a decade of string matching on error messages.\n\nThe habit that makes the verbosity pay is adding context at each layer. Returning an error unchanged from five levels of call stack produces a message with no idea where it came from; wrapping it with what this layer was attempting produces a sentence that reads like a trace, which is what somebody wants at three in the morning.\n\npanic is for programmer error and for situations where continuing is meaningless, not for a file that is missing or a request that failed. recover exists mainly to stop a panic in one request taking down a server, which is what an HTTP framework does at the top of each handler, and it should not be used as a general error mechanism because it hides the failure path the language deliberately exposes.\n\nThe thing to watch for is the ignored error, written as an underscore or simply omitted. It is the same swallowing that an empty catch block does in another language, with the difference that Go's linters can see it, which is why vetting for unchecked errors is standard in any serious pipeline.\n\nFailure is in the signature, so a caller cannot be unaware of it and a reader can see every path. The cost is verbosity, and the payment for it is context: wrapping at each layer is what turns a bare message into something that identifies where and why.","go-context":"Context, cancellation and deadlines\n\nA context carries a deadline, a cancellation signal and request-scoped values across API boundaries. By convention it is the first parameter of any function that does IO or can block, which is why almost every signature in a Go service starts with it, and the convention exists so that cancellation can reach anywhere without every layer inventing its own mechanism.\n\nCancellation propagates down a tree. Deriving a context gives a child that is cancelled when its parent is, so cancelling at the top reaches every operation started beneath it, and a deadline set at the edge becomes the budget for the whole call rather than a fresh timeout at each hop. That is the propagating deadline that other ecosystems build by hand, available as a language convention.\n\nUsing it correctly is a short list. Select on ctx.Done in any loop that might run long. Pass the context to every call that takes one, including the database driver and the HTTP client, since a cancellation that stops at your code and not at the query has cancelled nothing expensive. Call the cancel function returned when deriving a context, always with defer, because failing to do so leaks the context and its timer.\n\nValues in a context are the part most often misused. They are for request-scoped data that genuinely crosses layers, a trace id, a request id, an authenticated principal, not for passing dependencies that a function could have been given as parameters. Because the map is untyped, anything hidden in it is invisible to the compiler and to the next reader, which is why the practice is to keep it to a handful of well-known keys.\n\nThe most common bug is a cancelled context reaching only part of the work. A handler returns, the context is cancelled, and a goroutine started with the request context finds its context already dead, or worse, a background task was given the request context and dies when the request completes. Background work needs its own context with its own lifetime, and saying so explicitly is cheaper than diagnosing it.\n\nContext is how a deadline set at the edge becomes a budget the whole call tree respects, which is the property that stops a service working on results nobody is waiting for. It only holds if every layer passes it on: one call that ignores it becomes the place where cancellation stops.","cache-strategies":"Cache-aside, write-through, write-behind\n\nCache-aside is the default nearly everywhere: the application checks the cache, and on a miss it reads the database and populates the cache itself. The cache knows nothing about the database, which is the whole appeal. Any store can sit behind it, only the data actually asked for is ever cached, and the failure mode is mild, since a cache that is empty or unreachable costs you a database read rather than an error.\n\nRead-through moves that logic into the cache or its client library: the application asks the cache, and the cache fetches from the source on a miss. The behaviour is identical from outside; what changes is where the code lives and who is responsible for getting it right. It is worth having when a dozen services read the same data, because otherwise each of them implements the miss path slightly differently and one of them forgets the TTL.\n\nWrite-through writes to the cache and the database together, so the cache is never stale for data that has been written through it. Every write pays both costs, and you have made the cache part of the write path, which means a cache outage is now a write outage unless you are careful. It fits read-heavy data with strict freshness needs and a modest write rate.\n\nWrite-behind acknowledges the write once it is in the cache and flushes to the database later, in batches. It is the fastest option and the only one that can lose committed data: anything not yet flushed when the node dies is gone, and the application already told the user it was saved. That is acceptable for view counters and session activity, and not for anything a person would notice missing.\n\nRefresh-ahead is the fifth and least discussed: refresh an entry before it expires, based on the prediction that something read this often will be read again. It keeps hit rates high for a known hot set and wastes work on everything else, so it belongs on a small, identified set of expensive keys rather than as a general policy.\n\nThe choice is usually not about performance, because all five are fast on a hit. It is about what happens on a miss, on a write, and when the cache is unavailable, and those are three questions with different answers per dataset. A system with one caching strategy applied uniformly is a system where four of those questions were never asked.\n\nCache-aside wins by default because its failure mode is mild and its coupling is low: the cache can be flushed, restarted or lost entirely and the system still answers, more slowly. Write-behind trades durability for speed, which is only honest when the data is genuinely disposable, and write-through puts the cache in the write path, which is a decision worth making deliberately rather than inheriting from a library default.","invalidation":"Invalidation: TTL, explicit, and versioned keys\n\nTTL expiry is the simplest thing that works: entries die after a set time, and you accept staleness up to that time in exchange for writing no invalidation logic at all. The reason it survives contact with production is that it is self-healing. Any bug, any missed path, any inconsistency corrects itself within one TTL, which is a property none of the cleverer schemes have.\n\nExplicit invalidation deletes the key when the underlying data changes. It is fresher and it is fragile, because every write path must know every key derived from that data. Add a new page that composes the same record into a different key, forget the delete, and you have a permanently stale entry that no amount of waiting will fix, discovered eventually by a customer who says the price is wrong.\n\nVersioned keys sidestep deletion entirely. Put a version or an updated-at timestamp in the key, bump it on write, and old entries become unreachable rather than wrong. Invalidation becomes one write to one value instead of a fan-out of deletes, and the stale entries age out on their own. This is usually the best of the three, and the cost is that the old entries occupy memory until they expire, which is a memory problem rather than a correctness one.\n\nWhichever you choose, jitter the TTLs. Entries created together with identical lifetimes expire together, and a cache populated at deploy time will empty itself in one synchronised moment some hours later, which arrives as an unexplained database spike at an odd hour. A random spread of ten to twenty per cent is enough to turn the cliff into a slope.\n\nDistributed invalidation is where this gets genuinely hard, because deletes have to reach every node in every region, and a delete that is lost in transit leaves one region serving stale data indefinitely with nothing to detect it. The pragmatic answer most large systems reach is to stop trying: short TTLs plus versioned keys, so correctness never depends on a message arriving. There is one more failure worth naming: the stale set. A client reads a value, is slow, and writes what it read into the cache after a concurrent update has already invalidated it, so the cache now holds an old value with a fresh lifetime. Facebook's leases exist partly for this, since a lease token issued at read time can be invalidated by an intervening write, letting the cache reject the late set.\n\nVersioned keys make invalidation a property of the read path rather than a duty of every write path, and duties spread across write paths rot as the system grows. The general principle: prefer schemes where a missed step degrades to a slower read rather than to a wrong answer that persists.","redis-structures":"Redis beyond get and set\n\nRedis stores structures, not just strings, and treating it as a string cache is the most common way to leave performance on the table. Sorted sets maintain order by score and answer rank and range queries directly, which is a leaderboard or a time-ordered feed without the application fetching everything to sort it. Hashes let you update one field of an object without rewriting the whole record. Sets handle membership and deduplication. Streams add consumer groups and acknowledgements, which is real message processing rather than a list pretending to be a queue.\n\nThe operational model matters as much as the data model, and the key fact is that Redis executes commands on a single thread. Redis 6 added threaded I/O for reading and writing sockets, but the commands themselves still run one at a time, which has two consequences. One slow command blocks everything, so a KEYS scan over a large keyspace or an O(n) operation on a million-element set is an outage rather than a slow query. And a single hot key is bounded by one core, no matter how many nodes you add.\n\nMemory is managed by an eviction policy you choose, and the default is the one that surprises people: noeviction, which starts returning errors on writes when maxmemory is reached rather than making room. For a cache you almost certainly want allkeys-lru or allkeys-lfu. Both are approximations, sampling a handful of keys and evicting the worst rather than maintaining a true ordering, because exact LRU across millions of keys costs more than it saves. LFU, added in Redis 4.0, is the better choice when a small set is genuinely hot, since one burst of scanning traffic cannot evict everything the way it can with LRU.\n\nExpiry and eviction are different mechanisms and get confused constantly. Expiry removes a key because its TTL passed; it is checked lazily on access and sampled by a background cycle, so an expired key can occupy memory for a while after it logically died. Eviction removes a key because you are out of memory, regardless of TTL. A cache filling up with keys that have no TTL at all will evict things you wanted while holding things you did not.\n\nClustering shards the keyspace across 16,384 hash slots assigned to nodes, and the constraint to design around is that a multi-key operation only works when the keys live in the same slot. Hash tags, the braces in user:{42}:profile, force related keys together for exactly this reason. Get that wrong and transactions and Lua scripts that worked on a single node start failing in the cluster with a cross-slot error, usually the week after launch.\n\nFinally, persistence. Redis offers RDB snapshots and an append-only file, and both are useful, and neither turns a cache into a database. RDB loses everything since the last snapshot; AOF with the default fsync policy loses up to a second. Both are reasonable for a cache that would rather restart warm than cold, and neither is a promise you should make to a user about their data.\n\nChoosing the right structure often turns a read-modify-write round trip into one server-side operation, which is the difference between three network hops and one. The single-threaded execution model is the constraint behind most Redis incidents: it makes one expensive command everyone's problem, and it makes a hot key a single-core problem that horizontal scale does not solve.","cache-failures":"Stampedes, avalanches and hot keys\n\nA stampede happens when a popular key expires and every concurrent request misses at once, so all of them hit the database together to compute the same value. The load is proportional to concurrency rather than to traffic, which is why it appears suddenly at a scale that was fine yesterday. The standard fix is a short lock: one request wins the right to recompute while the others wait briefly or serve the stale value they can still see. Facebook's version is a lease. On a miss the cache hands one client a token granting permission to recompute, and tells everyone else to wait or use stale data. It is the same shape as a lock, expressed as something the cache issues, which also lets the cache reject a set whose lease was invalidated by an intervening write. Probabilistic early expiry is the other approach: as an entry approaches its TTL, each reader has a small and rising chance of refreshing it early, so the recomputation happens before the expiry rather than at it, spread across readers.\n\nAn avalanche is the same problem multiplied: many keys given identical TTLs expire simultaneously, usually because the cache was warmed in one pass at deploy time. The database sees the entire working set arrive as cold reads in a few seconds. Jitter fixes it, and it is worth adding at the moment you write the TTL rather than after the first incident.\n\nA hot key is one entry so popular that a single node becomes the bottleneck, and it is not solved by adding nodes, because the key still hashes to one of them. Redis executing commands on a single thread makes it a single-core limit. The two fixes are replication of that key under several suffixed names with clients choosing at random, and a small in-process cache in front of the shared one, holding the top few keys for a second or two. The second is the more effective and the more dangerous, since every process now has its own slightly different copy.\n\nCache penetration is the quieter cousin: requests for keys that do not exist anywhere, so nothing is ever cached and every one becomes a database read. It appears naturally with user-supplied identifiers and it is the standard shape of a cheap denial of service. The fixes are negative caching, storing a short-lived marker meaning this does not exist, and a Bloom filter holding the set of ids that do exist, which answers definitely not present in memory.\n\nThe pattern behind all four is synchronisation. Requests miss together, keys expire together, traffic concentrates on one key, or absent keys share a path with no memory. Each fix is a deliberate desynchronisation: a lock so one goes first, jitter so they separate, replication so they spread, a marker so the second one is cheap. When you can name which of the four you are looking at, the fix is usually already obvious.\n\nThese are the failures that only appear under real traffic, which is why they are asked about in interviews and why they arrive on a Friday in production. They are also the failures where the cache makes things worse than no cache at all, because a stampede concentrates load that would otherwise have been spread across the whole period.","http-caching":"Caching at the edge\n\nThe cheapest cache is the one you do not operate. HTTP has caching built into it, and a correctly labelled response can be held by the browser, by any proxy in the path and by a CDN with a few hundred points of presence, none of which you pay for or run. The whole mechanism is a handful of headers, which is why getting them wrong is both easy and expensive.\n\nCache-Control carries the instructions. max-age is how long any cache may reuse the response; s-maxage overrides it for shared caches only, which is how you say five seconds at the CDN and none in the browser. private means only the browser may store it, which is what you want for anything user-specific. no-store means keep no copy at all, and it is what people mean when they wrongly write no-cache, which actually means store it but revalidate before reuse.\n\nValidators handle the revalidation. The server sends an ETag or a Last-Modified, the client sends it back as If-None-Match or If-Modified-Since, and the server answers 304 Not Modified with no body when nothing changed. The saving is the payload, and on APIs it can be most of the traffic: GitHub does not count a conditional request that returns 304 against your rate limit, which turns polite polling into something the platform actively encourages.\n\nTwo extensions from RFC 5861 are worth more than they cost. stale-while-revalidate lets a cache serve a slightly stale copy immediately and refresh in the background, so the person waiting never pays for the refresh. stale-if-error lets it serve stale content when the origin is failing, which converts an origin outage into slightly old pages for anyone whose request the edge can answer.\n\nThe cache key is the part that bites. By default it is the URL, and Vary adds request headers to it, so Vary: Accept-Encoding is fine and Vary: User-Agent shatters your hit ratio into thousands of fragments, one per browser string. Query parameters count too, which is why an analytics parameter appended to a shared link produces a fresh miss for every recipient of that link. Normalising the key is often the single largest hit-ratio improvement available.\n\nPurging is where people reach first and should reach last. Content-addressed URLs, the hashed filenames every bundler emits, make purging unnecessary: a new build is a new URL, so the old one can be cached forever and the new one is never stale. That works precisely because the URL identifies the bytes. Applying the same immutable policy to a URL whose content can change is how a site serves a year-old page to everyone who visited during a bad deploy, and no purge fixes the copies already held in browsers.\n\nAn edge cache is the only tier that reduces latency and origin load at the same time, for a cost of nothing. The reason it is under-used is that its controls live in headers rather than in code, so they are invisible in review and nobody owns them, which is also why one wrong header can sit in production for months.","what-not-to-cache":"What not to cache\n\nCaching adds a second source of truth and a new class of bug, and it earns that when three things are true: reads dominate writes, the data tolerates some staleness, and recomputing it is genuinely expensive. Miss any one of them and you have added complexity for nothing, or worse, for a correctness problem that shows up as a customer complaint rather than as an alert.\n\nData that changes on nearly every read gains nothing. You pay the write cost, then miss anyway, and the cache becomes a tax on the read path plus an extra system to operate. Per-user data with no reuse is usually the same story: a cache entry read once before it expires has cost more than it saved, and a million of them evict the entries that were actually working.\n\nAnything where stale means wrong deserves a different answer entirely. A permission set cached for five minutes means access revoked five minutes ago still works, and that is a security failure rather than a latency tradeoff. If you must cache it, cache it for seconds, with an explicit revocation path, and write the staleness into the contract so it is a known property rather than an accident.\n\nThe arithmetic of hit ratios is worth internalising, because it is not linear and people reason about it as if it were. At a 1ms hit and a 50ms miss, a 95% hit ratio gives an average of about 3.5ms, and 99% gives about 1.5ms. That looks like a modest difference until you look at the origin instead of the average: going from 99% to 98% doubles the load reaching your database, and from 99% to 95% multiplies it by five. Cache work should be judged by what it does to the miss rate, not by what it does to the mean.\n\nIt follows that a cache is not a fix for a slow query, it is a way to run a slow query less often. The queries still run, on every miss, on every eviction, and on every cold start, which is exactly the moment you are least able to absorb them: a restart after an incident empties the cache and sends the full read load at a database that has just recovered. If the origin cannot survive its own traffic without the cache, the cache has become a load-bearing part of the system, and it should be designed as one rather than described as an optimisation. The honest question, then, is not what to cache but what staleness is acceptable for, and for how long. Answer it per dataset, write the answer down next to the TTL, and the caching design follows. If the answer is none, the fix is a faster query, a better index, or a different shape of data, and no amount of caching will substitute for it.\n\nEvery cache is a bet that stale data is cheaper than slow data, and the bet has to be made per dataset rather than per system. The failure mode people miss is the cold start: a cache is an optimisation right up until the origin cannot survive without it, at which point it is a dependency with a much weaker durability story than the database behind it.","lb-algorithms":"Round robin, least connections, hashing\n\nRound robin sends each request to the next server in turn. It is the default nearly everywhere and it is correct when requests cost roughly the same, because then counting requests is a good proxy for counting work. It stops being correct the moment one endpoint takes thirty seconds and another takes five milliseconds, since the balancer keeps feeding a server that is already saturated.\n\nLeast connections routes to whichever server currently has the fewest requests in flight, which is a much better proxy for load when durations vary. Least response time goes further and weights by observed latency, so a server that is degraded but still accepting connections gets less traffic rather than the same amount.\n\nConsistent hashing sends the same key to the same server, which is what you want when servers hold local state, a cache, or a session. It is also the option that makes your traffic distribution depend on your key distribution, so a single hot key becomes a single hot server and no amount of capacity elsewhere helps.\n\nTwo more worth knowing. Weighted round robin lets a fleet of mixed instance sizes share work in proportion to capacity, which matters during a migration between instance types. Power of two random choices is the elegant one: pick two servers at random and send the request to the less busy of the two. It needs no global state, and it gets remarkably close to least connections, which is why it is common in large distributed proxies where maintaining an exact count everywhere is impractical.\n\nLayer 4 and layer 7 is the other axis. A layer 4 balancer forwards packets by address and port and knows nothing about requests, so it is fast and it balances connections. A layer 7 balancer terminates the connection, reads the request, and can balance per request, route by path, retry a failure and add headers. With long-lived connections such as gRPC or HTTP/2, layer 4 balancing pins every request from a client to one backend, which is the trap that makes adding capacity do nothing at all.\n\nThe choice, then, follows from the traffic rather than from a preference. Uniform and stateless: round robin. Wildly variable durations: least connections or two random choices. Anything cached or held per server: hashing, with the hot key risk accepted deliberately. Long-lived connections: layer 7, or client-side balancing that knows the backend set.\n\nRound robin is the default and is wrong whenever request cost varies wildly, one slow endpoint drags a server down while the balancer keeps feeding it work.","consistent-hashing":"Consistent hashing\n\nHashing a key modulo the number of servers works perfectly until that number changes. With four servers and a fifth added, the modulus changes for every key, so roughly four keys in five map somewhere new. Every cache empties simultaneously, every request misses, and the database receives the entire working set as cold reads at the exact moment you were adding capacity because you were already under load.\n\nConsistent hashing places both servers and keys on a ring of hash values, and a key belongs to the first server clockwise from it. Add a node and only the keys between it and its predecessor move, which is roughly one over n of the total rather than nearly all of it. Remove a node and its keys go to its successor, and nothing else is disturbed.\n\nPlain rings are lumpy, because a handful of random positions rarely divide a circle evenly, and one server can end up owning a third of the space. Virtual nodes fix it: each physical server takes many positions on the ring, typically a hundred or more, so the law of large numbers does the balancing. It also gives you weighting for free, since a larger machine can simply be given more positions.\n\nTwo refinements are worth recognising. Bounded loads add a capacity limit per node, so a node that is already at its share passes the key onward rather than accepting it, which stops a hot region overwhelming one server. Rendezvous hashing reaches the same goal by a different route: hash the key with each server's name and pick the highest score, which needs no ring and handles weighting cleanly.\n\nThe property to hold on to is that this is a technique for making membership changes cheap, not for making distribution perfect. A single very hot key still lands on a single server whatever the scheme, and the answer to that is replication of that key or a small cache in front, which is a different problem with a different fix.\n\nThis is the technique that makes distributed caches and sharded stores survivable. Without it, scaling the cluster is an outage.","health-checks":"Health checks and draining\n\nA health check answers a question the load balancer is about to act on, so what it asks decides what happens during a failure. A shallow check confirms the process is alive and listening, which is cheap and catches a crashed or hung process. A deep check confirms the instance can reach the dependencies it needs to serve a request, which catches far more and is where the danger is.\n\nThe danger is correlation, and it is a property of the design rather than bad luck. Every instance checks the same database, so when that database wobbles every instance fails its check in the same second, the balancer removes all of them, and a dependency that was merely slow becomes a total outage with no healthy targets left to receive traffic. The failure path amplified the failure, which is worse than having no failure path at all, because at least some requests would have succeeded.\n\nThe compromise that works is a deep check that degrades rather than fails. Require several consecutive failures before reporting unhealthy, so a single slow response does not remove an instance. Give the check its own short timeout, well inside the interval, so a hanging dependency does not make the check itself hang. Never let a dependency the request path does not need mark you down, which sounds obvious and is routinely violated by a check that pings six services because six were available to ping. And where the balancer supports it, use the setting that stops removing instances once too few remain healthy, which converts the correlated failure into degraded service rather than none.\n\nKeep readiness and liveness separate, because they answer different questions and conflating them is how a slow dependency becomes a restart loop. Liveness asks whether to restart this process, so it should be shallow and almost never fail: a process that is running should say yes even while a dependency is down, because restarting it will not fix the database. Readiness asks whether to send it traffic, so it may legitimately say no while a cache warms or a migration finishes. Point liveness at a deep check and a database blip restarts the fleet, which loses the warm caches and the in-flight requests as well.\n\nConnection draining is the other half and it is what makes deploys invisible. When an instance is removed, in-flight requests need time to finish before the process exits, so the balancer stops sending new work, waits for a grace period longer than a normal request, and only then lets it go. Without it a rolling deploy drops live traffic on every step, which shows up as a small error spike at each deploy that everybody eventually stops noticing. The sequence that works is to fail readiness first, keep serving for a few seconds while the balancer notices, then stop accepting and finish what is open.\n\nMaking the health check depend on the database means a brief database blip marks every server unhealthy simultaneously, turning a degraded system into a total outage. The check is part of the failure path, and a failure path that amplifies is worse than no failure path at all.","global-routing":"Getting traffic to the right region\n\nLoad balancing inside a region is a solved problem with well-understood algorithms. Getting a request to the right region in the first place is a different question with three common answers, and they differ in how quickly they can change their mind.\n\nDNS-based routing hands out different addresses by geography or by health, which is simple and works everywhere. Its weakness is the one every DNS topic returns to: the answer is cached by resolvers and clients for a duration you request and do not control, so failing over means waiting for other people's caches to expire.\n\nAnycast advertises the same address from many locations and lets the network choose, so a client reaches whichever site is closest in routing terms. Failover happens in the routing layer within seconds, no cache is involved, and the address never changes. This is how large CDNs and public DNS resolvers work, and it is why they can lose a location without anybody noticing.\n\nThe third option is a global load balancer at the provider: one address, health-checked backends in several regions, traffic steered by latency or by policy. It is the least work and it puts the provider's control plane on the critical path, which is a dependency worth naming rather than assuming.\n\nWhichever routes the traffic, the harder problem sits underneath: data. Serving reads from a nearby region is straightforward; accepting writes in several is a consistency design, not a routing one. Most systems that describe themselves as multi-region are read-local and write-to-one, and being explicit about that is more honest than the label.\n\nThe routing choice is really a choice about how fast you can change your mind. DNS is minutes to hours because caches decide; anycast and provider load balancers are seconds because the network or the control plane decides. Everything else about them is secondary to that.","why-queues":"What a queue actually buys you\n\nA queue lets a request return before the work is done. The user gets a fast response, and the work happens behind them. That is the obvious benefit and the least interesting one; what a queue really buys is that the arrival rate and the processing rate stop having to match. Absorbing spikes is the clearest form of that. A synchronous system sized for average traffic falls over at four times average, because every arriving request needs a worker at the moment it arrives. The same system behind a queue turns that spike into a backlog that drains over the next few minutes, and nobody sees an error. You have traded latency for survival, deliberately, for work where latency is cheap.\n\nIt also decouples failure. When the email provider is down, a synchronous handler fails the user's request; a queued one keeps accepting work and drains when the provider returns. The blast radius of a dependency shrinks to the queue depth, which is a number you can watch, rather than to your error rate, which is a number your users watch.\n\nThe costs are real and they are all the same cost in different clothes: the system is now eventually consistent. You owe the user a way to see the outcome, which means a status somewhere and usually a notification. You owe yourself a way to see the backlog, which means monitoring the age of the oldest message rather than only its depth. And you owe the on-call an answer to what happens when the queue is 200,000 deep at 3am, which is a question worth answering before it is asked.\n\nThe failure people underestimate is that a queue hides overload instead of removing it. If the sustained arrival rate exceeds what the workers can process, the backlog grows without bound and the queue is now a very expensive way of delaying an outage: the work is late by a growing margin and eventually irrelevant. A queue smooths bursts. It cannot create throughput, and treating a growing backlog as something that will sort itself out is how a two-hour incident becomes a two-day one.\n\nSo the test for whether work belongs behind a queue is not whether it is slow. It is whether the caller needs the result to continue. Sending an email, generating a report, transcoding a video, syncing to a third party: none of those are things the user is watching. Charging a card while the customer looks at a spinner is, and putting it behind a queue means building a way to tell them what happened, which is more work than the queue saved.\n\nAdding a queue converts a latency problem into a state problem. That is usually a good trade for email, image processing or reports, and a bad one for anything the user is waiting to see.","delivery-guarantees":"At-most-once, at-least-once, exactly-once\n\nAt-most-once may drop messages. At-least-once may deliver twice. Exactly-once is what everyone wants, and end to end in a distributed system it is not achievable, because the acknowledgement itself can be lost and the sender cannot tell a lost ack from a lost message.\n\nWhat gets sold as exactly-once is at-least-once delivery plus idempotent processing, so a duplicate has no additional effect. Kafka's exactly-once semantics are real but scoped: they cover reading from a topic, writing to a topic, and committing the offset, as one atomic unit inside Kafka. The moment your handler calls Stripe, that guarantee has left the building.\n\nThe rate is the part worth understanding, and it is not a single number anyone can quote across brokers. In steady state duplicates are rare enough that a system can run for weeks without one being noticed. They do not arrive uniformly: they cluster during a consumer group rebalance, a partition, a failover or a deploy that restarts a worker mid-batch, which is precisely when everything else is also going wrong and nobody has the attention to reason about them. So the number to design against is not the average rate but the burst, and the only design that survives a burst is a handler where a repeat costs nothing.\n\nIdempotency in practice is a table of processed message ids with a unique constraint, checked inside the same transaction as the work. Insert the id, do the work, commit. A duplicate hits the constraint and rolls back having done nothing. Keep the ids for longer than your broker's maximum redelivery window, and remember that a natural key from the payload, an order id, is often better than the broker's message id, which changes on republish.\n\nThe question is never whether duplicates happen; it is whether the second one costs anything. Once the handler is idempotent, at-least-once is sufficient and the broker's guarantees stop being interesting, which is why buying a broker that advertises exactly-once buys you very little.","ordering":"Ordering and partitions\n\nOrdering guarantees are usually per partition, not global. Kafka orders within a partition; across partitions there is no order at all, and a consumer reading two partitions sees them interleaved arbitrarily.\n\nKeying by entity is what makes this workable. Hash the user id, or the order id, and every event for that entity lands in the same partition and arrives in the order it was produced. That is almost always the ordering you actually needed: nobody cares whether user A's update preceded user B's, but everyone cares that A's address change is not overtaken by A's earlier one.\n\nThe cost is that a partition is the unit of parallelism. Ten partitions means at most ten consumers in a group, and adding an eleventh does nothing. Choosing the count is a one-way door in practice, because increasing it changes which partition a key hashes to, so events for one entity end up split across the old and new partitions and ordering breaks for exactly the keys you were protecting.\n\nGlobal ordering means one partition, one consumer, and no parallelism. It is occasionally the right answer, for a ledger or an audit log, and it should be a deliberate decision with a throughput number attached rather than something arrived at by accident.\n\nThe useful question is not whether you need ordering but what you need it within. Almost every system that thinks it needs global ordering needs per-entity ordering, and the difference is the entire throughput of the system.","dead-letter":"Retries and dead letter queues\n\nA message that fails is usually retried. Without a limit, a permanently broken message is retried forever: it consumes a consumer slot, it is redelivered ahead of newer work in some brokers, and it can hold up an entire partition while everything behind it waits.\n\nA dead letter queue is where a message goes once it has failed enough times. The main queue keeps flowing, the failure is preserved rather than dropped, and someone can look at it. That last part is the point and the part usually skipped: a dead letter queue nobody reads is a slower way of deleting messages.\n\nRetries need backoff with jitter. Pure exponential backoff keeps every client synchronised, because they all wait the same intervals from the same failure, so the failing dependency is hit by the whole fleet at once, recovers, and is hit again. Full jitter, a random wait between zero and the current ceiling, spreads them out. AWS published the arithmetic on this and the difference is not marginal.\n\nDistinguish what is worth retrying. A 503 or a timeout is transient and deserves the full ladder; a 400, a schema violation or a foreign key that will never exist is permanent, and retrying it eight times over a day just delays the moment someone finds out. Route those to the dead letter immediately, with the error attached, so the queue tells you what is wrong rather than only that something is.\n\nThe retry policy is where a queue stops being a buffer and becomes a system with behaviour. Getting the limit and the backoff right is what decides whether a downstream blip is invisible or becomes your outage too.","broker-choice":"Choosing a broker\n\nThe first distinction is between a queue and a log, and most confusion about brokers comes from missing it. A queue holds work: a message is delivered, acknowledged, and removed, and the queue is empty when the work is done. A log holds a record: messages are appended and kept for a retention period, consumers track their own position, and reading does not consume anything. SQS and RabbitMQ are queues; Kafka is a log.\n\nThat difference decides more than it looks. In a log, a new consumer can start from the beginning and rebuild its state, several independent consumers can read the same stream at their own pace, and replaying a bad day is a matter of resetting an offset. In a queue, a message read by one consumer is gone, which is exactly what you want for a unit of work and useless for anything wanting a second opinion on the same events.\n\nSQS is the low-operations option: no cluster to run, effectively unlimited depth, at-least-once with best-effort ordering. Its mechanism worth knowing is the visibility timeout, which hides a message from other consumers while one works on it, defaulting to 30 seconds and extendable to twelve hours. A handler that takes longer than the timeout without extending it will find the same message being processed by somebody else, which is the most common way people discover their consumer is not idempotent. FIFO queues add ordering and deduplication with a throughput ceiling that standard queues do not have.\n\nKafka is the option that buys retention and fan-out, and charges in operational complexity. Ordering is per partition, consumers commit offsets, and the retention window is a design parameter rather than an implementation detail: it decides how far back you can replay and how long a consumer can be down before it loses data. RabbitMQ sits between them, with routing rules rich enough to express most topologies and per-message acknowledgement, and it is the right answer more often than its unfashionability suggests.\n\nRedis Streams deserve a mention because they are already installed. Consumer groups, acknowledgements and a pending entries list give real queue semantics, at a fraction of the operational cost, with the caveat that durability is Redis durability: an append-only file with a one-second fsync loses up to a second of messages on a hard failure. For work that can be recomputed, that is fine and the simplicity is worth a great deal.\n\nThe decision, then, is three questions rather than a comparison table. Does anything need to read these events more than once, or later, which means a log. Does the work need ordering, and within what, which sets the partitioning. And who operates it at 3am, which is the question that most often decides between running a cluster and paying someone else to.\n\nAlmost every broker argument is actually an argument about queue semantics versus log semantics, and it resolves the moment someone asks whether the events need to be readable twice. The operational question is the other half: a managed queue with fewer features is usually a better system than a self-hosted log nobody has time to run.","read-replicas":"Read replicas and replication lag\n\nA read replica copies the primary and serves reads, which scales read capacity. Writes still go to one place, so this buys nothing for a write-heavy workload; it is the right move when reads outnumber writes by ten to one or more, which most applications do.\n\nReplication is asynchronous by default, so a replica is always slightly behind. Usually single-digit milliseconds, and then a bulk update, a long transaction or a network hiccup pushes it to seconds or minutes, and the lag is worst exactly when traffic is highest.\n\nThat gap causes read-your-own-writes bugs. A user saves their profile, the next read lands on a replica that has not caught up, and they see the old value and conclude it did not save. They press save again. Now you have a duplicate, and a support ticket that says the site is broken, which it is.\n\nThe fix is not stronger consistency but a narrower rule: after a user writes, pin that user's reads to the primary for a few seconds. A cookie or a session flag carrying a timestamp is enough, and it costs almost nothing because only the writing user pays. The alternatives are worse: reading everything from the primary throws away the reason you added replicas, and waiting for synchronous replication makes every write as slow as your slowest replica.\n\nReplication lag is not a bug to be eliminated, it is the price of the read capacity. The engineering is in deciding which reads can tolerate it, and the honest answer is nearly all of them except the ones belonging to the person who just wrote.","sharding":"Sharding and choosing a key\n\nSharding splits data across independent databases so that writes scale, which is the one thing replicas cannot do. The shard key decides which shard holds a row, and it is the hardest decision in the system to reverse, because it is embedded in every query, every foreign key that no longer works, and every piece of application code that assumed a join was possible.\n\nA poor key creates hotspots, and the two classic mistakes are geography and time. Sharding by country puts most of the traffic on whichever country you are largest in. Sharding by timestamp puts every current write on the newest shard while the others sit idle holding history, which is the worst possible arrangement: all the storage cost of many machines and the write capacity of one.\n\nHashing an identifier distributes evenly regardless of skew, and gives up range queries in exchange. Range partitioning keeps ordered scans cheap and invites hotspots. Directory-based sharding, a lookup table saying which shard holds which entity, keeps the flexibility to move one noisy tenant on its own and costs a lookup on every query plus a component that must never be unavailable. Each is a real answer for a different read pattern.\n\nThe key has to match how the data is read, not how it is naturally grouped. A query that can be answered from one shard is a normal query; one that spans shards becomes a scatter-gather, where the slowest shard sets the latency and the application does the joining and sorting that the database used to do. Notion sharded Postgres by workspace precisely because almost every query in that product is scoped to one workspace, which turns nearly all reads into single-shard reads.\n\nThen there is everything you lose that nobody mentions in the design review. Cross-shard transactions are gone unless you build two-phase commit or sagas. Unique constraints across shards are gone, so identifiers have to be globally unique by construction. Auto-increment ids stop working, which is why Snowflake-style ids exist. Aggregate queries over the whole dataset need a separate analytics path. Each of these is solvable and each is work that did not exist the week before. Resharding later means moving live data while still serving traffic from it: double writes, backfill, verification, cutover, and a rollback plan for each. It is measured in months for a real system, which is why the honest order is a bigger machine, better indexes, caching and read replicas first. All four are reversible in an afternoon. Sharding is a change to the data model that you will live inside for years.\n\nShard last. Read replicas, caching, better indexes and a bigger instance all come first, because they are reversible. Sharding changes your data model permanently.","cap":"CAP, stated usefully\n\nWhen a network partition splits your system, you either refuse requests in order to stay consistent, or answer them and risk divergence. That is the whole choice, and everything else written about CAP is commentary on those two sentences.\n\nPartitions are not optional. Cables are cut, switches fail, a routing change isolates a rack, and a garbage collection pause long enough to miss every heartbeat looks exactly like a partition from outside. So sacrificing partition tolerance is not on the menu, and a system described as CA is a system whose behaviour during a partition has not been decided.\n\nThe choice is rarely uniform within one company, and this is where the theory becomes useful rather than academic. A core ledger chooses consistency and refuses the write. The cash machine in the lobby chooses availability, dispenses the money anyway, and reconciles afterwards with an overdraft fee, which is Brewer's own illustration and a reminder that the resolution rule can be a commercial decision rather than a technical one.\n\nThe extension worth knowing is PACELC: if there is a partition, choose availability or consistency, else, in normal operation, choose latency or consistency. That second half describes the trade you make every day, unlike the first half which describes a bad afternoon once a year. Every synchronous replication decision, every quorum size, every strongly consistent read is the else branch being answered.\n\nConsistency in CAP is also narrower than the word suggests: it means linearizability, one register behaving as though there is a single copy. It is not the C in ACID, which is about invariants holding within a transaction. Conflating the two is the most common way a CAP discussion goes wrong, and noticing the difference is a reliable signal that someone has read past the triangle diagram.\n\nSo the answer that carries weight is never a letter. It is: during a partition this system refuses writes to these entities and accepts them for those, conflicts on the second group resolve by this rule, and here is what a user sees while it happens. Anything shorter has left the interesting part out.\n\n'We chose AP' is meaningless without saying what happens to conflicting writes afterwards. The interesting engineering is the reconciliation, not the letter.","eventual-consistency":"Eventual consistency in the interface\n\nEventual consistency means replicas converge given no new writes. Read the definition carefully: it promises convergence and says nothing whatsoever about when, and in a system that is always being written to, the condition it depends on never actually occurs. The guarantee is real and it is weaker than the phrase makes it sound. There is a ladder of stronger session guarantees, and each is far cheaper than linearizability. Read-your-own-writes: you see your own changes. Monotonic reads: you never see time run backwards, which is what happens when consecutive reads hit replicas at different lag. Consistent prefix: you see writes in the order they were made, so a reply never appears before the message it answers. Most complaints filed as database inconsistency are one of these three, and each can be bought with routing rather than with consensus.\n\nThe engineering is mostly in the interface. Show the pending state, use an optimistic update so the change appears immediately, and do not pretend an action is complete when it is merely accepted. A spinner that says processing is honest; a green tick for work that is queued is a lie that generates a support ticket when the work later fails.\n\nConvergence needs a merge rule, and choosing it is a product decision wearing technical clothes. Last write wins is the default in many stores and is the lossiest: it discards a write silently and depends on clocks you do not control. Amazon's shopping basket takes the union of conflicting versions, which can resurrect a removed item but can never lose an added one, because losing a purchase costs more than an unexpected item at checkout.\n\nCRDTs are the version where the merge is a property of the data type rather than a decision at read time. A grow-only counter, an add-wins set, a sequence for collaborative text: each is defined so that merging in any order gives the same answer, which removes conflict resolution from the application entirely. They are not free, since the metadata to make that work can outgrow the data, and they are the right tool for exactly the cases they fit.\n\nThe practical test for a design is to ask what a user sees during the gap and what happens if two people act in it. If the answer to the first is nothing at all and to the second is one of them silently loses, the consistency model has not been designed, it has been inherited from a default.\n\nMost consistency complaints are interface problems, not database problems. Showing 'processing' honestly costs nothing and removes the perception of a bug.","rate-limiting":"Token bucket and sliding window\n\nA fixed window counter is simple, and it allows double the limit across a boundary: a full quota at the end of one window, another full quota at the start of the next. A limit of 100 a minute permits 200 in the two seconds either side of the boundary, which is exactly the burst you were trying to prevent, and it arrives at the least convenient moment because every client with a cron job fires on the minute.\n\nA sliding window fixes that by weighting the previous window and counting the current one in full: the estimate is the current count plus the previous count times one minus the fraction elapsed. Twenty seconds into a sixty second window that is everything counted so far plus two thirds of the last window, and the previous window's contribution fades linearly to nothing as the current one fills. It is an approximation, and it is cheap, two counters per key rather than a timestamp per request, which is why it is what large edge platforms actually run. Cloudflare reported that across 400 million requests only 0.003 per cent were allowed or rejected differently from a true sliding window, which is the number that justifies the approximation. A true sliding log, keeping every request timestamp, is exact and costs memory proportional to traffic, which is the wrong trade at the edge and the right one for a small number of very expensive operations.\n\nA token bucket takes a different approach: refill at a steady rate up to a maximum, spend one token per request. It usually fits an API best, because real traffic is bursty and a strictly even rate feels broken to whoever is using it. A bucket of 100 refilling at 10 a second lets a client that has idled for ten seconds fire 100 requests immediately, then settle to 10 a second, which is what a paginating script or a page loading twelve resources actually does. The leaky bucket is the same shape with the burst removed: a queue that drains at a fixed rate, right when the thing you are protecting cannot absorb a spike at all.\n\nWhere you count matters as much as how you count. Per-IP catches obvious abuse and punishes an office behind one NAT, or every user of a mobile carrier's gateway. Per-key is right for an authenticated API and useless before login, which is where the credential stuffing happens. Per-user-per-endpoint is the most correct and carries the most state. Most real systems run several limiters at once, on different keys, for different reasons.\n\nStripe published theirs, and the shape is worth copying: a request rate limiter for sustained traffic, a concurrency limiter for calls in flight at once because a slow endpoint can starve a fleet without ever breaching a rate, and two load shedders that reserve capacity for critical traffic when the fleet is under pressure, so a runaway batch job cannot stop a card being charged.\n\nWhatever you choose, tell the caller. Return 429 with a Retry-After, and publish the remaining budget and reset time on every response, not just the rejected ones. A client that cannot see the limit either hammers you until it hits the wall or backs off so conservatively that it never uses what it paid for. Both are worse for you than telling it, and the second one generates a support ticket saying your API is slow.\n\nLimits exist to protect a resource, so the shape should follow what that resource cannot absorb. A bucket that permits bursts is right when the cost is throughput; a strict rate is right when the cost is a downstream call you pay for per invocation; a concurrency limit is right when the cost is a held connection. Picking the algorithm before naming the resource is how you end up with a limit that blocks good traffic and misses the bad.","timeouts":"Timeouts and deadlines\n\nA timeout is the number that decides how long you are willing to hold a resource for an answer that may never come, and most systems get it by accident. The default in many HTTP clients is no timeout at all, or a connect timeout with no read timeout, which is the same thing where it matters: a socket that opens and then goes quiet holds a worker forever.\n\nLittle's Law gives you the arithmetic and it is worth doing on paper before an incident does it for you. Concurrency equals arrival rate times latency. A service taking 100 requests a second at 50ms needs 5 concurrent workers. The same service at 30 seconds needs 3,000. Nothing about the traffic changed and nothing about the code changed; the only thing that moved was latency, and the pool that comfortably held 5 is now short by a factor of six hundred. This is why a slow dependency is more dangerous than a dead one: the dead one returns instantly and you handle it.\n\nTimeouts have to decrease as you go down the call tree. If a browser gives up after 10 seconds, the API waiting 30 for a service that waits 30 for the database means every layer is working on a result nobody is waiting for, holding connections that live requests need. The rule is that each hop's timeout is shorter than its caller's, with room for a retry if the layer retries.\n\nThe better version of the rule is a deadline rather than a timeout: a wall-clock instant that travels with the request, so each hop passes on the time remaining rather than starting a fresh clock. gRPC does this natively, and it is why a deep call tree there degrades sensibly. Over HTTP you carry it yourself in a header and enforce it in a middleware. The difference shows up in the tail: with independent timeouts, four hops of 2 seconds each can spend 8 seconds serving a client that left after 3.\n\nChoose the number from the latency distribution, not from a round figure. A timeout at the 99th percentile plus a margin cuts off the pathological tail without failing the merely slow. Setting it at the mean fails a fifth of good requests; setting it at ten times the p99 means it never fires and you have written a comment rather than a control. The instinct during an incident is to raise the timeout, and it reliably makes things worse: the calls that were failing now wait longer before failing, holding more resources for longer, and the failure spreads from one dependency to everything sharing that pool. Failing fast is what frees capacity, and it is what turns a broken dependency into a degraded page instead of a total outage.\n\nEvery timeout is a statement about how long a result stays worth having, and that is a product question as much as an engineering one. Systems that survive dependency failures are the ones where that number was chosen deliberately, propagated down the tree, and set lower than the patience of whoever is waiting.","retries-backoff":"Retries, backoff and jitter\n\nA retry is the cheapest available fix for a transient failure and the fastest available way to turn a small problem into an outage. Both are true, and which one you get depends on three decisions: whether the call is safe to repeat, how long you wait between attempts, and whether the system as a whole has a budget for retrying at all.\n\nSafety comes first. Retrying a non-idempotent call after a timeout is how a customer gets charged twice, because a timeout tells you nothing about whether the work happened. The fix is an idempotency key generated by the client, stored server side with the result, so a repeat returns the original outcome rather than performing the work again. Without that, the only correct retry policy for a payment is no retries.\n\nImmediate retries are the wrong shape because the thing you are retrying is usually overload, and hitting it again straight away adds to exactly the load that caused the failure. Exponential backoff, doubling the wait each attempt with a ceiling, gives the dependency room to recover. But backoff alone leaves every client synchronised: they all failed at the same instant, so they all retry at the same instant, and the recovering service is hit by a wall.\n\nJitter is what breaks the synchronisation, and AWS published the arithmetic in 2015. Full jitter, sleeping a random duration between zero and the capped exponential interval, produced substantially less contention and less total work than plain exponential backoff in their simulations. It feels wrong to make the delay random and it is the single highest-value line in most retry implementations.\n\nThe failure that surprises people is amplification. Three attempts at each of four layers is not three times the load, it is up to 3^4, or 81 times, because every layer multiplies the layer below it. A dependency that is slightly unhealthy receives a load spike caused entirely by the reaction to its own slowness, which guarantees it stays unhealthy. Retry at one layer, ideally the one closest to the user, and let the others fail through.\n\nThe systemic answer is a retry budget rather than a per-call limit. Google's SRE practice caps retries at around 10% of requests over a window: below the cap, retries proceed; above it, they are refused, so a broadly failing dependency cannot be flooded by a fleet doing what each node individually considers reasonable. Pair it with servers that say which failures are worth retrying, since a 429 or a 503 with Retry-After is an invitation and a 400 is not, and never retry a call whose deadline has already passed.\n\nRetries convert independent failures into correlated load, which is the opposite of what they are meant to do. Everything in a good retry policy, backoff, jitter, budgets, retrying at one layer only, exists to stop the reaction to a failure from being larger than the failure.","circuit-breakers":"Circuit breakers and bulkheads\n\nA circuit breaker counts failures over a window and trips once they cross a threshold, then fails fast for a cooling period, then lets a single trial request through to test recovery. Closed, open, half-open. The half-open state is the part that matters: without it, the full load arrives the instant the timer expires, knocks the recovering dependency over again, and the breaker flaps between states while everyone watches the dashboard oscillate. What a breaker buys you is not error handling, it is capacity. When it is open, calls fail in microseconds instead of holding a worker for the timeout, so the pool stays available for the requests that can still be served. It converts a dependency's failure from something that consumes your service into something that returns an error from one part of your service.\n\nThe threshold should be a rate over a window with a minimum request count, not a raw count. Five failures means nothing at 10,000 requests a second and everything at ten a minute, and a breaker that trips on the first five errors after a quiet night will open on ordinary noise. The usual shape is at least twenty requests in the window and a failure rate above half.\n\nA bulkhead solves the neighbouring problem: it gives a dependency its own bounded pool of connections or threads, so calls to it cannot consume the resources that every other request needs. The name is from ship compartments, and the point is the same, one flooded compartment does not sink the vessel. A breaker stops you waiting on a broken thing; a bulkhead stops one thing taking everything with it while it breaks. They pair, and neither replaces the other.\n\nThe modern criticism of hand-tuned breakers is that the numbers are guesses that age badly. Netflix built Hystrix, ran it at scale, and then retired it in favour of adaptive concurrency limits, which infer the safe level of in-flight work from observed latency rather than from constants a human chose in 2014. The pattern survived; the fixed thresholds did not, and the lesson worth taking is that a threshold set once will be wrong after the next capacity change.\n\nWhatever trips has to have somewhere to go. A breaker that opens and returns a 500 has only made the failure faster, which is genuinely valuable, but the better outcome is a fallback: last known good data, a default, or the feature quietly absent from the page. That decision belongs at design time, per dependency, and it is the difference between an incident and a slightly worse page.\n\nWaiting longer holds resources longer, which is why the instinct to raise the timeout during an outage makes it worse. Failing fast frees capacity, and combined with a bulkhead it confines the damage: one broken dependency becomes one broken section rather than an unavailable service.","graceful-degradation":"Graceful degradation and load shedding\n\nNot every dependency is essential, but a system only knows that if someone wrote it down. If recommendations are down, the product page should still render, with a cached set, a default set, or without that section at all. Absent an explicit decision, the default behaviour is that any dependency failure becomes a total failure, and that is a design choice made by omission rather than by anyone in particular.\n\nThe exercise is to classify each dependency as critical or optional, and to give every optional one a named fallback: last known good value, a static default, or hide it. This costs almost nothing at design time and is expensive to retrofit, because by then the call is buried three layers down inside a function whose contract says it returns a list of recommendations, not a list or nothing.\n\nLoad shedding is the same idea applied to your own capacity. Past a certain arrival rate, some requests will not be served, and the only question is whether you choose which ones or let a queue choose for you. Choosing means rejecting cheaply at the edge with a 503 and a Retry-After, ideally by priority, so a checkout still works while a report export waits. Not choosing means every request sits in a queue getting slower until they all time out together, which is the strictly worse outcome where nobody is served and you paid for the work anyway.\n\nQueues need bounds and an eviction policy for the same reason. An unbounded queue during overload fills with requests that will have timed out by the time they are picked up, so the server spends its capacity producing answers nobody is waiting for. Facebook's answer was to switch the queue to LIFO under load with a CoDel-style controlled delay: serve the newest requests, which still have a chance of being useful, and discard the oldest, which almost certainly do not. It feels unfair and it is the correct behaviour.\n\nStatic stability is the other half. A system is statically stable when it keeps working with the data it already has while its control plane is unavailable: the caches keep serving, the routing keeps routing, and only changes stop. AWS designs this way deliberately, which is why running instances survive control plane incidents. It is a useful test to apply to your own design: if the configuration service is down for an hour, does traffic keep flowing, or does everything stop the moment a cache entry expires?\n\nFinally, degradation has to be visible or it becomes a lie. If the page renders without personalisation, something needs to say so, in a log at minimum and often in the interface, or the next person to look will conclude the personalisation service is fine because the page looks normal. The failure is confined, not fixed, and the difference matters to whoever is on call.\n\nThis is the difference between an outage and a degraded experience most users never notice, and the decision has to be made before the incident, because during one nobody has time to work out which of forty dependencies are optional. Shedding load deliberately is the same argument applied to yourself: partial service by choice beats total failure by queueing.","logs-metrics-traces":"Logs, metrics and traces\n\nThe three signals answer different questions and are priced very differently, which is why the choice between them is an engineering decision rather than a matter of taste. Metrics are numbers aggregated over time: cheap to store, cheap to query, and they tell you that something is wrong. Traces follow one request across every service it touches and tell you where the time went. Logs record discrete events with full detail and tell you why.\n\nRun only on logs and you are paying to store the answer to questions a metric answers instantly, then waiting on a text search to compute what a counter already knew. Run only on metrics and you know the error rate rose without knowing which call, which customer, or which deploy. Most teams arrive at the right mix by overspending on one of them first.\n\nA trace is a tree of spans, each with a start, a duration, and a parent, tied together by identifiers that travel with the request. That propagation is the whole mechanism, and it is standardised: W3C Trace Context defines the traceparent header so a trace survives crossing between systems written by different people. Miss the propagation in one hop and the trace silently splits into two unrelated halves, which is the most common reason tracing is installed and not useful.\n\nSampling is unavoidable at volume, since keeping every span for every request costs more than the service. Head-based sampling decides at the start, cheap and blind: Google's Dapper paper describes sampling as low as one request in 1,024 for high-throughput services. Tail-based sampling buffers the spans and decides after the fact, so you can keep every trace that was slow or errored and discard the boring majority, which is what you actually want and costs more to run.\n\nLogs earn their keep when they are structured. A line of prose has to be parsed with a regular expression that breaks the next time someone rewords the message; a JSON object with the same fields on every line can be filtered, counted and grouped without anyone writing a parser. Emit the request id in every line, and the log becomes a joinable dataset rather than a story. The connective tissue matters more than any of the three individually. A metric that alerts should link to the trace that shows the slow hop, and the trace should link to the logs for that request id. Without those links each signal is an island and every investigation starts with someone copying identifiers between four browser tabs at two in the morning.\n\nMetrics tell you something is wrong, traces tell you where, logs tell you why, and the cost per question rises in that order. Reaching for the expensive signal first is how observability bills grow faster than traffic while investigations stay slow.","percentiles":"Percentiles, not averages\n\nAn average hides the tail, and the tail is what people complain about. A system averaging 100ms can be taking five seconds on one request in a hundred: the mean barely moves, and the hundredth user is the one who writes to support. p50 describes the typical experience, p95 and p99 describe the worst of it, and only the second pair predicts churn.\n\nThe tail is also structurally worse than it looks in a system made of many services, and the arithmetic is the most useful thing in this topic. If a request fans out to 100 servers and each has a 1% chance of taking over a second, the chance that at least one of them does is 1 minus 0.99 to the power of 100, which is about 63%. A one-in-a-hundred event at the component becomes a two-in-three event at the request. That is the result from Dean and Barroso's paper on tail latency, and it explains why large systems fight for the 99th percentile of their dependencies rather than the mean.\n\nThere is a trap on the way to percentiles that catches almost everyone: you cannot average them. The mean of each host's p99 is not the fleet's p99, and it is not any statistic at all. Percentiles have to be computed over the whole population, which is why metric systems store histograms with fixed buckets, or sketches such as t-digest, rather than the summary number each host computed for itself.\n\nChoose the percentile from the number of chances a user gets to hit it. A page that makes 20 API calls gives the p95 twenty opportunities to appear, so roughly two thirds of page loads will contain at least one p95 request. For anything a user does repeatedly, p99 and p99.9 are the honest targets, and for a nightly batch job the mean is genuinely fine.\n\nMeasure at the edge as well as inside. Server-side latency excludes queueing before your process, DNS, connection setup, the mobile network and the time the browser spends rendering, and those can dominate. A service that is fast in its own dashboards and slow to its users usually has that gap, and finding it means measuring where the user is rather than where the code is.\n\nFinally, watch the shape and not only the number. A bimodal distribution, fast on cache hits and slow on misses, has a p50 and a p99 that describe two different populations and a mean that describes neither. Histograms show that immediately; a single summary number never will.\n\nReporting p99 instead of the mean is a small change that surfaces the problems users complain about and dashboards do not show. It also makes capacity conversations honest, because the tail is where a system runs out of headroom first.","slo":"SLIs, SLOs and error budgets\n\nAn SLI is a measurement, such as the proportion of requests served successfully in under 300ms. An SLO is the target for that measurement over a window, say 99.9% over 30 days. The gap between the target and 100% is the error budget, and it is the most useful idea in the set because it turns reliability from a virtue into a quantity. The budget makes the trade explicit. Spend it shipping quickly. When it is exhausted, stop shipping and spend the time on stability instead. That replaces a recurring argument about whether features or reliability come first with a number both sides agreed to in advance, when nobody was under pressure and nobody was defending a decision they had already made.\n\nThe arithmetic is worth knowing because the nines are less intuitive than they look. 99.9% over 30 days is about 43 minutes of budget; 99.99% is about 4 minutes and 20 seconds, which is less time than most teams take to acknowledge a page. Each additional nine costs disproportionately more and buys less, and choosing one because it sounds serious is how a team ends up permanently over budget and ignoring the whole scheme.\n\nThe SLI has to be measured where the user is, and it has to describe something a user would recognise. The fraction of successful requests at the load balancer is a reasonable proxy; CPU utilisation is not an SLI at all, because no user has ever noticed it directly. The good ones are usually availability, latency, correctness and freshness, and the test for a candidate is whether a person could describe it in a sentence without using the word server.\n\nPerfect reliability is the wrong target, and this is the part that sounds like heresy until you cost it. If the network between the user and you fails more often than your service does, the last nine you bought is invisible to everyone, and it was paid for with the features that were not shipped. An error budget that is never spent is a signal that you are shipping too slowly, and it should prompt exactly as much discussion as one that is exhausted.\n\nNone of this works without agreement about who owns the number. An SLO written by an infrastructure team and imposed on a product team becomes a stick; one agreed by both, with the budget policy written down before the first breach, becomes the thing that ends the argument. The mechanism is social as much as technical, which is why the SRE literature spends more pages on the policy than on the maths.\n\nChasing 100% is the wrong target: each extra nine costs disproportionately more, and perfect reliability means you shipped too slowly. The budget makes the trade explicit rather than political, and it is the rare metric that both sides of that argument can accept because they set it together.","alerting":"Alerts worth waking up for\n\nAn alert is a claim that a human should stop what they are doing right now. Judged that way, most alerting rules do not qualify, and the cost of the ones that do not is not the interruption but the habit: a person who has ignored forty pages will ignore the forty-first, and that is the one that mattered.\n\nThe first rule is to alert on symptoms rather than causes. Users notice slow pages and failed requests; they do not notice a full disk, a restarted pod or a queue with 4,000 messages in it, unless one of those is making pages slow, in which case the symptom alert already fired. Cause-based rules multiply as fast as the system grows and go stale silently, because nothing tells you when a threshold stopped mattering.\n\nWhat to measure for a symptom is well-trodden ground. Google's four golden signals are latency, traffic, errors and saturation. The RED method for request-driven services is rate, errors and duration; the USE method for resources is utilisation, saturation and errors. They overlap deliberately, and any of them is enough. Picking one and applying it consistently is worth more than picking the best one.\n\nAlert on error budget burn rate rather than on a raw threshold, because a raw threshold either pages on a blip or misses a slow bleed. The SRE Workbook's approach is multi-window and multi-burn-rate: page when the recent burn is fast enough to exhaust a meaningful share of the budget quickly, for example 14.4 times the sustainable rate over an hour, which spends 2% of a 30-day budget in that hour, and confirm it with a longer window so a one-minute spike cannot page anyone. A slower burn, several times the sustainable rate over six hours, is a ticket rather than a page.\n\nEvery page needs a runbook link, an owner and an action. If the honest answer to what should I do is wait and see, it should not have woken anyone, and the fix is to make it a dashboard or a ticket. This is also the test that keeps an alerting system from growing without limit: nobody adds an alert they have to write a runbook for unless they mean it.\n\nFinally, review pages the way you review code. Count them per week, name the ones that were not actionable, and delete or demote them in the same meeting. An alerting configuration is the only part of most systems that nobody ever deletes from, which is precisely why it ends up describing a system that stopped existing two architectures ago.\n\nThe scarce resource is the attention of the person carrying the pager, and it is spent whether the alert was useful or not. Alerting on symptoms with burn rates keeps the volume proportional to actual user harm, which is the only thing that makes a pager sustainable to carry.","cardinality":"Cardinality, sampling and the bill\n\nObservability costs scale with the questions you might ask, not with the questions you do ask, and the mechanism is cardinality. A metric is stored as one time series per distinct combination of its labels, so a request counter labelled by endpoint (50), status code (6) and region (3) is 900 series, which is nothing. Add customer id with 10,000 values and it is 9 million, and the monitoring system falls over before the service does.\n\nThe rule that follows is that labels are for values you would group by, and identifiers are not among them. User id, request id, session id, full URL paths with ids in them, and error messages containing a stack trace all belong in logs or traces, where the cost is per event rather than per distinct combination held in memory forever. The tell is a label whose set of values grows with your customer count.\n\nHigh-cardinality questions are legitimate, which is why the answer is not simply do not do that. What is this specific customer seeing, why is this one request slow: those need per-event data. The design that works is metrics for the aggregate and cheap alerting, traces and structured logs for the detail, sampled hard, and an index that lets you find the events belonging to one id. Trying to make one store do both is how observability bills come to rival compute bills.\n\nSampling is the other lever, and it should be deliberate rather than emergent. Head sampling at a fixed rate is cheap and keeps a representative picture but loses the rare failure, which is the thing you wanted. Tail sampling keeps what was slow or wrong. A practical compromise is to keep a small uniform sample for baselines and everything anomalous on top, and to make the sampling rate visible so nobody computes a rate from sampled data and forgets to scale it.\n\nRetention deserves the same scrutiny as volume, because the value of a log line falls off a cliff after about a week while its storage cost does not. Most of the questions asked of observability data are asked within hours. Keeping metrics for a year at low resolution and logs for two weeks at full detail costs a fraction of keeping everything for a year, and answers almost every real question. There is a failure mode worth naming: monitoring that takes the system down. An exporter that scrapes every series on every request, a logging call inside a tight loop, or a debug level left on in production can consume more resources than the work being observed. Instrumentation is code that runs on the hot path, and it deserves the same review as the rest of it.\n\nObservability is the one system whose cost is driven by the questions you might ask rather than by traffic, and cardinality is where that cost hides. Keeping identifiers out of labels and pushing per-entity detail into sampled events is what keeps the bill proportional to the service rather than to the customer list.","rest":"REST and resource modelling\n\nREST models a system as resources addressed by URL, with HTTP methods describing the action and status codes describing the outcome. The name comes from Roy Fielding's 2000 dissertation, which describes an architectural style with six constraints. Almost nothing called RESTful today satisfies all six, and that is fine: the constraint that pays for itself in practice is the uniform interface, the agreement that GET means read, that 404 means the thing is not there, and that these mean the same on your API as on everyone else's.\n\nThe reason to care is that a large amount of infrastructure acts on those meanings without ever reading your code. RFC 9110, the current HTTP semantics specification, divides methods into safe ones that do not change state (GET, HEAD, OPTIONS, TRACE) and idempotent ones that can be repeated without additional effect (those four plus PUT and DELETE). POST is neither. A CDN will cache a GET response at the edge. A proxy, a load balancer or a client library will retry an idempotent request after a dropped connection. Neither will do anything of the sort for a POST, because you have told the network that repeating it might charge a card twice.\n\nStatus codes are the other half of that contract, and the common failure is returning 200 with an error inside the body. Every layer above you now believes the call succeeded: the load balancer keeps the backend in rotation, the retry policy never fires, the dashboard shows a healthy service, and the only place the failure exists is in a field that nothing except your own client parses. A 5xx is not an admission of guilt, it is how you tell the rest of the system what happened.\n\nResource modelling is where people get stuck, usually on actions that are not obviously nouns. Publishing an article, refunding a payment, resending an invite. The trick that works most of the time is to notice that the action produces a thing, and to make that thing the resource: POST /payments/123/refunds creates a refund, which has its own id, its own status and its own history, all of which you wanted anyway the first time someone asked why a refund failed. Where that genuinely does not fit, a verb endpoint is better than a contorted noun. Consistency is worth more than purity.\n\nGetting the method wrong has a measurable price. A search implemented as POST /search cannot be cached by a CDN or the browser, cannot be bookmarked or shared, will not be retried by anything in the path, and in a browser it triggers a CORS preflight because a JSON content type is not on the short list of simple requests, which adds a full round trip before the real request leaves. The honest reason to use POST for search is a query too large for a URL, where roughly 2,000 characters is the safe practical ceiling across proxies and servers. That is a real constraint, and it deserves a comment in the code saying so.\n\nPagination deserves the same care, because it is the endpoint most likely to be hit by a script. Offset pagination, page=47, gets slower the deeper it goes, since the database still has to walk the rows it is about to discard, and it produces duplicates and gaps when rows are inserted mid-scan. Cursor pagination hands back an opaque marker for the last row seen and asks for what follows it, which is stable under writes and stays flat as the offset grows. Almost every large API has migrated in that direction, and the ones that have not are the ones with a slow page 500.\n\nStandard verbs and codes are not pedantry, they are leverage: they are how you get caching, retries, health checking and client library behaviour without writing or operating any of it. Every deviation is a piece of infrastructure you now have to replace with your own code, and the bill arrives later than the decision.","graphql":"GraphQL and its costs\n\nGraphQL began at Facebook in 2012, driven by a specific problem: the mobile news feed needed a different shape of data from the web one, over a slower network, and every screen change meant either a new bespoke endpoint or several round trips. The specification was published in 2015 and moved to the GraphQL Foundation in 2018. The idea is that the client sends a query describing exactly the fields it wants, and gets back a response with that shape and nothing else. The wins are real and they are mostly about clients. Over-fetching disappears, because nobody is shipping thirty fields to a screen that renders four. Round trips collapse, because a screen that was five REST calls with dependencies between them becomes one request. The schema is typed and introspectable, so tooling, autocomplete and generated client types come for free, and a breaking change is visible before it ships rather than after a client crashes.\n\nThe first thing you give up is HTTP caching, and it is a bigger loss than it sounds. Every query is a POST to a single endpoint, so the CDN, the browser cache and every proxy in between have nothing to work with, and a system that was serving most reads from an edge cache is suddenly serving all of them from origin. The standard recovery is persisted queries: register the query text once, send a hash of it, and let the request be a GET again so the edge can cache on the hash. It works, and it is one more piece of machinery you now operate.\n\nThe second thing you give up is control over cost, because the client now writes the query. A nested selection can fan out into thousands of database reads without anything in the request looking unusual. The defences are depth limiting, complexity analysis and pricing. GitHub's GraphQL API does the last of these openly: a query is scored before it runs, a call cannot request more than 500,000 nodes, and each token has a budget of 5,000 points an hour rather than a count of requests. That is the right shape of answer, because the honest unit of work is not one request.\n\nThe third is N+1, which is structural rather than accidental. Resolvers run per field, so a list of fifty authors resolves the author field fifty times, each with its own database call. DataLoader style batching collects the calls made within a single tick and issues one query for the fifty keys. It is standard equipment, it works, and it has to be applied deliberately at every level that can fan out. Nobody notices the missing one in development, because the list has three items.\n\nFinally, errors arrive with a 200 and an errors array beside partial data, which is deliberate: half a screen is better than none. It also means every dashboard, alert and load balancer health check that reads the status code sees a healthy service while a resolver is failing on a third of requests. Your monitoring has to parse the body, and this is the single most common reason a GraphQL outage is discovered by a customer rather than by a graph.\n\nGraphQL earns its keep when several clients need different shapes of the same data and you would otherwise be shipping a bespoke endpoint per screen. For a single first-party client it is usually complexity without payoff: you have given up edge caching and taken on query costing to solve a problem you did not have.","grpc":"gRPC and binary protocols\n\ngRPC is Protocol Buffers over HTTP/2: a compact binary encoding with a schema, generated client and server stubs in a dozen languages, and four call shapes rather than one. Unary is the familiar request and response; the other three stream, from the server, from the client, or in both directions at once over a single connection. It was open sourced by Google in 2015, and it is the public version of an internal system, Stubby, that had already been carrying the traffic between Google's services for over a decade.\n\nThe efficiency comes from several places at once. Protobuf writes a field as a small numeric tag plus a value rather than a repeated field name, and tags 1 to 15 fit in a single byte, which is why the convention is to spend those numbers on the fields that appear most often. HTTP/2 compresses headers with HPACK, so the repeated metadata that dominates small JSON calls largely disappears. Multiplexing lets many calls share one connection without queueing behind each other. And there is no JSON parsing, which on a service doing hundreds of thousands of small calls a second is not a rounding error.\n\nThe schema is the part that changes how teams work, because it makes compatibility a build-time question. The rules are simple and unforgiving: field numbers are the wire identity, so they are never reused, and a removed field's number is marked reserved so nobody can accidentally take it. Unknown fields are preserved through a decode and re-encode, so an old intermediary does not silently strip data written by a newer one. Break those rules and the failure is not a parse error, it is a value quietly landing in the wrong field.\n\nDeadlines are the underrated feature. A gRPC call carries a deadline as part of the call itself, and it propagates: a service that received a call with 300ms remaining passes what is left to whatever it calls next, so the whole tree agrees on when the work stops being useful. Do that with REST and you are wiring an X-Request-Deadline header through every hop by hand, and getting it wrong somewhere, which is how a client that gave up eight seconds ago ends up paying for a database query that is still running.\n\nThe costs are mostly at the edges. Browsers cannot emit or read HTTP/2 trailers, where gRPC puts its status, so calling gRPC from a browser needs gRPC-Web and a proxy such as Envoy to translate. Debugging by hand is harder, because you cannot read the bytes without the schema, and the tooling (grpcurl and friends) is one more thing to install on a machine where curl was already there.\n\nThere is an operational trap worth knowing before it finds you. gRPC holds long-lived connections, so a layer 4 load balancer that balances connections rather than requests will pin every call from a client to one backend, and adding capacity does nothing at all: the new instances sit idle while one melts. The fixes are a proxy that understands HTTP/2 request-level balancing, or client-side load balancing with a resolver that knows the backend set. The usual arrangement is gRPC between your own services and REST or GraphQL at the public edge, which is Google's own split.\n\ngRPC buys performance, typed contracts and propagating deadlines, and charges you in reach and legibility. Both ends being yours is the condition that makes the trade sensible, which is why the boundary between internal and public traffic is usually also the boundary between gRPC and JSON.","webhooks":"Webhooks and delivery\n\nA webhook inverts the direction. Instead of clients polling you, you call them when something happens, which removes the polling load and cuts the latency from an average of half the poll interval to roughly the time of one request.\n\nThe saving is larger than it looks. A thousand integrators polling a quiet endpoint every thirty seconds is 2,880,000 requests a day, and if events arrive for one integrator in a hundred per poll then 99% of that traffic exists to learn that nothing happened. The same information as webhooks is about 29,000 calls, made only when there is something to say.\n\nWhat you have bought instead is a delivery problem, and it is yours rather than theirs. Receivers go down, respond in eight seconds, return 200 while failing internally, or vanish for a fortnight. So a webhook sender is really four things: a queue, a retry policy, a dead letter path, and somewhere an integrator can see what failed and why.\n\nRetries need a budget and a shape. Immediate, then 1 minute, 5, 30, 2 hours, 6 hours, and stop at 24 gives eight attempts across a day and covers both a deploy and a night of downtime; Stripe runs the same idea out to three days. Jitter every interval, because a receiver that fell over under load will otherwise be hit by every sender's retry at the same second and fall over again.\n\nReceivers have to verify the call came from you. An HMAC-SHA256 over the raw body with a shared secret, sent as a header, and a timestamp in the signed payload so a captured request cannot be replayed a week later. Sign the raw bytes, not the parsed object: a receiver that re-serialises JSON before checking will compute a different digest for the same message and reject you, which is one of the more annoying afternoons in this line of work. Order is the part people forget. Retries mean the second event can land before the first, so a payload carries a sequence number or an event timestamp, and the receiver is expected to ignore anything older than what it already holds. Without that, a customer.updated for a stale version arriving after the current one silently reverts a record, and nobody notices until a support ticket says the address changed back on its own.\n\nSending a webhook is easy and delivering one reliably is the actual product, which is why the interesting design is all on the failure path. Signing matters most of all: without it, your webhook endpoint is an unauthenticated write API for anyone who learns the URL, and the URL leaks the moment it appears in a log.","versioning":"Versioning and breaking changes\n\nStart from what actually breaks, because it is narrower than people assume. Adding an optional request field, adding a response field, adding an endpoint or a new enum value in a field the client only writes: all safe, provided clients are tolerant readers that ignore what they do not recognise. Removing or renaming a field, tightening validation, making an optional input required, changing a status code, or changing the meaning of an existing value while keeping its name: all breaking, and the last one is the worst, because nothing in any schema catches it.\n\nWhere the version number lives matters less than people argue. In the path (/v2/charges) it is explicit, trivially routable and visible in every log line. In a header or media type it keeps URLs stable so a resource has one identity across versions. Both work. What does not work is mixing them, or versioning at three different granularities in the same API, because then no one can answer what version they are on without reading your source.\n\nStripe's model is the most demanding and the most instructive. An account is pinned to the API version that was current when it first integrated, and requests and responses are passed through a chain of transformations between that version and the current internal one. The cost is that every backwards-incompatible change ever made must exist forever as a piece of running code. What it buys is that they have effectively never broken an existing integration, on an API where breaking one means someone stops taking payments.\n\nRetirement is the part almost everyone skips, and it is the only part that determines whether versioning was worth doing. A version lives exactly as long as clients use it, so the prerequisite is per-version usage telemetry broken down by client: not how many requests hit v1, but which twelve integrators are responsible for them and when each last called. Without that, nobody can prove removal is safe, so nothing is ever removed and every version is maintained forever.\n\nWith the telemetry, deprecation becomes a process rather than a hope. Announce with a date. Advertise it in the responses themselves, using the Sunset header from RFC 8594 and the Deprecation header standardised in RFC 9745, so the machinery can see it and not only the mailing list nobody reads. Contact the identified clients directly, because they are a list rather than a crowd. Then run brownouts, short deliberate outages of the old version at announced times, which is the only reliable way to find the integrations whose owners left the company two years ago. Google's deprecation policy commits to a year of notice for stable APIs; a year with brownouts works, and a year of silence followed by a switch-off does not.\n\nThe last piece of advice is to version the smallest thing that changed. A global version bump forces every client to revalidate everything to receive one new field, so it gets deferred, so the old version never empties. A new field behind a flag, a new endpoint beside the old one, or a per-resource version keeps the blast radius proportionate to the change, and keeps the migration something a client can do in an afternoon.\n\nMost teams version and then never remove anything, which is the worst of both worlds: all the cost of maintaining parallel behaviour and none of the freedom to change. Per-version usage telemetry is what turns retirement into a decision someone can actually make.","push-options":"Polling, long polling, SSE, WebSockets\n\nPolling asks repeatedly on a timer. It is simple, it works through every proxy ever built, and it is wasteful in a way worth quantifying: a thousand clients polling every five seconds is 17 million requests a day, and if something happens once an hour per client then 99.9 per cent of that traffic exists to learn that nothing happened. Latency is bounded by the interval, so halving the latency doubles the load. Long polling holds the request open until there is news or a timeout, which removes the waste and keeps the simplicity, at the cost of a held connection per waiting client and a proxy somewhere that will close it after 30 or 60 seconds regardless of what you intended. It is the pragmatic choice more often than its reputation suggests.\n\nServer-sent events are a one-way stream from server to client over ordinary HTTP. The browser reconnects automatically, and it will send the last event id it saw so the server can resume rather than restart, which is a feature people reimplement badly on top of WebSockets. Everything in the path treats it as a normal HTTP response, so compression, authentication and proxies all behave.\n\nWebSockets give a persistent two-way channel after an HTTP upgrade handshake. That is what you want when the client also sends frequently: chat, collaborative editing, games, live cursors. What you have taken on is a protocol of your own design, since the frames carry bytes and nothing above that: message types, acknowledgements, resumption and heartbeats are now yours to define.\n\nRead the four as a cost ladder rather than as a ranking. Each step buys lower latency and charges you in held connections, server-side state and operational complexity. The right answer for a dashboard that updates every few seconds is often the one at the bottom of the ladder, and choosing WebSockets for it means writing reconnection logic to solve a problem polling never had.\n\nTwo practical notes that decide real implementations. Intermediaries close idle connections, so anything long-lived needs a heartbeat every 30 seconds or so, and both SSE and WebSockets need to tolerate a reconnect at any moment, which in turn means the server must be able to answer what did I miss. And HTTP/2 changed the arithmetic for SSE: the old six-connection-per-host limit that made it awkward applies per connection rather than per stream, so many streams now share one.\n\nSSE is underrated: if data only flows server to client, notifications, live prices, progress, it is far simpler than WebSockets and works through ordinary HTTP infrastructure.","scaling-connections":"Scaling persistent connections\n\nPersistent connections are stateful, which breaks the assumption that makes web services easy: that any server can handle any request. A message for a user must reach the exact process holding that user's socket, and nothing in a normal load balancer helps with that, because the message did not arrive as a request from that user.\n\nThe standard answer is a pub/sub layer. Each gateway subscribes to the channels for the connections it holds, and a publish from anywhere fans out to whichever gateway is holding the socket. The sender no longer needs to know where anyone is connected, which is the property that makes the design scale. Slack and Discord both work this way.\n\nThe alternative, a shared map of user to server, looks simpler and ages badly: it needs updating on every connect and disconnect, it is wrong during the seconds after a crash, and every sender must read it before sending. It is a directory that must be perfectly accurate to be useful, which is a hard property to buy.\n\nCapacity is a memory question rather than a CPU one. Each idle connection costs kernel buffers plus whatever your runtime allocates per connection, so the ceiling per machine is usually measured in tens or hundreds of thousands and is decided by that per-connection overhead. The classic C10K work, and the C10M writing that followed, is entirely about driving that number down: event loops instead of threads, careful buffer sizing, and avoiding per-connection allocations.\n\nDeploys are the part that surprises people. Restarting a gateway drops every connection it holds, so a rolling deploy across ten nodes is ten thundering herds of reconnections, each one a handshake and an authentication. Reconnect with exponential backoff and jitter on the client, drain connections gradually rather than all at once, and expect the reconnect storm to be the largest load your authentication path ever sees.\n\nFinally, resumption is what makes reconnection invisible. The client remembers the last message id it processed and asks for what followed; the server keeps a short buffer per channel to answer that. Without it, every reconnect is either a gap in the conversation or a full resynchronisation, and at scale the second one is its own outage.\n\nThis is why chat and presence systems are hard. Statelessness is what makes normal web services easy to scale, and holding a socket throws it away.","presence":"Presence and typing indicators\n\nPresence looks trivial in a specification and is among the most expensive features in a chat product. Every state change potentially notifies everyone who can see that user, so the cost tracks the social graph rather than the number of events. In a workspace of 10,000 people where everyone can see everyone, one person arriving in the morning is 10,000 notifications, and a morning is 10,000 of those.\n\nThe data is worthless within seconds, which is the property that makes it tractable. It lives in memory behind short expiries rather than in a database: a heartbeat every 30 seconds refreshes a key with a 45-second lifetime, and absence of a heartbeat is absence of a user. Nothing needs to be deleted, nothing needs to be reconciled after a crash, and a lost update corrects itself within one interval.\n\nFan-out is reduced by scoping rather than by cleverness. Only notify people who could actually observe the change: those with the conversation open, in the same channel, on the same screen. That turns a graph-sized problem into a viewport-sized one, and it is why presence is usually accurate for the handful of people you are looking at and stale for everyone else, which is exactly the right trade.\n\nBatching does the rest. Collect changes over a second or two and send one update rather than twenty, coalesce repeated transitions so a flapping connection produces one event, and let the client interpolate. Typing indicators are throttled hardest of all, often to one event every few seconds per conversation, because they are the first thing worth sacrificing under load and nobody notices their absence for a moment.\n\nThe last piece is degradation. Under pressure, presence should stop first, then typing indicators, then read receipts, before anything touches message delivery. Deciding that order in advance is what makes the feature safe to ship, because presence is exactly the kind of feature that will otherwise consume the capacity that messages needed.\n\nPresence is the standard example of a feature whose cost is invisible in the spec. Recognising it as a fan-out problem rather than a storage problem is the insight being tested.","inverted-index":"The inverted index\n\nA normal index maps a row to its values. An inverted index turns that around: each term points at the list of documents containing it, so a search for shoes reads one posting list rather than examining every row. That inversion is the entire reason full-text search is fast, and it is why the structure has to be different rather than merely bigger.\n\nText is normalised before it is indexed, and the same pipeline runs over the query so the two can meet. Lowercasing, tokenisation, stemming so running matches run, stop word removal, and often synonyms. Indexing and querying must use the same analyser: a mismatch produces a search that finds nothing for reasons no error message explains, which is among the more frustrating afternoons available.\n\nA LIKE query with a leading wildcard cannot use a B-tree at all, because a B-tree is ordered by prefix and a leading wildcard removes the prefix. So it scans every row, which is fine at ten thousand rows and hopeless at ten million. That is not the database being slow; it is the wrong data structure for the question.\n\nPostgres will do this for you with a GIN index over a tsvector, and that is usually the right first move. It gives stemming, ranking with ts_rank and phrase search inside the database you already run, with no second system to keep in sync. Trigram indexes cover the other common case, fuzzy and substring matching, which full-text search deliberately does not do.\n\nA dedicated engine starts paying when you need what an engine has and a database does not: faceting and aggregations over results, per-field boosting and tuneable relevance, typo tolerance, suggestions, and a scale where the index outgrows the primary. Those are real features rather than a performance argument, and they are the honest reason to take on a second datastore. The cost of that decision is one thing, repeated: the index is a second copy of the data and it can drift. Everything else, the mappings, the analysers, the ranking, is tuning. The copy is the design problem, and it is why the next topic exists.\n\nThis is why search moves to a dedicated engine. It is not that the database is slow; it is that the data structure required for text search is a different one.","relevance":"Relevance and ranking\n\nMatching is the easy half and it is the half that gets built. Ranking decides which of ten thousand matches appear in the first five results, and since almost nobody looks past those, ranking is what users experience as the quality of your search.\n\nThe classical scoring is TF-IDF and its better-behaved successor BM25: a term counts for more when it appears often in a document and less when it appears in many documents, so rare words carry the signal and common ones do not. BM25 adds saturation, so the twentieth occurrence of a word adds almost nothing, and length normalisation, so a long document does not win simply by containing more words. That saturation is the practical difference and it is why BM25 is the default in every serious engine.\n\nText score alone produces results that are correct and feel wrong, because relevance in a product is not only about words. Real ranking blends the text score with business signals: recency for news, popularity or conversion rate for commerce, stock availability, distance, and whatever personalisation you can justify. Each is a weight, and the weights are a product decision rather than a technical one.\n\nTwo search-specific mechanics are worth knowing. Boosting weights a field or a condition, so a match in the title counts for more than one in the body, which is usually the single highest-value tuning change available. Faceting turns the result set into counts by attribute, which lets people narrow rather than reformulate, and it is often more valuable than any ranking improvement because it changes what the user can do rather than what they are shown.\n\nMeasure it, or the tuning is a matter of taste. Click-through on the top results, the rate of searches that return nothing, the rate that lead to a session ending without a click, and the position of the item people eventually chose. Those numbers tell you whether a change helped, and they are the difference between search as an engineering exercise and search as a product.\n\nThe failure worth naming is the zero-result search. It is the clearest signal you have and it is usually caused by something fixable: no synonym handling, no typo tolerance, or a filter silently applied. Logging those queries and reading them weekly is the cheapest search improvement available, and it needs no model at all.\n\nTreating search as a matching problem produces technically correct results that feel broken. The measurable target is click-through and successful sessions, not recall.","search-sync":"Keeping the index in sync\n\nThe search index is a second copy of the data, so it can drift. Dual writes, writing to the database and the index in the same request, fail the moment one succeeds and the other does not, and nothing about that failure is visible until somebody searches for the missing thing.\n\nDrift is silent and cumulative. Each individual miss is rare, a failed call here, a timeout there, but nothing corrects them, so the index degrades steadily and the only signal is a user saying they cannot find a product that plainly exists. By then the gap is weeks old and there is no log of which writes were lost.\n\nThe reliable pattern is to write to the database and derive the index from its change log, so the database stays the single source of truth and the index is always a function of it. Deletes come through as well, which polling an updated_at column misses entirely: a deleted row simply stops appearing in the query, and the document stays in the index forever.\n\nYou also need a full reindex path, and you will use it more than you expect. Mappings change, an analyser is wrong, a bug corrupts a field. Build into a new index under a versioned name and swap an alias when it is ready: the swap is atomic, search never goes down, and the previous index is still there when the new one turns out to be worse.\n\nTwo systems, two commits and no atomicity is the whole problem, and no amount of retrying fixes it. Deriving one from the other's log replaces coordination with a single authoritative ordering.","autocomplete":"Autocomplete and suggestions\n\nAutocomplete looks like a smaller version of search and is a different problem. The latency budget is a keystroke, so under about a hundred milliseconds end to end, and every character typed is a request, which means a search box generates an order of magnitude more traffic than the search itself.\n\nThe data structure follows from the prefix requirement. A trie holds strings by shared prefix so a lookup walks the typed characters and returns the subtree, and the practical version precomputes the top few completions at each node so the answer is a read rather than a traversal plus a sort. Where a trie is inconvenient to operate, an inverted index with an edge n-gram analyser gets close enough by indexing every prefix as a term.\n\nRanking matters more than matching here, because a prefix of three characters matches thousands of things and the box shows five. Popularity is the usual backbone, weighted toward recency so that what people searched for this week outranks last year, with personal history above both because someone's own previous searches are the strongest signal available about what they mean.\n\nThe client is half the system. Debounce so a fast typist produces a handful of requests rather than one per character, cancel in-flight requests when a newer keystroke arrives, and be careful about out-of-order responses: the reply for a three-letter prefix arriving after the four-letter one will replace correct suggestions with stale ones unless each response is checked against the current input.\n\nEverything about this favours caching. Prefixes are short, popular ones are extremely popular, and results change slowly, so an edge cache with a short expiry absorbs most of the traffic. The remaining engineering is mostly about what not to suggest: profanity, other people's private data, and the long tail of queries that returned nothing, which should not be offered to the next person.\n\nThe constraint that shapes the design is the latency budget rather than the corpus size, because a suggestion that arrives after the next keystroke is worse than no suggestion at all. That is why the answers are precomputed and cached rather than searched, and why ranking is decided before the request rather than during it.","object-storage":"Object storage versus databases and disks\n\nObject storage holds immutable blobs addressed by key, with effectively unlimited capacity, eleven nines of durability in the major services, and a cost per gigabyte an order of magnitude below block storage. It is not a filesystem: there are no directories, only keys that contain slashes, and there is no partial update, only replacing an object wholesale. Putting files in a database instead inflates every backup, slows every replica, and spends expensive storage on bytes that no query will ever filter on. The standard shape is the file in object storage and its metadata in the database: key, size, owner, content type, checksum. That split keeps the database small enough to restore quickly, which is the property you care about on the day it matters.\n\nStorage classes are where the money is. Hot storage for what is served, infrequent access for what is occasionally read, archival tiers for what is kept because someone must. The archival tiers are dramatically cheaper per gigabyte and charge for retrieval and for a minimum storage duration, so moving the wrong data there costs more than leaving it. Lifecycle rules automate the transition by age, and setting one up is usually the largest single saving on a storage bill.\n\nThe costs that surprise people are not storage at all. Egress is charged per gigabyte leaving the provider and is frequently the largest line, which is why a CDN in front of a bucket pays for itself twice over: it cuts latency and it cuts the number of times the same object leaves the origin. Request charges matter too when the objects are small: a million tiny objects costs more in requests than in bytes.\n\nConsistency is no longer the trap it was. S3 has offered strong read-after-write consistency since December 2020, so an object is readable immediately after it is written, and the elaborate workarounds built for the old model can be deleted. Versioning is worth enabling deliberately, because it turns an accidental overwrite from a data loss incident into a lookup, and because it is what makes deletion recoverable.\n\nThe rule that survives all of it: store what you query in the database and what you serve in object storage. Blobs in Postgres make every operational task on that database slower forever, and the pain is paid at restore time, when it is least welcome.\n\nThe rule is to store what you query in the database and what you serve in object storage. Blobs in Postgres make every operational task on that database slower forever.","presigned-uploads":"Direct uploads with pre-signed URLs\n\nRouting uploads through your API means large files consume your bandwidth, your memory, your request timeouts and your autoscaling budget, to nobody's benefit. A single 2GB upload can occupy a worker for minutes, and a handful of them at once will exhaust a service sized perfectly well for its actual work.\n\nA pre-signed URL removes the problem rather than solving it. Your server authorises the upload and returns a time-limited URL; the client sends the bytes straight to object storage. The signature encodes what may be uploaded, to which key, until when, so the permission is narrow and expiring rather than a credential handed to a browser.\n\nThe constraints belong in the signature, not in a check afterwards. Sign a specific key so a client cannot choose where the object lands, set a content length range so nobody uploads a hundred gigabytes, pin the content type, and keep the expiry short, minutes rather than hours. Anything you fail to constrain is something the holder of that URL may do.\n\nCompletion has to be discovered rather than trusted. The client telling your API that it finished is a claim, and a malicious or merely buggy client can make it without uploading anything. The reliable signal is an event from the storage service itself, which is also what lets you verify size, type and checksum before treating the object as real.\n\nTwo more things worth building in. Multipart upload splits a large file into parts that upload in parallel and resume individually, which is what makes uploads survive a mobile connection; incomplete multipart uploads then linger and are billed, so a lifecycle rule to abort them after a few days belongs in the same change. And validate the content rather than the extension, because a file named image.png that contains something else is the oldest trick in the list.\n\nThe result is an API that stays small and fast because the large payloads never touch it. That is the general shape worth recognising: the best way to scale a piece of work is often to arrange for it not to arrive.\n\nThis removes an entire scaling problem rather than solving it. Your API stays small and fast because the large payloads never touch it.","video-delivery":"Video: transcoding and adaptive bitrate\n\nAn uploaded video is transcoded into a ladder of resolutions and bitrates, then split into segments of a few seconds each, with a manifest listing what exists. The manifest is the whole protocol: the player reads it, decides which rendition to fetch next, and asks for one segment at a time over ordinary HTTP, which is why every CDN can serve video without knowing anything about video.\n\nThe player measures throughput and switches renditions between segments, so a connection that degrades produces lower quality rather than a stall. That is adaptive bitrate, and it is the reason streaming survives a train tunnel when a single progressive MP4 does not. HLS and DASH differ in details and agree on this design.\n\nTranscoding is expensive and it is paid once per title against a saving on every playback, which is why it is worth doing carefully. A fixed ladder applied to every video is wrong in both directions at once: it wastes bits on an animation with flat colour and starves a grainy night scene. Per-title and per-shot encoding measure the content and choose the ladder for it, and Netflix has published savings in the region of 30 per cent at equal quality from exactly that.\n\nThe startup sequence is what users judge, and it is a specific engineering problem rather than a consequence of good encoding. Start on a low rendition so playback begins quickly, then climb once throughput is known. Keep the first segments short so the first frame arrives sooner. Preload the manifest and the first segment when intent is obvious. Time to first frame is the metric that correlates with abandonment, not average bitrate.\n\nLive is a different problem wearing the same clothes. Segment duration sets the floor on latency, since a segment cannot be served until it exists, so ordinary HLS sits several seconds behind. Low-latency variants deliver partial segments to cut that, at the cost of more requests and more complexity in the packager, and the question to ask is whether the product needs seconds or needs to be honest that it does not. Then storage and rights. Every rendition of every title is another copy, so a ladder of six renditions is six times the storage before any language or subtitle variant. Digital rights management adds licence servers and encrypted segments, and it is a business requirement rather than a technical one, which is worth saying out loud because it is the part that most complicates an otherwise clean pipeline.\n\nAdaptive bitrate exists because bandwidth is variable and unpredictable. Serving one file forces a choice between buffering for slow connections and wasting quality on fast ones.","authn-authz":"Authentication and authorisation\n\nAuthentication establishes who you are. Authorisation decides what you may do. They fail differently, they are fixed differently, and they are confused constantly in both code and conversation.\n\nMost serious access-control bugs are authorisation bugs. The user is correctly identified, the session is valid, and the system simply never checks whether this particular user may touch this particular record. Changing an identifier in a URL and seeing someone else's data has sat near the top of the OWASP list for years, and it is not a clever attack; it is a missing line.\n\nThe reason is structural rather than careless. Authentication happens once, in one place, at the edge, so it is nearly impossible to forget. Authorisation happens on every request against every resource, in every handler, so forgetting exactly one is the default outcome as a codebase grows. Counting on discipline across two hundred endpoints is not a security model.\n\nThe durable fix is to make the check impossible to skip by moving it to where the data is fetched. A repository whose queries always carry the tenant or owner constraint cannot return another tenant's row, whatever the handler above it forgot. Row-level security in the database is the strongest version of the same idea, since it holds even for a query someone writes by hand during an incident.\n\nThen there is the model itself. Role-based access control assigns permissions to roles and roles to users, which is simple and gets awkward when the answer depends on the object rather than the person: the same editor may edit their own drafts and not someone else's. Attribute-based rules describe the condition instead, and relationship-based systems, like Google's Zanzibar, store who is related to what and answer questions about the graph. Most products start with roles and grow a relationship model whether or not they name it.\n\nTwo rules survive every model. Deny by default, so a missing rule refuses rather than permits, and check on every request rather than only when rendering a link, because the button being hidden is not a control when the endpoint is still open.\n\nInsecure direct object reference, changing an id in a URL and seeing someone else's data, is consistently among the most common real vulnerabilities, and it is purely a missing authorisation check.","sessions-vs-jwt":"Sessions or JWTs\n\nA session id is a reference: an opaque random string that means nothing on its own. The server holds the state it points at, can change or delete it instantly, and pays a lookup on every request. That lookup is a cache hit in practice, measured in a fraction of a millisecond, which is worth remembering when statelessness is being justified on performance grounds. A JWT carries its claims and a signature, so any service holding the public key can validate it without asking anyone. That is the entire feature, and the cost follows directly from it: a token that validates locally cannot be revoked locally, because revocation is shared state and shared state is what the design removed. A user who logs out, an employee who is dismissed, a token that leaks: each stays valid until it expires.\n\nThe usual compromise is a short-lived access token, minutes rather than hours, alongside a longer-lived refresh token that is stored server side and can be revoked. That is a session with extra steps, and it is the right answer when the validating services genuinely cannot reach a shared store. It is worth being honest that the revocation window is now the access token lifetime, and choosing that number is choosing how long a compromised token keeps working.\n\nWhere the token is stored matters as much as its format. A cookie with HttpOnly is unreadable by JavaScript, which removes the main consequence of a cross-site scripting bug, and needs SameSite and a CSRF defence because the browser attaches it automatically. Local storage is readable by any script on the page, so one compromised dependency takes every token, and it is chosen mainly because it is convenient for a single-page application.\n\nThe claims themselves need care. Verify the signature before reading anything, reject the none algorithm outright, check the issuer and audience so a token minted for another service is not accepted by yours, and keep the payload small because it travels on every request and is readable by anyone holding it. A JWT is signed, not encrypted; putting anything private in it publishes it.\n\nSaid plainly: for one application talking to a database it already has open, a session is simpler, revocable and better. JWTs earn their keep when the validator cannot reach your store, across services, across companies, or at an edge with no database at all.\n\nJWTs are frequently chosen for statelessness and then paired with a revocation list, which reintroduces the lookup and leaves you with the drawbacks of both.","oauth":"OAuth and OpenID Connect\n\nOAuth is delegated authorisation: it lets an application act on a user's behalf without ever holding their password. The user is sent to the provider, approves a specific scope, and the application receives a token that grants exactly that. It is not a login protocol, and the many systems that treat it as one have a class of vulnerability in common.\n\nOpenID Connect is the thin layer on top that adds identity. Alongside the access token it returns an ID token, a signed JWT stating who the user is, which issuer authenticated them, which client it was issued for and when. Sign in with Google is OIDC. Inferring identity from an access token instead is the classic mistake, because an access token proves the bearer may call an API and says nothing about who the bearer is.\n\nThe flow worth knowing in detail is authorisation code with PKCE, which is now the recommendation for every client type rather than only for mobile. The application redirects the user with a challenge derived from a secret it keeps, the provider returns a short-lived code through the browser, and the application exchanges that code plus the original secret for tokens over a back channel. The code alone is useless to anyone who intercepts it, which is what PKCE exists to guarantee.\n\nTwo parameters carry most of the remaining security. The redirect URI must be matched exactly against a registered value, because a wildcard or a loose prefix lets an attacker have the code delivered to a host they control. The state parameter must be generated, sent and verified on return, since it is the cross-site request forgery defence for the callback and skipping it is easy because everything works without it.\n\nThe implicit flow, which returned tokens directly in the URL fragment, is deprecated for good reason: tokens ended up in browser history, in referrer headers and in server logs. If you find it in an existing integration, that is a finding rather than a style preference.\n\nScopes deserve a moment of thought rather than a copied list. Ask for the narrowest scope that does the job, because the consent screen is where users decide whether to trust you and because a token stolen from you can do whatever it was granted. An integration that requests full account access to read a profile is both a security risk and a conversion problem.\n\nConflating the two is the common error. If you need to know who the user is, you want OIDC's ID token, not an access token that merely proves you may call an API.","secrets":"Secrets and encryption\n\nSecrets belong in a manager with rotation and an audit trail, not in environment variables committed to a repository or baked into an image. The property that matters is not secrecy alone but revocability: when a credential leaks, the question is how quickly it can be replaced and how confidently you can tell what it touched. A secret nobody can rotate without a deploy is a secret that will not be rotated.\n\nAssume every secret leaks eventually, and design for the aftermath. Short lifetimes, one credential per service rather than one shared everywhere, and scoped permissions all shrink what a leak costs. Scanning for committed secrets is worth having, and rotating anything a scanner finds is the only correct response, because a secret that reached a repository has reached everyone who cloned it.\n\nPasswords are a separate problem with a settled answer. Hash them with an algorithm designed to be slow and memory-hard, Argon2id by preference, bcrypt or scrypt where it is not available, with a per-user salt that the algorithm handles for you. Never a fast general-purpose hash such as SHA-256: a modern graphics card tries billions of those a second, so the speed that makes it a good checksum makes it a poor password hash.\n\nEncryption in transit is table stakes and essentially free. Encryption at rest is worth less than people assume: it defends against a stolen disk and it satisfies an auditor, and it does nothing at all against an attacker holding valid application credentials, which is how data actually leaves. Field-level encryption for the few genuinely sensitive columns, with keys held separately, is the version that changes an attacker's outcome.\n\nKey management is the part that decides whether any of it holds. Keys in the same store as the data they protect are decoration. A key management service, an envelope scheme where a data key is encrypted by a master key, and a documented rotation path are the difference between encryption as a control and encryption as a checkbox. Finally, secrets leak through channels nobody calls a secret store: log lines, error messages, exception trackers, URL query strings, and the analytics tool watching the page. Redacting at the logging boundary, and keeping credentials out of URLs, closes the most common route, and it is cheaper than discovering a token in a third-party tool's search index.\n\nUsing SHA-256 for passwords is fast, which is precisely the flaw: an attacker with the hashes can try billions per second. The slowness of bcrypt is the feature.","leader-election":"Leader election\n\nMany systems need exactly one node doing something: running a scheduled job, accepting writes, compacting a table, coordinating a cluster. Leader election is how that one node is chosen and how it is replaced when it dies, and the whole difficulty lies in the second half.\n\nTwo nodes both believing they lead is a split brain, and it corrupts data quietly rather than loudly. Both are behaving correctly by their own reasoning; the system has simply given two nodes the same certainty. That is why the failure is usually discovered later, in inconsistent data, rather than at the moment it happens.\n\nThe naive implementation is a lock row with a timeout, and it fails for a reason worth internalising: there is no bound on how long a process can be paused. Garbage collection, a hypervisor descheduling the VM, a page swapped to disk, a suspended laptop. The lease expires, a new leader is elected, and the old one wakes up with no idea that any time has passed and writes as though it still leads.\n\nYou cannot prevent the pause, so the storage layer has to reject the write. Fencing tokens are the mechanism: the lock service issues a monotonically increasing number with each grant, every write carries it, and the resource refuses anything with a number lower than the highest it has seen. The zombie's write is rejected by the thing being written to, which is the only layer that can know.\n\nLeases need clocks, and clocks are the other soft spot. A lease based on absolute timestamps depends on clock synchronisation between the holder and the service; one based on elapsed monotonic time does not, and monotonic time is what you want here. Anything comparing wall-clock timestamps across machines to decide who leads is depending on NTP for correctness, which is a dependency nobody chose.\n\nAlmost nobody should implement this from scratch. etcd, ZooKeeper and Consul exist, do it correctly and have had their edge cases found by other people, and a database lease with fencing tokens is a reasonable middle path when adding a coordination service is not worth it. The good outcome is to spend your thinking on what the leader does rather than on how it is chosen.\n\nThe naive version, a lock row with a timeout, fails when the leader pauses for garbage collection, wakes up believing it still holds the lock, and writes over the new leader. Fencing tokens exist to reject those late writes.","consensus":"Consensus: Raft and Paxos\n\nConsensus is getting a group of nodes to agree on a value despite some of them failing or being unreachable. Paxos came first and is famously hard to reason about; Raft was designed for understandability, which is why it is what almost everything built since uses. Both give the same guarantee: one agreed sequence of decisions that survives a minority failing. The mechanism is a majority quorum. A value is committed once more than half the members have accepted it, which means two conflicting majorities cannot exist, because any two majorities of the same group overlap in at least one member. That overlap is the whole proof, and it is worth being able to state, because everything else in the algorithm is machinery serving it.\n\nCluster sizing follows from arithmetic rather than from tradition. Tolerating f failures needs 2f+1 members, so 3 tolerates one and 5 tolerates two. Four members tolerate exactly one, the same as three, while costing an extra machine and an extra acknowledgement, which is why even sizes are avoided. The common explanation about avoiding tie votes is wrong: a majority of four is three, so a majority cannot tie.\n\nRaft's structure is worth knowing in outline because it explains the failure modes. One leader takes all writes and replicates them to followers; a term number increases with each election so stale leaders are recognisable; a follower that misses heartbeats becomes a candidate and asks for votes. Randomised election timeouts stop every follower standing at once, which is the same desynchronisation trick as jitter in a retry policy.\n\nThe cost is a round trip to a quorum on every write, so consensus is correctness bought with latency, and the price is set by geography. A cluster inside one data centre pays a millisecond; one spanning continents pays the worst inter-region round trip on every single write, which is why global systems shard so that each shard's consensus group stays local rather than stretching one group across the world.\n\nFinally, know what it is not for. Consensus is for metadata, membership, leadership, configuration, small critical state. Running your primary data path through a consensus group means every write pays quorum latency, which is why systems that need both usually keep a small consensus group deciding who may write and a fast replication path doing the writing.\n\nThis is why you size clusters odd and why cross-region consensus is painful: a quorum spanning continents pays the worst inter-region latency on every write.","saga":"Distributed transactions and sagas\n\nA transaction across several services cannot use a database transaction, because there is no single database holding the locks. Two-phase commit exists and does provide atomicity, and it is rare in practice for one reason: it blocks. If the coordinator fails after participants have prepared, they hold their locks and wait, and a system where one component's crash freezes several others is a poor trade for a guarantee you can usually work around.\n\nA saga is the usual alternative: break the work into local transactions, each committing on its own, each paired with a compensating action that undoes its effect. Book the flight, book the hotel, charge the card; if the hotel fails, cancel the flight and refund. Every step commits immediately, so nothing is held, and the price is that partial states are real and visible.\n\nCompensation is not rollback, and the distinction is the entire design problem. A rollback erases history; a compensation is a new fact that offsets an old one. The flight was booked and then cancelled, and both appear in the record. Some actions have no compensation at all: you cannot unsend an email or unpublish something someone screenshotted, which forces you to order the saga so the irreversible steps come last.\n\nSagas are orchestrated or choreographed, and the choice matters more than it sounds. An orchestrator holds the state machine explicitly: one service knows the sequence, which makes the flow readable and the coordinator a component to run. Choreography spreads it across event handlers with no central owner, which couples less and means the actual sequence exists only in the aggregate of everyone's subscriptions, and can be reconstructed only by reading them all.\n\nEvery step needs to be idempotent, because every step will be retried. A saga is a long-running process across unreliable networks, so duplicate deliveries and repeated compensations are normal operation rather than an exception. A compensation that runs twice must leave the same state as one that runs once, which usually means recording that it has run rather than checking whether it needs to.\n\nThe last piece is visibility. A saga in progress is a state machine with a current step, and it needs to be inspectable: which sagas are running, which are stuck, which compensations failed. Without that you have a distributed process nobody can see, and the first sign of trouble is a customer noticing that a booking exists with no payment against it.\n\nThe design work is deciding what compensation means for each step. Some actions genuinely cannot be undone, which forces you to order the saga so the irreversible steps come last.","unique-ids":"Generating unique ids at scale\n\nAuto-increment ids are perfect until they are not. They need a single coordinator to hand out the next value, which is a bottleneck at high write rates and stops working entirely once the table is sharded, because two shards would issue the same numbers. They also leak information: sequential ids in a URL tell a competitor your order volume and let anyone enumerate your records.\n\nRandom UUIDs solve coordination completely and cost you index locality. Version 4 is random by construction, so consecutive inserts land in unrelated parts of the B-tree, which means constant page splits, a much larger working set in memory, and write performance that degrades as the table grows. At 16 bytes they also enlarge every secondary index that references them.\n\nSnowflake-style ids are the usual answer at scale: a timestamp in the high bits, a machine identifier, and a per-millisecond counter, packed into 64 bits. Unique without coordination, sortable by time, and small enough to be a comfortable primary key. The cost is that each generator needs a distinct machine id, which is a small piece of configuration that must genuinely be unique, and a clock that does not move backwards.\n\nUUIDv7 is the standardised version of the same idea and is the sensible modern default when a UUID is wanted: a millisecond timestamp in the leading bits followed by randomness, so it is globally unique with no coordination and still inserts near the end of the index. It gives most of Snowflake's benefit with none of the machine id configuration.\n\nTime-ordered ids have a cost worth naming: they disclose creation time and, with enough samples, creation rate. Where that matters, the fix is a separate opaque public identifier for external use, with the ordered id kept internal. That is also the clean answer to enumeration, since the internal key can stay sequential and useful while nothing outside can walk it. The general shape here appears in many places: coordination buys ordering, and avoiding coordination costs locality. The good designs recover the ordering some other way, usually by putting time in the high bits, rather than by reintroducing the coordinator they were trying to remove.\n\nTime ordering is the underrated property. Random UUIDs as a primary key cause page splits across the whole index; UUIDv7 and Snowflake ids keep inserts near the end where they belong.","oltp-olap":"OLTP and OLAP\n\nTransactional databases are tuned for many small reads and writes of individual rows, and they store data row by row, so fetching one complete record is one contiguous read. Analytical stores are tuned for scanning a few columns across billions of rows, and store data column by column, so the columns a query does not name cost nothing at all to skip.\n\nThat layout difference is the whole performance story, and it compounds. Column pruning means a query touching three of sixty columns reads a twentieth of the data. Compression then works far better, because a column holds values of one kind with low variety, so run-length and dictionary encoding do real work where mixed row data resists them. Vectorised execution processes a batch of values per instruction rather than a row at a time.\n\nRunning heavy analytics on the production database puts the two workloads in competition for the same buffer pool, the same disk and the same connection slots. A single analyst running a wide scan can evict the working set that was serving customers, which is how a dashboard causes an outage without anything being obviously wrong.\n\nThe order of remedies is worth knowing, because most teams jump to the last one. A read replica for reporting removes the contention for a small cost and no new technology. Materialised views precompute the expensive aggregates. Only when scans genuinely need to cross billions of rows does a warehouse earn its complexity, and by then the argument makes itself.\n\nModelling differs too. Transactional schemas are normalised so a fact lives in one place; analytical schemas are deliberately denormalised into facts and dimensions, the star schema, because a join across billions of rows is expensive and storage is cheap. Bringing normalised thinking to a warehouse produces queries that are correct and slow.\n\nThe two ends have been converging, and it is worth knowing where. Postgres has columnar extensions, DuckDB puts an analytical engine on a laptop, and lakehouse formats such as Iceberg and Delta bring transactions to files in object storage. The distinction that stays true is the physical layout, and that is what to reason about rather than the product names.\n\nColumnar storage is the reason a warehouse scans a billion rows in seconds. It is a different physical layout, not just a bigger machine.","batch-vs-stream":"Batch and streaming\n\nBatch processes a bounded chunk on a schedule. The inputs are fixed, so a run is reproducible; a failure is fixed by re-running it; and reasoning about correctness is tractable because the data stops moving while you look at it. The result is as old as the last run, which for most reporting is entirely acceptable. Streaming processes events as they arrive, which buys latency and charges in every dimension that batch made simple. The data never stops, so there is no natural moment at which a result is final, and the hard problems arrive with it: events out of order, events arriving late, and deciding what a window means when both are true.\n\nEvent time and processing time is the distinction that makes streaming difficult. An event happened at one moment and reaches you at another, and grouping by when it arrived produces figures that are wrong in ways nobody notices: a mobile app that was offline for an hour delivers its events at once, and they land in the wrong window. Windowing by event time is correct and requires deciding how long to wait for stragglers, which is what watermarks express.\n\nSo a streaming pipeline needs an explicit answer to what happens to data arriving after its window closed. Discard it, and totals quietly under-report during exactly the incidents you care about. Update the emitted result, and every downstream consumer must handle a figure that changes after publication. Hold windows open longer, and you have traded back the latency you paid for. There is no default that is right, only a decision.\n\nThe pattern that resolves most of this is to keep the log and treat processing as replayable. If events are durable and ordered, a bug in the pipeline is fixed by correcting the code and reprocessing from a position rather than by reconciling by hand, which is the operational property that makes streaming survivable. That is also why the same architecture serves both: batch is a replay over a bounded range.\n\nMost organisations need batch and believe they need streaming. The honest test is whether a decision changes when the data is minutes old rather than hours: fraud, pricing, alerting and operational routing pass it, and a dashboard read each morning with coffee does not. Streaming for a report nobody reads before nine is complexity bought with no return.\n\nStreaming is meaningfully harder to operate and debug. It is worth it when freshness changes an outcome, fraud, pricing, alerting, and rarely worth it for dashboards read each morning.","cdc":"Change data capture\n\nChange data capture reads the database's own replication log and turns each committed change into an event. Every insert, update and delete, in commit order, with no cooperation from the application and no query load on the primary. The database is already writing that log for its replicas; CDC is a second reader of it.\n\nCompared with polling an updated_at column it misses nothing. Polling cannot see deletes, because a deleted row simply stops appearing; it cannot see intermediate states, because two updates between polls look like one; and it adds load to the primary on a schedule forever. The log has all of it, already ordered, already written.\n\nThe structural argument is stronger than the practical one. Dual writes, where the application writes to the database and then publishes an event, have no atomicity: the process can die between the two, and nothing detects the gap. CDC has one commit, and everything downstream is derived from it, so the events cannot disagree with the data by construction. That is the same reasoning as the outbox pattern, and CDC is its lower-maintenance form.\n\nWhat you take on is a coupling to the database schema. Log events carry columns, so a rename that would be invisible behind an API breaks every consumer, and a consumer now depends on a physical detail rather than a contract. The usual discipline is a translation layer that maps log events into published domain events, so the schema can move without every downstream service moving with it.\n\nThe operational details that bite are worth knowing in advance. A replication slot that no consumer is reading holds write-ahead log on the primary, and a stalled consumer over a long weekend can fill the disk of the database it was reading. Initial snapshots of a large table are heavy and need a strategy. And delivery is at least once, so consumers must be idempotent regardless of how ordered the source is.\n\nUsed well, this is how one source of truth feeds a search index, a cache, a warehouse and three downstream services without any of them being able to drift, and without the application knowing they exist. That last part is the real prize: adding a consumer requires no change to the system that owns the data.\n\nCDC solves the dual-write problem structurally: there is one commit, and everything downstream derives from it. That is why it beats having the application publish events alongside its writes.","events-vs-commands":"Events, commands and who decides\n\nA command says do this and names a recipient. An event says this happened and names nobody. That grammatical difference is the whole architecture: a command couples the sender to the receiver and to the outcome, and an event couples the publisher to nothing at all, because it is a statement of fact that has already occurred.\n\nThe consequence is where the decision lives. With commands, the caller decides what should happen next, so adding a behaviour means changing the caller. With events, each consumer decides whether it cares, so adding a behaviour means adding a subscriber and changing nothing that already works. That is the property people are buying when they say decoupled, and it is real.\n\nIt is also why events are named in the past tense and describe facts rather than intentions. OrderPlaced is an event; SendConfirmationEmail is a command wearing an event's clothing, and publishing it means the publisher has decided what the consumer does, which is the coupling it was supposed to remove. If the publisher would be upset that nobody handled it, it was a command.\n\nEvent content is the next decision and it has two ends. A thin event carrying only an identifier forces every consumer to call back for the details, which is simple and puts read load on the publisher and reintroduces a runtime dependency. A fat event carrying the full state removes that call and means the payload is a copy that can be stale and must be versioned. The middle, an identifier plus the fields consumers actually need, is where most systems end up after trying both.\n\nThe honest summary is that events move the complexity rather than removing it. What was an explicit call becomes an implicit contract, and the system gains the ability to add consumers freely and loses the ability to read one file and know what happens next.\n\nThe choice is about who decides what happens next. Commands put that decision in the caller and keep it visible; events put it in each consumer and make it extensible. Both are legitimate, and the failure is publishing commands as events and expecting the decoupling anyway.","outbox":"The outbox, and why dual writes fail\n\nThe most common bug in an event-driven system is not in the broker. It is that a service updates its database and then publishes an event, and those are two commits with nothing tying them together. The process can die in between. The broker can be unreachable for the second. The result is a database that says the order is placed and a world that never heard about it, or the reverse.\n\nRetrying does not fix it, because the failure is that there is no atomicity, not that the publish was unlucky. Publishing first and writing second has the same problem with the halves swapped. Wrapping both in a database transaction does nothing, since the broker is not a participant, and adding a distributed transaction across the two is the two-phase commit that everybody avoided for good reasons.\n\nThe outbox pattern removes the problem rather than mitigating it. The event is written into a table in the same transaction as the state change, so either both are committed or neither is. A separate process then reads that table and publishes, marking rows as sent. There is still exactly one commit that matters, and the publisher becomes a retryable background job rather than part of the request.\n\nDelivery is then at least once by construction, because a crash after publishing and before marking the row means the event goes out twice. That is the correct trade: duplicates are survivable with an idempotent consumer, and a lost event is not. Anyone who wants exactly once is asking for something the network does not offer, and the honest version is at least once plus deduplication at the consumer.\n\nChange data capture is the same idea with less code, reading the database's replication log and turning committed changes into events without an outbox table at all. It removes the polling process and adds a coupling to the physical schema, which is why teams that want a designed event contract keep the outbox and teams that want the plumbing to disappear reach for the log.\n\nTwo commits with no atomicity is the whole problem, and no retry policy repairs it. The outbox turns it into one commit plus a retryable delivery, which is the same move as every other durable-work design: write the intention transactionally, then perform it separately.","event-schema":"Schemas, versioning and the contract nobody owns\n\nA synchronous API has an owner, a version and a small set of callers who can be found. An event topic has a publisher, an unknown set of consumers, and messages that may be replayed from months ago. That combination makes schema evolution harder than in a request-response system, and it is the part most teams discover late.\n\nThe rules that hold are the familiar compatibility rules applied strictly. Adding an optional field is safe. Removing a field or renaming one breaks any consumer reading it, and you cannot know who that is. Changing the meaning of a field while keeping its name is the change no schema check catches, and in an event stream it also silently rewrites the meaning of history.\n\nA schema registry is the mechanism that turns this from a convention into a check. Producers register a schema and the registry refuses an incompatible change, so the break happens at deploy time rather than in a consumer nobody remembered. Formats with defined evolution rules, Avro and Protocol Buffers among them, exist largely because this problem is old and the rules are known.\n\nReplay makes versioning permanent in a way request-response does not. If events are retained so a new consumer can rebuild its state from the beginning, then every schema version that was ever written is a version some consumer must still be able to read. Either the old messages are upgraded on read, or the consumers keep the code for every version, and choosing that deliberately is much cheaper than discovering it during a rebuild.\n\nThe organisational point sits underneath the technical one. An event is a public interface published by a team who often does not know who depends on it, so the discipline that makes it work is treating the schema as a product with an owner, a documented meaning per field, and a deprecation process, rather than as a serialisation detail of whatever the producing service happens to store.\n\nAn event schema is a public interface with anonymous consumers and a history that can be replayed, which makes it stricter than an API rather than looser. The registry is what converts the compatibility rules from good intentions into a deployment that fails.","event-debugging":"Debugging a system with no call stack\n\nThe cost nobody quotes when adopting events is that the flow disappears. In a synchronous system a request has a call stack and a trace, and you can read the code to see what happens next. In an event-driven system what happens next is the set of everything subscribed, which lives in configuration across several repositories, and there is no single place that describes the sequence.\n\nSo tracing has to be deliberate rather than emergent. A correlation identifier created at the edge must be carried in every event and logged by every consumer, or a business flow spanning six services is six unrelated log streams. Modern tracing standards define how to propagate context through messaging as well as HTTP, and using them is what makes an event-driven system observable rather than merely instrumented.\n\nThe failure modes are different too, and they are quieter. A consumer that is down is not an error anybody sees, it is a lag number that has to be watched. A poison message stalls a partition, which appears as one entity's updates having stopped while everything else looks healthy. A consumer that processes events out of order writes a state nobody can explain from the events themselves. None of these produces a failed request, so none of them alerts unless somebody built the alert.\n\nConsumer lag is therefore the central metric, and the useful version is the age of the oldest unprocessed event rather than the count. Depth is ambiguous, because ten thousand messages is fine at high throughput and an incident at low, while age answers the question a user would ask, which is how far behind reality this system currently is.\n\nThe last practice is to make replay a designed capability rather than an emergency measure. Being able to reprocess a topic from a position, into a fresh consumer, with idempotent handlers, is what turns a bug in a consumer into a re-run rather than a manual reconciliation. Systems that can do this recover from consumer bugs in an afternoon, and systems that cannot spend a week writing scripts to repair state by hand.\n\nRemoving the call stack removes the thing that made debugging tractable, and nothing replaces it by default. Correlation identifiers, lag measured as age, and a rehearsed replay path are what put back the ability to answer what happened to this order.","api-gateway":"What an API gateway is for\n\nA gateway is the single front door to a set of services, and its job is the work that would otherwise be repeated in each of them: terminating TLS, authenticating the caller, applying rate limits, routing by path or host, and emitting consistent logs and metrics. Doing those once, correctly, is worth more than doing them eleven times with three subtle differences.\n\nThe clearest argument for it is security. Authentication implemented per service is authentication implemented inconsistently, and the service that gets it wrong is the one nobody remembers exists. Terminating at the gateway means one implementation to audit, one place to rotate keys, and a uniform answer to who is calling, which the services behind it can then trust rather than reimplement.\n\nThe clearest argument against a large one is that gateways attract logic. Request transformation, response shaping, per-client special cases, a small amount of business rule that had nowhere else to go, and eventually the gateway is a shared component that every team must change and nobody owns. That is the enterprise service bus reborn with a modern name, and the reason to state the boundary explicitly: routing, authentication, limits and observability, and no domain logic at all.\n\nAggregation is the case that genuinely justifies more. A mobile client on a slow network making eleven calls to render one screen is paying eleven round trips, and a backend for frontends, a gateway specific to that client, can make those calls internally and return one response. The important part is that it is owned by the client team and is one per client type, rather than a shared layer with every client's needs in it.\n\nThe failure to plan for is that the gateway is now on the path of everything. It must be stateless and horizontally scaled, its configuration must be deployable without a restart, and its own failure has to be understood, because a gateway that is down is an outage of everything behind it regardless of how healthy those services are.\n\nThe gateway exists to make cross-cutting concerns consistent, and it earns its place exactly as long as it holds that line. Every piece of domain logic added to it converts a component nobody has to think about into one that every team must coordinate through.","service-mesh":"Service meshes, and whether you need one\n\nA mesh moves the concerns of service-to-service communication out of the application and into a proxy deployed beside each instance. Retries, timeouts, circuit breaking, mutual TLS, traffic splitting and per-call telemetry become configuration rather than library code, and crucially they become uniform across services written in different languages by different teams.\n\nThat last point is the honest argument. In a single-language estate, a shared client library does most of this, and a library is simpler to operate than a fleet of proxies. In a polyglot estate, a library means implementing and maintaining the same behaviour four times, which is where the sidecar's per-instance cost starts looking cheap.\n\nThe features that are genuinely hard to get any other way are mutual TLS everywhere with automatic certificate rotation, consistent traffic shifting for canaries at the network level, and telemetry that is identical across services because it is emitted by the same proxy. Each of those is possible without a mesh and each is fiddly enough that most teams do not do it consistently.\n\nThe cost is a second network to understand. A proxy per instance is CPU, memory and a latency addition on every hop, the control plane is a new dependency in the request path's configuration, and debugging now involves asking whether the problem is the application, its sidecar, the other sidecar or the control plane. That is a genuine increase in the number of places a failure can live.\n\nSo the honest test is the number of services and the number of languages. A handful of services in one language does not need a mesh and will feel the cost immediately. Fifty services in four languages, with a platform team to run it, is where the uniformity pays for the complexity. Everything in between is a judgement, and the sidecar-free variants that push the same functions into the kernel are a live attempt to lower the price.\n\nA mesh buys uniformity across services you do not control and cannot rewrite, which is worth a great deal at fifty services and nothing at five. The question is not whether the features are useful but whether a library would give you them for less.","north-south-east-west":"The edge and the interior are different problems\n\nTraffic entering the estate and traffic moving inside it have almost nothing in common, and conflating them produces designs that are wrong at one end. External traffic comes from clients you do not control, over networks you cannot measure, with credentials that may be stolen and volumes that may be hostile. Internal traffic comes from services you deployed, in a network you configured, and every caller has an identity you issued.\n\nSo the concerns differ. At the edge: authentication of untrusted callers, per-tenant rate limiting, request size limits, bot mitigation, TLS termination and caching for anonymous traffic. Inside: identity between services, timeouts and retries with budgets, circuit breaking, load balancing that understands long-lived connections, and tracing that spans the whole call tree.\n\nZero trust is the idea that the interior should not be treated as safe simply because it is the interior. A flat internal network where any service can call any other with no identity means one compromised component reaches everything, which is how a modest breach becomes a total one. Mutual TLS and per-service authorisation make lateral movement a series of separate obstacles rather than one.\n\nThe mistake in the other direction is applying edge machinery inside. A heavyweight gateway between every pair of internal services adds a hop, a failure domain and a bottleneck to calls that were fine. Internal calls want the lightest thing that provides identity, timeouts and telemetry, which is the argument for a sidecar or a library rather than for a second gateway.\n\nThe practical layering that most estates converge on is a CDN and a public gateway at the edge for untrusted traffic, and a mesh or a shared library inside for identity and resilience between services. Naming which layer a requirement belongs to is usually enough to settle an argument about where a feature should live.\n\nThe two directions differ in who the caller is and what can be trusted about them, and every feature decision follows from that. The most common design error is a single layer expected to serve both, which ends up too heavy for internal calls and too naive for external ones.","ml-serving":"Serving a model\n\nA model in production is a service with unusual resource requirements and entirely ordinary operational needs. It has a latency budget, a capacity limit, a deployment process and a rollback path, and treating it as a research artefact that happens to be reachable over HTTP is how most of the problems start.\n\nThe first decision is online against batch. Batch inference computes predictions on a schedule and stores them, which is simple, cheap, easy to monitor, and only works when the input is known in advance: recommendations for known users, risk scores updated nightly. Online inference computes on request, which is required when the input arrives with the request and costs you a latency budget and capacity planning for peak.\n\nBatching and hardware dominate the cost of the online path. Model inference is far more efficient on batches than on single requests, so a server collects requests for a few milliseconds and runs them together, which trades a little latency for a large increase in throughput. Deciding that window is the main tuning knob, and it is the same accumulate-then-flush trade as everywhere else in this material.\n\nLoading is the operational trap. Large models take a long time to load into memory, so a naive autoscaling policy scales up long after the traffic arrived and a rolling deploy briefly halves capacity. Pre-warming, keeping a spare instance and treating model load time as a first-class number in the deployment plan are what stop that being discovered during a spike.\n\nEverything else is ordinary discipline applied to an unusual payload. Version the model, be able to roll back to the previous one without a rebuild, keep the preprocessing code alongside the weights so they cannot drift apart, and log the inputs and outputs, which is the only way anything in the next three topics is possible.\n\nMost production failures in machine learning systems are ordinary systems failures: capacity, deployment, versioning and rollback. Treating the model as a normal service with an expensive payload gets you most of the way, and the parts that are genuinely different come later, in the data.","ml-features":"Features, and the skew that ruins them\n\nA model consumes features, which are values computed from raw data: how many orders this customer placed in the last week, the average basket size, whether the address changed recently. Computing those consistently is most of the engineering, and getting it inconsistent is the defining failure of production machine learning.\n\nTraining and serving skew is that failure. Training features are computed in a batch job over historical data, and serving features are computed in a service on a live request, and the two are written by different people at different times in different languages. Any difference in a default, a time zone, a rounding rule or a null handling produces a model that performs well in evaluation and worse in production, with nothing failing anywhere.\n\nTime travel is the subtler version and it flatters the model instead. If a training example for a purchase on Tuesday includes a feature computed from data that only existed on Wednesday, the model has learned from the future, and its offline accuracy is excellent and meaningless. Every feature has to be computed as of the moment the prediction would have been made, which is a discipline the data makes very easy to violate.\n\nA feature store exists to make those two problems structural rather than a matter of care. One definition per feature, used to compute both the training set and the serving values, with point-in-time correct joins for the historical case. Whether the tool is worth its complexity depends on scale, and the property it enforces is worth having even when the implementation is a shared library and a convention.\n\nThe freshness question then splits the store in two. Features derived from slow-moving data can be computed in batch and read at serving time, while features that depend on what the user did thirty seconds ago need a streaming path. Most production systems run both, and knowing which features need which is a product decision rather than an engineering preference.\n\nThe model is rarely the problem. Features computed one way for training and another for serving produce a system that evaluates well and performs badly, and nothing in the pipeline reports an error, which is why the same definition has to feed both paths.","ml-drift":"Drift, monitoring and knowing it still works\n\nA deployed model degrades without anything changing in its code, because the world it was trained on moves. Data drift is the input distribution shifting: new customer segments, a new device type, a marketing campaign bringing different traffic. Concept drift is the relationship changing: behaviour that predicted fraud last year predicts nothing now because the fraudsters adapted.\n\nThat makes monitoring a model different from monitoring a service. Latency and error rate say nothing about whether the predictions are still any good, and a model that has quietly stopped working produces perfectly healthy dashboards. What has to be watched is the input distribution against training, the output distribution over time, and, wherever possible, the actual outcome.\n\nGround truth usually arrives late and sometimes never. Whether a transaction was fraudulent is known when a chargeback arrives weeks later; whether a recommendation was good is inferred from a click that may mean several things. So monitoring has to work in two layers: proxy signals available immediately, such as the score distribution and the rate of predictions near a decision boundary, and the real measure when it eventually lands.\n\nFeedback loops are the failure that is specific to this domain. A model that ranks items influences which items are seen, which produces the training data for the next model, which learns from a world its predecessor shaped. Without deliberate randomisation, or holdout traffic that the model does not influence, a system can become extremely confident about a world it created rather than one it observed.\n\nRetraining is therefore a scheduled operational activity rather than a response to a complaint, and it needs the same rigour as a deploy: a new model version, an evaluation against a held-out set, a shadow or canary period against live traffic, and a rollback path. A model updated by copying a file onto a server is a deployment with none of the controls anyone would accept for code.\n\nA model can stop working while every service metric stays green, because the failure is in the relationship between input and outcome rather than in the software. That is why input and output distributions, and a path to real outcomes however delayed, are the monitoring that actually matters.","edge-what-moves":"What actually belongs at the edge\n\nThe edge stopped being a cache some time ago. Code now runs in the same locations that serve cached bytes, which means work can happen close to the user instead of at an origin that may be a continent away, and the interesting question is which work.\n\nThe clear wins are decisions that need little or no state. Routing, redirects, A/B assignment, geographic personalisation, request rewriting, authentication checks against a token that can be verified locally, and bot filtering. Each removes a round trip to origin for a decision that takes microseconds, and on a connection with a hundred millisecond round trip that is the entire perceived improvement.\n\nThe clear losses are anything that needs the database. If the code has to read the primary to decide, running it near the user has made the situation worse: the user's request now crosses the ocean from the edge rather than from a nearby origin, and it has added a hop. Edge compute is fast because it is near the user, and that advantage is spent the moment it has to talk to something that is not.\n\nBetween those sits the genuinely interesting case: serving a cached page and personalising it at the edge, so the invariant part is a cache hit for everyone and the variable part is filled in nearby. That is the technique that makes personalised pages cacheable at all, and it is why edge compute changed what a CDN is for rather than merely making it faster.\n\nThe rule to carry is that the edge is for decisions, not for data. A decision needs the request and a small amount of local information, and a decision made in three milliseconds near the user beats the same decision made in one millisecond a hundred milliseconds away, every time.\n\nMoving computation near the user only helps when the computation does not immediately need something far away. That single test, does this need the database, sorts almost every candidate workload correctly and prevents the usual mistake of moving a data-dependent handler to the edge and making it slower.","edge-constraints":"A different runtime, with different rules\n\nEdge platforms do not run ordinary servers. They run many small isolated contexts inside a shared runtime, started per request in microseconds rather than as containers, which is what makes them cheap enough to place in hundreds of locations. That model buys the startup time and imposes constraints that surprise anyone porting existing code.\n\nThe limits are real and worth knowing in advance. CPU time per request is measured in milliseconds rather than seconds. Memory is tens of megabytes. The available APIs are web standard rather than the full runtime of the language, so a library depending on the filesystem or on raw sockets may simply not work. And the process does not persist, so nothing can be kept in memory between requests as though it were a server.\n\nStatelessness is the constraint that shapes the design. There is no local disk and no reliable in-process cache, so state lives in a platform service: a key-value store with eventual consistency, an object store, a database designed to be reachable from many locations, or a coordination primitive that pins a piece of state to one place. Each has a latency and consistency profile that has to be chosen deliberately.\n\nData locality then becomes the central design question, because a globally distributed compute layer over a single-region database is a distributed system with a hundred fast front ends and one slow shared dependency. The patterns that work are reading locally from replicated storage, writing to a home region, and pushing anything genuinely global through a mechanism designed for it rather than through the database by accident.\n\nNone of this makes the edge unsuitable, it makes it a specific tool. Small, fast, stateless work with a nearby state store is what it does extremely well. A long-running job, a large in-memory model, or a handler that needs a transaction against a primary is what it does badly, and knowing the boundary is more valuable than any amount of tuning on the wrong side of it.\n\nThe edge is a different runtime rather than a nearer server: milliseconds of CPU, tens of megabytes, web-standard APIs and no persistence between requests. Those constraints are what make the model cheap enough to place everywhere, and they decide what can be moved there without a rewrite.","edge-security":"The edge as the security boundary\n\nThe most valuable property of the edge is not speed, it is that hostile traffic can be refused before it reaches anything you own. A request absorbed in a location near its source never consumes origin capacity, never opens a database connection and never runs a line of your code, which is a different kind of protection from a service that handles the request efficiently.\n\nVolumetric attacks make the argument by themselves. A distributed denial of service that would saturate an origin's link is spread across hundreds of locations, each seeing a fraction, and absorbed by capacity that exists anyway for legitimate traffic. No origin-side rate limiter helps with an attack that fills the pipe before arriving, because the damage is done upstream of anything you control.\n\nThe layer above that is deciding who is worth serving. Bot management, filtering by reputation, challenges for suspicious clients and blocking obviously malformed requests all belong here, because each is a decision needing only the request and some shared intelligence. Doing the same work at origin means paying for the connection and the compute to reject something you did not want.\n\nRate limiting at the edge is subtly better than at origin for a reason worth stating: the edge sees the whole picture. A limiter running on each origin instance sees only its own share of the traffic, so a limit of a hundred a minute becomes a hundred per instance, while the edge counts the caller across every location and applies the limit that was actually intended.\n\nThe mistake is treating the edge as sufficient. It is a filter, not an authorisation system, and anything reachable by a request that gets past it still needs its own checks, because a leaked origin address, an internal caller or a rule that is one release out of date will all bypass it. Defence in depth means the edge removes the volume and the origin still refuses what it should refuse.\n\nRefusing traffic near its source is qualitatively different from handling it efficiently at origin, because the cost is paid by capacity that already exists for other reasons. The corollary is that the edge is a filter rather than a control: everything behind it still has to defend itself.","what-docker-solves":"What Docker actually solves\n\nA container is a process on the host kernel with a restricted view of the world. Two Linux features do the work: namespaces, which decide what a process can see, its own process table, network stack, mount table and hostname, and cgroups, which decide what it can use, its share of CPU, memory and IO. There is no virtual machine and no second kernel, which is why it starts in milliseconds.\n\nThe problem it solves is environment drift. Before containers, works on my machine was a real and expensive category of bug, because the machine included a particular library version, a particular locale, a particular environment variable set by someone in 2019. An image is the environment, so the same artefact runs identically on a laptop, in CI and in production, and the failure mode moves from mysterious to reproducible.\n\nA virtual machine gives stronger isolation by running its own kernel, at the cost of size and startup time. That distinction matters for security: a container escape is a kernel exploit away, which is why untrusted multi-tenant workloads use VMs, or lightweight VMs such as Firecracker that keep the isolation while approaching container startup times.\n\nThe other half of the value is immutability. A container is replaced rather than updated, so there is no configuration drift accumulating on long-lived servers and no question of what has been done to a machine since it was provisioned. That property is what makes rollbacks trivial: the previous image still exists and still runs exactly as it did.\n\nWhat containers do not solve is worth stating plainly. They are not a security boundary strong enough to run untrusted code, they do not make a stateful service stateless, and they do not remove the need to think about how data outlives the process. A database in a container is a database with a storage question, not a solved problem. So the argument is reproducibility first and density second. If the deployment already produces one identical artefact, a static binary, a zip for a serverless runtime, the reproducibility case is weaker and containers buy less, which is worth acknowledging before adopting the ecosystem that comes with them.\n\nContainers are about reproducibility first and density second. If your deployment already produces an identical artefact, a single Go binary, a Lambda zip, the reproducibility argument is weaker and containers buy you less.","docker-vs-k8s":"Docker or Kubernetes, and why\n\nDocker builds and runs containers on one machine. Kubernetes schedules them across many: restarting what fails, rolling out new versions, scaling on demand, routing traffic to whatever is currently healthy. They are not alternatives, and the question that matters is not which to choose but whether an orchestrator is needed at all.\n\nWhat Kubernetes genuinely gives you is a declarative control loop. You describe the desired state and controllers work continuously to make reality match it, which is why a killed pod comes back and a failed node's work moves elsewhere without anyone acting. That model is the reason it won, and it is genuinely powerful when there is a fleet to manage.\n\nThe weight is equally real. Networking with its own model, ingress, RBAC, storage classes, cluster upgrades on a cadence you do not control, and an API surface large enough that expertise in it is a full-time skill. Debugging spans several layers, each with its own failure modes, and the mean time to understand an incident goes up before it comes down.\n\nThe middle option is the one most teams skip. A managed container platform, Cloud Run, ECS Fargate, App Runner, takes the same image and runs it with autoscaling, rollouts and health checking while handing you almost none of a cluster. For a handful of services with similar shapes, that is most of the benefit for a small fraction of the cost, and it covers a great many systems that reached past it.\n\nThe honest signals that Kubernetes is right: many services with different scaling profiles that benefit from bin-packing, a platform team standardising across several product teams, workloads that need scheduling features such as affinity or GPUs, or a multi-cloud requirement that is real rather than aspirational. One service and three engineers is not that list.\n\nThe cost that decides it is people rather than money. A cluster needs somebody who understands it when it misbehaves at three in the morning, and if that person is also the only backend engineer, the orchestrator has been bought with the time that was going into the product.\n\nReach for Kubernetes when you have many services with different scaling profiles, need bin-packing across a fleet, or are standardising across teams. For a handful of services, a managed container platform, ECS Fargate, Cloud Run, App Runner, gives most of the benefit with a fraction of the operational cost. Choosing Kubernetes for one service is buying a scheduling problem you did not have.","image-hygiene":"Image size, layers and security\n\nImages are built as layers and each layer is cached by the instruction that produced it and everything above it. Ordering a Dockerfile so dependency installation happens before source is copied means a code change rebuilds one small layer rather than reinstalling every package, which is the highest-leverage build optimisation available and costs nothing but attention. Multi-stage builds are the second. Compile in an image with the full toolchain, then copy only the artefact into a minimal runtime image, and hundreds of megabytes of compilers, headers and package caches never ship. The build stays convenient and the thing you deploy is small, which shortens every pull on every node on every deploy.\n\nSmaller is also safer, because most of a large base image is packages you never call and every one of them can carry a vulnerability that a scanner will report and someone will have to triage. Distroless images contain your application and its runtime and not a shell, which removes an entire category of post-exploitation. Alpine is small for a different reason, musl rather than glibc, which is occasionally a compatibility problem worth knowing about in advance.\n\nSecrets must not end up in layers. A file copied in and deleted in a later instruction is still present in the earlier layer and still readable by anyone with the image, which is a recurring way credentials leak. Build secrets exist for this, mounted for one instruction and never committed to a layer, and they are worth learning before the alternative teaches you.\n\nTags deserve more care than they get. Deploying latest means the image you tested and the image that runs are related only by hope, and a rollback has nothing to point at. Tag with the commit, or better with the image digest, so a deployment names exactly one artefact and can be reproduced next year.\n\nTwo habits complete the picture: run as a non-root user, because the default is root and a container escape from root is a much better day for an attacker, and scan images in the pipeline so a known vulnerability is a build result rather than a discovery.\n\nLayer ordering is the highest-leverage build optimisation available and costs nothing. Copying source before installing dependencies invalidates the dependency cache on every commit.","pipeline":"What a pipeline should do\n\nContinuous integration runs the checks on every change, build, tests, lint, type check, so breakage surfaces in minutes rather than at release. Continuous delivery keeps every passing commit deployable. Continuous deployment goes further and ships it without a human deciding. The three are often used interchangeably and describe different amounts of courage.\n\nThe value is proportional to speed, and the threshold is roughly ten minutes. Past that people stop waiting, start batching changes, and the benefit of small deploys evaporates: each release is larger, the failure is harder to attribute, and the rollback takes more with it. Pipeline duration is a product decision rather than an infrastructure detail.\n\nGetting there is mostly ordering and caching. Run the cheap checks first so an obvious failure costs seconds, cache dependencies between runs, run independent jobs in parallel, and only run the expensive end-to-end suite where it earns its place. A pipeline that runs everything in sequence because that was the order it was written in is the common case.\n\nWhat belongs in the pipeline is anything the project claims to be true. Formatting, types, tests, licence checks, a build that actually produces the artefact you will deploy. Every rule enforced by review and not by a check is a rule that holds until the day someone is in a hurry, and the checks that pay for themselves are usually the ones written after that day.\n\nFlaky tests deserve a policy rather than a habit. A test that fails randomly trains everyone to press retry, which is the same as having no test, and the cost is not the flake but the credibility of every other failure. Quarantine it, fix it or delete it, and treat a rising flake rate as a defect in the pipeline rather than as weather.\n\nThe build must be reproducible and the artefact must be the one that ships. Building once and promoting the same artefact through environments is what makes staging meaningful; rebuilding per environment means staging tested something adjacent to what production runs, which is a subtle way to make the whole pipeline decorative.\n\nPipeline duration is a product decision, not an infrastructure detail. Past roughly ten minutes people stop waiting, start batching changes, and the benefit of small deploys disappears.","deploy-strategies":"Blue-green, canary and rolling\n\nRolling replaces instances gradually: take one out, start the new version, wait for it to be healthy, continue. It is simple, needs no extra capacity beyond one instance, and has one requirement that people forget until it bites, which is that both versions run simultaneously and must therefore be compatible with each other and with the schema.\n\nBlue-green runs a complete second environment and switches traffic in one step. Rollback is switching back, which is as fast as a change to a load balancer, and that speed is the whole point. The costs are double the infrastructure for the duration and the fact that the database is usually shared, so the schema still has to work for both sides and the instant rollback does not extend to data.\n\nCanary sends a small share of traffic to the new version and watches before proceeding. It is the strongest default because it fails small: a problem appears on one per cent of requests rather than on all of them. What makes it work is the automated decision, which means metrics good enough to compare the two populations and a rule for aborting that does not need a human awake.\n\nProgressive delivery is the same idea generalised. Route by user, by region, by internal staff first, and increase in steps with an automatic hold when error rates or latency diverge. Cells make this natural, since a cell is already a hard boundary, and the deploy becomes cell by cell with the blast radius decided in advance.\n\nEvery strategy needs a rollback that has been tried. A rollback path nobody has exercised is a plan rather than a capability, and the moment to discover that the previous image no longer starts, or that the migration cannot be reversed, is not during an incident. Rehearsing it is cheap and the alternative is expensive exactly once. Finally, decouple deploy from release with flags where you can. When the code path can be turned on without shipping and off without redeploying, the deployment strategy stops being the only lever you have, and the fastest rollback available becomes a configuration change rather than a pipeline run.\n\nCanary is the strongest default because it fails small: problems surface on one percent of traffic, not on all of it. It requires metrics good enough to make the go or no-go call automatically.","migrations":"Database migrations without downtime\n\nSchema changes and code deploys are not atomic, and cannot be. There is always a window in which old code runs against the new schema, or new code against the old one, and every migration strategy is a way of making that window harmless rather than avoiding it.\n\nExpand and contract is the pattern that works. Add the new column, deploy code that writes both old and new, backfill the existing rows, switch reads to the new column, and only then, in a later release, drop the old one. Each step is safe with either version of the code running, which is what makes the sequence longer and the outage shorter.\n\nThe backfill needs its own care on a large table. Update everything in one statement and you hold a lock and generate a write burst that replicas struggle to apply; batch it in chunks with a pause between them and it takes longer and disturbs nothing. That patience is usually the difference between a migration nobody noticed and an incident report.\n\nLocks are the second failure mode and they are database-specific. Adding a nullable column is instant on a modern Postgres, adding one with a volatile default is not. Changing a type rewrites the table. Building an index the ordinary way takes a lock that queues every query behind it, which is precisely why the concurrent variant exists. Knowing which operation is cheap on your engine is not optional knowledge for anyone deploying to it.\n\nLock queues are worse than the lock itself. A migration waiting for a long-running query holds its place in the queue, and everything arriving afterwards waits behind it, so a table that was merely busy becomes unavailable. Setting a short lock timeout means the migration fails quickly instead of taking the table with it, and failing a migration is a much better outcome than an outage.\n\nThe rule that follows: every migration should be reversible, tested against production-sized data, and separated from the code that depends on it. Splitting a rename into three deploys feels slow, and it is the only version that stays up.\n\nThis is the most common cause of self-inflicted deploy outages. Splitting a rename into three deploys feels slow and is the only version that stays up.","feature-flags":"Feature flags\n\nA feature flag separates deploying code from releasing behaviour. Code ships dark, is enabled for a cohort, then for everyone, and turning it off is a configuration change rather than a redeploy. During an incident that difference is minutes against a pipeline run, which is why flags are how teams deploy on a Friday without pretending it is a normal Tuesday. There are several kinds and conflating them causes most flag problems. A release flag is temporary and exists to be deleted. An experiment flag exists to be measured and then decided. An operational flag, a kill switch for an expensive feature, is permanent by design. A permission flag is really entitlement and probably belongs in the authorisation model rather than in a flag system.\n\nFlags accumulate, and each one is a branch that doubles the number of possible behaviours. Ten live flags is a thousand combinations, of which you test perhaps three. That is why deletion has to be part of the process, with an owner and an expiry date, rather than something everyone intends to get to once the launch settles.\n\nThe flag service becomes a dependency on the request path, so it needs the treatment of one. Cache the values locally, define what happens when the service is unreachable, and make the default the safe path rather than whatever the client library returns for an unknown key. A flag system that takes the product down when it is unavailable has inverted its own purpose.\n\nFlags are also a testing problem. The combination you ship is the combination someone experiences, so at minimum test the on and off state of anything currently in flight, and be honest that beyond a handful of concurrent flags nobody is testing the matrix. That is another argument for deleting them promptly rather than a reason to avoid them.\n\nUsed well, they change the shape of a release: small, frequent, dark deploys, then a separate decision about exposure, with a rollback that does not need the pipeline. Used badly they are a permanent second configuration language that nobody owns and nobody can safely remove.\n\nFlags are how you deploy on Friday safely. The discipline that makes them work is deleting them: a codebase with two hundred live flags has an untestable number of behaviour combinations.","core-services":"The core services, by concept\n\nThe three major clouds offer the same primitives under different names: compute, object storage, a managed relational database, a queue, a CDN, a key-value store, a secrets manager. Learn the concepts rather than the catalogue. An interviewer asking about object storage does not care whether you say S3, GCS or Blob Storage, and neither does the design.\n\nThe differences that survive contact with reality are commercial and organisational. AWS has the broadest catalogue and the most third-party support, so whatever you need probably exists and probably has a Terraform provider. GCP is strong on data and on Kubernetes, which it originated. Azure wins where an organisation already runs Microsoft identity and licensing, and that argument is usually decisive on its own.\n\nThere is a hierarchy worth holding: infrastructure where you run the machine, platform where you hand over the artefact, and serverless where you hand over the function and the idle time. Each step upward trades control and portability for less operational work, and each is right for different things at different stages, often in the same system.\n\nLock-in deserves a clearer head than it usually gets. Compute and storage are close to commodities and move with effort. Managed databases move with real pain. Proprietary integration services, event buses, identity, and anything where the product is the glue, are where the cost concentrates, because they are the parts with no equivalent anywhere else. The honest question is not whether to be locked in but which lock-in is worth the leverage.\n\nMulti-cloud as a default is usually a mistake. It means building for the intersection of three platforms, so you get the least capable version of each, plus the operational burden of all of them, in exchange for a portability nobody exercises. Deliberate exceptions exist, DNS and CDN are commonly multi-vendor because outages there are total, and that is a targeted decision rather than a strategy.\n\nThe pragmatic default: use managed services aggressively while the team is small, keep the genuinely portable pieces portable, and spend your architectural allowance on the decisions the cloud does not make for you.\n\nChoosing a cloud is usually decided by existing commitments, team familiarity and pricing agreements, and seldom on technical superiority. The technical differences matter far less than the migration cost of being wrong.","right-sizing":"Right-sizing: build for the traffic you have\n\nDo the arithmetic before choosing anything. Ten thousand visitors a month, with a few pages each, is a handful of requests a minute at peak. A single small server or a static site on a CDN handles that without noticing. Multi-region clusters, autoscaling groups and service meshes answer problems that start several orders of magnitude higher, and the number is easy to compute and rarely computed.\n\nThe habit worth forming is converting everything into requests per second and bytes per second. A million requests a day is about twelve per second averaged, perhaps fifty at peak. A hundred gigabytes of storage is one disk. Those numbers deflate most architecture arguments before they begin, and the ones they do not deflate are the ones worth having.\n\nOver-engineering costs money and, far more expensively, costs change. Every additional moving part is something to operate, debug, secure, upgrade and explain to the next person, and it is paid for while the product is still unproven. The right architecture for an unvalidated idea is the one that can be replaced quickly, because most of its decisions will turn out to be wrong.\n\nModern hardware is also larger than most people's intuition. A single well-configured server with fast storage handles tens of thousands of requests a second for many workloads, and a managed database on a mid-sized instance serves traffic that would have needed a room of machines fifteen years ago. Vertical scaling remains the cheapest scaling available and stops being fashionable long before it stops being effective.\n\nThe exception is anything expensive to change later. Data models, tenancy boundaries, identifiers and the public shape of an API are decisions that get more expensive every month, so they deserve thought now. Instance sizes, autoscaling rules and even the choice of platform are reversible in an afternoon and deserve much less. So: build for the traffic you have and design for the traffic you can measure yourself heading towards. Scaling ahead of a forecast is speculating with the only resource an early product genuinely lacks, which is time.\n\nThe right architecture for an unvalidated product is the one you can change quickly. Premature scale locks in decisions before you know the access patterns, and those are the expensive ones to reverse.","cost-drivers":"Where cloud bills actually come from\n\nEgress is the most commonly underestimated line on a cloud bill. Data leaving the provider is charged per gigabyte while data arriving is usually free, and serving media directly from object storage costs several times what the same traffic costs through a CDN. Cross-zone and cross-region transfer is charged too, so a chatty architecture spread across zones pays for its own internal conversation.\n\nIdle provisioned capacity is next, and it is the quietest. Instances and databases sized for a peak that happens twice a year run at that size for the other 363 days. Non-production environments running at production size overnight and at weekends are the same waste with fewer excuses, and switching them off on a schedule is usually the fastest saving available.\n\nThen there is the tax on inattention: unattached volumes, old snapshots, forgotten load balancers, log retention set to forever, data sitting in an expensive storage class years after anyone read it. None of these is architecture; all of them are somebody's afternoon and a recurring charge until someone spends it.\n\nManaged services trade money for operational time, and that trade is excellent for a small team and increasingly poor at large volume, which is why big companies eventually build their own. Netflix building Open Connect and Dropbox moving off S3 are the same calculation at different points on the same curve: past some volume the provider's margin exceeds the cost of doing it yourself, and below it managed wins comfortably.\n\nThe habits that keep a bill honest are unremarkable. Tag everything so cost can be attributed to a team or a feature, set a budget alert so a surprise is a notification rather than a monthly discovery, and look at unit economics, cost per request or per customer, rather than the total, because the total is supposed to grow.\n\nThe last one is cultural. Engineers cannot economise on a bill they never see, and the single most effective cost control in most organisations is showing the people who create the spend what it costs, at the level of the feature they just shipped.\n\nNetflix building Open Connect and Dropbox moving off S3 are both the same calculation: past a certain volume, the margin a provider charges exceeds the cost of doing it yourself. Below that volume, managed wins easily.","serverless":"Serverless, containers or VMs\n\nThe three options are best understood by what you hand over. With a virtual machine you hand over the hardware and keep the operating system, the runtime, the process supervision and the patching. With a managed container platform you hand over an image and keep the code inside it. With a serverless function you hand over a function and, crucially, you also hand over the idle time, which is the part that changes the bill rather than the architecture.\n\nScaling to zero is the property serverless is bought for. A function with no traffic costs nothing, and one with a hundredfold spike gets a hundred instances without anybody having configured a scaling group. That suits work whose shape is spiky or unpredictable: a webhook receiver, a scheduled job, an admin endpoint used twice a week, the long tail of internal tooling that would otherwise each need a home.\n\nThe cold start is the cost of that, and it is worth being specific about where it comes from rather than treating it as a fixed tax. A request arriving with no warm instance waits for a sandbox to be created, the runtime to boot and the application to initialise, and the last of those three is usually the largest and is entirely yours: a framework that scans the classpath, a dependency tree that runs code at import, a client that opens a connection pool at module scope. Interpreted runtimes typically start in tens to low hundreds of milliseconds; a JVM or .NET application doing real initialisation can take seconds. The mitigations are provisioned concurrency or a minimum instance count, which work by keeping instances warm, which is to say by giving up scale to zero and paying for idle after all.\n\nThe cost crossover is a duty cycle rather than a request count, and it is arithmetic anyone can do before choosing. Per-invocation pricing charges memory multiplied by execution time, so a function allocated one gigabyte and busy for a whole month is billed for roughly 2.6 million gigabyte-seconds, which is the same order of magnitude as simply renting a small instance for that month. Below roughly a fifth to a third of full utilisation serverless is comfortably cheaper; well above it, always-on capacity is, and the exact crossover moves with the provider's prices, which is the argument for computing it rather than inheriting an opinion about it.\n\nThen there are the constraints that decide feasibility rather than cost. Execution has a hard ceiling, fifteen minutes on Lambda, so anything genuinely long-running needs a different home or needs breaking into steps. Nothing survives between invocations, so in-process caching is unreliable and any warm state is a coincidence. And instance count follows traffic, which means database connections follow traffic too, so the same property that makes scaling effortless is what exhausts a relational database's connection slots at peak, which is why a pooler is standard equipment rather than an optimisation.\n\nSo the decision follows the traffic shape and the work, not the fashion. Spiky, short, stateless and event-driven: serverless, and the operational saving is real. Steady, predictable and continuous: a container on a managed platform, where there are no cold starts and always-on capacity is the cheaper way to buy the same compute. Unusual system requirements, a specific kernel, a GPU, licensed software, a process that must not be restarted: a virtual machine, and accept the operational work as the price of the control. Most real systems contain all three, chosen per workload, which is the correct answer rather than a failure to standardise.\n\nThe deciding factor is traffic shape, and the two numbers that decide it are duty cycle and initialisation time. Spiky and low-volume favours serverless because idle costs nothing; steady and predictable favours containers because always-on capacity is cheaper than paying per invocation for work that never stops. Reaching for provisioned concurrency to fix cold starts is wor","test-pyramid":"The testing pyramid\n\nUnit tests are fast and numerous and isolate one piece of logic. Integration tests check that components work together, usually with a real database and a real HTTP layer. End-to-end tests drive the whole system through its interface, the way a user would. The pyramid says have many of the first and few of the last, because cost, runtime and flakiness all rise with scope. The reason is feedback rather than purity. A failing unit test names the broken function; a failing end-to-end test tells you something in the system is wrong and leaves you to find it. Inverted, mostly end-to-end, produces a suite that takes twenty minutes, fails for unrelated reasons twice a week, and is eventually ignored by everybody including the person who wrote it.\n\nThe layer that pays best in practice is the middle one, and it is the one most often skipped. A test that runs the real handler against a real database in a container catches the things unit tests cannot, wrong SQL, a missing migration, a serialisation mismatch, without the fragility of a browser. Containers made that layer cheap enough that the old argument for mocking the database has largely expired.\n\nMocking deserves a rule: mock what you do not own, and be sparing even then. Mocks encode your belief about how a dependency behaves, so a test suite built on them passes whenever your beliefs are self-consistent, which is not the same as being right. The classic outcome is a fully green suite against an API that changed its response shape last month.\n\nWhat to test is a better question than how much. The logic with branches, the boundaries where data enters or leaves, the failure paths that only run during an incident, and every bug you have already had, which is the highest-value test anyone writes. Trivial getters and framework behaviour are not tests, they are ceremony with a runtime cost.\n\nOne test type deserves a mention because it finds what examples cannot: property-based testing, where the framework generates inputs and checks an invariant. It is unreasonably effective on parsers, serialisers, date handling and anything with a round trip, because it explores the space you did not think of, which is precisely where the bugs are.\n\nThe shape follows from feedback speed. A failing unit test names the broken function; a failing end-to-end test tells you something in the system is wrong, and you still have to find it.","coverage":"Coverage and what it does not tell you\n\nCoverage measures which lines executed while the tests ran. It says nothing about whether anything was asserted, or whether the assertions mean anything. A test that calls a function and checks nothing gives that function full coverage and catches no regression whatever, which is the whole problem in one sentence.\n\nThat makes a coverage target a textbook case of a measure becoming a goal. Mandate eighty per cent and you will get eighty per cent, assembled from tests written to touch lines rather than to verify behaviour, and the number will be defended in review by people who know it means nothing. The metric was fine until it was made a target.\n\nIt is genuinely useful in the other direction, as a discovery tool. Sorted by least covered, it shows which modules nobody has tested, and diff coverage on a pull request answers a narrower and more sensible question: is the code being added tested at all. That is a prompt for a conversation rather than a gate.\n\nThe variants are worth knowing because they differ in strength. Line coverage is the weakest. Branch coverage checks that both sides of a condition ran, which catches a class of gap line coverage reports as complete. Mutation testing is the strongest by a distance: it changes the code deliberately and checks that a test fails, which measures assertions rather than execution and is the only common technique that can tell you a test suite is decorative.\n\nThe honest measures of a suite are elsewhere. Does it catch the regressions you actually have, does it fail for one reason rather than five, does it run fast enough that people wait for it, and does a failure name the problem. None of those has a percentage, which is exactly why coverage gets used instead.\n\nCoverage is useful as a discovery tool, finding untested areas, and harmful as a target, because it is trivially gameable and becomes the goal instead of correctness.","flaky-tests":"Flaky tests\n\nA flaky test passes and fails without the code changing. The causes are a short list and worth knowing by name: timing, where a fixed sleep stands in for waiting on a condition; shared state, where tests leak into each other through a database or a global; order dependence, where the suite passes in one sequence and not another; real network calls; and time itself, which is why tests written on a Tuesday afternoon fail at midnight or during a leap day.\n\nThe damage is cultural rather than technical. Once a suite is known to fail randomly, the correct response to any red build becomes pressing retry, and that response is applied to real failures too. Trust does not degrade gracefully: a suite that is ninety per cent reliable is treated exactly like one that is unreliable, because a human cannot tell which failure they are looking at.\n\nSo the response has to be immediate and structural. Quarantine the test out of the blocking suite the moment it flakes, so the signal for everyone else stays trustworthy, then fix it or delete it with an owner and a deadline. A quarantine with no expiry is just a slower way of deleting it, and being honest about that is better than pretending otherwise.\n\nMost fixes come from removing the source of nondeterminism rather than from making the test more patient. Wait for a condition instead of sleeping. Inject the clock so time is a value rather than an ambient fact. Give every test its own data, ideally its own schema or transaction, so nothing leaks. Run the suite in a random order deliberately, so order dependence is found by you rather than by a scheduler change six months from now.\n\nMeasure it. Flake rate per test and per suite, tracked over time, turns a vague sense that CI is annoying into a number with a trend, and it identifies the small number of tests responsible for most of the noise. In most suites, a handful of tests cause the overwhelming majority of the retries.\n\nThe uncomfortable case is a test that is flaky because the system is. A race condition in the code will present as an intermittent test, and quarantining it hides a real defect. The distinction is worth making before reaching for the quarantine label: is the test unreliable, or is it reporting accurately on something that is.\n\nTolerating flakiness destroys the value of the entire suite, not just the flaky test. The correct response is urgent, because trust does not degrade gracefully.","waterfall":"Waterfall\n\nWaterfall runs in sequence: gather requirements, design, build, test, release. Each stage completes before the next begins, sign-off moves the work along, and the customer sees the result at the end. Described that way it sounds naive, and it is worth knowing that the paper usually blamed for it, Royce in 1970, presented the pure sequence as the thing that does not work and recommended iterating.\n\nIt works where the cost of change late is genuinely enormous and the requirements genuinely cannot move: regulated work with a certification step, anything with manufacturing behind it, fixed-scope contracts where the scope is the contract. In those settings front-loading the decisions is not caution, it is the correct response to the shape of the cost curve.\n\nIt fails for software because requirements are discovered by using the thing. People cannot specify what they want from a product that does not exist yet, so the specification is a hypothesis, and waterfall defers testing that hypothesis until the moment change is most expensive. The defects found in the final phase are the ones introduced in the first.\n\nThe other failure is the handover. Each phase produces a document for the next, so context is repeatedly compressed and reconstructed by people who were not in the room. Most of what an implementer needs is the reasoning behind a requirement, and reasoning is exactly what survives a specification document least well.\n\nThe reason to know it properly is that the argument is about the cost-of-change curve rather than about culture. Where change is cheap and information arrives during construction, iterate. Where change is expensive and information arrives before construction, plan. Most software is the first and some genuinely is the second, and knowing which you are in is more useful than a preference.\n\nThe cost-of-change curve is the whole argument. If change is cheap and information arrives during building, front-loading every decision is the wrong bet.","agile":"Agile, scrum and kanban\n\nAgile is a set of preferences rather than a process: working software over comprehensive documentation, responding to change over following a plan, collaboration over contract negotiation, individuals and interactions over tools. Each is a preference between two goods, which is the part most often dropped when it is quoted.\n\nScrum implements those preferences with structure: fixed sprints, a product owner, a scrum master, a backlog, and ceremonies for planning, review and retrospective. Kanban drops the sprint entirely and limits work in progress instead, pulling the next item when capacity frees. Kanban suits interrupt-driven work such as support or platform; scrum suits planned feature work with a stable team and a plannable period.\n\nWork in progress limits are the most underrated idea in either. Starting five things at once does not make them arrive sooner; it makes all five arrive later and increases the chance that some are abandoned half-finished. Limiting concurrent work reduces cycle time for the same throughput, which is Little's Law applied to a team rather than to a service.\n\nThe common failure is adopting the ceremonies without the feedback. Standups with no working software to demonstrate, sprints whose plan cannot change, retrospectives with no action taken: that is waterfall with more meetings, and everyone in the room knows it, which is where the reputational damage to the word comes from.\n\nThe retrospective is the meeting that determines whether any of it works, because it is the only one whose output is a change to how the team operates. A retrospective that produces observations and no owned action is a therapy session; one that changes something small every fortnight compounds. Estimation deserves less energy than it gets. Story points are a relative measure whose only legitimate use is forecasting from historical throughput, and the moment they become a productivity metric they are inflated and stop forecasting anything. Counting finished items and measuring cycle time is simpler and harder to game.\n\nBoth fail the same way: adopting the ceremonies without the feedback. Standups and sprints with no working software to show and no willingness to change the plan is waterfall with extra meetings.","code-review":"Code review\n\nReview catches defects, and if that were the whole benefit it would not be worth what it costs. The larger effects are that context spreads across a team, that a second person can now maintain the thing, and that the codebase stays coherent enough for the next change to be small. A team where every file has exactly one person who understands it has a staffing problem disguised as a productivity advantage.\n\nPull request size is the single biggest lever on whether any of that happens. Small changes get read; large ones get approved, because nobody can hold two thousand lines in their head and the honest options are to spend a day on it or to trust the author. Everybody chooses trust. That is not laziness, it is capacity, which is why the fix is upstream: splitting work into reviewable pieces is a design skill, and a change that cannot be split usually has a boundary problem worth finding before it is written.\n\nLatency matters as much as depth and gets far less attention. A review that arrives in an hour costs the author nothing; one that arrives the next day costs them a context switch to return to work they had finished thinking about, and it costs the change a day of drift against the branch it will merge into. A team that reviews within a few hours and comments less thoroughly generally ships better code than one that reviews perfectly on a two-day delay.\n\nSeparate blocking concerns from suggestions, explicitly, in the comment itself. Reviewers and authors routinely disagree about whether a comment must be addressed, and the disagreement is invisible: the reviewer thought they were musing, the author thought they were blocked, and a day goes past. Saying which comments are required and which are taste removes an entire category of friction, and a convention as crude as a one-word prefix does it.\n\nThe most useful comment names the case that breaks rather than the preference that was violated. This is null when the user has never logged in can be acted on immediately and cannot be argued with. I would extract this starts a conversation about style with no way to settle it, and settles by seniority rather than by reasoning, which is corrosive in a way that is hard to attribute afterwards. Where a preference genuinely matters, it belongs in a linter or a formatter, enforced by a machine that nobody resents.\n\nTwo things are worth taking out of review entirely. Formatting, which a formatter should decide so that no human ever spends attention on it. And architectural direction, which is much cheaper to discuss before the code exists than in a comment thread on a finished implementation. A review is the wrong venue for a design argument, and the tell that it is happening is a pull request with forty comments and no defect among them.\n\nPR size is the single biggest lever on review quality. Splitting work into reviewable pieces is a design skill, and the reason large PRs get rubber-stamped is capacity, not laziness.","incidents":"Incidents and blameless postmortems\n\nDuring an incident, restore service first and investigate afterwards. Mitigation and diagnosis compete for the same people and the same minutes, and users care about the first. Roll back, fail over, disable the feature, and keep the evidence; understanding can wait, and it is easier with a system that is up.\n\nIncidents run better with roles assigned rather than assumed. One incident commander who coordinates and decides, one person communicating outward so the responders are not answering questions, and the rest investigating. The commander does not have to be the most senior engineer, and often should not be, because coordinating and debugging are different jobs competing for the same attention.\n\nA postmortem asks what made the failure possible rather than who typed the command. If one person's mistake could take production down, the finding is the system that allowed it: no confirmation, no staged rollout, no way to reverse. Human error is the beginning of the investigation, not its conclusion.\n\nBlameless does not mean consequence-free. It means the output is a change to the system rather than a name, because the alternative is worse in a specific and measurable way: if reporting a mistake is punished, people stop reporting, near misses go unrecorded, and you lose exactly the information that prevents the next one.\n\nActions need owners and dates or the document is a diary. A postmortem that produces twelve suggestions and no assignments has changed nothing, and the same incident recurs with a second document beside the first. Two owned actions beat twelve orphaned ones.\n\nTwo habits are worth adding once the basics hold. Track how long detection took separately from how long the fix took, because a fast fix after a slow detection is still a long outage and points at monitoring rather than at engineering. And read old postmortems periodically: the recurring theme across a year is a finding that no single incident can reveal.\n\nBlame produces hidden incidents. If reporting a mistake is punished, people stop reporting, and you lose the information that prevents recurrence.","coupling":"Coupling and cohesion\n\nCoupling is how much modules depend on one another. Cohesion is how related the contents of a single module are. The target is low coupling and high cohesion, and the reason is entirely practical: it decides how far a change spreads, which is the thing that makes a codebase pleasant or exhausting to work in. Tight coupling means a change here forces a change there, and the effect compounds. A module depended on by twenty others cannot be changed without twenty conversations, so it stops being changed, and workarounds accumulate around it instead. Codebases that resist every modification are usually not badly written line by line; they are wired together too tightly.\n\nCohesion is the same idea from the other side. Organising by technical layer, all controllers here, all models there, all services somewhere else, scatters every feature across the tree, so adding a field means editing six files in six directories. Organising by feature puts what changes together in one place, and the test is simply how many directories a typical change touches.\n\nCoupling comes in degrees worth naming. Depending on a module's published interface is loose and fine. Depending on its internal structure, its database tables, or its file layout is tight and brittle. Two services sharing a database table is the tightest coupling most systems contain, and it is usually invisible on the architecture diagram, which draws them as separate boxes.\n\nThe useful heuristic is to ask what would have to change together. Things that change for the same reason belong together; things that change for different reasons belong apart. That is the single responsibility principle stated in the form that is actually actionable, since almost anything can be described as one responsibility if the description is vague enough.\n\nBoth are means rather than ends. The goal is that a change is small, local and safe, and if a low-coupling design makes a common change require touching five modules, the design is wrong regardless of how well it scores. Optimise for the changes you actually make, which is a question with an answer in the commit history.\n\nOrganising by feature rather than by technical layer usually raises cohesion: everything that changes together lives together, so a change touches one directory.","premature-abstraction":"Premature abstraction\n\nAbstracting after one use is a guess at what varies, and the guess is usually wrong. The abstraction then obstructs the very change it was built to accommodate: the second case needs a flag, the third needs another, and within a year the shared function has six parameters, four of which are only used by one caller each.\n\nDuplication is cheaper to fix than a wrong abstraction, and the asymmetry is the whole argument. Copies are visible: you can find them, compare them and merge them mechanically once you can see what they have in common. A wrong abstraction hides the differences behind parameters and has callers that have adapted to its shape, so unwinding it means untangling every one of them.\n\nWaiting for the third occurrence is a heuristic rather than a law, and its value is that by then the axis of variation is usually visible. Two examples look identical because you have only seen two; the third tells you which part was incidental. Where the pattern is genuinely obvious from the first, abstract, and where it is not, copy and wait.\n\nThere is a related failure that is harder to see: an abstraction that is correct but adds a layer nobody needed. Indirection has a cost paid by every reader, who must now hold two files in their head to understand one behaviour, and the cost is invisible to the person who wrote it because they already have both files in their head.\n\nThe honest signals that an abstraction has gone wrong are specific. Parameters that only exist to switch behaviour for one caller. A name that had to be vague because the thing does several jobs. Callers passing arguments they do not care about. Anyone finding it easier to bypass than to use. Any of those is a prompt to inline it and start again from what the code actually does.\n\nUnderneath is a rule about the cost of being wrong. Reversible decisions deserve speed and irreversible ones deserve deliberation, and duplication is reversible while a widely adopted abstraction is not. That asymmetry, rather than any aesthetic preference, is why the advice is to wait.\n\nThe cost asymmetry is the point. Removing duplication later is mechanical; unwinding a wrong abstraction means untangling every caller that adapted to it.","tech-debt":"Technical debt as a decision\n\nThe metaphor is precise and worth using precisely. Deliberate debt is a shortcut taken knowingly to hit a date, recorded, with an intended repayment: we are hard-coding this for launch and will generalise it in March. That is a legitimate engineering trade, and it is the only kind that can be argued for in a planning meeting.\n\nAccidental debt is what accumulates from not knowing better at the time, and calling it debt flatters it. Nobody chose it and there is no plan, so it is closer to damage than to a loan. The distinction matters because the conversations are different: one is a decision to revisit and the other is work to schedule.\n\nThe interest is real and compounds. Every change in that area costs more, and the rate rises as more code comes to depend on the shortcut, so the cheapest moment to repay is always now and it gets worse monotonically. That is also why a shortcut in a rarely touched corner may be worth leaving forever, since interest is only paid where change happens.\n\nWhich makes the useful question where the debt is rather than how much there is. Debt in code nobody touches costs nothing. Debt in the module every feature passes through is taxing every piece of work the team does, and the same amount of untidiness produces wildly different costs depending on where it sits. Change frequency is the multiplier.\n\nMaking it visible is what turns it into a decision. A note in the code with a date, an issue linked from the shortcut, a section in the design document listing what was skipped and why. Debt nobody can point at cannot be argued for or against, and it turns into a general sense that the codebase is bad, which persuades nobody and prioritises nothing. Repayment works best as continuous rather than as a project. A rewrite proposal competes with features and loses; improving the code you are already touching does not, because it is part of the work rather than an alternative to it. That habit is also why the areas of a codebase that get worked on most tend to be the ones in the best condition, provided the team has the discipline to leave them slightly better each time.\n\nThe distinction matters because only deliberate debt can be argued for. 'We chose this and here is the repayment plan' is a decision; 'the code is messy' is a complaint.","netflix-ingest":"Ingest: what arrives before any of this works\n\nA title does not arrive as a file you can stream. It arrives as a mezzanine: a very high bitrate master, often hundreds of gigabytes for a feature, plus separate audio stems for every language, subtitle and caption files, forced-narrative tracks, and artwork in a dozen aspect ratios. None of it is playable by a client, and none of it is trusted.\n\nThe first job is inspection. A master that is subtly broken, a dropped frame, an audio channel out of phase, a caption file whose timings drift by a quarter second after an hour, will otherwise be found by a member somewhere at three in the morning, and by then it is on every appliance in the world. So the pipeline runs automated quality checks before it spends any money encoding: frame-level artefact detection, audio loudness measurement against the delivery standard, subtitle timing validation, and a check that the declared runtime matches the actual one.\n\nOnly then does the expensive part begin, and it is genuinely expensive. A single title is encoded into dozens of variants: multiple codecs because a 2013 smart television and a 2025 phone do not decode the same things, multiple resolutions, multiple bitrates within each resolution, and each of those with every audio language muxed or served separately. One film becomes thousands of files.\n\nThis is parallelised aggressively, and the unit of parallelism is worth noticing: the work is split by chunk, not by title. A two-hour film cut into short segments can be encoded by hundreds of machines at once and stitched back together, which turns a job that would take days on one machine into one that takes minutes on many. It also means a failure costs one chunk rather than the whole encode.\n\nThe reason inspection comes before encoding is arithmetic. Encoding a title into every variant is the single most expensive operation in the pipeline, and a defect found afterwards means paying for all of it twice, plus the cost of pulling the bad copies off every appliance that already has them. Validating at the boundary is cheap; validating after the fan-out is not.","netflix-encoding":"Per-title and per-shot encoding: the bitrate ladder\n\nFor years the industry used one fixed bitrate ladder for everything: 1080p at some bitrate, 720p at a lower one, and so on down, identical for every title. This is obviously wrong the moment you say it out loud. An animated film with flat colour and clean lines needs far fewer bits to look perfect than a handheld night scene full of grain and motion, yet both were given the same allocation. One was wasting bandwidth and the other was being starved of it.\n\nNetflix replaced this with per-title encoding: analyse the content, then build a ladder specific to it. The complex title gets more bits where it needs them; the simple one gets a ladder that tops out lower because there is nothing more to represent. The measure used to decide is VMAF, a perceptual quality metric Netflix developed and open-sourced, which correlates with what people actually report seeing rather than with mathematical error like PSNR.\n\nThen they went further, to per-shot. A film is not uniform: within one title there are dialogue scenes that are trivial to encode and action sequences that are not. Dynamic Optimizer detects shot boundaries, the hard cuts where one camera setup ends and another begins, and optimises each shot independently, choosing the resolution and quantiser that maximise VMAF for the bits available.\n\nThe reported savings are large. Netflix published reductions of roughly 28% for x264, 38% for libvpx VP9 and 34% for x265 at equal quality, with a further 10 to 15% on top of per-title encoding at the same VMAF. They enabled it for selected titles in 2018 and broadly for 4K in 2020.\n\nIt is worth being precise about what is being traded. This is enormous extra compute at encode time, spent once per title, to save bandwidth on every single playback of that title forever. For a catalogue watched billions of times, that trade is not close.\n\nThe general principle is that a fixed configuration applied to variable input is always wrong somewhere, usually in both directions at once: wasteful for the easy cases and inadequate for the hard ones. The fix is to measure the input and let the measurement choose the setting. The reason this is not done everywhere is that it needs a metric that actually correlates with the outcome you care about, and building VMAF was the hard part, not the optimisation on top of it.","netflix-abr":"Adaptive bitrate: the decision the client makes every few seconds\n\nOnce the ladder exists, something has to choose which rung to play, and that decision belongs on the client, not the server. The client is the only party that knows what is actually happening: how full its buffer is, how fast the last few segments arrived, whether the device is thermally throttled, whether the user just seeked.\n\nThe mechanism is simple. Video is cut into segments of a few seconds, each available at every rung of the ladder. The player keeps a buffer of upcoming segments and, for each new request, picks a rung. Ask for too high a rung and the segment arrives late, the buffer drains, and playback stalls, which is the single worst thing a video player can do. Ask for too low a rung and the picture is soft when it did not need to be.\n\nNaive algorithms estimate throughput from recent downloads and pick the highest rung that fits. This behaves badly in exactly the situations that matter, because throughput estimates are noisy and a brief dip triggers a visible quality drop that was not necessary. Better approaches use buffer occupancy as the primary signal: a full buffer is direct evidence that you can afford to be ambitious, and it is a measurement rather than a prediction.\n\nThere is a startup problem too, and it is the one users judge you on. At the moment of pressing play there is no buffer and no throughput history, so the player must guess. Guess high and the first segment is slow, which shows as a spinner. Guess low and playback starts quickly but looks poor for the first few seconds. Most players start conservatively and climb, because time to first frame is what people notice and remember.\n\nThe reason the decision sits on the client is that the server cannot see the constraint. A server knows what it sent and how fast it sent it; it does not know that the buffer is nearly empty, that the device is on a train, or that the user is about to seek. Putting a control loop where the measurements are is a general principle, and it is why backpressure works better when the party that is struggling is the one that gets to say so.","netflix-drm":"DRM: three ecosystems, one encrypted file\n\nContent licensing obliges Netflix to protect the video, and protection means encryption plus a licence server that decides who gets a key. The complication is that there is no single DRM system: Widevine covers Android and Chrome, PlayReady covers Windows and Xbox, and FairPlay covers Apple devices. Three ecosystems, three key formats, three sets of rules.\n\nEncrypting the same title three times would triple storage and triple the encoding cost, which is why Common Encryption exists. The video is encrypted once with one key, and each DRM system is given a way to deliver that same key to its own devices. One set of files, three licence paths, which turns a threefold storage problem into a licence-server problem.\n\nThe licence request is where the real decisions happen, and they are business decisions rather than cryptographic ones: whether this account is entitled to this title in this country, how many concurrent streams the plan allows, whether the device is permitted at this resolution. The last one is not arbitrary. Studio agreements commonly tie higher resolutions to stronger protection, which is why the same title plays at 4K on a certified television and at a lower resolution in a browser on the same account.\n\nOn the web this is mediated by Encrypted Media Extensions, a browser API that hands encrypted segments and licence responses to a Content Decryption Module the page cannot see inside. The browser deliberately gives the page no access to the decrypted frames, which is the entire point.\n\nThe design worth taking away is that encryption is cheap and centralised while authorisation is expensive and distributed. Encrypt once, then spend the effort on the licence path, because that is where the per-user, per-device, per-region logic lives and where it can be changed without re-encoding anything. Anyone who has re-encrypted a corpus to change an access rule has learned this the hard way.","netflix-cdn":"Open Connect: building the CDN rather than buying it\n\nNetflix is a large share of internet traffic at peak. Buying that from a commercial CDN would be enormously expensive, and it would still put the bytes on the wrong side of the congested links between networks.\n\nSo Netflix builds its own appliances and gives them to ISPs at no cost, installed inside the ISP's own network. The ISP supplies power, space and connectivity. Netflix supplies the hardware and operates it. By the time you press play, the video is already past the peering links that would otherwise be the bottleneck, sitting on a machine inside the network you are a customer of.\n\nThe published hardware makes the trade concrete. Storage appliances are 2U servers with up to 120TB of raw storage and around 200Gbps of operational throughput, connected by 6x10Gbps or up to 2x100Gbps. There is also a smaller global appliance with up to 60TB and around 80Gbps, aimed at smaller partners and emerging markets. Netflix states that Open Connect serves 100% of its video traffic, and that close to 90% of that traffic globally is delivered over direct connections between Open Connect and the residential ISPs its members use.\n\nContent arrives on those appliances during off-peak hours, chosen by predictive models of what each region will want. This is the part that makes the whole thing work: the catalogue is finite, demand is forecastable, and yesterday's data tells you a great deal about tomorrow's viewing. Popularity is not uniform, so an appliance does not need the whole catalogue to serve almost all requests locally.\n\nThe appliances do not decide who they serve. They are directed caches: an OCA only serves clients whose prefixes the ISP has advertised to it over BGP, so the ISP retains control of what traffic lands where, which is a precondition for any ISP agreeing to host equipment at all.\n\nThe insight is that streaming is a predictable, cacheable workload, which converts a bandwidth problem into a storage problem. Storage is far cheaper than peak bandwidth, and it can be filled at three in the morning when the network is idle and capacity costs nothing. Almost no other large workload has this property, which is why almost nobody else has built this.","netflix-steering":"Steering: choosing which appliance you talk to\n\nHaving thousands of appliances is useless without a good answer to the question of which one a given client should use, and the answer changes constantly. An appliance may be near you and busy, or slightly further and idle. It may hold the title you asked for, or not. It may have been healthy a minute ago and not now.\n\nSo the choice is made per session at playback time, not by DNS and not by anycast. When the client asks to play something, the control plane returns a manifest that already contains the URLs it should use, ranked, with fallbacks. This is a deliberate departure from how most CDNs steer traffic, and the reason is control: DNS-based steering is coarse, cached at resolvers beyond your control, and cannot see whether the specific title is present on the specific box.\n\nThe ranking considers proximity in network terms rather than geographic ones, current load on each candidate, health, and whether the appliance actually holds the title. Because the manifest carries fallbacks, a client that starts failing can move without another round trip to the control plane, which matters because the control plane is the thing you least want in the path of a recovery.\n\nNote the split this creates. The control plane runs in the cloud and handles authentication, entitlement, and steering, all small requests. The data plane is the appliances, and it handles essentially all of the bytes. The two scale independently and fail independently, which is the property that makes the design robust.\n\nReturning ranked URLs with fallbacks inside the response is the important move. The alternative, asking the control plane again when something goes wrong, puts a dependency in the recovery path at exactly the moment the system is already degraded. Giving the client what it needs to recover on its own means a control plane outage does not become a playback outage for sessions already running.","netflix-microservices":"Microservices and the compounding failure problem\n\nThe backend is hundreds of services, and a single home page assembles rows from many of them. The arithmetic here is unforgiving and worth doing explicitly. If each of two hundred dependencies is independently available 99.9% of the time, the probability that all two hundred are healthy simultaneously is 0.999 raised to the power of 200, which is about 82%. Roughly one page load in five would fail if every dependency were treated as required.\n\nThat number is the whole argument. At this many dependencies, you cannot buy your way out with better uptime per service, because the compounding beats you. The only viable answer is to make most dependencies optional: render the page with whatever is available, and degrade the parts that are not. If personalisation is unavailable, show a sensible default row rather than an error page.\n\nThe tooling exists to make that the default rather than an aspiration. Zuul is the gateway at the edge, rewritten on Netty for asynchronous non-blocking I/O to hold very large numbers of connections. Eureka handles service discovery. Hystrix, now retired in favour of newer libraries, popularised the circuit breaker and, more importantly, the idea that every remote call should have a declared fallback.\n\nChaos engineering came from this problem. A fallback that has never been exercised is a hypothesis, not a safety net, and the only way to find out whether it works is to break the dependency and watch. Doing that deliberately, in production, during working hours, is far better than discovering the answer at three in the morning.\n\nThe instinct to make every dependency required is what produces systems that are less reliable than any of their parts. The discipline is to ask, for each call, what the page should show if this never answers, and to accept that the honest answer is usually something rather than nothing. A service with no fallback is a service that can take the whole page down.","netflix-caching":"EVCache: caching across regions without distributed transactions\n\nEVCache is the distributed caching layer, built on memcached, replicated within a region across availability zones and also across regions. Reported throughput is on the order of hundreds of millions of operations per second, holding things like viewing history, session metadata and precomputed personalisation.\n\nThe interesting decision is not the cache itself but how invalidation is handled, because invalidation across regions is where most designs quietly become distributed transactions. A delete that must succeed in every region before a write is acknowledged is a coordination protocol, and it will fail exactly when the network between regions is already degraded, which is to say at the worst possible moment.\n\nNetflix leans instead on short time to live values and versioned keys. A write bumps a version, so subsequent reads look for a key that only the new value occupies. Old entries are not deleted; they become unreachable and then expire on their own. Nothing needs to be coordinated, and a region that is temporarily unreachable simply serves slightly stale data until its entries age out.\n\nCross-region replication also exists for a reason that is easy to miss: it is not only about read latency, it is about failover. If a region is evacuated and its traffic lands elsewhere, the receiving region needs warm caches. A cold cache under a doubled load is its own outage, because every miss becomes an origin request at exactly the moment origin is busiest.\n\nThe general lesson is to make staleness bounded and self-healing rather than trying to make it zero. Zero staleness across regions costs coordination, and coordination fails under precisely the conditions that make you want it. A version bump needs no agreement from anyone, and an entry that expires on its own needs no successful delete.","netflix-regions":"Active-active regions and practising the evacuation\n\nNetflix runs multiple AWS regions active-active, serving members from more than one at the same time rather than keeping a standby that has never taken real traffic. The reason for active-active over active-passive is not capacity, it is confidence: a standby region is a claim about what would happen, and claims about untested code paths are usually wrong.\n\nFailover is therefore a traffic shift rather than a promotion. If a region degrades, its traffic is moved to the others, and because those regions were already serving, there is no cold start of anything, no first-time-ever code path, no configuration that was correct a year ago.\n\nThey rehearse it. Chaos Kong is the exercise that evacuates an entire region, and it is run as a matter of routine rather than as a drill after an incident. Published accounts describe keeping traffic out of a region for over twenty-four hours before shifting it gradually back to an even split, and shifting more slowly than an emergency would require, specifically so services can scale up and caches can warm. That detail is the interesting one: they discovered that the speed of the shift is itself a failure mode.\n\nThe write path is the genuinely hard part and it is worth being honest about it. Reads are easy to serve from anywhere. Writes accepted in two regions at once need either a partitioning scheme that keeps a given member's writes in one place, or eventual convergence with a defined conflict rule. There is no version of this that is simultaneously fast, globally consistent and available during a partition, which is the practical face of the CAP theorem rather than the textbook one.\n\nActive-active costs more than active-passive and buys something specific: every path in the failover has been exercised by real traffic before you need it. A passive region is a set of assumptions, and assumptions that have never been tested at load are where outages live. The gradual shift matters for the same reason, because instant failover is itself an untested thundering herd.","netflix-data":"Personalisation and the data platform underneath it\n\nAlmost every row on the home page is chosen, ordered and titled for the member looking at it, and even the artwork shown for a title can differ between members. That is a great deal of computation to attach to a page load, and the design answer is that most of it is not attached to the page load at all.\n\nThe heavy work happens offline. Models are trained on viewing behaviour in batch, and candidate rows are precomputed and written to a store the serving path can read quickly, which is where EVCache earns its place. At request time the service is mostly assembling and lightly reranking things that were computed earlier. This is what makes a personalised page affordable.\n\nThe event pipeline feeding this is substantial in its own right: play events, pause events, searches, impressions of rows that were never clicked, all streamed and processed continuously. The negative signals matter as much as the positive ones, because a row shown many times and never clicked is evidence.\n\nThere is a cold start problem that no amount of infrastructure solves. A new member has no history, so the first session is populated from what they told you during signup, from what is popular in their region, and from the device they are on. It gets better quickly, and the honest framing is that the first session is the worst one they will ever have.\n\nIt is worth naming the failure mode too. Feedback loops are real: recommending what was clicked yesterday makes it more clickable today, and a system optimised only for immediate engagement will narrow what people see. This is why the objective being optimised matters more than the model, and why measuring long-term retention rather than the next click is the harder and more honest choice.\n\nThe pattern is to move work out of the request path whenever the input changes more slowly than the requests arrive. Viewing history changes a few times a day; the home page is requested constantly. Precomputing against the slow-changing thing and assembling at read time is what makes the fast path fast, and it is the same reasoning behind materialised views and denormalised read models.","uber-geo":"H3: why the world is tiled in hexagons\n\nThe core query is deceptively simple: which drivers are near this rider. Doing it directly means comparing a point against every driver's coordinates and sorting by distance, which is fine at a hundred drivers and hopeless at scale, because the work grows with the size of the fleet rather than with the size of the neighbourhood.\n\nThe standard fix is to index space itself: divide the world into cells, put each driver in a cell, and look only at the rider's cell and its neighbours. Uber built H3 for this and open-sourced it, and the choice of shape is the interesting part.\n\nSquares have a problem that sounds pedantic and is not. A square has eight neighbours at two different distances: four sharing an edge and four sharing only a corner, which are further away. So 'the neighbouring cells' is an ambiguous phrase, and any radial search has to decide what to do about the diagonals. A hexagon has six neighbours, all at the same distance from the centre, so expanding a search outward is unambiguous. Uber's own framing is that hexagons have one distance between a centre and its neighbours, against two for squares and three for triangles.\n\nThe second reason is quantisation error. People move continuously and cells are discrete, so a moving driver keeps crossing boundaries. Hexagons are closer to circles than squares are, which means the error introduced by snapping a moving point to a cell is smaller and more uniform in every direction.\n\nH3 has sixteen resolutions, each cell holding roughly one seventh the area of the level above, so the same system serves a city-wide heat map and a street-level match by changing one number. It projects the sphere onto an icosahedron, twenty flat faces rather than one, which keeps cells much closer to equal area than a single flat projection would. The catch is that you cannot tile an icosahedron with hexagons alone: twelve pentagons are unavoidable, and H3 orients the solid so they fall in the ocean.\n\nThe general move is to convert a geometry problem into a lookup problem. Comparing distances is expensive and scales with the fleet; looking up a cell key is cheap and scales with the neighbourhood. Almost every spatial system does some version of this, and the differences between geohash, S2 and H3 are mostly about which distortions they accept in exchange.","uber-dispatch":"Dispatch: matching is an assignment problem, not a search\n\nThe naive dispatch is to give each request the nearest free driver, first come first served. It is simple, it is obviously fair, and it is measurably worse than the alternative.\n\nConsider two requests arriving seconds apart and two available drivers. Greedy assignment gives the first request its nearest driver, which may be the same driver that was much closer to the second request, leaving the second rider with a driver several minutes away. Considering both requests together and minimising total pickup time can leave both riders better off than serving one of them optimally. That is the difference between a search, which answers one query at a time, and an assignment, which optimises a batch.\n\nSo dispatch batches over a short window, typically a few seconds, and solves a small assignment problem. The window is a real trade: longer windows produce better matches and make everybody wait, and the right length is an empirical question rather than a principled one.\n\nDistance is also the wrong objective, which is easy to miss. What matters is time to pickup, and a driver eight hundred metres away across a river with no bridge is further, in the sense the rider cares about, than one two kilometres away on the same road. Real dispatch uses estimated travel time from a routing engine over the road network, not straight-line distance.\n\nThe system also has to reason about drivers who are not free yet. A driver two minutes from dropping off may be a better match than an idle driver ten minutes away, which means dispatch is matching against predicted future supply, not just present supply.\n\nThe transferable idea is that batching changes what is possible, not just what is efficient. A queue processed one item at a time can only ever make locally optimal choices; a queue processed in small batches can make globally better ones. The cost is latency, and the design question is how much latency the batch is worth.","uber-surge":"Surge: a control loop that people can see\n\nSurge pricing is a control loop. Demand in a cell exceeds supply, price rises, which suppresses some demand and attracts drivers from nearby cells, and the imbalance closes. H3 is what makes it computable: supply and demand are measured per hexagon, and a multiplier is set per hexagon.\n\nThe complications are almost all about the loop's dynamics rather than its economics. Set the multiplier from an instantaneous reading and it oscillates: price spikes, drivers converge, price collapses before most arrive, drivers leave, price spikes again. Any control loop with delayed feedback does this if you do not damp it, and the delay here is however long it takes a driver to physically drive somewhere.\n\nCell size is a genuine tension. Small cells give precise pricing and noisy estimates, because a handful of requests in a small area is not much of a signal. Large cells give stable estimates and price people incorrectly, because the busy corner and the quiet street two hundred metres away get the same number. H3's resolutions let this be tuned rather than argued about, and the answer is usually different for a dense city centre and a suburb.\n\nThere is also a boundary problem that is entirely artificial and very visible: two riders standing next to each other on opposite sides of a cell boundary should not see very different prices. Smoothing across neighbouring cells is what keeps the abstraction from leaking into someone's fare, and hexagons help precisely because 'neighbouring' is unambiguous.\n\nThe lesson is that any feedback loop acting on a system with delay needs damping, and that the delay is usually physical and irreducible. The instinct to react quickly to the newest reading produces oscillation, and oscillation in a pricing system is far more damaging than being slightly slow, because customers experience it as arbitrary.","uber-trip-state":"The trip as a state machine, and why that matters\n\nA trip is a long-lived object that moves through defined states: requested, matched, driver en route, arrived, in progress, completed, paid. Or cancelled, from most of them. Modelling it explicitly as a state machine rather than as a row with a handful of booleans is one of those decisions that looks like bureaucracy until the first time it saves you.\n\nThe reason is that the transitions are where all the rules live, and the rules are unintuitive. A rider can cancel before pickup, sometimes with a fee. A driver can cancel, which returns the request to dispatch rather than ending the trip. A trip can complete without payment succeeding, and that is a normal case rather than an error. With booleans, each of these becomes a condition scattered across the codebase, and the set of reachable combinations quietly becomes unknowable.\n\nEvery state change is also a message to several other systems: the rider app, the driver app, pricing, the receipt, driver earnings, fraud checks. Doing that synchronously would make the trip's write path depend on all of them being healthy, so the durable transition is recorded first and the notifications follow from it. Consumers are idempotent because at-least-once delivery means duplicates are certain rather than possible.\n\nThe genuinely hard part is that the phone is unreliable, and the trip is happening in the physical world regardless. A driver goes through a tunnel mid-trip and comes out with events queued up, possibly arriving out of order. The state machine has to reject impossible transitions rather than trust the order things arrive in: a 'trip started' message for a trip already completed is not a state change, it is a late duplicate, and the only safe response is to ignore it.\n\nThe value of an explicit state machine is that illegal states become unrepresentable rather than merely unlikely. With booleans, cancelled and completed can both be true and nothing stops it; with states, the transition simply is not defined, and a late or duplicated event is rejected by the model rather than by a condition somebody remembered to write.","uber-sharding":"Sharding by city, and what breaks at the edges\n\nTrips are overwhelmingly local. A rider in Manchester is matched with a driver in Manchester, priced by Manchester's supply and demand, and paid in pounds. That locality is a gift, because it means the data can be partitioned geographically and almost every query stays inside one partition.\n\nSharding by city therefore gives near-linear scaling for the common case. Each shard holds its own trips, its own driver index, its own surge state, and a busy Friday in one city does not consume capacity in another. It also gives a natural failure boundary: a shard problem is a city problem, not a company problem.\n\nThe edges are where it gets interesting, and they are real rather than theoretical. A trip from one city to a neighbouring one crosses a boundary. Airports sit outside city limits and serve several. Cities grow, and a shard that was comfortable becomes hot. Some things are genuinely global and cannot be sharded at all: a rider's account, their payment methods, their lifetime history, and any fraud signal worth having, since fraud that is invisible within one city is often obvious across several.\n\nThe practical answer is not one scheme but two. Geographic sharding for the operational data, which is local and high volume, and a separate globally-scoped store for identity and money, which is low volume and must be correct everywhere. Trying to force both into one partitioning scheme is how you end up with cross-shard transactions in the hot path.\n\nThe lesson is to shard by the boundary that the workload already has, and to accept that some data will not fit it. The mistake is not having two schemes; it is pretending one scheme covers everything and discovering the exceptions in production, when a query that was supposed to be local turns out to fan out across every shard.","uber-payments":"Payments: money is the part you cannot retry casually\n\nEverything up to this point tolerates approximation. A slightly worse match, a surge multiplier that is a little stale, a location a second out of date: all survivable. Payments do not work that way, because charging a rider twice is not a degraded experience, it is a defect they will remember and tell people about.\n\nThe mechanism that makes this safe is the idempotency key. The client generates a key for the payment attempt and sends it with the request. If the response is lost to a timeout and the client retries with the same key, the payment service recognises it and returns the original outcome rather than charging again. This matters because a timeout is genuinely ambiguous: the caller cannot distinguish a request that never arrived from one that succeeded and whose response was lost, and without a key the only options are to risk a double charge or to risk not charging at all.\n\nPayment is also not one operation. A card is authorised at the start of a trip, the final amount is not known until it ends, and capture happens afterwards for an amount that may differ. Any of those steps can fail independently. The driver's earnings are a separate flow again, on a different schedule, through a different rail, and a trip can legitimately be complete, paid by the rider, and not yet paid out to the driver.\n\nSo the money is modelled as a ledger rather than as a balance field. Every movement is an immutable entry, and the balance is derived by summing them. This is slower to read and it is what makes the system auditable: you can answer why a number is what it is, and a mistake is corrected by writing a compensating entry rather than by overwriting history and destroying the evidence.\n\nThe principle is that any operation with an external side effect needs a key that makes retries safe, because the network guarantees you will retry in an ambiguous state eventually. The second principle is that append-only beats mutable for anything you may have to explain later, and money is the canonical example of something you will have to explain.","uber-realtime":"Keeping millions of connections open\n\nBoth apps need a live channel. The driver app reports location continuously; the rider app wants the car moving on the map without polling for it. That means long-lived connections at very large numbers, which is a different engineering problem from serving requests.\n\nThe first consequence is that connections cost memory whether or not they are doing anything. A thread-per-connection server runs out of memory long before it runs out of processor, which is why gateways in this shape are built on asynchronous non-blocking I/O, where an idle connection is a small object rather than a stack.\n\nThe second is that connection state has to live somewhere, because a driver's connection lands on one gateway instance and a message for that driver may arrive at another. Something has to know where each connection is, which is a registry with all the usual problems: it must be fast to read, it changes constantly as mobile clients reconnect, and it is wrong the moment a client silently disappears.\n\nThe third is that mobile networks drop connections constantly, so reconnection is the normal case rather than the exception. Clients need backoff so a network blip does not become a reconnection storm, and jitter so every client that dropped together does not come back in lockstep and do it again.\n\nIt is also worth being honest that not everything needs this. Location updates do, and a receipt does not. Sending everything down a live channel because you built one is a common and expensive mistake, since it makes the connection layer part of the critical path for things that were happily asynchronous.\n\nThe core insight is that connections are state, and state at that scale is the constraint. Every design decision follows from it: asynchronous I/O because idle connections must be cheap, a registry because the state is distributed, backoff and jitter because reconnection is constant and correlated. Treating a live channel as free is how a system that works at ten thousand connections fails at a million.","uber-data":"The data platform: the same events, twice\n\nEvery location ping, trip transition, price calculation and payment produces events, and there are two very different consumers of them. The operational side needs them in seconds: dispatch needs current supply, surge needs current demand, the rider needs the car moving on the map. The analytical side needs them completely and correctly, but can wait: finance, forecasting, model training, regulatory reporting.\n\nThese requirements conflict enough that trying to serve both from one system serves neither well. The fast path optimises for latency and accepts approximation, because a supply count that is a second stale is fine. The slow path optimises for completeness and accepts latency, because a revenue figure that is missing a percent is not fine at all.\n\nSo the same event stream feeds both. A streaming layer maintains the aggregates dispatch and pricing read, in memory, updated continuously, deliberately approximate. A batch layer writes everything to durable storage and recomputes properly on a schedule, with late-arriving events folded in.\n\nThat last detail is the one worth remembering. Events arrive late, sometimes hours late, from a phone that was in a tunnel or a basement. The fast path has already moved on and cannot incorporate them; the batch path can, because it recomputes over a window rather than incrementing a counter. This is exactly why the batch numbers and the real-time numbers disagree, and why the batch ones are the ones finance uses.\n\nThe general rule is that latency and completeness are different requirements, and a single pipeline forces one to be sacrificed. Serving both from the same stream, with different guarantees and an explicit understanding of which is authoritative, is cheaper and more honest than making the real-time system pretend to be exact.","fanout":"Fan-out on write against fan-out on read\n\nA home timeline is the union of everything the accounts you follow have posted, most recent first. There are exactly two places that union can be computed, and the entire design follows from which one you pick.\n\nFan-out on read computes it when the timeline is requested: look up who this person follows, fetch recent posts from each, merge, sort. Writing is trivial, a single row. Reading is expensive and gets worse the more accounts someone follows, and it happens far more often than writing.\n\nFan-out on write does the work at post time: when someone posts, push the post's identifier into a precomputed list for every follower. Reading becomes a single lookup of an already-assembled list, which is exactly what you want for the operation that dominates. The cost is that one post becomes as many writes as the author has followers.\n\nThe ratio decides it. Timelines are read vastly more often than posts are written, so paying at write time to make reads cheap is the right trade for almost everyone. Twitter maintains materialised timelines of roughly the most recent 800 post identifiers per user, held in a Redis-derived store where pushing to the head and trimming the tail are both cheap operations, which is precisely the access pattern fan-out needs.\n\nNote what is stored: identifiers, not posts. Eight hundred identifiers per user is a manageable amount of memory; eight hundred full posts per user, duplicated across every follower, would not be. The timeline is an index into the posts, and the posts themselves are fetched and cached separately.\n\nThe choice is not about which is faster in the abstract, it is about which operation is more frequent. Precomputing at write time is right when reads dominate, and it inverts the moment writes dominate. The question to ask of any denormalisation is the read-to-write ratio, and if you do not know it, you are not yet in a position to choose.","celebrity-problem":"The celebrity problem, and the hybrid that solves it\n\nFan-out on write has one failure mode and it is severe. An account with fifty million followers posts once, and that single action becomes fifty million writes. Post ten times in a day and it is five hundred million. The work is unbounded in the follower count, and a handful of accounts can saturate the infrastructure that everyone else depends on.\n\nWorse, it is bursty in exactly the wrong way. Popular accounts post at moments of high general activity, so the fan-out spike lands while read traffic is also peaking. A queue absorbs some of it, but a large enough fan-out delays every other user's timeline behind it.\n\nThe answer is a hybrid, and it is what Twitter, Instagram and Facebook all independently arrived at. Ordinary accounts fan out on write, because their follower counts are small and the cost is trivial. Accounts above a threshold, commonly cited around ten thousand followers, do not fan out at all. Their posts are fetched at read time instead.\n\nSo a timeline read does two things and merges them: fetch the precomputed list, and separately fetch recent posts from the small number of large accounts this person follows. Merge by time, return the top slice. The pull side is bounded because nobody follows very many celebrities, and the push side is bounded because ordinary accounts have few followers. Each strategy is applied exactly where its cost is small.\n\nThe threshold itself is an operational dial rather than a principle. Too low and too many accounts are pulled, making every read do more work. Too high and the write spikes return. It also creates a boundary that has to be handled: an account crossing the threshold needs its existing followers' timelines to stay coherent while the strategy changes underneath them.\n\nThe generalisable idea is that a strategy which is optimal on average can be catastrophic in the tail, and the fix is usually not a better single strategy but a different one for the tail. Identifying that your distribution has a heavy tail, and treating it separately, is often the entire architectural insight.","timeline-ranking":"Ranking: when the timeline stops being chronological\n\nA reverse-chronological timeline is simple, predictable, and increasingly useless as the number of accounts someone follows grows. Follow two thousand accounts and the last hour contains more posts than anyone will read, so the question shifts from what is newest to what is worth showing.\n\nRanking changes the shape of the system in a way that is easy to underestimate. Chronological order can be maintained incrementally: a new post goes at the head, and nothing else moves. A ranked order can change for a post that already exists, because it gained engagement, or because the reader's interests shifted, or because the model was retrained. The timeline is no longer an append-only list.\n\nThe usual resolution is to keep fan-out producing a candidate set in time order, then rank that candidate set at read time. Retrieval stays cheap and incremental; scoring happens on a few hundred candidates rather than everything ever posted. This is the same retrieve-then-rank shape that search engines use, and for the same reason: scoring is too expensive to apply to the full corpus.\n\nThe costs are worth naming honestly. A ranked timeline is not reproducible, so two people cannot compare what they saw. Debugging becomes statistical rather than deterministic. And engagement-based ranking has a well-documented pull towards content that provokes, because provocation generates engagement, which means the objective function is a product decision with consequences rather than a technical parameter.\n\nThe structural insight is that ranking converts an append-only problem into a scoring problem, and the way to keep it affordable is to separate retrieval from ranking. Retrieve cheaply and broadly, score expensively and narrowly. Trying to rank at retrieval time is what makes these systems impossible to scale.","social-graph":"The follower graph, and why it is not just a table\n\nUnderneath everything is a graph: who follows whom. It looks like a two-column table and behaves like nothing of the sort, because of how it is queried and how skewed it is.\n\nBoth directions are needed and they are wildly asymmetric. Fan-out asks for an account's followers, which for a large account is tens of millions of rows and is read on every post. A timeline asks who someone follows, which is usually hundreds. Storing one relationship and deriving the other means one of these queries is always expensive, so both directions are typically materialised and kept consistent.\n\nThe distribution is the real problem. Most accounts have a handful of followers, and a few have tens of millions. A single row's worth of relationship for one account and a hundred million for another cannot sit comfortably in the same partitioning scheme: shard by account and the shard holding a very large account is enormously hotter than its neighbours.\n\nThe graph also mutates constantly, and each mutation has consequences. A follow means the new follower's timeline is missing that account's history, so either it is backfilled or their timeline is simply thin until new posts arrive. An unfollow means existing entries are now wrong. Blocks and mutes add filtering that must be applied at read time, because applying them at write time would mean rewriting timelines whenever a block changes.\n\nThat last point is a useful general lesson. Some things must be evaluated at read time, not because it is cheaper but because the alternative is a rewrite of everything already written.\n\nThe recurring theme is that a heavily skewed distribution defeats a uniform strategy. Uniform sharding assumes roughly equal partitions; a follower graph provides nothing of the sort. Recognising skew early and planning for the tail is what separates a design that scales from one that works in testing and falls over on the account that matters.","twitter-search":"Search and trends: a different index over the same firehose\n\nTimelines answer what the people I follow said. Search answers who said anything about this, which is a completely different access pattern over the same data, and it needs its own index rather than a query against the post store.\n\nReal-time search has an awkward requirement: something posted seconds ago should be findable. Traditional inverted indexes are built in batches, because merging into a large index is expensive. The usual resolution is tiered: a small in-memory index absorbs the newest posts and is searched alongside larger, older segments that are rebuilt less often. Queries hit both and merge.\n\nTrends are a different problem again, and the naive version is wrong in an obvious way once stated. The most frequent terms are not trending, they are common: the same ordinary words appear constantly. What is interesting is the deviation from the expected rate for that term, at that time, in that place. A term appearing a thousand times an hour is unremarkable if it usually appears nine hundred times, and extraordinary if it usually appears twice.\n\nThat framing makes trends a statistics problem rather than a counting one, and it brings the problems that come with statistics. Baselines have to be maintained per term and per region. Manipulation is a constant, since a group can coordinate to lift a term artificially. Time windows matter enormously: too short and noise dominates, too long and nothing ever looks sudden.\n\nThe lesson is that the same underlying data usually needs several purpose-built indexes rather than one general store queried cleverly. A timeline index, a search index and a trend baseline are three different shapes over identical events, and trying to serve all three from one structure produces something that serves none of them well.","whatsapp-connections":"A process per connection, and why Erlang\n\nMessaging is not a request-response workload. Every user holds a connection open for as long as the app is running, because a message can arrive at any moment and polling for it would be both slow and expensive. So the primary resource is not processor time, it is connections, and the engineering problem is how cheap an idle connection can be made.\n\nWhatsApp is well known for handling extraordinary numbers of connections per server, with published accounts describing a million or more on a single machine, and for running an unusually small engineering team while doing it. The choice that made both possible was Erlang, which is not a fashionable answer but is very nearly the correct one for this specific shape of problem.\n\nErlang's processes are not operating system threads. They are extremely lightweight, scheduled by the runtime, with tiny initial stacks that grow only if needed. That makes one process per connection viable, where one thread per connection would exhaust memory at a fraction of the count. The model is direct: a connection arrives, a process owns it, the process holds the session state, and when the user disconnects the process exits and everything it held is gone. No pooling, no multiplexing, no shared mutable state to reason about.\n\nThe second property matters as much and is less often mentioned. Erlang processes are isolated and fail independently: a crash takes down one connection's process and a supervisor restarts it, without touching the other million. In a language where connections share a heap, a bug triggered by one user's malformed input can take down everything on that machine.\n\nThere is also a deployment property that mattered for a small team. Erlang supports upgrading code in a running system, which for a service holding millions of long-lived connections is the difference between a routine deploy and disconnecting everybody.\n\nThe general lesson is to choose the runtime that matches the resource you are actually constrained by. Almost every mainstream stack optimises for throughput of short requests, and a messaging server is not that: it is millions of mostly-idle connections where the constraint is memory per connection and the failure mode is one bad connection taking others with it. Erlang was designed for telephone switches, which is the same shape of problem.","chat-delivery":"Delivery: the ticks, and what each one actually promises\n\nThe tick marks are a user interface for a distributed systems problem, and they are unusually honest about it. One tick means the server has the message. Two ticks mean the recipient's device has it. Two blue ticks mean the recipient's app has displayed it. Three separate facts, because in a distributed system they are genuinely three separate facts and collapsing them would be a lie.\n\nThe mechanics behind the first tick are what make the rest possible. The sender transmits, the server persists the message, and only then acknowledges. That ordering matters: acknowledging before persisting would mean a server crash silently loses a message the sender believes was sent. Persist, then acknowledge, and a crash before the acknowledgement causes a retry rather than a loss.\n\nIf the recipient is connected, the server pushes immediately. If not, the message waits in a per-recipient queue until they reconnect, which is the ordinary case rather than an exception: phones lose signal, go into tunnels, and run out of battery constantly. Once delivered and acknowledged, the server has no further need for it, and WhatsApp's design deliberately discards it at that point rather than retaining a copy.\n\nOrdering is subtler than it appears. Messages from one sender to one recipient must arrive in the order they were sent, which a sequence number per conversation gives you. Ordering across different senders in a group is not achievable in any meaningful sense, because there is no global clock and two people genuinely can send at the same instant. What actually gets shipped is a consistent order rather than a true one, which is enough because nobody can tell the difference.\n\nEvery step needs idempotency. Retries are certain on mobile networks, so each message carries an identifier and a redelivery of one already seen is dropped rather than shown twice.\n\nThe design principle is to persist before acknowledging, always, for anything you promise to deliver. The window between accepting a message and durably storing it is a window in which a crash loses data the sender believes is safe, and that is the one failure people never forgive. Every reliable messaging system pays this latency cost deliberately.","e2e":"End-to-end encryption, and designing so the server cannot help\n\nWhatsApp uses the Signal Protocol, which combines an initial key agreement with a ratcheting scheme that derives a fresh key for every message. The consequence that matters architecturally is that the server routes ciphertext it cannot read, and that constraint shapes everything else in the system.\n\nThe two properties worth naming are forward secrecy and post-compromise security. Forward secrecy means compromising a key today does not decrypt yesterday's messages, because those keys were derived and discarded. Post-compromise security means the ratchet heals: after an attacker obtains a key, continued normal messaging derives new keys they cannot follow. Together they bound the damage of a compromise in both directions in time.\n\nSending to a device you have never messaged requires a key exchange, and both parties being online simultaneously is not a realistic assumption. So each client uploads a batch of pre-keys to the server in advance. A sender takes one, performs the agreement against it, and sends. The server distributes public key material without ever holding the private keys, which is the property that keeps it honest.\n\nGroups do not encrypt once per group, because there is no shared secret the server could hold without being able to read everything. Each sender establishes a sender key distributed to each member over their existing pairwise encrypted sessions, so the message body is encrypted once and the key is delivered individually. Cost grows with membership, which is a real constraint on group size.\n\nMulti-device is where this gets genuinely hard, and WhatsApp's answer since 2021 is that each device has its own identity and its own session with every contact. A message to a contact with four devices is four encryptions. The alternative, sharing one key between a person's devices, would be simpler and would mean compromising any one device compromises all of them.\n\nThe honest limitation is that encryption protects content, not metadata. The server necessarily knows who talked to whom and when, because it has to route the message, and that metadata is often as revealing as the text would be.\n\nThe design lesson is that the strongest guarantee comes from making it impossible for the server to comply rather than promising it will not. A server that could read messages and undertakes not to is one subpoena, one breach or one policy change away from doing so. A server holding only ciphertext has nothing useful to hand over, and that property survives changes of ownership and jurisdiction.","whatsapp-media":"Media: the message is not the file\n\nSending a photo through the message path would be a mistake. Messages are small, ordered and latency-sensitive; media is large, unordered and throughput-sensitive. Putting a fifty megabyte video through the same channel as a text message means one upload blocks a conversation.\n\nSo media goes out of band. The sender encrypts the file locally, uploads the ciphertext to a blob store, and sends a message containing a reference and the key needed to decrypt it. The message stays small and the transfers are independent, which also means an upload can resume after a network drop without affecting anything else.\n\nThe encryption ordering is the important detail. The file is encrypted on the device before upload, so the blob store holds bytes it cannot interpret, exactly like the message service. If it were uploaded in the clear and encrypted server-side, the end-to-end property would be broken by the largest and most sensitive payloads in the system.\n\nForwarding is where the design pays off. The same encrypted blob can be referenced by many messages without re-uploading, which is why forwarding something to twenty people is instant while the original send was not. The key travels with each message; the bytes are stored once.\n\nThumbnails are the small detail that makes it feel fast. A low-resolution preview is small enough to embed directly in the message, so a recipient sees something immediately while the full file downloads. It is the same instinct as a progressive image: show something true as early as possible rather than nothing until everything is ready.\n\nThe pattern is to separate the reference from the payload whenever their characteristics differ. Small, ordered, latency-sensitive control messages belong on one path; large, unordered, throughput-sensitive data belongs on another. Mixing them means the constraints of the larger one govern the smaller.","whatsapp-groups":"Groups: where the costs stop being linear\n\nA one-to-one message is one encryption and one delivery. A group message to two hundred people, each with several devices, is hundreds of deliveries and a key distributed to every one of them. The cost per message grows with membership, and because the server cannot read the content it cannot do anything clever to reduce it.\n\nThe sender key scheme keeps the encryption cost sane. Rather than encrypting the message body separately for every member, the sender generates a sender key, distributes it once to each member over the existing pairwise sessions, and then encrypts each message once with it. Distribution is linear in membership; ongoing messaging is not.\n\nMembership changes force key rotation, and the reason is a genuine security requirement rather than tidiness. When someone leaves, the remaining members must rotate, otherwise the departed member can still decrypt everything sent afterwards, having kept the old sender key. In an active group this means rotations are frequent, and each one is another round of distribution.\n\nDelivery fan-out is the other half. Every message multiplies by members and again by devices per member, and the offline case makes it worse: a member offline for a week has a queue that must be retained and then delivered in a burst when they return. This is why group size limits exist. They are not arbitrary product decisions, they are where the fan-out arithmetic stops being affordable.\n\nIt is worth contrasting this with a broadcast channel, which looks similar and is not. A channel has one sender and many passive readers, so it can be built as fan-out on read with no per-recipient encryption at all. The moment every participant can also send, the costs change entirely.\n\nThe lesson is that end-to-end encryption removes the server's ability to optimise. A server that can read content can deduplicate, compress, and fan out at the edge; a server holding ciphertext keyed per recipient cannot do any of it. That is a real cost paid for a real property, and pretending otherwise leads to designs that assume server-side optimisations which cannot exist.","url-shortener":"URL shortener: the interview question that is actually about ids\n\nThe apparent problem is mapping a short code to a long URL, which is a hash table and would be a dull question. The real problem is generating the codes, and that is where every interesting decision lives.\n\nHashing the URL is the first instinct and it is wrong in a specific way. A hash truncated short enough to be a usable code will collide, so you need collision handling, and the same URL submitted twice produces the same code, which sounds like a feature until someone wants two codes with different analytics or different expiry.\n\nA counter is better than it sounds. Take an incrementing number, encode it in base 62 using digits and both cases of the alphabet, and you have short codes with no collisions by construction. Seven characters of base 62 is about 3.5 trillion combinations, which is more than enough. The objection is that codes are sequential and therefore guessable, and the objection is correct: anyone can enumerate every link ever created. If links are ever private, this alone disqualifies it.\n\nThe usual answer is to keep the counter and break the sequence. Give each server a block of the range to hand out, so they do not coordinate on every request, and either encode the number through a reversible permutation or mix in random characters so consecutive ids do not produce consecutive codes.\n\nThen the read path, which is where the traffic actually is. Reads outnumber writes by orders of magnitude and the mapping never changes once created, which is the ideal shape for caching. A cache in front of the store serves almost everything, and the redirect itself should be an HTTP 301 or 302 chosen deliberately: 301 is permanent and browsers cache it aggressively, which is fast and means you stop seeing the traffic, so anyone who wants click analytics must use 302 and accept the load.\n\nThe lesson is that the interesting part of a system is rarely the part the question names. Storage here is trivial; identifier generation carries the trade-offs between coordination, guessability and length, and the redirect status code silently decides whether you have analytics at all.","rate-limiter":"Rate limiter: four algorithms and the one you should reach for\n\nA rate limiter answers whether this caller may make this request now, and the algorithm chosen determines exactly how it behaves at the edges, which is where all the complaints come from.\n\nA fixed window counts requests per calendar window: a hundred per minute, reset on the minute. It is trivial and it has an obvious flaw. A caller making a hundred requests at 10:00:59 and a hundred more at 10:01:00 has made two hundred in one second while never exceeding the stated limit. The boundary is a hole.\n\nA sliding window log fixes it exactly by storing a timestamp per request and counting those within the last minute. It is precise, and it costs memory proportional to the request rate per caller, which for a busy API is a great deal of memory to spend on bookkeeping.\n\nA sliding window counter is the usual compromise: keep the current and previous window counts and weight the previous one by how far into the current window you are. It approximates the sliding log closely, with two integers instead of a list.\n\nA token bucket is different in kind and is usually the right answer. Tokens are added at a fixed rate up to a maximum, and each request consumes one. The rate sets the sustained throughput and the bucket size sets how large a burst is tolerated, which means bursts are permitted deliberately rather than as an artefact. Real traffic is bursty, and a limiter that forbids bursts entirely rejects requests that the system could comfortably have served.\n\nTwo implementation details matter more than the algorithm. Distributed limiters need shared state, and a strictly consistent counter puts a coordination round trip on every request; most systems accept approximate limits with eventually consistent state, because the goal is protecting the service rather than perfect accounting. And the limiter should tell the caller what happened: a 429 with RateLimit headers and Retry-After lets a well-behaved client back off correctly instead of guessing.\n\nThe choice is really about what you want to happen at the boundary and during a burst. Fixed windows are cheap and wrong at the edges; logs are exact and expensive; token buckets encode the two things you actually care about, sustained rate and burst tolerance, as two independent numbers you can reason about.","ticket-booking":"Ticket booking: the seat that two people want\n\nSelling a finite inventory of unique items to concurrent buyers is one of the few genuinely hard consistency problems that appears in ordinary products. Two people click the same seat within milliseconds and exactly one must get it. There is no eventual consistency answer that is acceptable, because the failure is selling one seat twice.\n\nOptimistic concurrency is usually the right mechanism. Each seat carries a version, and the update says set this seat to sold where the version is still what I read. One update matches and succeeds; the other matches nothing and fails, and its buyer is told the seat has gone. No locks are held while a human decides, which matters because humans are slow and a lock held across a checkout flow is a lock held for minutes.\n\nThe reservation window is where the design gets interesting, because a purchase is not instant. A buyer needs time to enter payment details, so the seat is held. Holding it forever means abandoned baskets consume the inventory; not holding it means someone completes payment for a seat that has just gone, which is far worse. So holds carry an expiry, typically a few minutes, and expired holds are released. The expiry must be enforced by the system rather than by the client, because a browser that closed will never tell you anything.\n\nThe queue is what makes a high-demand sale survivable. Ten thousand people arriving for a hundred seats will collapse a system that lets all of them into the checkout, and the answer is a virtual waiting room that admits people at the rate the inventory system can actually serve. It converts an unbounded spike into a controlled flow, and it is more honest with the customer than a page that fails.\n\nPayment failure is the last piece and needs care. A hold succeeds, payment fails, the seat must return to inventory. But payment failure is sometimes ambiguous, as a timeout tells you nothing about whether the charge succeeded, so the resolution needs an idempotency key and a reconciliation step rather than an immediate guess.\n\nThe principle is to hold locks for as short a time as possible and never across a human. Optimistic concurrency lets you detect the conflict at the moment of commit instead of preventing it for minutes beforehand, and a time-bounded reservation gives the human their thinking time without giving them a lock.","file-sync":"File sync: conflicts are not an edge case\n\nSyncing files across devices sounds like copying and is not. The moment two devices can edit while offline, you have concurrent writes without coordination, and the interesting question is not how to move bytes but what to do when both changed.\n\nThe transfer itself has a well-known optimisation: split files into content-addressed chunks. A file becomes a list of chunk hashes, and syncing means transferring only the chunks the other side lacks. Change one paragraph in a large document and one chunk moves. Chunking also gives deduplication for free, since identical chunks anywhere are stored once, which is why uploading a file someone else already has can be nearly instant.\n\nDetecting conflict needs causality rather than timestamps. Wall clocks on two devices disagree, and last-write-wins by timestamp silently discards edits when a clock is wrong. Version vectors record what each device had seen when it wrote, which makes it possible to distinguish a genuine concurrent edit from one device simply being behind.\n\nOnce detected, something must resolve it, and the honest options are limited. Automatic merge works for structured data with defined semantics and not for arbitrary binary files. Last-write-wins is simple and loses work. Keeping both versions, which is what most sync products do, is the only option that never destroys data, and it hands the problem to the person, which is where it belongs when the system genuinely cannot know.\n\nDeletion is the subtle one. A file deleted on one device and edited on another is ambiguous, and worse, a deletion cannot simply be the absence of a file, because absence is indistinguishable from never having received it. Deletions must be recorded as explicit tombstones that propagate, and tombstones have to be retained long enough for every device to see them, including one that has been offline for months.\n\nThe lesson is that offline editing makes conflict a normal operating condition rather than an exception, so the design must have an answer before it ships. The systems that go wrong are the ones that treat conflict as rare, because the resolution path is then written late, under pressure, by someone guessing.","distributed-cache":"Distributed cache: hashing, stampedes and the hot key\n\nA cache spread across many nodes needs an answer to which node holds a given key, and the naive answer breaks badly. Hashing the key modulo the node count means adding or removing one node changes the destination of almost every key at once, so a single node joining invalidates the entire cache and sends the full load to the origin.\n\nConsistent hashing fixes this. Nodes and keys are placed on a ring, and a key belongs to the next node clockwise, so adding a node reassigns only the keys between it and its predecessor. Virtual nodes, several ring positions per physical node, are what make the distribution even, because a handful of random positions produces very uneven arcs.\n\nThe cache stampede is the failure that catches people. A popular key expires, and every request that wanted it simultaneously misses and goes to the origin, which now receives the full unmitigated load for that key at once. The fixes are to have one request recompute while others wait or serve stale, and to jitter expiry times so a batch of keys written together does not expire together.\n\nA hot key is a different problem with a similar smell. Consistent hashing distributes keys evenly and says nothing about traffic, so one extremely popular key sends all of its traffic to one node however many nodes you have. The answers are local caching in front of the distributed one, or replicating that key deliberately across several nodes.\n\nEviction policy is the last decision and it is usually less important than people think, with one exception. Least-recently-used is the sensible default. The exception is a scan, where something walks a large number of keys once, evicting everything useful in favour of things that will never be read again. This is why some systems segment the cache so a scan cannot evict the working set.\n\nThe pattern across all three failures is that uniformity in one dimension does not give uniformity in another. Consistent hashing distributes keys evenly while traffic remains skewed; simultaneous expiry distributes storage evenly while creating a synchronised event. Ask what is uniform and what is not, because the failure is always in the dimension you did not consider.","notifications":"Notifications: fan-out with a person on the other end\n\nA notification system takes an event and delivers it to people across channels: push, email, in-app, sometimes SMS. The infrastructure is a fan-out problem and the hard part is not the fan-out, it is deciding what deserves to be sent at all.\n\nStructurally it is a pipeline. An event arrives, preferences are resolved to decide who wants what through which channel, messages are rendered, and each channel is dispatched through its own provider. Every stage can fail independently, and channels have wildly different characteristics: push is fast and unreliable, email is slow and durable, SMS costs real money per message.\n\nSo delivery has to be per-channel rather than per-notification. Retrying a push aggressively is fine; retrying an SMS aggressively costs money and irritates someone. Each channel needs its own retry policy, its own rate limits and its own failure handling, which is why treating them uniformly behind one abstraction usually goes wrong.\n\nDeduplication and batching are what make the system tolerable to receive. Ten people commenting produces ten events and should produce one notification saying ten people commented. This is a windowing problem, and the window is a product decision rather than a technical one: too short and it fragments, too long and it arrives after it mattered.\n\nPreferences must be enforced at the point of sending, not at the point of subscribing. People change their minds, unsubscribe, mute a thread, or turn off a category, and any of those can happen between an event being queued and it being delivered. Checking at queue time means a message sent minutes after someone unsubscribed, which is at best rude and, for marketing categories, potentially unlawful.\n\nFinally, quiet hours and priority. A security alert should wake someone; a weekly digest should not. Without a priority model everything becomes urgent, and once everything is urgent people turn all of it off, which is the real failure mode of notification systems.\n\nThe design lesson is that the constraint is human attention rather than throughput. Any notification system can deliver more than people will read, so the valuable engineering is in suppression: deduplicating, batching, respecting preferences at send time and distinguishing what is worth interrupting someone for.","payments-flow":"Authorisation, capture and settlement\n\nA card payment is not one event. Authorisation asks the issuing bank to check the card and hold the amount, and it succeeds or declines within a second or two. Capture tells the network to actually take the held money, and it can happen immediately or days later. Settlement is the movement of funds between banks, which happens in batches and takes days. Three steps, three different latencies, and three different things that can fail.\n\nSplitting authorisation from capture is what makes normal commerce work. A shop authorises when you order and captures when the item ships, because charging for something not yet sent is both a refund waiting to happen and, in many jurisdictions, not allowed. An authorisation expires if it is not captured, typically within about a week depending on the card scheme and the merchant category, and an expired authorisation means the money was never taken and the customer saw a pending charge that vanished.\n\nThe failure that defines the domain is that the network can time out at any point and the timeout tells you nothing. A request that times out during authorisation may have been authorised, or not. There is no way to tell from the timeout itself, so the system must be able to ask afterwards, which is why every payment has an identifier the merchant chose before sending it and why looking up by that identifier is a first-class operation rather than an afterthought.\n\nRegional rules add a second asynchronous step. In Europe, strong customer authentication under PSD2 means many payments require the cardholder to confirm through their bank, so the flow pauses, hands the customer to the issuer, and resumes on the way back. Any design that treats authorisation as a single synchronous call will meet this and have nowhere to put it.\n\nThe shape that survives all of this is a state machine with an identifier: created, requires action, authorised, captured, failed, refunded. Each transition is recorded, each is idempotent, and the current state is the answer to the question a support ticket is really asking, which is what happened to this payment and when.\n\nThe three steps have different latencies and different failure modes, so a design that models a payment as one call has already lost. The identifier chosen before the request leaves is what makes a timeout recoverable, because it is the only way to ask afterwards what happened.","payments-idempotency":"Idempotency when the retry is inevitable\n\nEverything in the previous topic makes retries certain. The network times out, the client gives up, the user presses the button again, the queue redelivers, the deploy restarts a worker mid-flight. A payments API that is not idempotent will double-charge, and the only question is how often.\n\nThe mechanism is a key chosen by the client, unique per logical operation rather than per attempt, sent with the request and stored server-side alongside the outcome. A repeat of the same key returns the stored result rather than doing the work again. That distinction, per operation and not per attempt, is where implementations go wrong: a key regenerated on retry makes every attempt a new payment, which is precisely the situation it was meant to prevent.\n\nThe concurrency is the hard half, because two retries can arrive at the same instant. Recording the key after the work completes lets both pass the check and both charge. Recording it before, with a unique constraint, means the second insert collides and can be refused, which is why the standard shape is to insert the key immediately in a pending state, do the work, then update it with the result. A concurrent duplicate gets a conflict, and a caller receiving that knows to retry rather than to assume failure.\n\nThen there is the crash between charging and recording, which no single database can prevent because the charge happened somewhere else. This is why the stored record must be written in the same transaction as the local state change, and why the result of the remote call is recovered by asking the provider about the merchant's identifier rather than by guessing. A pending key older than a few minutes is stale and must be resolved by looking it up, not by retrying blindly.\n\nStripe stores idempotency keys for 24 hours, which is a deliberate boundary rather than an implementation limit: retries beyond that horizon are no longer the same operation in any meaningful sense, and a key reused a week later is treated as a new request. Choosing that window is a real design decision, and choosing to keep keys forever is a slow leak with a large table at the end of it.\n\nA timeout is ambiguous, so the system must be able to tell a retry from a new request, and no amount of care at the call site can substitute for that. The interesting part is not storing a key, it is what happens when two retries arrive at once and what happens when the process died between charging and recording.","payments-ledger":"The ledger, and why it is double entry\n\nA balance stored as a number on a row is a design that cannot answer the questions a payments system is asked. It says what the balance is and not how it got there, so a discrepancy has no explanation, a correction destroys the evidence, and two processes updating it race. Every serious money system stores movements rather than balances, and derives the balance by summing them.\n\nDouble entry is the discipline that makes those movements checkable. Every transaction writes at least two entries that sum to zero: money leaving one account and arriving in another, with fees and taxes as their own accounts rather than as adjustments. If the sum of all entries is not zero, something is wrong, and that check can run continuously rather than being noticed by a customer.\n\nEntries are append only. A mistake is corrected by writing a compensating entry, not by editing history, which means the record of what happened and the record of what should have happened both survive. That is a requirement in regulated environments and it is also simply how you keep the ability to explain a balance to somebody who disagrees with it.\n\nThe performance question that always follows is whether summing entries is too slow, and the answer is that you keep a materialised balance and treat it as a cache of the truth rather than as the truth. It is recomputed and compared, and a mismatch is an alert rather than a silent correction, which is exactly the property a stored-balance design cannot offer.\n\nThe last piece is that a ledger entry is not a payment. The payment is a state machine with an external dependency; the ledger is an internal record of money that has moved. Keeping them separate means an authorisation that never captures leaves no ledger entry, a refund is two entries rather than a subtraction, and a dispute writes entries of its own. Collapsing the two produces a ledger that has to model card network semantics, which is where the design stops being explainable.\n\nStoring movements rather than balances is what makes a discrepancy explainable, and double entry is what makes it detectable without a customer reporting it. The balance is still there as a materialised value, but it is a cache of a derivation rather than the source of truth.","payments-async":"Outcomes that arrive later\n\nPlenty of what happens to a payment happens after the response. A bank authorisation can be reversed. A dispute arrives weeks later. A bank transfer that looked successful can fail days afterwards. A subscription renews on a schedule nobody is watching. So the merchant needs a channel for outcomes that were not available at request time, which is what webhooks are for.\n\nThat makes webhook handling a core part of a payments integration rather than an optional convenience, and it inherits every property of delivery over an unreliable network. Events can arrive twice, out of order, or after a long delay while a receiver was down. Handlers must be idempotent by event id, must tolerate an older event arriving after a newer one, and must not assume that the absence of an event means the absence of the outcome.\n\nThe reconciliation of local state with the provider's is where the design earns its keep. A merchant's database and a payment provider's are two systems with no shared transaction, so they will disagree at some point. Treating the webhook as the only source of updates makes a missed delivery permanent, which is why a periodic sweep that asks the provider about anything in a non-final state is a required component and not a belt-and-braces addition.\n\nOrdering deserves a specific mention because payments make it visible. A charge succeeded event and a charge refunded event delivered out of order will, in a naive handler, leave a refunded payment marked as succeeded. Handlers that check the state transition rather than applying the event blindly, or that fetch the current object rather than trusting the payload, avoid a whole family of these bugs.\n\nThe user-facing consequence is that the interface has to be honest about pending. Telling somebody their payment succeeded when it has only been accepted produces the worst possible support conversation later. Showing a state that can still change, and notifying when it does, is both more accurate and, in practice, less alarming than a success that quietly reverses.\n\nThe response to the request is not the outcome, which means the merchant's state is a prediction until the provider confirms it. Everything else here, idempotent handlers, tolerance of reordering, and a sweep for anything still pending, is a consequence of that one fact.","payments-failures":"Declines, retries and the cost of trying again\n\nDeclines are not errors in the usual sense. A hard decline means the card is closed, stolen or invalid and will never work; retrying is pointless and, at volume, damages the merchant's standing with the networks. A soft decline means insufficient funds or a temporary block, and a retry in a few days has a genuine chance. Treating the two identically is the most common expensive mistake in subscription billing.\n\nThe reason to care is that the response codes are the only signal, and they are coarse. Issuers deliberately do not explain much, partly for fraud reasons, so a system has to map a small set of codes into a policy: retry, retry later with a different strategy, or stop and ask the customer. Getting that mapping wrong shows up as either lost revenue or an unhappy conversation with a payment provider about retry ratios.\n\nFor recurring payments, the practice built on this is dunning: a schedule of retries spread over days, combined with messages to the customer, because many failures are fixed by the cardholder rather than by the system. The retries are timed to when they are more likely to succeed, such as after a typical payday, and the whole sequence has an end, after which the subscription is cancelled rather than retried indefinitely.\n\nNetwork-level failures need the opposite instinct. Where a decline is an answer, a timeout is the absence of one, and retrying a timeout without an idempotency key is how double charges happen. So the two paths must be distinguished in code: a decline is a business outcome to be recorded and acted on, a timeout is an unknown to be resolved by lookup before anything else is attempted.\n\nCard details also expire and change, which is a background failure mode with a documented remedy: account updater services that keep stored credentials current, and network tokens that survive a card being reissued. For a business with stored cards, that machinery recovers a meaningful share of otherwise-lost revenue, and it is invisible to anyone who has not been told it exists.\n\nA decline is an answer and a timeout is not, and confusing them produces either double charges or abandoned revenue. The rest is policy: which codes are worth retrying, on what schedule, and when to stop and involve the customer instead of the system.","payments-reconciliation":"Reconciliation, and the money that does not match\n\nAt the end of every day, three records exist of the same money: what the merchant's system believes, what the payment provider reports, and what the bank actually moved. They will not agree exactly, and the job of reconciliation is to explain every difference rather than to hope there are none.\n\nMost differences are timing rather than error. A capture on one side of midnight and a settlement on the other, a refund issued today and settled in three days, a payout batching Friday and Saturday together. A reconciliation process that cannot express in flight will produce alarming numbers every single day and will therefore be ignored, which is worse than not having one.\n\nThe differences that are not timing fall into a small set: a payment recorded locally that the provider has never heard of, usually a request that timed out and was never resolved; a provider payment with no local record, usually a webhook that was missed and a sweep that never ran; and amount mismatches, usually fees or currency conversion applied somewhere the local model does not represent.\n\nWhich is why the reconciliation is also the test of everything upstream. A system with correct idempotency, a working sweep and an honest ledger reconciles with a small, explainable set of in-flight items. A system without them reconciles with a list of mysteries, and the mysteries are the same bugs described in the earlier topics arriving in aggregate at the end of the month.\n\nThe operational shape that works is a daily automated match with a named owner for the exceptions, and a small tolerance that is a deliberate decision rather than an accident. Rounding in currency conversion is real, and pretending otherwise produces an exception queue nobody can clear. What matters is that the tolerance is written down, monitored for drift, and never used to absorb a difference that is growing.\n\nReconciliation is where every upstream mistake becomes visible, which makes it a test of the system rather than an accounting chore. The measure of a payments integration is not whether the numbers match, it is whether every difference has an explanation somebody can give.","search-crawl":"Crawling a web nobody controls\n\nA crawler is a breadth-first traversal of a graph with no map, no schema and no cooperation. The frontier, the set of URLs known and not yet fetched, is the central data structure, and it is not a simple queue: it has to be prioritised by importance and freshness, deduplicated, and partitioned so that the same host is not being fetched by twenty machines at once.\n\nPoliteness is the constraint that shapes everything. A crawler must respect robots.txt, must rate limit per host rather than globally, and must identify itself so an operator can block it. This is not only courtesy: a crawler that hammers a small site is a denial of service, and the practical consequence of getting it wrong is being blocked by exactly the sites you wanted.\n\nSo the frontier is partitioned by host, with one queue per host drained at a polite rate and many hosts in flight simultaneously. That single design decision explains why a crawl is wide rather than deep, and why crawl capacity is measured in hosts and politeness delays rather than in raw bandwidth.\n\nThe web then attacks the crawler in ways nobody designs for. Infinite calendars generate a new URL for every future date. Session identifiers make every visit a new address. Redirect chains loop. Pages are generated by JavaScript and are empty without a renderer, which turns a cheap fetch into an expensive browser. Each of these needs a specific defence: URL normalisation, depth and pattern limits, budget per host, and a decision about which pages are worth rendering.\n\nThe last problem is knowing when to come back. Content changes at wildly different rates, so recrawl is scheduled per URL from observed change frequency: a news front page in minutes, an archived page in months. Getting that wrong wastes the crawl budget on pages that never change while missing the ones that do, which is the trade the freshness topic later returns to.\n\nThe frontier is the system. Everything a crawler does well or badly, politeness, coverage, freshness and resistance to traps, is a property of how that queue is prioritised and partitioned rather than of how fast it can fetch.","search-dedup":"Duplicates, canonicals and near-identical pages\n\nA large fraction of the web is duplicated. The same article is syndicated across a dozen sites, the same product page exists under printable, mobile and tracking-parameter variants, and entire sites are mirrored. An index that stores every copy wastes space and, far worse, returns ten results that are the same page, which is a bad result set no ranking improvement can rescue.\n\nExact duplicates are cheap to detect: hash the normalised content and compare. That catches mirrors and identical variants and misses everything interesting, because most duplication is near-duplication with a different header, an inserted advertisement or a changed date.\n\nNear-duplicate detection is where the technique gets specific. Shingling breaks the document into overlapping word sequences and compares the sets, and comparing every pair is quadratic, so a fingerprinting scheme is used instead: MinHash estimates set similarity from a small signature, and SimHash produces a hash where similar documents differ in few bits, making near-duplicates findable by looking for hashes within a small Hamming distance. Both turn an impossible comparison into an index lookup.\n\nChoosing which copy to keep is a ranking problem in itself. The canonical should be the version most likely to be wanted: the original publisher rather than the syndicator, the version without tracking parameters, the one with the most inbound links. Publishers can state a preference with a canonical link element, which is a hint from an interested party and therefore treated as evidence rather than as instruction.\n\nThe consequence for the results page is diversity. Even after canonicalisation, ten results from one domain about the same topic is a worse answer than a mix, so the final ranking applies clustering and per-site limits. That is a deliberate reduction in per-result relevance for a large increase in the usefulness of the page as a whole, which is a trade worth recognising because it appears in every ranked list of anything.\n\nDeduplication is not a storage optimisation, it is a results-quality feature: ten copies of one page is a failed search regardless of how relevant each copy is. The techniques matter because near-duplication is the common case and exact matching cannot see it.","search-index":"An index too large for one machine\n\nThe index is inverted: each term points at the list of documents containing it, with positions so that phrases can be matched. At web scale those posting lists are enormous, so they are compressed aggressively, using delta encoding of document identifiers and variable-length integers, because the index is read constantly and every byte saved is bandwidth and cache.\n\nIt cannot live on one machine, so it is sharded, and the choice of how is consequential. Sharding by document means each shard holds a slice of the corpus and a query goes to every shard, which is more network traffic and balances naturally. Sharding by term means a query touches only the shards holding its terms, which sounds efficient and produces terrible skew, because a common term's posting list is vast and multi-term queries need intersections across machines. Document sharding wins in practice, and the fan-out that follows is the defining property of search serving.\n\nDocuments are assigned identifiers in an order that makes compression effective, typically by clustering similar or related documents so that posting lists contain runs of nearby identifiers. That is an unglamorous decision that changes index size by a large factor, and it illustrates a general point: the encoding and the ordering are as much of the design as the structure.\n\nTiering is the other lever. Not every document deserves the same treatment, so an index is split into a small tier of high-quality documents held in memory and larger tiers on cheaper storage. Most queries are answered from the top tier, and the lower tiers are consulted only when the top does not produce enough good results, which is what makes the economics work at all.\n\nUpdates make this harder than a static structure. Posting lists are expensive to modify in place, so new documents accumulate in a small fresh index and are merged into the main one periodically, with deletions handled by a separate list of identifiers to suppress. That is the same design as a log-structured merge tree, arrived at independently for the same reason, which is that sequential writes and periodic merges beat random updates at scale.\n\nSharding by document is what forces every query to touch every shard, which sets the shape of serving and makes tail latency the central problem. Everything else here, compression, identifier ordering, tiering and merge-based updates, exists because the index is read far more often than it is written.","search-serving":"Serving a query in under a second\n\nA query arrives, is parsed and expanded with synonyms and spelling corrections, and is sent to every shard. Each shard finds its candidates, scores them cheaply, and returns its best few. A root node merges those, applies expensive scoring to the small combined set, adds diversity rules and returns the page. The whole thing has a budget measured in a few hundred milliseconds.\n\nThe dominant problem is tail latency, and it is arithmetic rather than bad luck. If a query touches a thousand shards and each has a one per cent chance of taking longer than a second, the chance that at least one does is essentially certain, so the median shard's latency is irrelevant and the request is as slow as its slowest participant. Dean and Barroso's tail at scale paper is the canonical treatment, and the numbers are why every large fan-out system takes the mitigations seriously.\n\nThe mitigations are specific. Hedged requests: send a duplicate to a second replica after a short delay and take whichever answers first, which costs a few per cent more work and removes most of the tail. Tied requests, where the two replicas coordinate so the loser drops its copy. Micro-partitioning so slow shards can be rebalanced. And returning early with what has arrived, because a slightly incomplete result page delivered on time is better than a complete one that missed the budget.\n\nCaching helps less than intuition suggests and still matters. Query popularity is a long tail, so a cache of full result pages hits on the head of the distribution and misses on most of the traffic. Caching posting list intersections and per-shard partial results is more effective, and the freshness of everything cached has to be bounded because the index behind it keeps changing.\n\nThe design principle underneath is that a request touching a thousand machines cannot depend on all of them behaving. It must be able to answer with most of them, cut off the ones that are late, and degrade the result rather than the deadline. That is the same shed-load-deliberately argument as anywhere else, applied at the level of a single query.\n\nFan-out to a thousand shards makes the tail the median. That single fact explains hedged requests, early return with partial results, and why a search engine is engineered around latency budgets rather than around throughput.","search-freshness":"Freshness: from batch to incremental\n\nFor years the index was rebuilt in batches. Google's pre-Caffeine pipeline ran crawled documents through roughly a hundred sequential MapReduce stages, and a document took two to three days to travel from being crawled to being searchable. That was acceptable when the web changed slowly and unacceptable once it did not.\n\nThe reason batch was chosen first is worth stating plainly, because it is the same trade everywhere: batch processing is simple, restartable and efficient per document, and it is inherently latent, because a stage cannot start until the previous one has finished with everything. Adding one new document meant waiting for the next full pass.\n\nCaffeine, the indexing system Google moved to in 2010, replaced that with incremental processing built on Percolator, which added transactions and change notifications on top of the storage layer so that a single document could be updated in place and trigger the work that depends on it. The published result is precise: the same number of documents processed per day, the median document moving through more than a hundred times faster, and the average age of a document in results cut by half.\n\nThe cost of that is real and worth naming: incremental processing runs many small distributed transactions instead of a few enormous batch jobs, so the system uses substantially more resources per document and is far more complex to operate. Google's own paper is explicit that the trade was made for latency rather than efficiency, which is the honest way to describe most moves from batch to streaming.\n\nThe general lesson generalises well beyond search. Batch is the correct default because it is simple and cheap; incremental is what you buy when the age of the data is a product problem rather than an engineering preference. And the way to know which you are in is to ask what changes when the data is two days old, which for a search engine in a world with breaking news is quite a lot.\n\nThis is the clearest published example of the batch to incremental trade, with numbers on both sides: a hundredfold improvement in latency, bought with more resources per document and considerably more operational complexity. Most streaming decisions are the same trade with less measurement.","search-ranking":"Ranking, and knowing whether it improved\n\nMatching produces thousands of documents and the page shows ten, so ranking is the product. The signals fall into three groups: how well the document matches the query, how good the document is independent of any query, and what the person is likely to want given their language, location and context.\n\nQuery-independent quality is the idea that made web search work. PageRank treated a link as a vote and weighted votes by the authority of the linking page, which gave a way to tell an authoritative page from a keyword-stuffed one at a time when text matching alone could not. It is one signal among hundreds now, and the principle it established, that the structure of the graph carries information the documents do not, long outlived the specific formula.\n\nModern ranking is a learned function over those signals, trained on labelled judgements and behavioural data, and the interesting engineering is the pipeline shape rather than the model. Cheap scoring runs on every candidate in every shard, and expensive scoring runs only on the small set the root has already merged, because the expensive model cannot be run on a million documents inside the latency budget. That two-stage retrieve-then-rerank pattern is now the standard shape for search and recommendation systems generally.\n\nAdversaries are a permanent part of the problem, which is unusual among ranked systems. Every published signal becomes a target, so link farms, keyword stuffing, cloaking and generated content all exist because ranking exists. That is why signals are numerous, partly undisclosed and constantly adjusted, and it is why a search team spends as much effort on defending the ranking as on improving it.\n\nKnowing whether a change helped is the hardest part and is entirely a measurement problem. Human raters give labelled judgements against published guidelines, offline metrics compare rankings against those labels, and online experiments measure what people actually did. Both are needed because they answer different questions: the offline number says the ranking matches expert judgement, and the experiment says it changed behaviour, and a change can move one without the other.\n\nRanking is where a search engine is judged, and the two things that make it tractable are the two-stage pipeline, which keeps expensive scoring off the hot path, and a measurement discipline that can tell an improvement from a change. Without the second, ranking work is a matter of opinion.","framework":"A structure for the 45 minutes\n\nSpend the first five minutes on requirements, and be specific rather than polite about it. What must it do, for how many people, what is the read to write ratio, what latency is acceptable, what may be stale and what may not, what is explicitly out of scope. Write the numbers on the board, because they are what every later decision will be justified against.\n\nThen estimate out loud for about five minutes: requests per second, storage per year, bandwidth. Orders of magnitude are the goal. A design for twelve requests a second and one for twelve thousand are different systems, and getting that wrong invalidates everything after it, whereas being twenty per cent out costs nothing.\n\nThe high-level design should be five or six boxes and no more. Client, edge, service, data store, queue, and whatever this particular problem genuinely needs. Draw the request path first and say what happens at each hop, because a diagram nobody has walked through is a picture rather than a design.\n\nThen depth, which is where most of the assessment happens. Pick the two or three parts that are genuinely hard for this problem, the fan-out, the hot key, the ordering, the consistency boundary, and go deep on those rather than touring everything evenly. If the interviewer steers you somewhere else, go there: they are telling you what they want to assess.\n\nLeave five minutes for bottlenecks and for ten times the load. Say what breaks first and why, what you would measure to see it coming, and what the next change would be. That closing minute or two is where a good candidate becomes an obviously experienced one, because it demonstrates having watched a system get bigger rather than having designed one on paper.\n\nThroughout: think aloud, write down assumptions as you make them, and revisit requirements openly if the design has drifted from them. The interviewer cannot score reasoning they cannot hear, and silence during the hard part is indistinguishable from being stuck.\n\nCandidates who start drawing boxes immediately fail on scope, not knowledge. Establishing constraints first is what makes every later decision defensible instead of arbitrary.","estimation":"Back-of-envelope estimation\n\nEstimation is arithmetic done out loud, and the numbers to memorise are few. A day is 86,400 seconds, near enough 100,000. So a million events a day is about 12 a second, and a billion is about 12,000 a second. Peak is commonly two to three times average, and traffic is never spread evenly across the day.\n\nStorage follows the same shape. A thousand bytes per record at a million records a day is a gigabyte a day, roughly a third of a terabyte a year. A million users with a kilobyte of profile each is a gigabyte in total, which is a laptop rather than a cluster and worth saying so plainly when someone proposes sharding it.\n\nA few latency numbers anchor everything else and are worth knowing to an order of magnitude: memory access in tens of nanoseconds, an SSD read in tens of microseconds, a data centre round trip around half a millisecond, and a cross-continental round trip well over a hundred milliseconds. Those are the ratios behind almost every caching and placement decision you will ever justify.\n\nRound aggressively and say that you are. Use 100,000 seconds for a day, treat a kilobyte as a thousand bytes, call a month thirty days. Nobody is checking your arithmetic, and precision is the enemy of finishing the calculation in the ninety seconds it deserves.\n\nThe point is not accuracy but the class of system. Twelve requests a second is one server and no discussion. Twelve thousand is a fleet, a cache and a real conversation about the data layer. Being twenty per cent out changes nothing; being out by a factor of a thousand invalidates every design decision that follows, which is the failure the exercise exists to prevent. Sanity-check the answer against something you know. If your estimate implies more storage than a large public dataset, or more traffic than a well-known service, one of the inputs is wrong. That instinct, checking a result against the world rather than against the sum, is what people mean by engineering judgement in this context.\n\nThe point is not precision, it is knowing whether you are designing for 10 requests per second or 100,000, because those are entirely different systems. Being off by 20 percent is fine; being off by a factor of a thousand is the failure.","tradeoffs":"Talking about tradeoffs\n\nEvery design decision costs something, and naming the cost unprompted is the strongest signal available in this format, because it is the thing that distinguishes having operated a system from having read about one. Interviewers are not testing whether you can name Redis; they are testing whether you know when it is the wrong answer.\n\nThe shape of a strong statement is decision, cost, and the condition that would change it. I would cache this with a sixty second expiry is a decision. Accepting up to a minute of staleness, because prices update hourly, is the cost. And if the prices became real-time I would drop the cache and read through is the condition. Three clauses, and the third is the one almost nobody says.\n\nVolunteer the failure mode as well. What happens under partition, on a hot key, when the cache is cold after a restart, when the third party is down. Saying it yourself is worth far more than being asked, because being asked means the interviewer noticed the gap before you did, and that difference is exactly what the rubric is trying to detect.\n\nQuantify wherever a number is available, even roughly. Adds a network hop, so maybe two milliseconds on the read path. Doubles our write volume. Saves an engineer a week of operating it. Numbers turn a preference into an argument, and an argument can be examined, which is the conversation you want to be having.\n\nSay what you would not do and why, because a design defined only by inclusions looks like a list of technologies. I would not shard this yet, because the whole dataset fits in memory and sharding is the one decision that is genuinely hard to reverse. That single sentence demonstrates more judgement than three additional components.\n\nFinally, be willing to change your mind when the interviewer pushes, and say what changed it. Defending a position past the point of evidence is a stronger negative signal than the original choice, and updating cleanly in front of someone is the behaviour they are hoping to see in a design review next quarter.\n\nInterviewers are testing judgement, not recall. Anyone can name Redis; the differentiator is knowing when it is wrong and saying so unprompted.","senior-vs-staff":"Senior versus staff\n\nA senior engineer owns the delivery of a complex system end to end. They design it, build it, handle the failure modes, and are trusted to do so without supervision. The scope is a system and a team, and the assessment is whether the work lands reliably and whether other people's work gets better for being near it. A staff engineer works across teams, on problems that are ambiguous before they are hard. A large part of the job is deciding what should be built at all, and then getting enough agreement that it happens. The output is often a document, a prototype that settles an argument, or a decision that saves two teams from solving the same problem twice.\n\nThe clearest way to see the difference is the shape of the leverage. A senior engineer multiplies their own effectiveness. A staff engineer multiplies other people's, which is why a staff promotion is rarely earned by writing more code and why the most cited staff contributions are ones where nothing was built.\n\nThere are recognisable archetypes and it helps to know which one you are. The tech lead who runs a team's technical direction. The architect who holds a domain across teams. The solver who is deployed onto whatever is currently hardest. The right hand who works on whatever the organisation most needs. They are different jobs sharing a title, and interviewing well means being clear about which you are describing.\n\nIn interviews the visible difference is scope. Seniors discuss the system; staff discuss the system in the context of the organisation that has to run it, the teams who will own its parts, the migration path, and what will still be true in two years. Answers that never leave the diagram read as senior regardless of the years behind them.\n\nThe uncomfortable part is that staff work is often unglamorous: writing the document that stops a project, sitting in the meeting that aligns two teams, deleting a service. If your best examples are all projects you personally built, that is a senior portfolio, and the gap is worth knowing about before someone else names it.\n\nPromotion to staff is rarely earned by writing more code. It comes from multiplying other people's output and choosing the right problem, which is why the interview probes influence and judgement more than depth alone.","behavioural":"Behavioural answers with substance\n\nStructure the answer, because an unstructured one is scored on whatever the interviewer manages to extract. Situation, what you specifically did, what happened, and what you took from it. The common weakness is the collective we: it hides which decisions were yours, and an interviewer who cannot tell what you did cannot credit you for it.\n\nPrepare stories rather than answers. Two or three real ones, each with numbers, cover most questions asked at senior level, because the same project can answer conflict, ownership, failure and influence depending on which part you tell. Preparing per question produces a memorised list; preparing per story produces material you can actually think with.\n\nNumbers matter more than they should. Reduced deploy time from forty minutes to six lands differently from improved our deployment process, not because the interviewer is checking, but because specificity is evidence of having been there. Vagueness is what an answer sounds like when it is being reconstructed rather than recalled.\n\nThe failure story is the one that separates candidates. Everybody has one; the ones who describe it precisely, own their part without over-apologising, and name what changed afterwards are demonstrating exactly the self-assessment that senior work requires. Certainty about a past decision reads as never having examined it.\n\nConflict questions are asking about how you handle being wrong and how you handle being right when someone disagrees. The strong version names the other person's reasoning fairly, says what evidence would have changed your mind, and describes what you did once the decision went one way or the other. Winning is not the point; the point is whether the team could keep working afterwards.\n\nFinally, prepare questions of your own and make them real. What does the on-call rota look like, what happened in the last incident, how does a decision like this one get made here. Those answers are the ones you will live with, and asking them signals someone evaluating the job rather than hoping to be chosen.\n\nThe failure story is the one that separates candidates. Everyone has one; the ones who can describe it precisely, without deflecting, are demonstrating exactly the self-assessment senior work requires.","coding-round":"The coding round\n\nRestate the problem in your own words and confirm an example before writing anything. It costs a minute and it catches the misunderstanding that would otherwise cost twenty, and clarifying an ambiguous constraint is itself part of what is being scored rather than a delay before the real work.\n\nSay the brute force and its complexity, then improve on it. A working slow solution beats an unfinished clever one every time, and stating the naive version first demonstrates that you can find an optimisation rather than that you have seen this problem before. If you already know the optimal answer, show the progression anyway; the reasoning is the thing being sampled.\n\nNarrate while you work. Silent brilliance scores worse than narrated competence, because the interview is a proxy for what working with you is like, and a colleague who thinks silently through hard problems is genuinely harder to work with. Say what you are trying, and say when something is not working and what you will try instead.\n\nTest unprompted, with cases you choose: empty input, one element, duplicates, the maximum size, a value at a boundary. Walking through your own code with a concrete input catches the off-by-one before the interviewer does, and doing it without being asked is one of the strongest signals available in the format.\n\nWrite code you would accept in review. Real names, small functions, no cleverness that needs explaining. Under time pressure people produce single-letter variables and one dense line, and it reads as exactly what it is, which is a habit the reviewer would rather not inherit. If you get stuck, say so and say what you are stuck on. Interviewers can offer a hint to someone who has articulated the obstacle and cannot help someone staring silently at a screen. Being stuck is normal; being stuck without communicating it is the part that scores badly.\n\nThe interview is a proxy for working with you. Silent brilliance scores worse than narrated competence, because collaboration is the thing actually being sampled.","the-loop":"What each stage of the loop is for\n\nA hiring loop is not one assessment repeated. Each stage is scored against something different, and knowing which is which changes how to spend the time in it. The recruiter screen checks the basics and the story: level, availability, salary band, and whether your background matches what the role says. Precision here saves everyone a fortnight.\n\nThe technical screen is a filter, so the objective is to pass it cleanly rather than to impress. Finish the problem, communicate while you work, and treat it as a lower-variance version of the onsite. Nobody has been hired on a screen and plenty have been eliminated on one.\n\nThe onsite rounds are the real assessment and they are usually split deliberately: coding for whether you can build, system design for judgement at scale, behavioural for how you work with people, and often a domain round for the specific thing the team does. They are scored separately by people who then meet, which is why an outstanding design round does not compensate for a coding round nobody could follow.\n\nThe debrief is where the decision is actually made, and understanding it explains a lot about what to optimise for. Interviewers write feedback separately, then argue it. Specific evidence travels between people, and impressions do not, so an interviewer who can quote something concrete you said is far more useful to you than one who thought it went well. Give people evidence they can repeat.\n\nThe offer conversation is a separate skill from all of it and worth preparing with the same seriousness. Ask about the level and the band before discussing numbers, because the level determines the range and is much harder to change afterwards. And ask the questions that decide whether you would stay: what the on-call rota looks like, what happened in the last incident, how a technical disagreement got resolved recently.\n\nEach stage answers a different question, so the same performance is scored differently depending on where it happens. The most useful shift is realising that the debrief is a conversation between people who were not in your other rounds, which makes concrete, quotable evidence worth more than a general good impression.","the-common-pool":"The same thirty questions\n\nPublicly shared interview reports from Google, Meta, Amazon, Microsoft, Netflix, Uber, Stripe and Airbnb describe largely the same pool of twenty-five to thirty problems. That is the single most useful fact about preparation: work done for one company transfers almost entirely to the next, so the sensible strategy is depth on the common set rather than breadth across company-specific lists.\n\nThe recurring set is short enough to write down. A news feed, a chat system, a video streaming service, a URL shortener, a rate limiter, a ride-hailing match, a payment flow, a notification system, a search or autocomplete, a file sync service, a ticket booking system, and a web crawler. Most loops draw from those twelve, and the rest are variations on them wearing a different product name.\n\nThey repeat because each one exercises two or three underlying decisions that recur everywhere: fan-out on write against fan-out on read, geospatial indexing, consistency under contention, ordering across partitions, idempotent writes, and delivery to a connection you are holding open. Learning the decisions rather than the answers is what makes the preparation transfer.\n\nThe variation between companies is less in the question than in what they press on afterwards, which is why the same prompt can go in four directions depending on who is asking. Two people can both be asked to design a chat system and spend forty minutes on completely different problems, one on connection scaling and one on message ordering.\n\nThe practical consequence is to prepare a small number of designs properly rather than a long list superficially. Six or seven done to the depth where you can defend every component under pressure covers more ground than twenty you have read about, because the components repeat and the defending is the part being scored.\n\nThis is why breadth beats memorising one company's list. Each question exercises two or three underlying decisions, fan-out, geospatial indexing, consistency under contention, and those decisions repeat across all of them.","by-company":"What each company tends to press on\n\nGoogle leans toward scale and data-intensive problems: crawlers, indexing, autocomplete, distributed storage. Expect the follow-up to be what happens at a billion of something, and expect the estimation to be checked rather than accepted. Meta favours social graph problems, news feed, friend recommendation, live comments, where fan-out and the celebrity case are the crux and where the interesting answer is usually a hybrid.\n\nAmazon presses on operational ownership and cost, in keeping with its leadership principles, and it is the loop where the behavioural round carries the most weight relative to the technical ones. Uber and Lyft go to geospatial matching, surge and the state machine of a trip. Stripe and payment companies go to consistency, idempotency and what happens when a retry arrives after a timeout.\n\nThe steering that follows from this is modest and worth doing. At a payments company, volunteer idempotency before being asked. At a social company, raise the celebrity case yourself. At Amazon, say what it costs to run and who gets paged. None of that is gaming the interview; it is answering the question the domain actually poses, which is what a colleague there would do.\n\nBe careful with how much weight this carries, though. Loops vary by team more than by company, interviewers bring their own preferences, and any published mapping of company to emphasis is indicative rather than sourced. Treat it as a prior to update rather than as a script, and drop it the moment the interviewer indicates otherwise.\n\nThe one universally safe move is to ask what they care about. Which part of this would you like me to go deeper on is a question every interviewer is glad to answer, and it replaces guessing about emphasis with knowing it.\n\nKnowing the emphasis lets you steer the depth phase toward what that interviewer values. At a payments company, volunteering idempotency early is worth more than describing a CDN.","cost-and-ops":"Cost and operations are part of the answer\n\nThe questions have barely changed in years. What separates candidates has shifted toward the dimensions that used to be extra credit, and at senior level they are now part of the answer rather than a bonus attached to it.\n\nExpect to be asked what it costs to run, what happens at three in the morning when it breaks, who is paged, and how it is rolled back. A design with no answer to those reads as unfinished, because it describes a topology rather than a system somebody has to operate. Saying this runs about two hundred a month, and if the cache tier fails we serve stale for up to five minutes rather than going down, is the differentiator.\n\nMigration is the other under-prepared dimension. Almost nothing is built on an empty field, so being asked how you would get there from the system that exists is both realistic and revealing. The strong answer is incremental: run both, move traffic gradually, keep a way back, and have a signal that tells you which is behaving better.\n\nSome loops now run without a whiteboard at all, asking you to design aloud and then revising the requirements mid-way to see how the design bends. That format rewards the same habits as the written one: state assumptions, keep the reasoning audible, and say which decisions the change invalidates rather than quietly patching the diagram.\n\nNone of this replaces the technical core. It is the layer that turns a correct answer into a credible one, and it is the layer most candidates leave out, which makes it the cheapest available way to be memorable for the right reason.\n\nA technically correct design with no cost estimate and no failure story now reads as incomplete. Saying 'this runs about two hundred a month, and if the cache tier fails we serve stale for up to five minutes rather than going down' is the differentiator.","practice-plan":"A practice plan that works\n\nPick six problems that span the underlying decisions rather than twenty that repeat the same one. A feed, for fan-out on write against fan-out on read. A chat system, for held connections and delivery guarantees. A ride match, for geospatial indexing and assignment. A rate limiter, for algorithms and distributed counter state. A video service, for the pipeline and the cost of precomputation. A payment flow, for idempotency and consistency under an ambiguous timeout. Between them those six exercise almost every decision the common pool draws on, which is why six done properly beats twenty read about.\n\nDo each one out loud, against a timer, in the format you will actually face. The constraint in the real thing is the clock and the talking, not the knowledge, and both are trainable in a way that reading is not. Forty five minutes, spoken, with a diagram you are producing while you speak, is a genuinely different exercise from thinking the same thoughts silently, and the gap between the two is where most well-prepared candidates lose the round.\n\nThen redo one with a changed constraint, which is the highest-value drill available because it rehearses the thing interviewers actually do. Ten times the traffic. A hard consistency requirement where you had assumed staleness was fine. A regulatory rule that the data cannot leave a country. A budget a tenth of what you implicitly assumed. Watch whether your design bends or breaks, and notice which decisions you have to unwind: those are the ones that were load-bearing, and knowing which they are is most of what senior judgement looks like from outside.\n\nRecord yourself once, however unpleasant that is, because the recording tells you things no amount of self-assessment will. The usual findings are the same across most people: long silences during the hard part, assumptions stated once and then quietly contradicted, a component named and never justified, and no time left for the failure modes. Each of those is cheap to fix once seen and invisible from the inside.\n\nKeep a written list of the numbers you keep having to look up, and stop looking them up. Requests per second from a daily total. Storage per year from a row size. The latency ladder from memory to cross-continental. A quorum size for a given failure tolerance. Those are the arithmetic you will do under pressure, and doing it from memory is the difference between a fluent five minutes of estimation and a stalled one.\n\nFinally, do at least two of the six with another person rather than alone, because the part you cannot rehearse by yourself is being interrupted. Someone pushing on a decision, asking for a number you did not prepare, or steering you away from the part you wanted to talk about is the actual format, and the first time it happens should not be the real thing.\n\nRedoing a problem under a changed constraint is the drill that transfers, because it rehearses what interviewers actually do, which is push on a design until something gives. Practising six designs to the depth where every component can be defended covers more of the pool than twenty read once, because the components repeat and the defending is the part being scored.","sql-injection":"Injection and parameterised queries\n\nInjection happens when input is concatenated into something that will be parsed as a command: SQL, a shell line, an LDAP filter, an XPath expression, a template. The input ends the data and starts being instruction, and the parser cannot tell the difference because by the time it sees the string, there is no difference to see.\n\nEscaping is the intuitive fix and the wrong one, because it requires getting every context right forever, and the contexts nest. A value escaped for SQL and then interpolated into a LIKE pattern, or into a JSON document, or into a shell argument, needs different escaping at each layer, and one missed call site is enough. The defence has to remove the possibility rather than handle each case.\n\nParameterised queries do that by sending the statement and the values on separate channels. The database parses the statement first, then binds values into slots, so a value can never be parsed as syntax no matter what it contains. This is not escaping done well; it is a different mechanism, and it is why the advice is absolute rather than contextual.\n\nIdentifiers are the exception that catches experienced people. Table names, column names and sort directions cannot be bound as parameters, because they are part of the statement rather than values in it. A dynamic ORDER BY built from user input therefore needs an allowlist mapping permitted inputs to known identifiers, and nothing else will do.\n\nAn ORM is not automatic protection. Most expose a raw query escape hatch, and that is where injection reappears, usually in the reporting endpoint written under time pressure. The same applies beyond SQL: a shell command assembled by concatenation, a template rendered from user-supplied source, a NoSQL query built from a request body that arrives as an object rather than a string.\n\nDefence in depth belongs here even though parameterisation is complete. Least-privilege database accounts so an injection cannot read tables the feature never needed, no dynamic SQL where a static query would do, and monitoring for queries whose shape does not match anything the application should emit.\n\nParameterisation removes the class of bug rather than the instance. Escaping means every new call site is another chance to get it wrong, and one miss is enough.","xss":"Cross-site scripting\n\nCross-site scripting is injection into a page rather than into a query. Attacker-controlled text is rendered as markup, so their script runs with your origin's privileges: it can read the DOM, call your API as the user, and exfiltrate anything the session can reach. It is not a defacement bug; it is arbitrary code execution in your users' browsers. The three shapes are worth naming. Stored XSS is persisted and served to everyone who views it, which is the worst case. Reflected XSS comes back in a response from something in the request, and needs a victim to follow a link. DOM-based XSS never touches the server at all: client-side code takes something from the URL and writes it into the page, so a server-side filter never sees it.\n\nModern frameworks close most of it. React escapes interpolated values by default, which means the common path is safe without anyone thinking about it. What remains are the deliberate bypasses, and they are few enough to audit by name: dangerouslySetInnerHTML and its equivalents, values used as a href or src where a javascript: URL executes on click, and anything written into a script, style or event-handler context where HTML escaping is not the right escaping.\n\nA Content Security Policy is the layer that limits damage when something slips through. Refusing inline script and restricting which origins may execute turns a working injection into a blocked one, and modern policies use a nonce or a hash rather than an origin allowlist, because an allowlisted CDN hosting a vulnerable library is a hole in the policy. Report-only mode exists so a policy can be measured before it is enforced.\n\nCookies deserve a mention here because they decide what XSS costs. A session cookie marked HttpOnly cannot be read by script, so an injection cannot simply steal it, and the attacker is reduced to acting through the page while it is open. That does not make XSS acceptable; it makes the difference between a stolen session that outlives the visit and one that does not.\n\nSanitisation is a last resort with one correct implementation: a maintained library parsing to a tree and allowlisting elements and attributes, never a regular expression. If a feature needs to accept rich text, that is the tool. If it does not, escaping everything and rendering as text is both simpler and safer.\n\nFramework escaping handles the common path, so the remaining risk concentrates in the few places you deliberately bypass it. Those are worth auditing by name rather than trusting the framework globally.","csrf":"CSRF and SameSite\n\nCross-site request forgery abuses ambient authority: the browser attaches cookies to requests for your origin regardless of which page caused them. Another site submits a form to yours, the session cookie rides along, and the request arrives authenticated even though the user never intended to make it. The attacker cannot read the response, which is why CSRF is about actions rather than about theft.\n\nSameSite is the mechanism that fixed most of it. Lax, now the default in current browsers, means the cookie is not sent on cross-site requests except top-level navigations that are safe methods, which kills the cross-site form post. Strict withholds it even on ordinary navigation, so a user following a link from elsewhere arrives logged out, which is usually too blunt. None sends it everywhere and requires the Secure attribute.\n\nA synchroniser token is the explicit defence and remains worth having for sensitive actions: a random value tied to the session, rendered into the form, and required on submission. The attacker's page cannot read it, because the same-origin policy prevents reading your pages, so it cannot construct a valid request. The double-submit variant compares a cookie against a header and avoids server-side state, at the cost of being weaker if any subdomain can set cookies.\n\nWhether you need any of this follows directly from how the session travels. Cookies are ambient authority and need protection. A bearer token in an Authorization header that your own script sets is not attached by the browser to a third-party form post, so classic CSRF does not apply. Mirror that token into a cookie for convenience and the exposure returns immediately.\n\nThere are two adjacent cases worth remembering. Simple cross-origin requests do not trigger a preflight, so a form post with a plain content type reaches your endpoint before CORS has any say, which is why CORS is not a CSRF defence. And a GET that changes state is exposed to a bare image tag, which is one of several reasons safe methods should be safe.\n\nThe practical setup is short: SameSite=Lax plus Secure and HttpOnly on session cookies, tokens on state-changing forms, no state changes behind GET, and CORS configured as a separate concern that answers a different question.\n\nWhether you need CSRF protection follows directly from how you carry the session. Cookies are ambient authority and need it; an explicit Authorization header is not sent by a third-party page and does not.","ssrf":"SSRF and the metadata endpoint\n\nServer-side request forgery is making your server fetch a URL an attacker chose. Any feature that accepts a URL is a candidate, and there are more of them than anyone expects: webhook registration, image import from a link, link preview generation, a PDF renderer fetching stylesheets, an avatar imported from a third party, an XML parser resolving an external entity, a health check whose target is configurable. Each was added for a good reason by somebody who was not thinking about this.\n\nThe damage comes from where your server sits rather than from what it fetches. It is inside the network, past the firewall, holding an identity, and a request it makes on an attacker's behalf inherits all of that. So the reachable surface is internal services with no authentication because they were never exposed, admin panels bound to localhost, a Redis or Elasticsearch instance listening on a private address, and on a cloud instance the metadata endpoint, which hands out role credentials to anything that asks from that machine because being on the machine is the authentication.\n\nThat metadata endpoint deserves naming precisely because the defence is available and often unenabled. The first version of the AWS service answers a plain unauthenticated GET, which is exactly what an SSRF gives an attacker. Version two requires a PUT to obtain a token first, and sets a hop limit so a response cannot be relayed onward, which defeats the ordinary case. Requiring version two is a single instance setting and it closes the specific path the 2019 Capital One breach took.\n\nBlocklisting is the intuitive defence and it does not hold, for a reason worth understanding rather than memorising: you validate a string, and the connection is made to whatever the name resolves to at the moment it is made. A hostname the attacker controls can resolve to a private address. It can resolve to a public address when you check and a private one when you connect, which is a DNS rebind. A response can be a redirect to an internal address after validation has already passed. And the address space is larger than the obvious ranges, with IPv6 forms, the many spellings of the loopback address, and decimal or octal encodings of the same host.\n\nWhat works is checking the address rather than the name, at the point the socket is opened, on every hop of every redirect, against an allowlist where one is possible and a deny list of the whole private space where it is not. Better still, do not make the check the developer's job at all: send outbound fetches through an egress proxy that permits only known destination hosts, so a new URL-fetching feature is covered by the network rather than by whoever wrote it having heard of the rule. The proxy is the only one of these that stays correct as the codebase grows.\n\nTwo habits complete it. Treat the response as hostile too, because an attacker who cannot see the response can still learn from it: distinguishable timings and error messages turn a blind SSRF into a port scanner, so failures should look the same whatever went wrong. And give the fetching component its own network egress rules and its own credentials, so that even a successful request reaches as little as possible, which is the same bulkhead argument applied to the network rather than to a thread pool.\n\nValidating the URL string is the intuitive fix and the one that does not hold, because the string is not what gets connected to. The check has to happen on the resolved address, at connect time, on every hop.","supply-chain":"Dependencies and supply chain\n\nMost of what ships is code nobody on your team wrote, and the threat model follows from one fact: a postinstall script in any transitive dependency runs with your build's privileges. That is a direct path to CI secrets, deploy credentials and the artefact you are about to publish, and it does not require anyone to import the package or call a function in it.\n\nThat reframes the question. It is not whether a library is any good; it is whether you trust its author, and everyone its author trusts, with execution on your build machine. A tree of a thousand packages is a thousand maintainer accounts, several of which will be handed to a new maintainer this year without anyone downstream noticing.\n\nLockfiles are necessary and not sufficient. They pin what you resolved, with integrity hashes so the bytes cannot change under a pinned version, which is genuinely valuable. They do not help when a legitimate maintainer publishes a malicious version you then upgrade to, and they do nothing about a typosquatted name that sat one keystroke away from the real one when it was first added.\n\nThe controls that pay for a small team are unglamorous. Fewer dependencies, treated as a cost rather than a convenience. Automated updates so you are never far behind, because staying current is also how you get the fix. CI tokens scoped to exactly what the job needs and expiring quickly. Builds that do not run arbitrary install scripts where the ecosystem allows that to be disabled. And a review step for new dependencies that asks who maintains this rather than does it work.\n\nBeyond that, provenance is where the ecosystem is going. A software bill of materials records what actually went into a build, so the question after a disclosure is a lookup rather than an investigation. Signed provenance attestations, from npm and from Sigstore, let a consumer verify that a package was built from the repository it claims and by the pipeline it claims, which closes the gap that lockfiles cannot. The lesson from the incidents that made the news, the event-stream package, the ua-parser-js compromise, the left-pad removal, is that all three were dependency-of-a-dependency problems. Nobody chose them and everybody shipped them, which is the reason the count itself is the number worth managing.\n\nThe threat model is that adding a dependency grants its author execution on your build machine. That reframes 'is this library any good' into 'do I trust this author with my deploy credentials', which is the question that actually matters.","isolation-levels":"The four levels and what each permits\n\nThe four levels are defined by the anomalies they forbid, which is a more useful way to hold them than as a ladder of strength. Read uncommitted allows dirty reads, seeing another transaction's uncommitted work. Read committed forbids that and still allows non-repeatable reads: the same query run twice in one transaction can return different values, because someone committed in between.\n\nRepeatable read forbids that too for rows already read, and in the classical definition still permits phantoms: rows appearing in a range you queried earlier. Serializable is the only level that guarantees the outcome matches some serial order of the transactions, which is the guarantee people assume they have all along.\n\nThe defaults differ and matter more than the definitions. Postgres defaults to read committed and its repeatable read is really snapshot isolation, which does prevent phantoms in the classical sense while still permitting write skew. MySQL InnoDB defaults to repeatable read and prevents many phantoms with gap locks, which is a different mechanism with different deadlock behaviour. Two engines, the same words, materially different behaviour.\n\nSerializable is implemented in two very different ways, and knowing which decides how it fails. Two-phase locking makes transactions wait, so contention appears as blocking. Postgres uses serializable snapshot isolation, which lets them run and aborts one when a dangerous pattern is detected, so contention appears as failed transactions your application must retry. Turning on serializable without a retry loop turns a rare anomaly into a visible error rate.\n\nThe practical approach is per transaction rather than per system. Leave the default in place for the majority, and raise the level only on the specific paths where a business rule spans rows, with a retry on the serialisation failures that follow. A blanket serializable default is usually a way of paying for isolation everywhere to protect two code paths.\n\nThe one thing not to do is assume. The letters ACID are a promise about a configuration, the configuration has a default nobody chose, and the anomalies permitted at that default are the shape of the bugs you will eventually be debugging with no error message to guide you.\n\nEvery level above read committed costs concurrency, which is why nobody defaults to serializable. The engineering question is which anomalies your specific workload can tolerate, not which level sounds safest.","mvcc":"MVCC and snapshot isolation\n\nMulti-version concurrency control keeps several versions of each row rather than updating in place. A reader sees the snapshot that existed when its transaction began, while writers create new versions alongside. Readers do not block writers and writers do not block readers, which is the property that makes a database usable under mixed load and is the reason MVCC won. An update is therefore an insert of a new version plus a mark on the old one, not an overwrite. That has consequences people meet later: an update costs roughly what an insert costs, updating one column of a wide row still writes the whole row in Postgres, and every index entry has to be maintained for the new version unless the update qualifies for the in-page optimisation.\n\nThe old versions accumulate and have to be reclaimed, which is what vacuum does. It can only remove a version once no transaction might still need it, so the oldest open transaction sets the horizon for the entire database. That is the mechanism behind the classic production incident: a connection left idle in transaction by a pool or a debugger, holding the horizon still while a busy table bloats and its queries slow down, with write volume completely normal.\n\nBloat is the visible symptom and the space is not returned to the operating system by ordinary vacuum, only made reusable, so a table that ballooned stays large until it is rewritten. Autovacuum is tuned per table for a reason: a hot table with a high update rate frequently needs it running more aggressively than the defaults, and the settings that matter are the scale factor and the cost limits.\n\nWhat MVCC buys at the isolation level is snapshot isolation, and it is worth being precise that this is not serializable. A snapshot is consistent, so every read in a transaction agrees with every other, and two transactions can still each read a consistent snapshot, each check a rule that holds in it, and each write something that makes the rule false. That is write skew, and it is the subject of the next topic.\n\nThe practical habits follow directly. Keep transactions short, never hold one open across user interaction or an external call, monitor the age of the oldest transaction as a first-class metric, and treat idle in transaction as an alertable state rather than a curiosity in a connection list.\n\nThe failure this creates in production is rarely a correctness bug, it is an idle transaction left open by a connection pool, blocking vacuum until the table bloats and queries slow down.","write-skew":"Write skew\n\nWrite skew is the anomaly people are most confident their database prevents. Two transactions read an overlapping set of rows, each checks a rule that still holds in its own snapshot, and each writes a different row. Neither touches what the other wrote, so nothing conflicts, both commit, and the rule is now false.\n\nThe textbook case is on-call cover: two doctors each check that at least one other doctor remains on duty, each sees a snapshot where that is true, and each takes themselves off the rota. Both checks passed. Nobody is on call. No database error was raised at any point, because from each transaction's perspective nothing was wrong.\n\nThe shape to recognise is a rule about a set combined with a write to a member of that set. Reserving the last seat, keeping a balance above zero across several accounts, enforcing at most one active subscription, allocating unique meeting rooms, maintaining a minimum staffing level. Every one of those is a constraint that no single row can express, which is exactly why the database cannot enforce it for you.\n\nThere are three fixes and they trade differently. Serializable isolation detects the dangerous read-write pattern and aborts one transaction, which is correct and needs a retry loop. Materialising the conflict gives the rule a row of its own, a rota row or a counter, that every participant must lock, which turns an invisible predicate into a real write conflict the database can see. A predicate lock does the same explicitly where the engine supports it.\n\nThe cheapest fix is often to change the model rather than the isolation level. A unique constraint or an exclusion constraint expresses many of these rules directly, and a constraint is checked by the database on every path, including the script somebody runs by hand at midnight. Where the rule can be made into a constraint, that is a better answer than any transaction setting.\n\nThe reason this stays hidden is that it needs concurrency and a rule spanning rows, which no unit test has and every production system does. It will not appear in review, will not appear in staging, and will appear on the day two people click at the same moment, which is why recognising the shape is worth more than remembering the name.\n\nThis is the anomaly people assume their database prevents. It is invisible in testing because it needs concurrency and a constraint spanning rows, which is exactly the shape of most real business rules.","wal-outbox":"The write-ahead log and the outbox\n\nWriting to your database and then publishing an event is two operations that can fail independently. Commit succeeds, the process dies before the publish, and the event is gone: the order exists and nothing downstream will ever hear about it. Publish first and you have the opposite problem, an event for an order that was never created.\n\nThe outbox pattern makes it one operation. The event is written to an outbox table in the same transaction as the data, so both commit or neither does. A separate relay reads that table and publishes, marking rows as sent.\n\nDelivery is still at-least-once. The relay can publish and die before recording that it did, so the same event goes out twice, which is why consumers stay idempotent. What the outbox removes is the possibility of losing an event entirely, and losing one is far worse than seeing one twice.\n\nThe relay can poll the table or read the database's replication log directly. Polling every second is simple and adds a query and a second of latency; change data capture via the write-ahead log has no polling delay and no query load, at the cost of running Debezium or similar. Either way the outbox needs an index on unsent rows and a cleanup job, because a table nobody prunes becomes the slowest part of the write path.\n\nThis is dual writes solved properly rather than mitigated. Retries and reconciliation reduce how often the gap between commit and publish loses an event; putting the event inside the transaction removes the gap.","quorums":"Quorum reads and writes\n\nIn a leaderless system every replica accepts writes, so there is no failover and no leader election to get wrong. With N replicas, a write waits for W acknowledgements before returning and a read collects R responses before answering. Those three numbers are the entire consistency model, exposed as configuration.\n\nWhen R plus W exceeds N the read set and the write set must overlap in at least one replica, so a read is guaranteed to see at least one copy of the latest acknowledged write. That is the whole argument, and it is the same overlap argument that makes consensus work. Versioning then decides which of the returned values is newest, usually with vector clocks or a last-write-wins timestamp.\n\nTuning those numbers tunes the trade rather than switching a mode. W equal to N gives maximally durable writes and no write availability if a single node is unreachable. W of one returns quickly and risks losing the write if that node dies before replicating. R of one is a fast possibly-stale read; R equal to N is a slow read that has consulted everyone.\n\nThe guarantee is weaker than it looks in several specific ways worth knowing, because they are how quorum systems surprise people. A write that reaches fewer than W nodes may still have been applied on some of them, so a failed write is not an undone write. Concurrent writes to different replicas produce siblings that the application must reconcile. And with sloppy quorums, where unavailable nodes are substituted by others holding hinted handoffs, R plus W greater than N no longer guarantees overlap at all.\n\nRepair is the part that makes it work in practice. Read repair fixes stale replicas it notices while answering a read, hinted handoff replays writes to a node that was down when it returns, and anti-entropy compares replicas in the background using Merkle trees so differences are found without transferring everything. Without those, a quorum system converges only where traffic happens to look. The reason to know this is that it is the design behind Dynamo, Cassandra and Riak, and it explains why those systems ask you to choose consistency per query rather than per cluster. The dial is genuinely yours, which is a feature when you have workloads with different needs and a hazard when nobody decided.\n\nR plus W greater than N is where consistency becomes a dial rather than a mode. It is also the answer to why Dynamo-style stores can offer both behaviours from one design.","pacelc":"PACELC: the half of CAP nobody quotes\n\nCAP describes behaviour during a partition, which is a real but rare event. PACELC adds the branch that dominates every other day: else, when the network is healthy, you still trade latency against consistency on every single request. Both halves are decisions; only one of them is a decision you make thousands of times a second.\n\nThe else branch is concrete. Reading from the nearest replica is fast and possibly stale. Reading through a quorum is consistent and pays the round trips to reach it. Acknowledging a write locally is fast and risks losing it; waiting for a remote replica is durable and slower by the distance between them. No partition is required for any of those choices to exist.\n\nThe notation is worth reading properly, because it says two things. A system described as PA/EL chooses availability when partitioned and latency the rest of the time, which is Dynamo and Cassandra with their default settings. PC/EC chooses consistency in both, which is a system like a single-primary relational database with synchronous replication. Some are PC/EL: strict during a partition, fast when healthy, which is a legitimate and common combination.\n\nIt also explains a class of decision that CAP alone makes look arbitrary. When a database offers strongly consistent reads at higher cost and eventually consistent ones by default, that is the else branch exposed as an API parameter. The same design answering both is not a contradiction; it is a system that declined to make the trade on your behalf.\n\nThe habit worth forming is to state both halves when describing a system. During a partition, this refuses writes to these entities and accepts them for those. In normal operation, reads default to the nearest replica and these three paths pay for quorum. That is a specification. The letter on its own is a slogan.\n\nAnd the geography question follows immediately, because latency in the else branch is set by distance. A quorum spanning continents pays the worst inter-region round trip on every write, which is why systems that need both properties shard so that each shard's quorum stays inside one region.\n\nPartitions are rare and the else branch is every single request. Discussing only CAP means discussing the exceptional case and ignoring the one that determines how the system feels in normal operation.","cells":"Cells, bulkheads and blast radius\n\nA cell is a complete, independent copy of the stack serving a subset of users: its own compute, its own database, its own cache. Nothing is shared, which is the entire property. A failure inside one cell has no path to the others, not because it is unlikely to spread but because there is no mechanism by which it could. That converts availability from a probability into arithmetic. Instead of arguing about how likely a total outage is, you decide in advance that the worst single failure affects one over n of your users. A bad deploy, a poison request, a corrupted cache, a runaway migration: each is contained to the cell it happened in, and the incident is a fraction rather than an event.\n\nDeploys follow the same structure and become the main day-to-day benefit. Ship to one cell, watch it, then continue, which is a canary with a hard boundary rather than a percentage of traffic that shares a database with the other 95 per cent. Most bad changes are caught with one cell's users affected, and rolling back is a decision about one cell.\n\nThe routing layer is the part that has to be right, because it is shared by definition. It maps a user or tenant to a cell and must be simple enough to be nearly incapable of failing: a lookup, a hash, a static assignment, ideally cached at the edge and statically stable so it keeps working when its own control plane is unavailable. A clever, dynamic router is a single point of failure in front of an architecture built to have none.\n\nThe costs are real and worth stating. More infrastructure, because each cell needs its own everything and cannot pool spare capacity with its neighbours. Anything genuinely global, a report across all users, a search over everything, a migration, becomes a fan-out across cells with its own coordination. Cells also have to be sized, and a tenant that outgrows one is an awkward conversation.\n\nAWS builds this way and publishes the reasoning, which is that a fault should have a bounded and known set of affected customers. That is the sentence worth keeping: not that failures are prevented, but that their extent is decided in advance rather than discovered during the incident.\n\nIt converts availability from a probability into an arithmetic fact. Rather than arguing about how likely total failure is, you decide in advance that the worst single failure affects one over n of your users.","tail-at-scale":"Tail latency: hedged requests and Little's Law\n\nIn a request that fans out to many servers, the slowest response decides the total, so the distribution you care about is not each server's but the maximum across all of them. That maximum behaves very badly as the fan-out grows, and the arithmetic is the whole topic. Fan out to a hundred servers, each independently exceeding a second on one per cent of requests, and the chance that at least one does is one minus 0.99 to the hundredth, about 63 per cent. A one-in-a-hundred event at the component has become a two-in-three event at the request, which means each component's 99th percentile is roughly the request's median.\n\nThat reframes what to work on. Tuning the median of a dependency changes almost nothing for a fan-out request, because the median was never what you were waiting for. Cutting a dependency's 99.9th percentile changes a great deal. It is also why large systems set latency targets on their internal services at percentiles that look absurd in isolation: the caller's fan-out is what makes them reasonable.\n\nThe causes of a component tail are mostly not the component's code, which is why they resist the usual profiling. Shared resources contended by a neighbour on the same machine. A background task, compaction, garbage collection, log rotation, taking the CPU for a moment. Queueing behind a request that arrived first. Power and thermal management changing clock speed. A retry underneath you. Each is brief, uncorrelated with your request, and unavoidable, which is why the strategy is to tolerate the tail rather than to eliminate it.\n\nHedging tolerates it directly and cheaply. Send the request to one replica, and if it has not answered within a threshold set near the 95th percentile, send a duplicate to a second replica and take whichever returns first. Because only the slowest few per cent are ever duplicated, the extra load is a few per cent while the tail improvement is large. Dean and Barroso reported a case where hedging after a 10ms delay cut the 99.9th percentile of a thousand-server read from 1,800ms to 74ms for around two per cent more requests, which is the clearest available demonstration that the tail is a distribution problem rather than a capacity problem.\n\nTied requests are the refinement worth knowing. Send to two replicas immediately, and tell each which other replica has the request, so whichever dequeues it first cancels the other. That removes the waiting threshold entirely and costs less duplicated work than hedging, at the price of needing the replicas to talk to each other. The other mitigations are structural: many small partitions rather than few large ones, so a slow server's share can be moved elsewhere; and returning early with whatever has arrived, because a slightly incomplete answer inside the budget beats a complete one outside it.\n\nLittle's Law is the other half, and it connects the numbers you actually control: concurrency equals arrival rate multiplied by latency. It is the reason a tail is not merely a latency problem but a capacity problem in disguise. A service taking 100 requests a second at 50ms holds 5 in flight; the same service at 5 seconds holds 500, with nothing about the traffic or the code having changed. So when a dependency's tail lengthens, the in-flight work grows in proportion, the pool fills with requests that are waiting rather than working, and the queue runs away. That is why failing fast frees capacity and raising a timeout during an incident makes it worse, and it is why a concurrency limit is often a better control than a rate limit: it bounds the product rather than one of its terms.\n\nTail latency is a structural property of fan-out rather than a slow service you can find and fix, because the causes are shared resources, background work and queueing rather than your code. Hedging attacks the distribution directly, which is why it keeps working after tuning individual services has stopped helping. Little's Law is ","cursor-packfiles":"Why a Git repository resists being distributed\n\nStart with what a repository actually is on disk, because every difficulty later follows from it. Git stores content as objects addressed by the hash of their contents: a blob for a file's bytes, a tree for a directory listing, a commit pointing at a tree and its parents.\n\nWritten one file per object that would be unusable at any real size, so Git periodically gathers them into a packfile, a single large binary file holding many objects, delta-compressed against one another, with a separate index file mapping each object's hash to its offset.\n\nThat format is very good at what it was designed for and hostile to everything else. Reading one object means consulting the index, seeking to an offset, and then walking a chain of deltas to reconstruct the content, each step landing somewhere unpredictable in a file that may be several gigabytes. It is random access across a large binary blob, repeated thousands of times to serve one clone, and the code doing it assumes a local filesystem because that is what it was written against.\n\nSo the obvious move, putting the repository on network storage and letting any server read it, performs terribly. Every one of those small random reads becomes a network round trip, and there are an enormous number of them. This is the constraint the whole article is about: Git's storage format cannot simply be moved somewhere shared, so hosting it at scale means deciding what to distribute instead.\n\nThere is a second property that matters as much and is easier to miss. A push is not an append. It is a negotiation, in which the client and server work out which objects the server lacks, followed by a packfile of exactly those objects, followed by an update to a reference such as a branch tip. The reference update is the part that must be atomic and ordered, because two people pushing to one branch is a conflict that has to be resolved by somebody, and the data itself is content-addressed and therefore harmless to have twice.\n\nHolding those two facts together explains the shape of every solution in this space. The bytes are immutable and addressed by their content, so they can be copied freely and never conflict. The references are small, mutable and contended, so they need consensus. Any hosting architecture is a choice about where each of those two halves lives, and most of the differences between designs are about the second half rather than the first.\n\nThe temptation is to treat this as a storage problem and reach for a network filesystem, and the packfile format is the reason that fails: it was designed for random access on a local disk, and every random read becomes a round trip. Recognising that the immutable content and the mutable references have opposite requirements is what makes the rest of the design legible, because the content can be replicated carelessly and the references cannot be replicated at all without an ordering.","cursor-spokes":"GitHub's answer, and the wall it reaches\n\nGitHub's system, Spokes, has been the industry reference since around 2013, and the article is fair to it: its three central choices were correct for the problem as it stood. Replicate at the packfile level rather than trying to distribute Git itself, so ordinary Git tooling keeps working. Keep each replica as a real Git repository on local NVMe, so reads have the access pattern Git expects. And hold the replicas strongly consistent with three-phase commit, so a push is either everywhere or nowhere.\n\nThree-phase commit is worth understanding rather than treating as a label, because its cost is the whole reason for the article. A coordinator asks every replica whether it can accept the push, waits for all of them to say yes, tells them to prepare, waits again, then tells them to commit. Every phase waits for every participant, which means the latency of a push is set by the slowest replica in the set, not the median and not the fastest.\n\nThat gives the architecture a property nobody wants: adding replicas makes writes worse. Three is the standard set, and three is plenty for a repository whose readers are humans. It is not plenty for a large enterprise monorepo whose readers are continuous integration jobs, where the read load is enormous and would happily be spread over dozens of machines. But going from three replicas to twelve means every push waits on twelve, so the thing that would fix reads directly damages writes. Read capacity and write throughput are coupled by the consistency protocol, and the coupling has the wrong sign.\n\nThere is a second cost that is operational rather than architectural, and the article's phrase for it is the memorable one: the repositories are pets rather than cattle. Because a replica holds state that exists nowhere else in a directly usable form, the system has to know exactly which machines hold which repository, keep that mapping in an external database, and continuously check that each replica is healthy and in agreement. Every one of those is a component that can be wrong, and a stale mapping is a repository nobody can find.\n\nNone of this is a design error. It is what happens when a set of correct decisions meets a load nobody was designing for, which in this case is two loads at once: monorepos whose CI fan-out wants many more readers than three, and a newer shape entirely, millions of small repositories created by agents, where the per-repository overhead of tracking and health-checking a pet dominates everything else about them.\n\nThe lesson is not that three-phase commit is bad but that it couples two things a hosting system wants to scale independently. Because every phase waits for every replica, the replica count sets both the read capacity and the push latency, so there is no setting that serves a monorepo's CI fan-out and its developers at the same time. Any architecture with that coupling has a ceiling, and the ceiling arrives as a choice between slow pushes and insufficient read capacity.","cursor-continuity":"Continuity: a write-ahead log in a bucket\n\nThe replacement inverts what is authoritative. Instead of the repositories on disk being the truth and the object store being a backup, an append-only write-ahead log in S3-compatible object storage is the truth, and every repository on every disk is derived from it. Each entry in that log describes one accepted change, and the packfiles it refers to are stored as their own objects beside it, so the log stays small and the bulk data is separate.\n\nThat is a familiar move with an unfamiliar substrate. A database's write-ahead log records the intention before the change is applied, so recovery is a matter of replaying it; here the same idea is applied across machines rather than across a crash, so a machine that has fallen behind is not repaired, it simply replays. What makes it possible at all is that object storage acquired the one primitive the design needs.\n\nThat primitive is an atomic compare-and-swap.\n\nS3 now supports conditional writes: put this object only if it does not already exist, or only if its current version matches the one I read. That turns appending to the log into a race that exactly one participant wins, which is the ordering the mutable half of Git needs. So the consensus that Spokes ran between its own replicas is delegated to the object store, which already provides it, rather than being implemented and operated.\n\nThe consistency claim that follows is unusually strong for something built this way, and the article states it plainly: no push is acknowledged until it has been fully persisted, all pushes are linearizable through those atomic operations, and every view of every repository is fully consistent. The order in that first clause is the important part. Persist, then acknowledge, which is the same discipline as a messaging server that stores a message before sending the tick, and for the same reason: the window between accepting and durably storing is the window in which a crash loses something the client believes is safe.\n\nThe price is a round trip to object storage in the write path, and it is worth being clear that this is a real cost rather than a free lunch. What it buys is that durability and ordering both come from a service whose availability and durability are somebody else's problem, at eleven nines, with no cluster of your own to keep in agreement. For a workload where pushes are frequent but not latency-critical in the way a database commit is, that is an excellent trade, and the throughput numbers later in the piece are what make the case.\n\nThe interesting move is not using object storage for bulk data, which everyone does, but making it the authority for ordering. That is only possible because conditional writes give a compare-and-swap, and it is what removes the consensus protocol from the system entirely: the ordering exists, and no cluster of yours has to agree on it. Delegating the hard guarantee to a service that already provides it is usually cheaper than implementing it, and until conditional writes existed this particular delegation was not available.","cursor-warm-cache":"The repository on disk is a cache\n\nOnce the log is authoritative, the repositories on disk change status entirely. They are no longer state that must be protected, tracked and health-checked; they are a warm cache, materialised from the log when a node needs one and discardable at any moment. The article's word for what that buys is stateless: nothing on a node is irreplaceable, so a node is interchangeable with any other node.\n\nThat is the change that turns pets into cattle, and the consequences are mostly about what stops being necessary. No external database recording which machine holds which repository, because any machine can hold any repository. No continuous validation that replicas agree, because agreement is checked on every read against the log rather than maintained between peers. No careful repair of a divergent replica, because a divergent replica is thrown away and rebuilt from the log, which is strictly cheaper than reconciling it.\n\nWhich node materialises which repository is then decided by rendezvous hashing rather than by a lookup. Every node can compute, from the repository's name and the current node list, which nodes should hold it: hash the name together with each node's name, sort the scores, take the top few. Because it is a computation rather than a stored mapping, there is nothing to keep current and nothing to be stale, and when the node list changes only the repositories whose top scores moved need to relocate. It is consistent hashing's cousin, and it handles weighting more cleanly.\n\nThe replica count then becomes a per-repository dial rather than an architectural constant, which is the payoff for all of this. A large monorepo whose CI generates enormous read load can be materialised on hundreds of nodes, because replicas no longer participate in a consensus round and therefore no longer slow pushes down. At the other end, a tiny repository created by an agent can live on exactly one node, because the object store is the availability guarantee: if that node goes away, another materialises the repository from the log.\n\nIt is worth noticing what the trade actually is. A read served by a node that does not yet hold the repository must wait for it to be materialised, which is a cold start, and the cost of that is proportional to the repository's size. The design does not eliminate that cost, it relocates it: cold starts happen on the read path instead of divergence happening on the write path, and a cold start is at least a failure mode that resolves itself.\n\nMaking the on-disk repository a cache is what decouples the replica count from the write path, and that single decoupling is what the whole architecture was for. Spokes could not add readers without slowing writers because replicas were consensus participants; here they are caches, so the count can be one or hundreds according to the repository. Rendezvous hashing matters for the same reason the log does: a computed placement cannot go stale, so there is no mapping to maintain and nothing to be wrong.","cursor-read-path":"How a read proves it is current\n\nThis is the part of the design that repays the most attention, because it is where the correctness actually comes from. When a replica is asked to serve a read, it does not assume that whatever it holds is up to date. It asks the object store, using a conditional GET with the ETag it already has for the log's head.\n\nThere are exactly two answers and both are cheap. A 304 Not Modified means nothing has changed since the version the replica holds, so it can serve immediately from its warm on-disk repository; the article reports that this metadata-only operation takes less than 10ms on average. A 200 means the log has advanced and comes with the latest index, so the replica catches up on the entries it is missing before answering, and is then current by construction.\n\nSo the guarantee is not that replication succeeded. It is that a stale replica cannot serve a stale answer, because it finds out that it is stale before it answers. That is a materially different property from the usual arrangement, where a replica serves whatever it has and the system's consistency is whatever the replication lag happens to be, and it is why the design can claim that every view of every repository is fully consistent while replicating optimistically.\n\nThe cost is one small round trip to the object store per read, and this is the trade to weigh rather than skim. Ten milliseconds is a great deal next to a local disk read and almost nothing next to cloning a repository, which is what makes it acceptable here: Git operations are large enough that a fixed 10ms verification disappears into them, while the same overhead on a per-key cache lookup would be absurd. The technique is not universal; it fits because of the size of the operations it protects.\n\nIt is also what makes the write path's promise mean something. Because pushes are linearizable through the log and every read verifies against the log, a client that has just been told its push succeeded will see that push from any replica it subsequently reads from, which is read-your-own-writes without pinning anybody to anything. The two halves of the design are one mechanism seen from two directions.\n\nVerifying on read rather than trusting replication is what buys consistency without a consensus protocol. The usual arrangement makes correctness depend on replication having worked, so the consistency of the system is whatever the lag is; here a replica discovers it is behind before it answers, so replication is free to be unreliable. The reason it is affordable is the ratio: a 10ms metadata check is invisible inside a Git operation and would be ruinous inside a cache lookup.","cursor-gossip":"Replication that is allowed to fail\n\nGiven that every read verifies against the log, replication no longer has a correctness job. Its only job is to make the common case fast by getting updates to replicas before anybody asks for them, and something whose only job is to be an optimisation can be built very differently from something that has to be right.\n\nSo it is gossip over UDP. A node that accepts a push tells the others, best effort, in a datagram that may simply not arrive. There is no acknowledgement, no retransmission and no delivery guarantee, and the article is direct about why that is acceptable: a lost packet costs a replica one 200 instead of one 304 on its next read, which is to say it costs one catch-up that would have happened anyway. Nothing is incorrect, only slightly slower.\n\nThe sentence worth taking from this section is the design goal it states: always correct when degraded, and always fast when healthy. Those two properties usually pull against each other, and the reason they do not here is that they have been assigned to different mechanisms. Correctness lives entirely in the conditional read against the log. Speed lives entirely in the gossip. Neither mechanism is trying to provide both, so neither has to be compromised for the other.\n\nThat separation is the transferable idea, and it inverts the usual instinct. Most systems make replication reliable and then read from replicas hopefully; this one makes replication unreliable and reads carefully. The second arrangement is easier to build, because an unreliable broadcast has no failure modes worth the name, and it degrades better, because a replication outage produces slower reads rather than wrong ones. It is also what makes hundreds of replicas practical. Reliable delivery to hundreds of nodes is an increasingly expensive proposition, with acknowledgement tracking and retry state per peer, and it is exactly the cost that made adding replicas painful in the consensus design. Fire-and-forget datagrams cost the sender almost nothing per additional recipient, so the replica count stops being a factor in what replication costs.\n\nAssigning correctness and speed to different mechanisms is what lets each be built for one purpose. Because the conditional read makes staleness self-correcting, the replication layer can be a best-effort UDP broadcast with no acknowledgements, which has almost no failure modes and no per-peer cost. The usual arrangement, reliable replication plus hopeful reads, is harder to build and degrades worse, because a replication failure produces incorrect answers rather than slower ones.","cursor-numbers":"The numbers, and where the bottleneck moved\n\nThe measurements are what turn the argument into evidence, and there are three worth holding. On S3 Standard the system sustains up to 120 pushes per second while simultaneously compacting and replicating the compacted data to every other node. On S3 Express One Zone, the lower-latency variant, it exceeds 300 pushes per second. And stress testing showed linear scaling for reads up to 100 replicas with no regression in push throughput at all.\n\nThat third number is the claim the whole architecture was built to make. In the consensus design, replicas and push throughput were coupled by the protocol, so reads could only be scaled by making writes worse. Here they are independent, and the measurement says so: a hundredfold increase in read capacity for no measurable write cost. Everything else in the design is machinery in service of that one property.\n\nThe second number is interesting for a different reason: what it says about where the limit now sits. Moving to faster storage roughly tripled push throughput and then stopped being the constraint, because the system became bottlenecked by the speed at which Git can compact the on-disk data. The bottleneck moved out of the storage layer and into Git itself, which is the honest sign that the storage problem has been solved as far as this design can solve it. Optimising the object store further would now buy nothing.\n\nCompaction is also where the design gets one more benefit that is easy to skip past. Only the primary compacts, and the result is applied to both its on-disk repository and the log at once, so every other replica downloads the already-compacted packs from the object store rather than repacking the same data itself. The article's phrase for this is trading bandwidth for CPU, and it removes a genuine hazard from the previous generation, where repacking a large repository across several nodes was an availability risk on all of them at the same time.\n\nOne last property is worth naming because it comes free and is the sort of thing you only appreciate after an incident. Every push is in the log, permanently, which means the system has a complete and authoritative record of what happened without any external database keeping it. The article notes this as what lets a Git bug be analysed and remediated retrospectively, and that is a genuine operational difference: the question of what state a repository was in last Tuesday is a lookup rather than an investigation.\n\nThe number that matters is the one that shows the coupling gone: a hundred read replicas with no push throughput regression. That is what the log, the warm cache and the unreliable gossip were all for, and without the measurement it would only be an argument. The second lesson is where the bottleneck went: once faster storage stopped helping and Git's own compaction became the limit, the storage problem was as solved as this design can make it, and further work on the object store would be optimising something that is no longer the constraint.","oai-what-was-claimed":"What was claimed, and what was not\n\nStart here, because the headline version of this story is wrong in a way that matters. The claim is that a proof was produced showing that the three-dimensional incompressible Navier-Stokes equations can develop a singularity in finite time, with a smooth forcing term applied. Not the unforced equations. The forcing term is in the statement, and leaving it out is the difference between a specific technical result and a claim about one of the seven Millennium Prize Problems.\n\nThe distinction is not a quibble, and the reason is in how the Clay Mathematics Institute wrote the problem. Fefferman's official description offers four statements, any one of which would qualify: two assert existence and smoothness, and two assert breakdown. The breakdown statements permit a forcing term subject to decay conditions, which is why this result targets those rather than being outside the problem entirely. So the honest description is that it addresses a recognised route to the prize, and whether the formal statement satisfies the conditions Fefferman specified is exactly what is under review.\n\nOpenAI is explicit that it does not intend to claim the prize, and the process explains why that is not modesty. The Clay rules require publication in a peer-reviewed mathematics journal, followed by at least two years, followed by general acceptance in the community. None of those has happened. A published manuscript and a machine-checked formalisation are evidence offered for scrutiny, and the scrutiny is the part that has not begun.\n\nWhat was published is a manuscript of around 166 pages, titled after finite-time blowup, together with a formalisation in Lean. That is a substantially more checkable artefact than a claim on its own, and it is the right thing to have released. It is also not the same as a verified solution to the Millennium Problem, and the gap between those two things is the subject of the last topic in this card.\n\nThe reason to open a technical dissection with a section on framing is that this is the engineering lesson, not a disclaimer attached to one. A system that produces a result also produces a claim about that result, and the claim is where almost all of the failures live: too strong, insufficiently scoped, or stated in a way that invites a reader to hear something the evidence does not support. Getting the scope of a claim right is part of the work, and it is the part that is easiest to skip when the result is exciting.\n\nThe forcing term is the whole distinction between a specific result and a solved Millennium Problem, and every summary that dropped it produced a claim the evidence does not support. The transferable point is that the scope of a claim is part of the deliverable: a system that generates a result and a system that states what the result means are the same system, and the second half is where the errors concentrate.","oai-topology":"The shape of a ten thousand agent run\n\nThe system is described as coordinating agents running on an internal model, subdivided into groups, with agents able to communicate with others in their own group. The Navier-Stokes work involved on the order of ten thousand concurrent agents. The two structural details worth extracting are that the population was partitioned rather than flat, and that communication was scoped to the partition rather than global.\n\nBoth of those are the obvious engineering answer to the obvious problem, which is that a flat population of ten thousand communicating peers is not a system, it is a broadcast storm.\n\nCommunication cost between n participants who can all talk to each other grows with the square of n, so ten thousand peers is on the order of fifty million possible channels and no useful signal in any of them. Partitioning into groups makes the cost linear in the number of groups and quadratic only within one, which is the same argument as sharding, and the same argument as a service mesh preferring a hierarchy to a full graph.\n\nCross-group communication then needs a deliberate mechanism, and the account names one: Codex was used to consolidate promising intermediate ideas from different groups. That is a reduce step. Groups explore independently, something reads across them and pulls out what is worth propagating, and the result seeds further work. It is map-reduce with a language model as the reducer, and the shape is much older than the tooling.\n\nThere is an earlier phase reported that is more instructive than the headline, because it shows the resource allocation being managed rather than simply spent. Nearly a hundred agents worked for about fifty hours on the related Euler equations before resources were redirected. That is a bet placed, evaluated and abandoned, which is what a search over an expensive space has to look like: run cheap exploratory arms, decide, and concentrate the budget on the one that is moving.\n\nThe timeline gives the whole thing a scale. Training of the model began on 28 August, agents were launched on 1 September, and the result was reached on 5 September, about 88 hours after the first agents started. Then a further 17 hours of formalisation and checking. So the compute is enormous and the wall-clock is four days, which is the trade being demonstrated: an embarrassingly parallel search converts money into elapsed time at a rate no individual can match.\n\nPartitioning with scoped communication is the load-bearing decision, because all-to-all messaging between ten thousand participants is quadratic and produces no usable signal. Groups make the exploration parallel and the coordination affordable, and a separate consolidation step is then required to move an insight between them. The earlier hundred-agent arm that was abandoned after fifty hours is the part worth copying: an expensive search needs explicit reallocation, not just scale.","oai-economics":"2.7 million messages and 130 billion tokens\n\nThe reported figures are unusually specific and worth doing arithmetic on, because the arithmetic is where the intuition comes from. Across all of the mathematical problems attempted, the agents exchanged 4.9 million messages and generated roughly 300 billion output tokens. The Navier-Stokes work alone accounted for 2.7 million messages and approximately 130 billion output tokens. Divide those by the population and the picture sharpens considerably. Ten thousand agents and 2.7 million messages is about 270 messages per agent across the run. Ten thousand agents and 130 billion output tokens is about 13 million output tokens each. Over 88 hours that is roughly 150,000 output tokens per agent per hour, or on the order of 40 per second, sustained, per agent, for the better part of four days.\n\nTwo things follow from those numbers. The first is that this is not a chat workload, it is a batch compute workload that happens to be expressed in tokens, and it should be reasoned about the way any large batch job is: total throughput, cost per unit of work, and whether the result justified the spend. The second is that the message count is small relative to the token count, about 48,000 output tokens per message, which says the agents were producing substantial artefacts rather than conversing. That is the right shape. An agent population whose token budget goes into coordination rather than into work is a population doing very little.\n\nThe ratio between the two problems is also informative: Navier-Stokes took roughly 55 per cent of the messages and 43 per cent of the output tokens of the whole programme, so it was the largest single consumer but not the only one. That is what a portfolio looks like rather than a single bet, and it is consistent with the earlier arm being abandoned: several things were being tried and the accounting reflects it.\n\nThe honest caveat is that output tokens are not the cost. Input tokens are not reported, and in an agent system they usually dominate, because every step re-reads context, tool results and other agents' messages. A system generating 130 billion output tokens is plausibly reading many times that, which is why the public figure is a lower bound on the work rather than a measure of it. Anyone estimating the cost of something like this from the output count alone will be out by a large multiple, and the direction of the error is always the same.\n\nThe useful move is to divide the totals by the population, because the aggregates are unintuitive and the per-agent numbers are not: roughly 270 messages and 13 million output tokens each, which is about 40 output tokens per second sustained for four days. That reframes the run as a batch compute job rather than a conversation. The ratio matters too: 48,000 output tokens per message means the budget went into producing artefacts rather than into coordination, which is the shape a working agent population should have.","oai-tools":"Tools: running code, and a cached internet\n\nThe agents are described as having two capabilities beyond talking to each other: they could run code, and they could consult a cached version of the internet. Both choices are more interesting than they sound, and the second is the one worth pausing on.\n\nCode execution is what makes a search over mathematics tractable rather than merely fluent. A conjecture can be tested numerically, a counterexample can be looked for by brute force, an algebraic manipulation can be checked by a computer algebra system, and a candidate construction can be simulated. That converts a large class of questions from things an agent asserts into things an agent finds out, and the difference in reliability between those two is the entire reason tool use exists.\n\nA cached internet rather than a live one is a deliberate constraint and it buys three things at once. Reproducibility: ten thousand agents hitting live sources would each see a slightly different web, so the corpus would not be a fixed input and the run would not be repeatable. Rate limiting: ten thousand concurrent agents making live requests is a denial of service against whatever they are reading, and would be blocked within minutes. And determinism of the boundary: with a snapshot, the question of what the system had access to has an answer, which matters enormously for a claim about novelty and matters again for the provenance dispute this card returns to later.\n\nThat last point is worth stating as a general principle, because it applies well outside this. Freezing an external dependency turns an unbounded question into a bounded one. What did the system know is unanswerable against a live internet and answerable against a snapshot, and any system whose output will be scrutinised for originality wants that property before it produces anything rather than after. The pairing is also the standard shape for this kind of work, and recognising it is more useful than treating it as novel. A generator proposes, a cheap checker rejects most proposals, and only what survives is worth an expensive step. Code execution is the cheap checker here and Lean is the expensive one, and the whole run is a funnel with those two filters in it. Without a checker, scaling a generator up just produces more unverified output, which is not progress in any direction.\n\nA cached corpus is the choice that does the most work: it makes the run reproducible, prevents ten thousand agents from behaving as a denial of service against live sources, and gives the question of what the system had access to an actual answer. That last property is what a claim of novelty depends on, and it has to be arranged before the run rather than reconstructed afterwards. Code execution matters for a different reason, which is that it converts assertions into findings.","oai-lean":"Lean as the referee, and what it does not settle\n\nAfter the agents reached their result, a further 17 hours went into formalising and checking the proof in Lean. Lean is an interactive theorem prover with a small trusted kernel: you write a statement and a proof in its language, and the kernel mechanically verifies that the proof establishes the statement from the axioms. It does not read prose, it does not accept a gap, and it does not get tired at page 140 of 166.\n\nThat is a genuinely strong guarantee and it is precisely bounded, which is why it deserves stating carefully. What a Lean check establishes is that the formal statement follows from the axioms by the rules of the logic. Every step is present and every step is valid. For a long analytic argument, where the traditional failure mode is a lemma that is true in the cases the author considered and false in one they did not, this closes off the most likely way for a claimed proof to be wrong.\n\nWhat it does not establish is that the formal statement is the theorem anyone cares about.\n\nBetween Fefferman's problem description and a Lean statement sits a translation, and the translation is done by humans and machines with no formal check of its own. Getting the function spaces right, the decay conditions right, the quantifiers in the right order, the notion of solution matching the one specified: each is an opportunity to formalise something adjacent to the intended claim and prove that instead. Nothing in the Lean toolchain can catch it, because the toolchain's job starts after the statement is written.\n\nSo the verification is real and the gap is in a specific place, and naming that place is more useful than either dismissing the check or over-reading it. The proof is machine-verified. Whether the machine verified the right proposition is a question for mathematicians reading the statement, which is exactly what the review period exists for, and it is why the Lean artefact accelerates the scrutiny rather than replacing it.\n\nTerence Tao's objection is aimed somewhere else again and is worth carrying, because it is about value rather than validity. The primary worth of mathematical work, on his framing, is the digestible insight rather than the fact that a problem is closed; if the search process remains a black box, its value to mathematics is close to zero. A verified proof that nobody can extract a technique from settles one question and teaches nothing, and for a field whose output is understanding rather than answers, that is a real deficiency rather than a philosophical complaint.\n\nA machine-checked proof closes the most likely failure mode of a long analytic argument, which is a gap nobody noticed, and leaves untouched the one nothing in the toolchain can check: whether the formalised statement is the intended one. That translation from an informal problem description into a formal statement is where the remaining risk lives, and knowing exactly where it lives is what makes the artefact useful rather than either conclusive or worthless.","oai-disputes":"The disputes, and why provenance is an engineering problem\n\nTwo mathematicians, Tristan Buckmaster at NYU and Levent Alpöge, had spent months working with AI systems on a closely related line of research on forced Euler equations. The overlap in subject and timing produced a question about where OpenAI's result came from, and the question has not been resolved. Buckmaster has said plainly that he has not seen the proof and does not know what the model did or how it worked.\n\nOpenAI's position, as reported, is that the model did not have access to their specific work before publication, while acknowledging that it cannot rule out that de-identified data contributed to training. Those two statements are compatible and the second is the significant one, because it is an admission about a system property rather than about an incident: if product data flows into training in de-identified form, then the question of whether a particular user's work influenced a particular output is not answerable, by construction.\n\nThat is what makes this an engineering topic rather than a news item. Provenance of inputs is a property you build in or lack permanently, and it cannot be reconstructed after the fact. The cached corpus discussed earlier is the good version: a frozen snapshot means the question of what the system could read has an answer. The training data is the bad version, not because anything improper is established, but because the pipeline was not designed to answer questions of this shape and so cannot answer them now.\n\nThe priority dimension is separate and simpler. Two groups working the same problem is ordinary in mathematics and the discipline has machinery for it: preprints with dates, journal submission records, seminar talks. What is new is the possibility that one party's tooling saw the other's unpublished work, which the existing machinery has no answer for, because it was designed for a world where reading someone's drafts required them to hand them to you.\n\nThe transferable rule is short and unpopular. If a system's outputs will be scrutinised for originality, the provenance of its inputs has to be a designed property with an audit trail, decided before the system runs. Everything else, including a completely honest denial, reduces to a claim that cannot be checked, and a claim that cannot be checked is worth very little precisely when it matters most. The cached internet in this same system shows that OpenAI knows how to do this; the training pipeline shows what it costs when a component was not built for the question later asked of it.\n\nThe admission that de-identified training data cannot be ruled out is a statement about the architecture, not about an incident: a pipeline that anonymises inputs cannot afterwards answer whether a specific person's work influenced a specific output. That is why provenance has to be designed in rather than investigated later. The same system shows both answers, since the frozen corpus makes what the agents could read a knowable fact and the training data does not."};
